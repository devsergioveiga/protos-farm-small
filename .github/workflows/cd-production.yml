name: CD Production

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm build:backend
      - run: pnpm build:frontend

      # TODO: Substituir pelo deploy real quando a infraestrutura (US-003) estiver pronta.
      # O deploy usará secrets configurados no GitHub (ex: ${{ secrets.PRODUCTION_DEPLOY_KEY }}).
      # O environment "production" requer aprovação manual configurada no GitHub.
      - name: Deploy (placeholder)
        run: echo "Deploy to production environment — placeholder for US-003"
