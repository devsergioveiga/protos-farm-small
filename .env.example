# ===========================================
# Protos Farm — Variáveis de Ambiente
# ===========================================
# Copie este arquivo para infra/env/.env.dev (ou o ambiente desejado)
# e ajuste os valores conforme necessário.
# Documentação completa: docs/ENV_VARS.md

# --- Aplicação ---
NODE_ENV=development
PORT=3000

# --- PostgreSQL ---
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=protos
POSTGRES_PASSWORD=protos
POSTGRES_DB=protos_farm

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6379

# --- JWT / Auth ---
JWT_SECRET=dev-jwt-secret-do-not-use-in-production
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=604800
