---
phase: 32-integra-o-financeira-cont-bil-e-dashboard-rh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/prisma/schema.prisma
  - apps/backend/src/modules/payroll-runs/payroll-runs.service.ts
  - apps/backend/src/modules/payroll-runs/payroll-runs.routes.ts
  - apps/backend/src/modules/payroll-runs/payroll-runs.types.ts
  - apps/backend/src/modules/payroll-runs/payroll-date-utils.ts
  - apps/backend/src/modules/payroll-runs/payroll-runs.routes.spec.ts
autonomous: true
requirements: [INTEGR-01]
must_haves:
  truths:
    - "closeRun creates CPs for IRRF (dia 20), VT (dia 5 util), pension (dia 5 util) in addition to existing salary/INSS/FGTS"
    - "Each CP created by closeRun includes PayableCostCenterItem entries with correct percentage rateio"
    - "GET cp-preview returns dry-run list of CPs-to-be-created without writing to DB"
    - "revertRun cancels ALL payable originTypes including the new ones"
    - "Cost-center percentages always sum to exactly 100"
  artifacts:
    - path: "apps/backend/src/modules/payroll-runs/payroll-date-utils.ts"
      provides: "Extracted nthBusinessDay helper"
      exports: ["nthBusinessDay"]
    - path: "apps/backend/src/modules/payroll-runs/payroll-runs.service.ts"
      provides: "Extended closeRun + revertRun + cpPreview + buildCostCenterItems"
    - path: "apps/backend/src/modules/payroll-runs/payroll-runs.routes.ts"
      provides: "New GET /:id/cp-preview endpoint"
  key_links:
    - from: "payroll-runs.service.ts closeRun"
      to: "tx.payable.create"
      via: "new originTypes PAYROLL_EMPLOYEE_IRRF, PAYROLL_EMPLOYEE_VT, PAYROLL_EMPLOYEE_PENSION"
      pattern: "originType.*PAYROLL_EMPLOYEE"
    - from: "payroll-runs.service.ts closeRun"
      to: "tx.payableCostCenterItem.createMany"
      via: "buildCostCenterItems helper using TimeEntryActivity groupBy"
      pattern: "buildCostCenterItems"
    - from: "payroll-runs.service.ts revertRun"
      to: "tx.payable.updateMany"
      via: "OR filter with all 6 originTypes"
      pattern: "PAYROLL_EMPLOYEE_IRRF|PAYROLL_EMPLOYEE_VT|PAYROLL_EMPLOYEE_PENSION"
---

<objective>
Complete the Contas a Pagar generation at payroll close (INTEGR-01 backend). Extend closeRun to create CPs for employee IRRF, VT, and pension with correct due dates and cost-center rateio. Add a dry-run cp-preview endpoint. Extend revertRun with new originTypes.

Purpose: Currently closeRun only generates 3 CP types (net salary, employer INSS, FGTS). This plan adds the missing types and cost-center allocation to close the payroll-to-payables cycle.
Output: Extended payroll-runs module with 6 CP types, cost-center rateio, preview endpoint, and comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-integra-o-financeira-cont-bil-e-dashboard-rh/32-RESEARCH.md
@apps/backend/src/modules/payroll-runs/payroll-runs.service.ts
@apps/backend/src/modules/payroll-runs/payroll-runs.routes.ts
@apps/backend/src/modules/payroll-runs/payroll-runs.types.ts
@apps/backend/src/modules/payroll-runs/payroll-runs.routes.spec.ts
@apps/backend/prisma/schema.prisma

<interfaces>
<!-- Existing patterns executor needs -->

From payroll-runs.service.ts:
- function nthBusinessDay(year, month, n): Date — private helper at line 51, extract to shared file
- closeRun creates CPs at lines 691-770 using tx.payable.create with originType/originId pattern
- revertRun at line 851 uses OR filter on 3 originTypes (PAYROLL_RUN_ITEM, PAYROLL_EMPLOYER_INSS, PAYROLL_EMPLOYER_FGTS)
- tx.payable.create used directly inside withRlsContext (NOT payables.service.createPayable — per STATE.md decision)

From payables.service.ts:
- settlePayment at line 356 handles payment — Plan 02 will hook accounting reversal here
- PayableCostCenterItem pattern: createMany after payable.create with {payableId, costCenterId, farmId, allocMode, percentage}

From payroll-provisions.types.ts:
- AccountingEntryStub interface at line 73 — documents debit/credit accounts (used by Plan 02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + extract nthBusinessDay + build cost-center helper</name>
  <files>
    apps/backend/prisma/schema.prisma,
    apps/backend/src/modules/payroll-runs/payroll-date-utils.ts,
    apps/backend/src/modules/payroll-runs/payroll-runs.service.ts
  </files>
  <action>
    1. Add `alimonyDeduction Decimal? @db.Decimal(14, 2)` field to PayrollRunItem model in schema.prisma. Run `cd apps/backend && npx prisma db push` to apply (per Phase 25/26 pattern — no migration file needed for db push).

    2. Create `payroll-date-utils.ts` — extract the existing `nthBusinessDay(year, month, n)` function from payroll-runs.service.ts (line 51) into this new shared file. Export it. Update payroll-runs.service.ts to import from the new file instead of using the private function. Keep the signature identical: `export function nthBusinessDay(year: number, month: number, n: number): Date`.

    3. In payroll-runs.service.ts, create `buildCostCenterItems()` async helper (per RESEARCH.md Pattern 1):
       - Accepts (tx: TxClient, employeeId: string, referenceMonth: Date, farmId: string)
       - Returns Promise<Array<{costCenterId, farmId, allocMode, percentage}>>
       - Groups TimeEntryActivity by costCenterId for the employee in that referenceMonth
       - Calculates percentage per center using Decimal arithmetic
       - Applies rounding fix: round all except last, last = 100 - sum(others)
       - Falls back to EmployeeContract.costCenterId at 100% if no time entries found
       - Returns empty array if no cost center available at all

    4. Extend closeRun to create 3 new CP types AFTER the existing 3:
       - **PAYROLL_EMPLOYEE_IRRF**: one CP per run (not per employee), sum all items' irrfAmount, originType 'PAYROLL_EMPLOYEE_IRRF', originId = runId, dueDate = 20th of next month, supplierName = 'IRRF Retido — Folha [Mes/Ano]', category 'PAYROLL' as any. Only create if sum > 0.
       - **PAYROLL_EMPLOYEE_VT**: per employee where vtDeduction > 0, originType 'PAYROLL_EMPLOYEE_VT', originId = item.id, dueDate = nthBusinessDay(nextYear, nextMonth, 5), supplierName = 'Vale-Transporte'.
       - **PAYROLL_EMPLOYEE_PENSION**: per employee where alimonyDeduction > 0 (new field), originType 'PAYROLL_EMPLOYEE_PENSION', originId = item.id, dueDate = nthBusinessDay(nextYear, nextMonth, 5), supplierName = 'Pensao Alimenticia — [Employee Name]'.

    5. For ALL payable.create calls in closeRun (both existing 3 and new 3), add cost-center items:
       - Call buildCostCenterItems(tx, employeeId, run.referenceMonth, farmId) for per-employee CPs
       - For aggregated CPs (INSS patronal, FGTS, IRRF), use the run-level farm's default cost center at 100% (or skip if no cost center)
       - After each tx.payable.create, if costCenterItems.length > 0, call tx.payableCostCenterItem.createMany with the items

    ANTI-PATTERNS:
    - Do NOT create FUNRURAL CP — already handled by tax-guides module (per RESEARCH.md)
    - Do NOT call payables.service.createPayable — use tx.payable.create directly (per STATE.md decision)
    - Cost-center percentages must sum to exactly 100 — use Decimal rounding fix
  </action>
  <verify>
    <automated>cd apps/backend && npx prisma db push --accept-data-loss 2>&1 | tail -5 && npx jest --testPathPattern="payroll-runs" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    - alimonyDeduction field exists on PayrollRunItem
    - nthBusinessDay extracted to payroll-date-utils.ts and imported in service
    - buildCostCenterItems helper creates correct percentage allocations with rounding fix
    - closeRun creates IRRF, VT, pension CPs with cost-center items
    - All existing CPs also get cost-center items
    - Existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: cp-preview endpoint + revertRun extension + tests</name>
  <files>
    apps/backend/src/modules/payroll-runs/payroll-runs.service.ts,
    apps/backend/src/modules/payroll-runs/payroll-runs.routes.ts,
    apps/backend/src/modules/payroll-runs/payroll-runs.types.ts,
    apps/backend/src/modules/payroll-runs/payroll-runs.routes.spec.ts
  </files>
  <action>
    1. In payroll-runs.types.ts, add interfaces:
       ```typescript
       export interface CpPreviewItem {
         type: string;          // "Salario Liquido", "INSS Patronal", "FGTS", "IRRF", "VT", "Pensao"
         employeeName?: string;
         amount: number;
         dueDate: string;       // ISO date
         costCenterItems: Array<{ costCenterName: string; percentage: number }>;
       }
       export interface CpPreviewResponse {
         items: CpPreviewItem[];
         totalAmount: number;
         runTotalNet: number;
         reconciled: boolean;   // abs(totalAmount - runTotalNet) < 0.01
       }
       ```

    2. In payroll-runs.service.ts, create `cpPreview(rls, runId)` function:
       - Load run with items + employees
       - Run the same logic as closeRun to compute CP amounts BUT do not write to DB
       - Return CpPreviewResponse with all CPs that would be created
       - Include FUNRURAL from tax-guides (query existing TaxGuide records for same referenceMonth)
       - Calculate reconciliation: totalAmount of employee net-salary CPs vs run.totalNet

    3. In payroll-runs.routes.ts, register GET `/:id/cp-preview` BEFORE the existing `/:id` route to prevent Express 5 param shadowing (per Phase 29 decision pattern):
       - Permission: payroll-params:read
       - Returns CpPreviewResponse JSON

    4. Extend revertRun: add PAYROLL_EMPLOYEE_IRRF, PAYROLL_EMPLOYEE_VT, PAYROLL_EMPLOYEE_PENSION to the OR filter in the payable cancellation query. The existing filter is at the revertRun function around line 870.

    5. Add/extend tests in payroll-runs.routes.spec.ts:
       - Test cp-preview returns correct structure for a run in CALCULATED state
       - Test cp-preview includes IRRF, VT entries when applicable
       - Test cp-preview reconciliation flag is true when amounts match
       - Test closeRun now creates 6 CP types (verify count of payables with PAYROLL_ originType)
       - Test each new CP has PayableCostCenterItem entries
       - Test revertRun cancels ALL 6 originTypes (verify no active payables remain)
       - Test FUNRURAL is NOT duplicated (count of FUNRURAL-related payables remains same before/after close)
       - Use `as const` for enum values in test data (per CLAUDE.md)
  </action>
  <verify>
    <automated>cd apps/backend && npx jest --testPathPattern="payroll-runs" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    - GET /:id/cp-preview returns dry-run CP list without DB writes
    - Preview includes all 6 CP types + FUNRURAL from tax-guides
    - Reconciliation flag correctly computed
    - revertRun cancels all 6 originTypes
    - All new and existing tests pass
    - No FUNRURAL duplication
  </done>
</task>

</tasks>

<verification>
- `cd apps/backend && npx jest --testPathPattern="payroll-runs" --no-coverage` — all pass
- `cd apps/backend && npx prisma validate` — schema valid
- Manual: GET /org/:orgId/payroll-runs/:id/cp-preview returns expected structure
</verification>

<success_criteria>
- closeRun creates 6 CP types (net salary, INSS, FGTS, IRRF, VT, pension) with cost-center rateio
- cp-preview endpoint returns dry-run list with reconciliation
- revertRun cancels all 6 originTypes
- FUNRURAL not duplicated
- Cost-center percentages sum to exactly 100
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/32-integra-o-financeira-cont-bil-e-dashboard-rh/32-01-SUMMARY.md`
</output>
