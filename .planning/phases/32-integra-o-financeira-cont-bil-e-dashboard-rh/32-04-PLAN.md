---
phase: 32-integra-o-financeira-cont-bil-e-dashboard-rh
plan: 04
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - apps/frontend/src/types/payroll-integration.ts
  - apps/frontend/src/types/accounting-entries.ts
  - apps/frontend/src/hooks/useCpPreview.ts
  - apps/frontend/src/hooks/useAccountingEntries.ts
  - apps/frontend/src/components/payroll/PayrollCpReviewModal.tsx
  - apps/frontend/src/pages/PayrollRunsPage.tsx
  - apps/frontend/src/pages/AccountingEntriesPage.tsx
  - apps/frontend/src/pages/AccountingEntriesPage.css
  - apps/frontend/src/App.tsx
  - apps/frontend/src/components/layout/Sidebar.tsx
autonomous: true
requirements: [INTEGR-01, INTEGR-02]
must_haves:
  truths:
    - "User clicks Fechar Folha and sees PayrollCpReviewModal with grouped CP preview before confirming"
    - "Modal shows reconciliation status (green check if total matches, orange warning if divergent)"
    - "Confirming close triggers actual closeRun and shows success toast"
    - "Estorno uses ConfirmModal variant=danger with correct copy"
    - "AccountingEntriesPage shows filtered list with typed badges and drill-down accordion"
    - "CSV export downloads file with correct data"
  artifacts:
    - path: "apps/frontend/src/components/payroll/PayrollCpReviewModal.tsx"
      provides: "Pre-close review modal with grouped table"
    - path: "apps/frontend/src/pages/AccountingEntriesPage.tsx"
      provides: "Accounting entries list page with filters, badges, accordion, CSV"
    - path: "apps/frontend/src/hooks/useCpPreview.ts"
      provides: "Hook to fetch CP preview data"
    - path: "apps/frontend/src/hooks/useAccountingEntries.ts"
      provides: "Hook to fetch and export accounting entries"
  key_links:
    - from: "PayrollCpReviewModal"
      to: "GET /org/:orgId/payroll-runs/:id/cp-preview"
      via: "useCpPreview hook"
      pattern: "api\\.get.*cp-preview"
    - from: "PayrollRunsPage Fechar Folha button"
      to: "PayrollCpReviewModal"
      via: "useState showCpReview + selected runId"
      pattern: "setShowCpReview"
    - from: "AccountingEntriesPage"
      to: "GET /org/accounting-entries"
      via: "useAccountingEntries hook"
      pattern: "api\\.get.*accounting-entries"
---

<objective>
Build the PayrollCpReviewModal (INTEGR-01 frontend) and AccountingEntriesPage (INTEGR-02 frontend). Wire the "Fechar Folha" flow through the review modal, add estorno with ConfirmModal, and create the accounting entries read-only page with filters, badges, and CSV export.

Purpose: Completes the user-facing integration for payroll closing (review before confirm) and accounting visibility (drill-down entries page).
Output: PayrollCpReviewModal component, AccountingEntriesPage with all interactions, route registration, sidebar entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-integra-o-financeira-cont-bil-e-dashboard-rh/32-RESEARCH.md
@.planning/phases/32-integra-o-financeira-cont-bil-e-dashboard-rh/32-UI-SPEC.md
@apps/frontend/src/pages/PayrollRunsPage.tsx
@apps/frontend/src/hooks/usePayrollRuns.ts
@apps/frontend/src/components/payroll/PayrollRunDetailModal.tsx
@apps/frontend/src/App.tsx
@apps/frontend/src/components/layout/Sidebar.tsx

<interfaces>
<!-- Backend contracts from Plans 01 and 02 -->

From Plan 01 (CpPreviewResponse):
```typescript
interface CpPreviewItem {
  type: string;
  employeeName?: string;
  amount: number;
  dueDate: string;
  costCenterItems: Array<{ costCenterName: string; percentage: number }>;
}
interface CpPreviewResponse {
  items: CpPreviewItem[];
  totalAmount: number;
  runTotalNet: number;
  reconciled: boolean;
}
```

From Plan 02 (AccountingEntry):
```typescript
enum AccountingEntryType {
  PAYROLL_SALARY, PAYROLL_CHARGES, VACATION_PROVISION,
  THIRTEENTH_PROVISION, TAX_LIABILITY, SALARY_REVERSAL
}
// GET /org/accounting-entries?referenceMonth=&farmId=&entryType=&page=&limit=
// GET /org/accounting-entries/export/csv
// GET /org/accounting-entries/:id
```

Existing frontend patterns:
- usePayrollRuns uses useState + useCallback (no SWR)
- ConfirmModal at @/components/ui/ConfirmModal with variant="danger"
- PayrollRunStatusBadge at @/components/payroll/PayrollRunStatusBadge
- api.get / api.post from @/services/api
- Skeleton loading with CSS pulse animation
- ALL CAPS table column headers per design system
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + hooks + PayrollCpReviewModal + wire to PayrollRunsPage</name>
  <files>
    apps/frontend/src/types/payroll-integration.ts,
    apps/frontend/src/hooks/useCpPreview.ts,
    apps/frontend/src/components/payroll/PayrollCpReviewModal.tsx,
    apps/frontend/src/pages/PayrollRunsPage.tsx
  </files>
  <action>
    1. Create types/payroll-integration.ts:
       - CpPreviewItem interface (type, employeeName?, amount, dueDate, costCenterItems)
       - CpPreviewResponse interface (items, totalAmount, runTotalNet, reconciled)

    2. Create hooks/useCpPreview.ts:
       - `useCpPreview(runId: string | null)` — useState + useCallback pattern (per Phase 25 decision)
       - Calls GET `/org/${orgId}/payroll-runs/${runId}/cp-preview` using api.get
       - Returns { data: CpPreviewResponse | null, isLoading, error, refetch }
       - Fetches when runId changes (useEffect)

    3. Create components/payroll/PayrollCpReviewModal.tsx per UI-SPEC Screen 1:
       - Props: `{ isOpen, onClose, runId, referenceMonth, onConfirmClose }`
       - Uses useCpPreview(runId) to fetch data
       - **Header:** "Revisao de Contas a Pagar -- [Mes/Ano]" + PayrollRunStatusBadge
       - **Body:** Grouped table with collapsible `<details>` sections:
         - "Salarios Liquidos" — per-employee items with type PAYROLL_RUN_ITEM
         - "Encargos Patronais" — INSS + FGTS items
         - "Impostos a Recolher" — IRRF items
         - "Outros" — VT, pension items
         - Each section <summary> shows section total in JetBrains Mono
         - Table columns: TIPO | COLABORADOR | VALOR | VENCIMENTO | CENTRO DE CUSTO (ALL CAPS headers)
         - Monetary values: font-family var(--font-mono), text-align right
       - **Reconciliation row:** Bottom of body — "Total CPs gerados: R$ X.XXX,XX -- Total folha liquida: R$ X.XXX,XX"
         - Green CheckCircle icon if reconciled=true
         - Orange AlertTriangle icon + warning text if reconciled=false (per UI-SPEC copy)
       - **Footer:** "Voltar a Folha" (secondary, left) + "Confirmar Fechamento" (primary green, right)
         - Confirm triggers onConfirmClose callback
       - **Loading:** Skeleton pulse in table cells
       - **Error:** Inline red border-left alert with AlertCircle icon
       - Modal animation: 300ms ease-out open, ease-in close (per UI-SPEC animation contract)

    4. Wire PayrollCpReviewModal into PayrollRunsPage.tsx:
       - Add state: `showCpReview` boolean + `selectedRunIdForClose` string
       - Change "Fechar Folha" button (for CALCULATED runs) to set these states instead of directly calling closeRun
       - onConfirmClose: call existing closeRun function, then close modal, show success toast "Folha fechada. Contas a pagar e lancamentos contabeis gerados."
       - Render PayrollCpReviewModal at bottom of page

    5. Wire estorno in PayrollRunDetailModal (or PayrollRunsPage depending on where "Estornar" button lives):
       - "Estornar Folha" button triggers ConfirmModal with variant="danger"
       - Heading: "Estornar fechamento da folha?"
       - Body copy per UI-SPEC copywriting contract
       - Confirm CTA: "Estornar Folha", Cancel: "Manter Fechamento"
       - On confirm: call revertRun, show toast "Estorno concluido. A folha voltou ao estado de calculada."

    DESIGN SYSTEM:
    - Touch targets minimum 48x48px
    - Font: DM Sans 600 for headings, Source Sans 3 for body, JetBrains Mono for monetary values
    - Colors per UI-SPEC color contract
    - pt-BR colloquial copy per copywriting contract
    - Skeleton loading, never spinner
    - HTML semantic elements: <table>, <details>, <summary>, <button>
    - Focus visible: outline 2px primary-500
    - aria-required on any form fields, role="alert" on error messages
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - PayrollCpReviewModal renders grouped CP preview with reconciliation
    - "Fechar Folha" button opens review modal instead of direct close
    - Confirm in modal triggers actual close
    - Estorno uses ConfirmModal variant=danger with correct copy
    - All monetary values in JetBrains Mono
    - ALL CAPS column headers
    - Loading skeleton, error alert
    - Accessible markup
  </done>
</task>

<task type="auto">
  <name>Task 2: AccountingEntriesPage + route + sidebar</name>
  <files>
    apps/frontend/src/types/accounting-entries.ts,
    apps/frontend/src/hooks/useAccountingEntries.ts,
    apps/frontend/src/pages/AccountingEntriesPage.tsx,
    apps/frontend/src/pages/AccountingEntriesPage.css,
    apps/frontend/src/App.tsx,
    apps/frontend/src/components/layout/Sidebar.tsx
  </files>
  <action>
    1. Create types/accounting-entries.ts:
       - AccountingEntryType enum matching backend
       - AccountingEntry interface with all fields
       - ENTRY_TYPE_LABELS: Record<AccountingEntryType, string> for pt-BR display labels
       - ENTRY_TYPE_BADGE_COLORS: Record<AccountingEntryType, { bg: string; text: string }> per UI-SPEC badge color table

    2. Create hooks/useAccountingEntries.ts:
       - `useAccountingEntries(filters)` — useState + useCallback pattern
       - Calls GET `/org/${orgId}/accounting-entries` with query params
       - Returns { entries, isLoading, error, totalCount, refetch }
       - `exportCsv(filters)` function: calls GET /org/accounting-entries/export/csv, triggers download using blob URL

    3. Create AccountingEntriesPage.tsx per UI-SPEC Screen 2:
       - **Breadcrumb:** Dashboard > Contabilidade > Lancamentos
       - **Filter bar:** month/year picker + fazenda select + entry type multi-select — single row below breadcrumb
       - **Table:** COMPETENCIA | TIPO | DEBITO | CREDITO | VALOR | CENTRO DE CUSTO | ORIGEM
         - Column headers ALL CAPS, 14px, weight 600
         - TIPO column: colored badge per ENTRY_TYPE_BADGE_COLORS
         - ORIGEM column: hyperlink pill "[FOLHA-YYYY-MM]" linking to payroll-runs page
         - Monetary values: JetBrains Mono, right-aligned
       - **Drill-down:** Clicking row toggles inline accordion showing debitLabel + creditLabel + notes + reversedBy link
       - **Mobile:** Table transforms to stacked cards below 768px (per CLAUDE.md rule)
       - **Export:** "Exportar CSV" secondary button in page header (right side)
       - **Empty state:** Heading "Nenhum lancamento contabil ainda" + body + CTA "Ir para Folha de Pagamento" linking to /payroll-runs
       - **Loading:** Skeleton pulse rows
       - **Pagination:** Page/limit controls at bottom

    4. Create AccountingEntriesPage.css for page-scoped styles:
       - Badge color classes for each AccountingEntryType
       - Accordion expand/collapse animation 200ms ease-out
       - Mobile card transformation at max-width 768px
       - Table cell alignment, mono font for amounts

    5. Register route in App.tsx: `<Route path="/accounting-entries" element={<AccountingEntriesPage />} />`

    6. Add sidebar entry in Sidebar.tsx under "RH" group (or new "CONTABILIDADE" group if appropriate):
       - `{ to: '/accounting-entries', icon: BookOpen, label: 'Lancamentos Contabeis' }`
       - Use BookOpen icon from lucide-react

    DESIGN SYSTEM:
    - Spacing: 4px multiples only
    - Touch targets 48x48px minimum
    - Focus visible outline
    - Semantic HTML: <table>, <details> for accordion
    - aria-label on icon-only buttons, role="alert" on errors
    - prefers-reduced-motion respected for animations
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - AccountingEntriesPage renders with filters, typed badges, drill-down accordion
    - CSV export triggers file download
    - Mobile responsive card layout
    - Empty state with CTA to payroll
    - Route registered at /accounting-entries
    - Sidebar entry visible
    - Badge colors match UI-SPEC
    - ALL CAPS headers, JetBrains Mono for amounts
  </done>
</task>

</tasks>

<verification>
- `cd apps/frontend && npx tsc --noEmit` — no type errors
- `cd apps/frontend && npx vitest run` — all tests pass
- Manual: navigate to /accounting-entries, verify table renders
- Manual: click "Fechar Folha" on PayrollRunsPage, verify review modal appears
</verification>

<success_criteria>
- PayrollCpReviewModal shows grouped CP preview with reconciliation before close
- Close confirmation triggers actual payroll close with success toast
- Estorno uses ConfirmModal variant=danger per UI-SPEC
- AccountingEntriesPage with filters, badges, accordion, CSV export
- Both screens accessible and responsive
- Routes and sidebar entries registered
</success_criteria>

<output>
After completion, create `.planning/phases/32-integra-o-financeira-cont-bil-e-dashboard-rh/32-04-SUMMARY.md`
</output>
