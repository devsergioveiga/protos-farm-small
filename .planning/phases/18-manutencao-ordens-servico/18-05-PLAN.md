---
phase: 18-manutencao-ordens-servico
plan: '05'
type: execute
wave: 2
depends_on: ['18-02', '18-03']
files_modified:
  - apps/frontend/src/pages/MaintenanceDashboardPage.tsx
  - apps/frontend/src/pages/MaintenanceDashboardPage.css
  - apps/frontend/src/components/maintenance/WorkOrderCloseWizard.tsx
  - apps/frontend/src/components/maintenance/WorkOrderCloseWizard.css
  - apps/frontend/src/components/maintenance/MaintenanceKanban.tsx
  - apps/frontend/src/components/maintenance/MaintenanceProvisionModal.tsx
  - apps/frontend/src/components/maintenance/MaintenanceProvisionModal.css
  - apps/frontend/src/components/assets/AssetMaintenanceTab.tsx
  - apps/frontend/src/hooks/useMaintenanceDashboard.ts
  - apps/frontend/src/hooks/useMaintenanceProvisions.ts
  - apps/frontend/src/App.tsx
autonomous: true
requirements:
  - MANU-05
  - MANU-06
  - MANU-07
  - MANU-08
  - CCPA-03

must_haves:
  truths:
    - 'Gerente pode ver dashboard com disponibilidade mecanica, MTBF, MTTR, custo YTD e alertas'
    - 'OS kanban com 3 colunas (ABERTA, EM_ANDAMENTO, AGUARDANDO_PECA) com drag-and-drop'
    - 'Assistente de classificacao contabil em 3 passos funciona ao encerrar OS'
    - 'AssetMaintenanceTab mostra planos e historico de OS do ativo (substitui placeholder)'
    - 'Gerente pode configurar provisao de manutencao via modal'
  artifacts:
    - path: 'apps/frontend/src/pages/MaintenanceDashboardPage.tsx'
      provides: 'KPI cards + kanban + cost table + alerts'
    - path: 'apps/frontend/src/components/maintenance/WorkOrderCloseWizard.tsx'
      provides: '3-step modal: cost summary → accounting treatment → confirm'
    - path: 'apps/frontend/src/components/maintenance/MaintenanceKanban.tsx'
      provides: 'OS kanban wrapping KanbanBoard with OS columns'
    - path: 'apps/frontend/src/components/assets/AssetMaintenanceTab.tsx'
      provides: 'Real content replacing placeholder stub'
  key_links:
    - from: 'WorkOrderCloseWizard.tsx'
      to: 'useWorkOrders.ts closeWorkOrder'
      via: 'submits CloseWorkOrderInput'
      pattern: 'closeWorkOrder'
    - from: 'MaintenanceKanban.tsx'
      to: 'KanbanBoard component'
      via: 'wraps with OS column definitions'
      pattern: 'KanbanBoard|KanbanColumn'
    - from: 'MaintenanceDashboardPage.tsx'
      to: 'useMaintenanceDashboard.ts'
      via: 'hook for dashboard data'
      pattern: 'useMaintenanceDashboard'
---

<objective>
Frontend dashboard, kanban, close wizard, provisions, and AssetMaintenanceTab: completes the maintenance frontend experience.

Purpose: MANU-05 (dashboard), MANU-06 (accounting classification wizard), MANU-07 (deferral UI), MANU-08 (provision config), CCPA-03 (CC display in close wizard).
Output: Dashboard page, kanban, 3-step close wizard, provision modal, real AssetMaintenanceTab.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-manutencao-ordens-servico/18-RESEARCH.md
@.planning/phases/18-manutencao-ordens-servico/18-UI-SPEC.md
@.planning/phases/18-manutencao-ordens-servico/18-02-SUMMARY.md
@.planning/phases/18-manutencao-ordens-servico/18-03-SUMMARY.md
@.planning/phases/18-manutencao-ordens-servico/18-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkOrderCloseWizard + MaintenanceKanban + provision modal + hooks</name>
  <files>
    apps/frontend/src/components/maintenance/WorkOrderCloseWizard.tsx,
    apps/frontend/src/components/maintenance/WorkOrderCloseWizard.css,
    apps/frontend/src/components/maintenance/MaintenanceKanban.tsx,
    apps/frontend/src/components/maintenance/MaintenanceProvisionModal.tsx,
    apps/frontend/src/components/maintenance/MaintenanceProvisionModal.css,
    apps/frontend/src/hooks/useMaintenanceDashboard.ts,
    apps/frontend/src/hooks/useMaintenanceProvisions.ts
  </files>
  <read_first>
    - apps/frontend/src/components/kanban/KanbanBoard.tsx (KanbanBoard component — props, column definitions, onDragEnd)
    - apps/frontend/src/components/kanban/KanbanColumn.tsx (KanbanColumn — props, styling)
    - apps/frontend/src/components/kanban/KanbanCard.tsx (KanbanCard — props, drag handle)
    - apps/frontend/src/components/depreciation/DepreciationConfigModal.tsx (multi-step modal pattern, conditional fields with display:none)
    - apps/frontend/src/hooks/useWorkOrders.ts (closeWorkOrder handler)
    - apps/frontend/src/hooks/useAssets.ts (hook pattern for dashboard)
    - .planning/phases/18-manutencao-ordens-servico/18-UI-SPEC.md (WorkOrderCloseWizard spec — 3 steps, radio cards, stepper header, colors)
  </read_first>
  <action>
**WorkOrderCloseWizard.tsx:**
3-step modal wizard per UI-SPEC. Props: `isOpen, onClose, onSuccess, workOrder: WorkOrder`.

State: `step` (1|2|3), `accountingTreatment` (null initially), `deferralMonths` (null).

**Step 1 — Resumo de Custos:**

- Read-only cost breakdown: Pecas (totalPartsCost) + Mao de obra (totalLaborCost) + Custo externo (externalCost) = Total
- All values in JetBrains Mono (`font-family: var(--font-mono)`)
- If total < 1000: info banner "Classificacao automatica: Despesa imediata" with "Alterar classificacao" link to go to Step 2 anyway
- CTA: "Continuar para classificacao"

**Step 2 — Classificacao Contabil:**

- 3 radio card options (entire card is click target, `cursor: pointer`):
  - "Despesa imediata" — neutral card border, description: "Custo vai direto para o resultado do periodo. Use quando a manutencao nao estende a vida util do ativo."
  - "Capitalizacao" — `var(--color-sky-100)` background when selected, `var(--color-primary-600)` border 2px, description: "Custo e somado ao valor do ativo e comeca a depreciar. Use quando a manutencao restaura ou aumenta a capacidade produtiva."
  - "Diferimento" — `var(--color-earth-100)` background when selected (if this token doesn't exist, use `var(--color-neutral-200)` warm tone), description: "Custo e distribuido nos proximos meses. Use para manutencoes programadas de grande porte."
- If DIFERIMENTO selected: show numeric input "Distribuir em X meses" — toggled via `display:none`/`display:block`, NO animation per CLAUDE.md locked decision
- Radio inputs visually hidden, card is the target with `role="radio"`, `aria-checked`
- CTA: "Confirmar e encerrar"
- Focus: 2px `var(--color-primary-500)` outline on cards

**Step 3 — Confirmar Encerramento:**

- Summary: OS #sequentialNumber, asset name, classification chosen, total cost
- If CAPITALIZACAO: warning banner with `var(--color-warning-100)` bg, `var(--color-warning-500)` border-left 4px, `AlertTriangle` 20px icon, text: "O valor contabil do ativo sera atualizado para R$ X.XXX,XX. Revise a configuracao de depreciacao apos o encerramento."
- CTA: "Encerrar OS" in `var(--color-primary-600)` (not red — closing is not destructive)
- Secondary: "Voltar"
- On submit: call `closeWorkOrder(workOrder.id, { accountingTreatment, deferralMonths, closedBy: currentUser.name })`

**Stepper header:** 3 dots. Active: `var(--color-primary-600)`, completed: `var(--color-success-500)`, inactive: `var(--color-neutral-300)`.

**Wizard step transitions:** `display:none`/`display:block` — zero animation (locked decision).

**MaintenanceKanban.tsx:**
Wraps existing KanbanBoard component. Props: `workOrders: WorkOrder[], onStatusChange: (id, newStatus) => void`.

Column definitions per RESEARCH.md Pattern 6:

```
const OS_KANBAN_COLUMNS = [
  { id: 'ABERTA', label: 'Abertas', color: 'var(--color-info-500)' },
  { id: 'EM_ANDAMENTO', label: 'Em andamento', color: 'var(--color-warning-500)' },
  { id: 'AGUARDANDO_PECA', label: 'Aguardando peca', color: 'var(--color-error-500)' },
];
```

Each card: OS number (mono), asset name, type badge, age in days (computed from openedAt).
Column border-top 3px using column color.
Column count badge in header.
On drag end: call `onStatusChange(workOrderId, newStatus)` which calls `updateWorkOrder` from hook.
Per UI-SPEC: valid drops highlighted with `var(--color-primary-100)` bg + 2px `var(--color-primary-500)` border. Optimistic update + revert on error.

**MaintenanceProvisionModal.tsx:**
Single-step modal, max-width 560px. Props: `isOpen, onClose, onSuccess, provisionId?: string`.
Fields: Ativo (optional searchable selector — null = fleet-level), Valor mensal (BRL input), Centro de custo (optional selector), Descricao (textarea), Ativo toggle.
Footer: "Salvar" primary + "Cancelar".

**useMaintenanceDashboard.ts:**

- `fetchDashboard()` — GET `/api/org/work-orders/dashboard`
- State: dashboard (MaintenanceDashboard | null), loading, error
- Return { dashboard, loading, error, fetchDashboard }

**useMaintenanceProvisions.ts:**
Follow useMaintenancePlans.ts pattern:

- CRUD: fetchProvisions, createProvision, updateProvision, deleteProvision
- `fetchReconciliation(year, month)` — GET `/api/org/maintenance-provisions/reconciliation?year=&month=`
- State: provisions, reconciliation, loading, error
  </action>
  <verify>
  <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria> - WorkOrderCloseWizard.tsx contains 3 steps (step === 1, step === 2, step === 3) - WorkOrderCloseWizard.tsx contains `DESPESA` and `CAPITALIZACAO` and `DIFERIMENTO` radio options - WorkOrderCloseWizard.tsx contains `display: 'none'` or `display:none` for conditional DIFERIMENTO field (no animation) - WorkOrderCloseWizard.tsx contains warning banner text about `valor contabil` - WorkOrderCloseWizard.tsx contains `closeWorkOrder` call - MaintenanceKanban.tsx contains `KanbanBoard` import - MaintenanceKanban.tsx contains 3 column definitions (ABERTA, EM_ANDAMENTO, AGUARDANDO_PECA) - MaintenanceProvisionModal.tsx contains `monthlyAmount` or `Valor mensal` field - useMaintenanceDashboard.ts contains `fetchDashboard` - useMaintenanceProvisions.ts contains `fetchReconciliation` - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>3-step close wizard, OS kanban, provision modal, and 2 new hooks. All compiling cleanly.</done>
  </task>

<task type="auto">
  <name>Task 2: MaintenanceDashboardPage + AssetMaintenanceTab + route registration</name>
  <files>
    apps/frontend/src/pages/MaintenanceDashboardPage.tsx,
    apps/frontend/src/pages/MaintenanceDashboardPage.css,
    apps/frontend/src/components/assets/AssetMaintenanceTab.tsx,
    apps/frontend/src/App.tsx
  </files>
  <read_first>
    - apps/frontend/src/pages/DepreciationPage.tsx (pattern for KPI cards + table layout)
    - apps/frontend/src/pages/DepreciationPage.css (CSS pattern for dashboard)
    - apps/frontend/src/components/assets/AssetMaintenanceTab.tsx (current placeholder stub to replace entirely)
    - apps/frontend/src/components/assets/AssetDrawer.tsx (how tab is rendered — props passed to tab)
    - .planning/phases/18-manutencao-ordens-servico/18-UI-SPEC.md (MaintenanceDashboardPage spec, AssetMaintenanceTab spec)
    - apps/frontend/src/App.tsx (current routes)
  </read_first>
  <action>
**MaintenanceDashboardPage.tsx:**
Follow DepreciationPage.tsx layout. Per UI-SPEC:

**KPI row (4 cards):**

1. Disponibilidade Mecanica — percentage in `--text-xl` DM Sans 500, e.g. "94.2%"
2. MTBF — "X horas" in JetBrains Mono, or "N/D" with tooltip "Nao ha OS corretivas registradas para calcular este indicador." when null
3. MTTR — "X horas" in JetBrains Mono, or "N/D" with same tooltip
4. Custo YTD — BRL currency in JetBrains Mono, formatted with `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })`

For null MTBF/MTTR: display "N/D" with a `<span title="Nao ha OS corretivas registradas para calcular este indicador.">N/D</span>`.

**Overdue alerts section:** `AlertTriangle` icon 20px `var(--color-error-500)` + "X planos de manutencao vencidos" or individual list with "Manutencao vencida ha X dias".

**OS Abertas section:** Embed `MaintenanceKanban` component with open work orders.

**Custo por Ativo section:** Table with columns: Ativo | Custo Total | % do Total. Sorted by cost desc.

**Primary CTA in header:** "Ver todas as OS" link → /work-orders.

**Empty state (no data):** "Nenhuma OS ainda" + "As metricas de disponibilidade e custo aparecerao aqui assim que a primeira OS for encerrada."

**Skeleton loading** while fetching.

Register route in App.tsx:

```typescript
const MaintenanceDashboardPage = lazy(() => import('./pages/MaintenanceDashboardPage'));
<Route path="/maintenance-dashboard" element={<MaintenanceDashboardPage />} />
```

Add to Sidebar under PATRIMONIO: { label: 'Dashboard Manutencao', path: '/maintenance-dashboard', icon: BarChart3 }

**AssetMaintenanceTab.tsx:**
Replace the entire placeholder stub. Props: `assetId: string`. Per UI-SPEC:

Top right of tab: "Nova OS" button (secondary variant `var(--color-primary-600)` outline, not filled — to not compete with page-level primary) + "Novo Plano" secondary button.

- "Nova OS" opens WorkOrderModal with assetId pre-filled
- "Novo Plano" opens MaintenancePlanModal with assetId pre-filled

**Sub-section "Planos de Manutencao":**
Compact table: Nome | Gatilho | Proxima execucao | Status (ativo/inativo badge)

- Fetch from useMaintenancePlans with assetId filter

**Sub-section "Historico de OS":**
List of last 10 work orders: OS # (mono) + status badge + custo + data abertura

- Fetch from useWorkOrders with assetId filter, limit 10

**Empty state (no plans or OS):** "Nenhuma manutencao registrada" + "Abra uma OS ou configure um plano preventivo para este ativo." with small "Novo Plano" link-style button.

State management: use local `showWorkOrderModal` and `showPlanModal` booleans. On modal success, refetch both lists.
</action>
<verify>
<automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
</verify>
<acceptance_criteria> - MaintenanceDashboardPage.tsx contains `useMaintenanceDashboard` - MaintenanceDashboardPage.tsx contains 4 KPI card renderings (availability, mtbfHours, mttrHours, totalCostYTD) - MaintenanceDashboardPage.tsx contains `N/D` fallback for null metrics - MaintenanceDashboardPage.tsx contains `MaintenanceKanban` component usage - MaintenanceDashboardPage.tsx contains `AlertTriangle` for overdue alerts - MaintenanceDashboardPage.tsx contains empty state text "Nenhuma OS ainda" - AssetMaintenanceTab.tsx does NOT contain placeholder text like "Em breve" or "coming soon" - AssetMaintenanceTab.tsx contains `useMaintenancePlans` and `useWorkOrders` - AssetMaintenanceTab.tsx contains `WorkOrderModal` and `MaintenancePlanModal` - AssetMaintenanceTab.tsx contains `Nova OS` and `Novo Plano` buttons - AssetMaintenanceTab.tsx contains empty state "Nenhuma manutencao registrada" - App.tsx contains `MaintenanceDashboardPage` lazy import - App.tsx contains `/maintenance-dashboard` route - tsc --noEmit exits 0
</acceptance_criteria>
<done>Dashboard page with 4 KPI cards, kanban, cost table, alerts. AssetMaintenanceTab replaced from placeholder to real content with plans table and OS history.</done>
</task>

</tasks>

<verification>
- `tsc --noEmit` from apps/frontend exits 0
- Dashboard shows KPI cards with N/D fallback
- Close wizard enforces 3-step flow
- Kanban wraps existing KanbanBoard
- AssetMaintenanceTab shows real content
</verification>

<success_criteria>

- MaintenanceDashboardPage with 4 KPIs, kanban, cost table, alerts
- WorkOrderCloseWizard with 3 steps and all 3 accounting treatments
- MaintenanceKanban wrapping KanbanBoard with 3 OS columns
- AssetMaintenanceTab with plans and OS history (placeholder removed)
- MaintenanceProvisionModal for provision config
- Routes registered, sidebar entry added
  </success_criteria>

<output>
After completion, create `.planning/phases/18-manutencao-ordens-servico/18-05-SUMMARY.md`
</output>
