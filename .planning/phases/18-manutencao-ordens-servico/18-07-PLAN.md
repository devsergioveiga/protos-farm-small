---
phase: 18-manutencao-ordens-servico
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/main.ts
autonomous: true
gap_closure: true
requirements:
  - MANU-01
  - MANU-08

must_haves:
  truths:
    - "startMaintenanceAlertsCron() is called at server startup in non-test environment"
    - "startMaintenanceProvisionCron() is called at server startup in non-test environment"
  artifacts:
    - path: "apps/backend/src/main.ts"
      provides: "Cron registration for maintenance alerts and provisions"
      contains: "startMaintenanceAlertsCron"
  key_links:
    - from: "apps/backend/src/main.ts"
      to: "apps/backend/src/shared/cron/maintenance-alerts.cron.ts"
      via: "import + function call"
      pattern: "startMaintenanceAlertsCron\\(\\)"
    - from: "apps/backend/src/main.ts"
      to: "apps/backend/src/shared/cron/maintenance-provision.cron.ts"
      via: "import + function call"
      pattern: "startMaintenanceProvisionCron\\(\\)"
---

<objective>
Wire maintenance cron functions to server startup — closing Gaps 1 and 2 from VERIFICATION.md.

Purpose: Both `startMaintenanceAlertsCron()` and `startMaintenanceProvisionCron()` are fully implemented but never imported or called in `main.ts`, so they will never run in production. This is a 4-line fix following the exact pattern of the existing `startDigestCron()` and `startDepreciationCron()` calls.
Output: Updated main.ts with both crons registered at startup.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-manutencao-ordens-servico/18-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register maintenance crons in main.ts</name>
  <files>apps/backend/src/main.ts</files>
  <read_first>
    - apps/backend/src/main.ts
    - apps/backend/src/shared/cron/maintenance-alerts.cron.ts
    - apps/backend/src/shared/cron/maintenance-provision.cron.ts
  </read_first>
  <action>
Add two imports and two function calls to `apps/backend/src/main.ts`, following the exact pattern of the existing `startDigestCron` and `startDepreciationCron` registrations.

1. Add these two import lines after line 5 (`import { startDepreciationCron } ...`):

```typescript
import { startMaintenanceAlertsCron } from './shared/cron/maintenance-alerts.cron';
import { startMaintenanceProvisionCron } from './shared/cron/maintenance-provision.cron';
```

2. Add these four lines after line 16 (`logger.info('Depreciation cron scheduled');`), inside the `if (process.env.NODE_ENV !== 'test')` block:

```typescript
    startMaintenanceAlertsCron();
    logger.info('Maintenance alerts cron scheduled');
    startMaintenanceProvisionCron();
    logger.info('Maintenance provision cron scheduled');
```

The final file should have 6 import lines and 4 cron registrations inside the NODE_ENV block.
  </action>
  <verify>
    <automated>grep -c "startMaintenanceAlertsCron\|startMaintenanceProvisionCron" apps/backend/src/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/backend/src/main.ts contains `import { startMaintenanceAlertsCron } from './shared/cron/maintenance-alerts.cron';`
    - apps/backend/src/main.ts contains `import { startMaintenanceProvisionCron } from './shared/cron/maintenance-provision.cron';`
    - apps/backend/src/main.ts contains `startMaintenanceAlertsCron();`
    - apps/backend/src/main.ts contains `startMaintenanceProvisionCron();`
    - apps/backend/src/main.ts contains `logger.info('Maintenance alerts cron scheduled');`
    - apps/backend/src/main.ts contains `logger.info('Maintenance provision cron scheduled');`
    - grep -c returns 4 (2 imports + 2 calls)
    - `cd apps/backend && npx tsc --noEmit --pretty 2>&1 | tail -5` exits 0 (no type errors)
  </acceptance_criteria>
  <done>Both maintenance crons are registered at startup, matching the existing digest/depreciation cron pattern. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `grep "startMaintenanceAlertsCron\|startMaintenanceProvisionCron" apps/backend/src/main.ts` shows 4 matches (2 imports + 2 calls)
- Both calls are inside the `NODE_ENV !== 'test'` guard
- TypeScript compiles: `cd apps/backend && npx tsc --noEmit`
</verification>

<success_criteria>
Both `startMaintenanceAlertsCron()` and `startMaintenanceProvisionCron()` are imported and called in main.ts inside the NODE_ENV guard, following the same pattern as `startDigestCron()` and `startDepreciationCron()`.
</success_criteria>

<output>
After completion, create `.planning/phases/18-manutencao-ordens-servico/18-07-SUMMARY.md`
</output>
