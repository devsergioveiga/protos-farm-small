---
phase: 09-cota-o-e-pedido-de-compra
plan: 05
type: execute
wave: 3
depends_on: [09-03]
files_modified:
  - apps/frontend/src/types/purchase-order.ts
  - apps/frontend/src/hooks/usePurchaseOrders.ts
  - apps/frontend/src/pages/PurchaseOrdersPage.tsx
  - apps/frontend/src/pages/PurchaseOrdersPage.css
  - apps/frontend/src/components/purchase-orders/PurchaseOrderModal.tsx
  - apps/frontend/src/components/purchase-orders/PurchaseOrderModal.css
  - apps/frontend/src/components/purchase-orders/PurchaseOrderDetailModal.tsx
  - apps/frontend/src/components/purchase-orders/PurchaseOrderDetailModal.css
  - apps/frontend/src/components/layout/Sidebar.tsx
  - apps/frontend/src/App.tsx
autonomous: true
requirements: [PEDI-01]

must_haves:
  truths:
    - 'Comprador pode ver lista de pedidos com filtros, status badges, e alerta de prazo vencido'
    - 'Comprador pode criar pedido emergencial com justificativa obrigatoria'
    - 'Comprador pode duplicar pedido existente como rascunho'
    - 'Comprador pode baixar PDF do pedido'
    - 'Comprador pode enviar pedido por email (modal com email pre-preenchido)'
    - 'OC emitida mostra precos congelados sem opcao de edicao'
    - "Sidebar COMPRAS tem item 'Pedidos' linkando para /purchase-orders"
  artifacts:
    - path: 'apps/frontend/src/pages/PurchaseOrdersPage.tsx'
      provides: 'Purchase orders list page with overdue badges'
      min_lines: 100
    - path: 'apps/frontend/src/components/purchase-orders/PurchaseOrderModal.tsx'
      provides: 'Emergency PO creation modal'
      min_lines: 80
    - path: 'apps/frontend/src/components/purchase-orders/PurchaseOrderDetailModal.tsx'
      provides: 'PO detail with PDF download, email send, status transitions'
      min_lines: 100
  key_links:
    - from: 'PurchaseOrdersPage.tsx'
      to: '/api/org/purchase-orders'
      via: 'usePurchaseOrders hook'
      pattern: 'usePurchaseOrders'
    - from: 'PurchaseOrderDetailModal.tsx'
      to: '/api/org/purchase-orders/:id/pdf'
      via: 'PDF download link'
      pattern: 'purchase-orders.*pdf'
    - from: 'App.tsx'
      to: 'PurchaseOrdersPage'
      via: 'lazy route at /purchase-orders'
      pattern: 'path="/purchase-orders"'
---

<objective>
Build the complete Purchase Orders frontend: types, hooks, list page with overdue alerts, emergency PO modal, detail modal with PDF download and email send.

Purpose: Implements the PEDI-01 UI — the formal purchase document that gets sent to suppliers. Includes emergency PO creation, PDF download, and status tracking.
Output: Full purchase orders UI integrated into the app.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cota-o-e-pedido-de-compra/09-CONTEXT.md
@.planning/phases/09-cota-o-e-pedido-de-compra/09-RESEARCH.md
@.planning/phases/09-cota-o-e-pedido-de-compra/09-03-SUMMARY.md

<interfaces>
From apps/backend/src/modules/purchase-orders/purchase-orders.types.ts (backend contracts):
- OC statuses: RASCUNHO, EMITIDA, CONFIRMADA, EM_TRANSITO, ENTREGUE, CANCELADA
- CreateEmergencyPOInput: { supplierId, items[], justification, notes?, internalReference?, expectedDeliveryDate? }
- DuplicatePOInput: { sourcePurchaseOrderId, notes? }
- UpdatePOInput: { notes?, internalReference?, expectedDeliveryDate?, items[]? }
- TransitionPOInput: { status, reason? }

API endpoints:

- GET /org/purchase-orders — list with pagination + overdue filter
- POST /org/purchase-orders — create emergency PO
- POST /org/purchase-orders/duplicate — duplicate PO
- GET /org/purchase-orders/:id — detail
- GET /org/purchase-orders/:id/pdf — download PDF
- PATCH /org/purchase-orders/:id — update (RASCUNHO only)
- PATCH /org/purchase-orders/:id/transition — status change
- DELETE /org/purchase-orders/:id — soft delete

From apps/frontend/src/types/quotation.ts (just created):

- SC_STATUS_LABELS pattern for OC status labels/colors
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Types + hook + PurchaseOrdersPage + sidebar + App.tsx routing</name>
  <files>apps/frontend/src/types/purchase-order.ts, apps/frontend/src/hooks/usePurchaseOrders.ts, apps/frontend/src/pages/PurchaseOrdersPage.tsx, apps/frontend/src/pages/PurchaseOrdersPage.css, apps/frontend/src/components/layout/Sidebar.tsx, apps/frontend/src/App.tsx</files>
  <read_first>
    - apps/frontend/src/pages/QuotationsPage.tsx (just created — follow same pattern)
    - apps/frontend/src/pages/QuotationsPage.css (CSS pattern)
    - apps/frontend/src/hooks/useQuotations.ts (hook pattern)
    - apps/frontend/src/types/quotation.ts (type + status labels pattern)
    - apps/frontend/src/components/layout/Sidebar.tsx (current state — Plan 04 added Cotacoes)
    - apps/frontend/src/App.tsx (current state — Plan 04 added QuotationsPage)
    - apps/frontend/src/services/api.ts (api.get, api.post, api.getBlob pattern)
  </read_first>
  <action>
**1. Create `types/purchase-order.ts`:**
```typescript
export interface PurchaseOrder {
  id: string;
  sequentialNumber: string;
  status: 'RASCUNHO' | 'EMITIDA' | 'CONFIRMADA' | 'EM_TRANSITO' | 'ENTREGUE' | 'CANCELADA';
  isEmergency: boolean;
  emergencyJustification: string | null;
  notes: string | null;
  internalReference: string | null;
  expectedDeliveryDate: string | null;
  issuedAt: string | null;
  confirmedAt: string | null;
  cancelledAt: string | null;
  createdAt: string;
  updatedAt: string;
  isOverdue?: boolean; // computed by backend
  supplier: { id: string; name: string; tradeName: string | null };
  quotation?: { id: string; sequentialNumber: string; purchaseRequest?: { sequentialNumber: string } } | null;
  items: PurchaseOrderItem[];
  creator?: { id: string; name: string };
  _count?: { items: number };
}

export interface PurchaseOrderItem {
id: string;
productName: string;
unitName: string;
quantity: number;
unitPrice: number;
totalPrice: number;
notes: string | null;
}

export const OC_STATUS_LABELS: Record<string, string> = {
RASCUNHO: 'Rascunho',
EMITIDA: 'Emitida',
CONFIRMADA: 'Confirmada',
EM_TRANSITO: 'Em Transito',
ENTREGUE: 'Entregue',
CANCELADA: 'Cancelada',
};

export const OC_STATUS_COLORS: Record<string, string> = {
RASCUNHO: 'badge--neutral',
EMITIDA: 'badge--info',
CONFIRMADA: 'badge--success',
EM_TRANSITO: 'badge--warning',
ENTREGUE: 'badge--success',
CANCELADA: 'badge--error',
};

````

**2. Create `hooks/usePurchaseOrders.ts`:**
- `usePurchaseOrders(filters)` — GET `/org/purchase-orders` with pagination, returns { purchaseOrders, isLoading, error, total, totalPages, refetch }
- `usePurchaseOrder(id)` — GET single `/org/purchase-orders/${id}`
- `createEmergencyPO(input)` — POST to `/org/purchase-orders`
- `duplicatePO(input)` — POST to `/org/purchase-orders/duplicate`
- `updatePO(id, input)` — PATCH to `/org/purchase-orders/${id}`
- `transitionPO(id, input)` — PATCH to `/org/purchase-orders/${id}/transition`
- `deletePO(id)` — DELETE to `/org/purchase-orders/${id}`
- `downloadPOPdf(id, sequentialNumber)` — use `api.getBlob(`/org/purchase-orders/${id}/pdf`)` pattern (same as suppliers export), create download link with filename `OC-${sequentialNumber}.pdf`

**3. Create `pages/PurchaseOrdersPage.tsx` + `.css`:**
Follow QuotationsPage pattern:
- Breadcrumb: Compras > Pedidos de Compra
- Title: "Pedidos de Compra" with subtitle "Emita e acompanhe pedidos de compra aos fornecedores"
- Two CTAs (only one primary):
  - Primary: "Pedido Emergencial" button (opens PurchaseOrderModal)
  - Secondary/outline: "Ver Cotacoes" link to /quotations (not a button, just navigation)
- Filters: status dropdown (all OC statuses), search input (placeholder "Buscar por numero ou fornecedor..."), overdue toggle checkbox ("Apenas atrasados")
- Table columns: Numero (OC-YYYY/NNNN), Fornecedor (supplier name), Itens (count), Status (badge), Emissao (issuedAt formatted), Previsao Entrega (expectedDeliveryDate), Acoes
- **Overdue indicator:** If isOverdue is true, show red warning icon (AlertTriangle from lucide) next to delivery date + "Atrasado" text in red
- **Emergency indicator:** If isEmergency, show small "Emergencial" badge next to number
- Mobile: transform table to cards
- Skeleton loading, empty state with "Nenhum pedido de compra" + CTA
- Click row opens PurchaseOrderDetailModal
- Delete action on RASCUNHO only (ConfirmModal)

**4. Update `Sidebar.tsx`:**
Add to COMPRAS group items array (after Cotacoes, which was added by Plan 04):
```typescript
{ to: '/purchase-orders', icon: ClipboardList, label: 'Pedidos' },
````

Import `ClipboardList` from lucide-react.

**5. Update `App.tsx`:**
Add lazy import: `const PurchaseOrdersPage = lazy(() => import('@/pages/PurchaseOrdersPage'));`
Add route after quotations: `<Route path="/purchase-orders" element={<PurchaseOrdersPage />} />`
</action>
<verify>
<automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/frontend && npx tsc --noEmit --pretty 2>&1 | tail -3</automated>
</verify>
<acceptance_criteria> - File exists: apps/frontend/src/types/purchase-order.ts with `export interface PurchaseOrder` - File exists: apps/frontend/src/hooks/usePurchaseOrders.ts with `export function usePurchaseOrders` - File exists: apps/frontend/src/pages/PurchaseOrdersPage.tsx with breadcrumb "Compras" > "Pedidos de Compra" - File exists: apps/frontend/src/pages/PurchaseOrdersPage.css - PurchaseOrdersPage.tsx contains `isOverdue` (overdue indicator logic) - PurchaseOrdersPage.tsx contains `isEmergency` (emergency badge logic) - Sidebar.tsx contains `{ to: '/purchase-orders'` in COMPRAS group - App.tsx contains `const PurchaseOrdersPage = lazy` - App.tsx contains `path="/purchase-orders"` - No TypeScript errors
</acceptance_criteria>
<done>Purchase orders list page with overdue alerts, emergency badge, sidebar item, and routing all operational</done>
</task>

<task type="auto">
  <name>Task 2: PurchaseOrderModal (emergency) + PurchaseOrderDetailModal (detail + PDF + email + transitions)</name>
  <files>apps/frontend/src/components/purchase-orders/PurchaseOrderModal.tsx, apps/frontend/src/components/purchase-orders/PurchaseOrderModal.css, apps/frontend/src/components/purchase-orders/PurchaseOrderDetailModal.tsx, apps/frontend/src/components/purchase-orders/PurchaseOrderDetailModal.css</files>
  <read_first>
    - apps/frontend/src/components/quotations/QuotationModal.tsx (modal pattern)
    - apps/frontend/src/components/quotations/QuotationDetailModal.tsx (detail modal pattern)
    - apps/frontend/src/hooks/usePurchaseOrders.ts (just created — createEmergencyPO, duplicatePO, downloadPOPdf, transitionPO, updatePO)
    - apps/frontend/src/types/purchase-order.ts (PurchaseOrder, PurchaseOrderItem, OC_STATUS_LABELS)
    - apps/frontend/src/components/ui/ConfirmModal.tsx (for cancel action)
    - apps/frontend/src/services/api.ts (api.getBlob pattern)
  </read_first>
  <action>
**1. Create `PurchaseOrderModal.tsx` + `.css`:**
Modal for creating emergency PO (no SC):

Props: `{ isOpen: boolean; onClose: () => void; onSuccess: () => void }`

Title: "Pedido Emergencial"
Warning banner at top: AlertTriangle icon + "Pedidos emergenciais pulam o processo de cotacao. Justifique o motivo."

Fields:

- Supplier selection: dropdown listing active suppliers (fetch from `/org/suppliers?status=ACTIVE&limit=100`), show name + CNPJ truncated
- Justification: textarea (REQUIRED, minLength 10) — label "Justificativa \*" with helper "Explique por que este pedido nao pode passar por cotacao"
- Expected delivery date: date input (optional)
- Internal reference: text input (optional) — label "Referencia interna"
- Notes: textarea (optional)
- Items section:
  - Dynamic item rows with "Adicionar item" button
  - Each row: productName (text input), unitName (text input), quantity (number input), unitPrice (currency input formatted BRL on blur)
  - Remove row button (Trash2 icon) — minimum 1 row
  - Row total: quantity \* unitPrice (auto-computed, displayed read-only)
  - Grand total at bottom

Validation: supplier required, justification required (min 10 chars), at least 1 item with productName + quantity + unitPrice > 0
Submit: POST createEmergencyPO, on success call onSuccess + toast "Pedido emergencial criado com sucesso"

**2. Create `PurchaseOrderDetailModal.tsx` + `.css`:**
Detail modal for viewing and managing OC:

Props: `{ isOpen: boolean; purchaseOrderId: string | null; onClose: () => void; onUpdate: () => void }`

**Header section:**

- OC number (e.g., "OC-2026/0001") + status badge + emergency badge if applicable
- Supplier name + CNPJ
- Dates: Emissao, Confirmacao, Previsao Entrega (with overdue warning if past due)
- SC/RC reference links (if from quotation)

**Action buttons (based on status):**

- RASCUNHO: "Emitir" (transitions to EMITIDA via transitionPO), "Editar" (inline edit mode), "Excluir" (ConfirmModal)
- EMITIDA: "Confirmar Recebimento Fornecedor" (-> CONFIRMADA), "Baixar PDF", "Enviar por Email", "Duplicar", "Cancelar" (ConfirmModal with reason)
- CONFIRMADA: "Marcar Em Transito" (-> EM_TRANSITO), "Baixar PDF", "Cancelar"
- EM_TRANSITO: "Marcar Entregue" (-> ENTREGUE), "Baixar PDF", "Cancelar"
- ENTREGUE: "Baixar PDF", "Duplicar"
- CANCELADA: "Duplicar" (creates new from cancelled)

**Items table (always visible):**

- Columns: Produto, Unidade, Quantidade, Preco Unitario (R$), Total (R$)
- All prices formatted BRL with Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })
- If status !== RASCUNHO: show "(precos congelados)" label above table
- Grand total row at bottom

**Edit mode (RASCUNHO only):**

- Inline editing of items (same as PurchaseOrderModal item rows)
- "Salvar" button calls updatePO, on success refetch + toast

**PDF download:**

- "Baixar PDF" button calls downloadPOPdf(id, sequentialNumber)
- Button shows loading spinner during download

**Email send modal (inline section, not separate modal):**

- Triggered by "Enviar por Email" button
- Shows: email input pre-filled with supplier.email (if available), subject "Pedido de Compra {sequentialNumber}", message textarea with default template
- "Enviar" button: for now, placeholder — show toast "Funcionalidade de envio sera implementada em breve" (email is placeholder per CONTEXT.md: "Envio real pode ser placeholder inicialmente")
- This is per Claude's discretion from CONTEXT.md

**Duplicate action:**

- "Duplicar" button calls duplicatePO({ sourcePurchaseOrderId: id })
- On success: toast "Pedido duplicado como rascunho — OC-{newNumber}", call onUpdate

**Status transitions:**

- Each transition button calls transitionPO with appropriate status
- Cancel requires ConfirmModal with variant="danger", title "Cancelar pedido?", message "Esta acao nao pode ser desfeita."
- On success: refetch + toast with status-appropriate message (e.g., "Pedido emitido com sucesso", "Pedido confirmado pelo fornecedor")
  </action>
  <verify>
  <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/frontend && npx tsc --noEmit --pretty 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria> - File exists: apps/frontend/src/components/purchase-orders/PurchaseOrderModal.tsx - File exists: apps/frontend/src/components/purchase-orders/PurchaseOrderModal.css - File exists: apps/frontend/src/components/purchase-orders/PurchaseOrderDetailModal.tsx - File exists: apps/frontend/src/components/purchase-orders/PurchaseOrderDetailModal.css - PurchaseOrderModal.tsx contains "Pedido Emergencial" title - PurchaseOrderModal.tsx contains justification validation (required field) - PurchaseOrderDetailModal.tsx contains `downloadPOPdf` or `pdf` (PDF download) - PurchaseOrderDetailModal.tsx contains "precos congelados" or similar frozen price indicator - PurchaseOrderDetailModal.tsx contains `duplicatePO` (duplicate action) - PurchaseOrderDetailModal.tsx contains `transitionPO` (status transitions) - PurchaseOrderDetailModal.tsx contains `ConfirmModal` (cancel confirmation) - No TypeScript errors
  </acceptance_criteria>
  <done>Emergency PO creation modal and PO detail modal with PDF download, email placeholder, duplication, and status transitions all operational</done>
  </task>

</tasks>

<verification>
- `npx tsc --noEmit` — no TypeScript errors in frontend
- PurchaseOrdersPage renders with sidebar link and route
- Emergency PO modal validates justification
- Detail modal shows frozen prices, PDF download, and status transitions
</verification>

<success_criteria>
Full purchase orders UI operational: buyer can browse OCs, create emergency POs, view details with frozen prices, download PDF, duplicate, and transition status.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cota-o-e-pedido-de-compra/09-05-SUMMARY.md`
</output>
