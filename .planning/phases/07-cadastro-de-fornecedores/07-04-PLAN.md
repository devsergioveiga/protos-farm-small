---
phase: 07-cadastro-de-fornecedores
plan: 04
type: execute
wave: 3
depends_on: ['07-02', '07-03']
files_modified:
  - apps/frontend/src/components/suppliers/SupplierImportModal.tsx
  - apps/frontend/src/components/suppliers/SupplierImportModal.css
  - apps/frontend/src/components/suppliers/SupplierRatingModal.tsx
  - apps/frontend/src/components/suppliers/SupplierRatingModal.css
  - apps/frontend/src/hooks/useSupplierRating.ts
  - apps/frontend/src/pages/SuppliersPage.tsx
  - apps/frontend/src/pages/SuppliersPage.css
autonomous: false
requirements:
  - FORN-02
  - FORN-03

must_haves:
  truths:
    - 'User can import suppliers via CSV/XLSX with preview showing valid/invalid/skip rows'
    - 'User can download a CSV import template'
    - 'User can export filtered supplier list as CSV (immediate download)'
    - 'User can export filtered supplier list as PDF (immediate download)'
    - 'User can rate a supplier on 4 criteria (1-5 stars) with optional comment'
    - 'User can see rating history for a supplier'
    - 'User can see Top 3 suppliers by category in cards above the table'
    - 'Top Fornecedores section updates when category selector changes'
  artifacts:
    - path: 'apps/frontend/src/components/suppliers/SupplierImportModal.tsx'
      provides: 'Import modal with drag-drop + preview table'
      min_lines: 150
    - path: 'apps/frontend/src/components/suppliers/SupplierRatingModal.tsx'
      provides: 'Rating modal with 4-criteria star selector'
      min_lines: 100
    - path: 'apps/frontend/src/hooks/useSupplierRating.ts'
      provides: 'Rating submission and history hook'
      exports: ['useSupplierRating']
  key_links:
    - from: 'SuppliersPage.tsx'
      to: 'SupplierImportModal'
      via: 'import + conditional render'
      pattern: 'SupplierImportModal'
    - from: 'SuppliersPage.tsx'
      to: 'SupplierRatingModal'
      via: 'import + conditional render'
      pattern: 'SupplierRatingModal'
    - from: 'SuppliersPage.tsx'
      to: '/api/org/suppliers/top3'
      via: 'fetch in useEffect for Top 3 section'
      pattern: 'top3'
    - from: 'SuppliersPage.tsx'
      to: '/api/org/suppliers/export/csv'
      via: 'download link'
      pattern: 'export/csv'
---

<objective>
Complete the frontend with import modal, rating modal, Top 3 ranking section, and export actions. Wire all remaining buttons on SuppliersPage.

Purpose: Complete FORN-02 (import/export UI) and FORN-03 (rating/ranking UI) to finish the phase.
Output: Fully functional supplier management page with all features.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-cadastro-de-fornecedores/07-CONTEXT.md
@.planning/phases/07-cadastro-de-fornecedores/07-UI-SPEC.md
@.planning/phases/07-cadastro-de-fornecedores/07-02-SUMMARY.md
@.planning/phases/07-cadastro-de-fornecedores/07-03-SUMMARY.md

<interfaces>
<!-- From Plan 03 outputs -->
```typescript
// types/supplier.ts
export interface Supplier { id, type, name, tradeName, document, ..., averageRating?, ratingCount?, categories }
export interface SupplierRating { id, deadline, quality, price, service, comment?, ratedBy, createdAt, average }
export type SupplierCategory = 'INSUMO_AGRICOLA' | 'PECUARIO' | ...;
export const SUPPLIER_CATEGORY_LABELS: Record<SupplierCategory, string>;

// hooks/useSupplierForm.ts
export function useSupplierForm(onSuccess: () => void): {
createSupplier, updateSupplier, deleteSupplier, isSubmitting, error
}

// hooks/useSuppliers.ts
export function useSuppliers(params): { suppliers, total, isLoading, error, refetch }

```

<!-- Backend API endpoints from Plan 02 -->
```

POST /api/org/suppliers/import/preview → { valid, invalid, existing }
POST /api/org/suppliers/import/execute → { imported, skipped, failed, errors }
GET /api/org/suppliers/import/template → CSV file download
GET /api/org/suppliers/export/csv → CSV file download
GET /api/org/suppliers/export/pdf → PDF file download
POST /api/org/suppliers/:id/ratings → { id, deadline, quality, price, service, comment, ... }
GET /api/org/suppliers/:id/ratings → SupplierRating[]
GET /api/org/suppliers/top3?category=X → Supplier[] (max 3, with averageRating, ratingCount)

````
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import modal + export actions + rating hook</name>
  <files>
    apps/frontend/src/components/suppliers/SupplierImportModal.tsx,
    apps/frontend/src/components/suppliers/SupplierImportModal.css,
    apps/frontend/src/hooks/useSupplierRating.ts
  </files>
  <read_first>
    apps/frontend/src/pages/SuppliersPage.tsx,
    apps/frontend/src/types/supplier.ts,
    apps/frontend/src/hooks/useSupplierForm.ts,
    apps/frontend/src/styles/tokens.css,
    .planning/phases/07-cadastro-de-fornecedores/07-UI-SPEC.md
  </read_first>
  <action>
**1. SupplierImportModal.tsx** — Two-step modal per UI-SPEC.md:

Props: `isOpen: boolean`, `onClose: () => void`, `onSuccess: () => void`.
Width: 640px max on desktop.
Header: "Importar Fornecedores" + close button.

**Step 1 (Upload):**
- Drag-and-drop zone: 180px height, dashed 2px border `var(--color-neutral-300)`, Upload icon 48px `var(--color-neutral-300)`.
- Text: "Arraste o arquivo aqui ou clique para selecionar".
- Subtitle: "CSV ou Excel (.xlsx) . Maximo 5 MB".
- Hidden `<input type="file" accept=".csv,.xlsx">` triggered on zone click.
- "Baixar modelo de importacao" link below: Download icon 16px + text, `var(--color-sky-500)`. On click: `window.open('/api/org/suppliers/import/template')`.
- onDrop/onChange: send file to `POST /api/org/suppliers/import/preview` as FormData. Show spinner inside modal during parsing.
- On error: show error message "Nao foi possivel processar o arquivo. Verifique o formato e tente novamente." in `var(--color-error-500)`.

**Step 2 (Preview):**
- Summary bar: "X fornecedores prontos para importar" (green count) + "Y linhas com erro (serao ignoradas)" (red count) + "Z CNPJ/CPF ja cadastrados (serao ignorados)" (neutral count).
- Scrollable `<table>` with max-height 400px:
  - Headers: #, Nome, CNPJ/CPF, Categorias, Status.
  - Valid rows: `var(--color-success-100)` bg + CheckCircle2 icon.
  - Invalid rows: `var(--color-error-100)` bg + XCircle icon + error text in `--text-sm`.
  - Skip rows (existing CNPJ): `var(--color-neutral-100)` bg + MinusCircle icon + "CNPJ ja cadastrado" label.
  - `<caption>` describing content. `<th scope="col">` on headers.
- Footer: "Cancelar" (ghost) + back arrow button to re-upload + "Importar X fornecedores" (primary, disabled if X=0).
- On confirm: `POST /api/org/suppliers/import/execute` with same file. Loading state on button.
- Success: close modal + toast. If skipped > 0: "X fornecedores importados. Y ja existiam e foram ignorados." Else: "X fornecedores importados com sucesso."
- Error: show error inside modal, keep open.

Modal: `role="dialog"`, `aria-modal="true"`, focus trap, Escape to close. Animation 300ms ease-out open, 200ms ease-in close.

**2. SupplierImportModal.css** — Styles per UI-SPEC spacing and color contracts.

**3. hooks/useSupplierRating.ts:**

```typescript
export function useSupplierRating(onSuccess: () => void) {
  // submitRating(supplierId: string, input: { deadline, quality, price, service, comment? })
  //   → POST /api/org/suppliers/:id/ratings
  //   → onSuccess on 201

  // getRatingHistory(supplierId: string)
  //   → GET /api/org/suppliers/:id/ratings
  //   → returns SupplierRating[]

  // State: isSubmitting, error, ratings, isLoadingRatings

  return { submitRating, getRatingHistory, ratings, isSubmitting, isLoadingRatings, error };
}
````

Follow existing hook pattern: `useAuth()` for token, fetch with Authorization header.
</action>
<verify>
<automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
</verify>
<acceptance_criteria> - SupplierImportModal.tsx contains `Importar Fornecedores` title - SupplierImportModal.tsx contains `Arraste o arquivo` text - SupplierImportModal.tsx contains `Baixar modelo` link - SupplierImportModal.tsx contains `/import/preview` fetch call - SupplierImportModal.tsx contains `/import/execute` fetch call - SupplierImportModal.tsx contains `role="dialog"` - SupplierImportModal.tsx contains `aria-modal="true"` - SupplierImportModal.tsx contains `CheckCircle2` and `XCircle` icons - useSupplierRating.ts contains `export function useSupplierRating(` - useSupplierRating.ts contains `/ratings` in fetch URL - TypeScript compiles without errors
</acceptance_criteria>
<done>Import modal with drag-drop upload and preview table works. Rating hook ready for submission and history fetch.</done>
</task>

<task type="auto">
  <name>Task 2: Rating modal + Top 3 section + wire all SuppliersPage actions</name>
  <files>
    apps/frontend/src/components/suppliers/SupplierRatingModal.tsx,
    apps/frontend/src/components/suppliers/SupplierRatingModal.css,
    apps/frontend/src/pages/SuppliersPage.tsx,
    apps/frontend/src/pages/SuppliersPage.css
  </files>
  <read_first>
    apps/frontend/src/pages/SuppliersPage.tsx,
    apps/frontend/src/pages/SuppliersPage.css,
    apps/frontend/src/components/suppliers/SupplierImportModal.tsx,
    apps/frontend/src/hooks/useSupplierRating.ts,
    apps/frontend/src/types/supplier.ts,
    apps/frontend/src/styles/tokens.css,
    .planning/phases/07-cadastro-de-fornecedores/07-UI-SPEC.md
  </read_first>
  <action>
**1. SupplierRatingModal.tsx** — Per UI-SPEC.md:

Props: `isOpen: boolean`, `onClose: () => void`, `onSuccess: () => void`, `supplier: Supplier`.
Width: 480px max on desktop, full-width on mobile.

- Header: "Avaliar Fornecedor" + supplier name below in neutral-500.
- Body: 4 criteria rows, each with:
  - Label: "Prazo de Entrega", "Qualidade do Produto", "Preco", "Atendimento".
  - 5 interactive stars, 24px each. Unselected: `var(--color-neutral-300)`. Selected/hovered: `var(--color-sun-500)`.
  - Each star is a `<button>` with `role="radio"` inside a `<div role="radiogroup" aria-label="{criterion label}">`. Each button gets `aria-label="{N} estrelas"`.
  - Selected value text "(N/5)" in `--text-sm` neutral-500.
- Comment textarea: label "Comentario (opcional)", 3 rows, placeholder "Descreva sua experiencia com este fornecedor".
- Rating history: link "Ver historico de avaliacoes" below comment. On click, fetch ratings via `useSupplierRating.getRatingHistory(supplier.id)` and render inline list below:
  - Each item: date (`--text-sm` neutral-500), 4 mini star rows (12px stars), comment (`--text-sm` neutral-600), divider.
  - Criteria labels in `--text-sm` neutral-500.
- Footer: "Cancelar" (ghost) + "Registrar Avaliacao" (primary). Primary disabled until all 4 criteria have values.
- On submit: call `useSupplierRating.submitRating`. Success: close + toast "Avaliacao registrada com sucesso" + onSuccess.
- Modal: `role="dialog"`, `aria-modal="true"`, focus trap, Escape to close.

**2. SupplierRatingModal.css** — Star styling, history list, responsive.

**3. SuppliersPage.tsx updates** — Wire all remaining actions:

**(a) Top Fornecedores section** — Replace placeholder with full implementation:

- Heading: "Top Fornecedores" (H2, DM Sans 500, `--text-lg`).
- Category `<select>` (200px width) with all SupplierCategory options.
- Fetch `GET /api/org/suppliers/top3?category={selected}` in useEffect when category changes.
- Render 3 cards side-by-side (desktop), stacked (mobile <768px):
  - Rank badge: #1 Trophy icon `var(--color-sun-500)`, #2 Medal icon `var(--color-neutral-400)`, #3 Medal icon `var(--color-earth-500)`.
  - Supplier name: DM Sans 500, `--text-base`.
  - 5 stars, 16px, filled up to averageRating.
  - Average: `--text-lg` (20px), DM Sans 700.
  - Count: "(N avaliacoes)" `--text-sm` neutral-500.
- Empty state: "Sem avaliacoes nesta categoria" + "Avalie fornecedores apos receber pedidos para ver o ranking."
- Section background: `var(--color-neutral-50)`, padding 24px, `--radius-md`.
- Card hover: translateY(0 -> -2px) + shadow-md -> shadow-lg, 200ms ease-out.

**(b) Wire Import button** — Open SupplierImportModal. Import the component. On success: refetch supplier list.

**(c) Wire Export CSV** — On click: `window.open('/api/org/suppliers/export/csv?{current_filters}')`. Include auth token as query param or use fetch + blob download pattern:

```typescript
const res = await fetch(`/api/org/suppliers/export/csv?${queryString}`, {
  headers: { Authorization: `Bearer ${token}` },
});
const blob = await res.blob();
const url = URL.createObjectURL(blob);
const a = document.createElement('a');
a.href = url;
a.download = 'fornecedores.csv';
a.click();
```

**(d) Wire Export PDF** — Same blob download pattern for `/api/org/suppliers/export/pdf?${queryString}`. Loading state on button during generation.

**(e) Wire Star (rate) action** — Table action button opens SupplierRatingModal with selected supplier. On success: refetch list + refetch top3.

**(f) SuppliersPage.css updates** — Add styles for Top 3 cards, rank badges, star rows.
</action>
<verify>
<automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/frontend && npx tsc --noEmit && npx vite build 2>&1 | tail -10</automated>
</verify>
<acceptance_criteria> - SupplierRatingModal.tsx contains `Avaliar Fornecedor` title - SupplierRatingModal.tsx contains `role="radiogroup"` for star selectors - SupplierRatingModal.tsx contains `Registrar Avaliacao` button text - SupplierRatingModal.tsx contains `Ver historico` link text - SuppliersPage.tsx contains `SupplierImportModal` import - SuppliersPage.tsx contains `SupplierRatingModal` import - SuppliersPage.tsx contains `top3` in a fetch URL - SuppliersPage.tsx contains `export/csv` in a fetch URL or window.open - SuppliersPage.tsx contains `export/pdf` in a fetch URL or window.open - SuppliersPage.tsx contains `Trophy` or `Medal` icon import for ranking - SuppliersPage.tsx contains `Top Fornecedores` heading - TypeScript compiles without errors - Vite build succeeds
</acceptance_criteria>
<done>Rating modal works with 4-criteria star selector and history. Top 3 section shows ranked suppliers by category. Import/export/rate buttons all wired on SuppliersPage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete supplier management flow</name>
  <what-built>Complete supplier management module: CRUD, import CSV/XLSX, export CSV/PDF, rating with Top 3 ranking, sidebar navigation.</what-built>
  <how-to-verify>
    1. Navigate to http://localhost:5173/suppliers — verify COMPRAS group in sidebar, page loads with empty state
    2. Click "+ Novo Fornecedor" — verify modal opens with PF/PJ toggle, all 7 sections visible
    3. Toggle PF/PJ — verify fields adapt (CNPJ vs CPF, IE hides for PF)
    4. Enter invalid CNPJ — verify inline error "CNPJ invalido" on blur
    5. Fill valid data and save — verify toast "Fornecedor cadastrado com sucesso", table shows supplier
    6. Try creating duplicate CNPJ — verify "Este CNPJ ja esta cadastrado" error with link
    7. Edit supplier (pencil icon) — verify modal pre-fills data, save shows "atualizado" toast
    8. Use search filter — verify results filter by name/CNPJ
    9. Use category and status dropdowns — verify filtering works
    10. Click Star icon on supplier — verify rating modal opens, select stars, submit
    11. Verify Top Fornecedores section shows rated supplier after rating
    12. Click "Importar" — verify modal with drag-drop zone
    13. Download template — verify CSV file downloads
    14. Click "Exportar CSV" — verify CSV downloads with data
    15. Click "Exportar PDF" — verify PDF downloads with org header and table
    16. Delete supplier (trash icon) — verify ConfirmModal, then "Fornecedor excluido" toast
    17. Resize to mobile (<768px) — verify table becomes cards, modal is full-width
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
cd apps/frontend && npx tsc --noEmit && npx vite build
cd apps/backend && npx jest --testPathPattern=suppliers --no-coverage
</verification>

<success_criteria>

- Import modal: drag-drop upload, preview with valid/invalid/skip rows, execute with report
- Export: CSV with BOM downloads, PDF with org header downloads
- Rating modal: 4 criteria star selectors, comment, history inline, accessible
- Top 3 section: category selector, 3 ranked cards with Trophy/Medal icons
- All buttons on SuppliersPage wired and functional
- Human verified end-to-end flow
  </success_criteria>

<output>
After completion, create `.planning/phases/07-cadastro-de-fornecedores/07-04-SUMMARY.md`
</output>
