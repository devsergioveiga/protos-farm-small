---
phase: 17-engine-de-deprecia-o
plan: '00'
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/backend/src/modules/depreciation/depreciation-engine.spec.ts
  - apps/backend/src/modules/depreciation/depreciation-batch.spec.ts
  - apps/backend/src/modules/depreciation/depreciation.routes.spec.ts
autonomous: true
requirements: [DEPR-01, DEPR-02, CCPA-01, CCPA-02]

must_haves:
  truths:
    - 'Failing test stubs exist for depreciation-engine (arithmetic), depreciation-batch (batch processing), and depreciation.routes (API endpoints)'
    - 'Running pnpm test --testPathPattern=depreciation shows all stubs as failing (red phase of TDD)'
  artifacts:
    - path: 'apps/backend/src/modules/depreciation/depreciation-engine.spec.ts'
      provides: 'Failing stubs for engine arithmetic: straight-line, pro-rata, residual clamping, accelerated, hours-of-use, units-of-production'
      min_lines: 40
    - path: 'apps/backend/src/modules/depreciation/depreciation-batch.spec.ts'
      provides: 'Failing stubs for batch: eligible asset query, idempotent re-run, CC item creation, reversal'
      min_lines: 30
    - path: 'apps/backend/src/modules/depreciation/depreciation.routes.spec.ts'
      provides: 'Failing stubs for routes: config CRUD, run trigger, reversal, report with assetId filter, export'
      min_lines: 30
  key_links: []
---

<objective>
Create failing test stub files for the depreciation module — engine arithmetic, batch processor, and API routes. These stubs define the behavioral contract before any implementation begins (Nyquist Wave 0).

Purpose: Establishes the RED phase of TDD. All three spec files must exist with descriptive test names that fail (no implementation yet). Plans 01 and 02 will make them green.
Output: Three spec files with failing `it.todo()` or `it()` stubs.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-engine-de-deprecia-o/17-RESEARCH.md
@.planning/phases/17-engine-de-deprecia-o/17-VALIDATION.md

<interfaces>
<!-- Test pattern from existing spec files -->
From apps/backend/src/modules/assets/assets.routes.spec.ts:
```typescript
import request from 'supertest';
import { app } from '../../app';
// jest.mock('../../database/prisma', () => ({ prisma: { ... } }));
// describe('Asset Routes', () => { ... });
```

From apps/backend/jest.config.ts:

```typescript
// Jest with @swc/jest transform, testPathPattern matching
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stubs for depreciation module</name>
  <files>
    apps/backend/src/modules/depreciation/depreciation-engine.spec.ts,
    apps/backend/src/modules/depreciation/depreciation-batch.spec.ts,
    apps/backend/src/modules/depreciation/depreciation.routes.spec.ts
  </files>
  <read_first>
    - apps/backend/src/modules/assets/assets.routes.spec.ts (test file pattern: imports, jest.mock, describe structure)
    - apps/backend/jest.config.ts (test runner config)
  </read_first>
  <action>
Create the `apps/backend/src/modules/depreciation/` directory if it doesn't exist.

**1. Create `depreciation-engine.spec.ts`** with `it.todo()` stubs:

```typescript
describe('Depreciation Engine', () => {
  describe('daysInMonth', () => {
    it.todo('returns 29 for February 2024 (leap year)');
    it.todo('returns 28 for February 2025');
    it.todo('returns 31 for January');
    it.todo('returns 30 for April');
  });

  describe('getProRataDays', () => {
    it.todo('returns full month days when no acquisition in period');
    it.todo('returns partial days for mid-month acquisition (Jan 15 = 17 days)');
    it.todo('returns partial days for disposal on day 10');
  });

  describe('computeDepreciation', () => {
    it.todo('STRAIGHT_LINE with rate: 120000 value, 10% rate, full month = 1000.00');
    it.todo('STRAIGHT_LINE with usefulLifeMonths: 60000 / 120 months = 500.00');
    it.todo('STRAIGHT_LINE pro-rata: Jan 15 acquisition, 17/31 days = 548.39');
    it.todo('stops at residual: remaining 500, monthly would be 1000 = amount 500');
    it.todo('fully depreciated: openingBookValue equals residualValue = skipped');
    it.todo('ACCELERATED: 100000 book value, 10% rate, factor 2.0 = 1666.67');
    it.todo('HOURS_OF_USE without periodicHours = skipped');
    it.todo('HOURS_OF_USE with periodicHours: 100000 / 10000h * 150h = 1500.00');
    it.todo('UNITS_OF_PRODUCTION without periodicUnits = skipped');
  });
});
```

**2. Create `depreciation-batch.spec.ts`** with `it.todo()` stubs:

```typescript
describe('Depreciation Batch Service', () => {
  describe('runDepreciationBatch', () => {
    it.todo('processes eligible DEPRECIABLE_CPC27 assets and creates entries');
    it.todo('skips EM_ANDAMENTO assets');
    it.todo('skips assets without DepreciationConfig');
    it.todo('catches P2002 and marks as skipped (idempotent re-run)');
    it.todo('creates DepreciationRun with correct counts');
    it.todo('creates DepreciationEntryCCItem for asset with costCenterId');
    it.todo('reconciliation: sum(ccItems.amount) === depreciationAmount');
    it.todo('rejects duplicate run when force=false (409)');
    it.todo('allows force re-run when force=true');
    it.todo('handles empty organizationId by querying all organizations');
  });

  describe('reverseEntry', () => {
    it.todo('creates reversal entry with negative amounts');
    it.todo('marks original entry with reversedAt');
    it.todo('rejects already-reversed entry with 400');
    it.todo('deletes CC items of original entry');
  });
});
```

**3. Create `depreciation.routes.spec.ts`** with `it.todo()` stubs:

```typescript
describe('Depreciation Routes', () => {
  describe('POST /config', () => {
    it.todo('creates config for depreciable asset (201)');
    it.todo('rejects non-depreciable asset (400)');
    it.todo('rejects duplicate config (409)');
  });

  describe('GET /config/:assetId', () => {
    it.todo('returns config (200)');
    it.todo('returns 404 when not found');
  });

  describe('PATCH /config/:assetId', () => {
    it.todo('updates method and rates (200)');
  });

  describe('DELETE /config/:assetId', () => {
    it.todo('deletes config (204)');
  });

  describe('POST /run', () => {
    it.todo('triggers batch for period (202)');
    it.todo('rejects duplicate run without force (409)');
    it.todo('allows force re-run (202)');
  });

  describe('POST /entries/:entryId/reverse', () => {
    it.todo('creates reversal (200)');
    it.todo('rejects already reversed (400)');
  });

  describe('GET /report', () => {
    it.todo('returns paginated entries (200)');
    it.todo('returns empty array when no entries (200)');
    it.todo('filters by assetId when query param provided (200)');
  });

  describe('GET /last-run', () => {
    it.todo('returns latest run (200)');
    it.todo('returns null when no run (200)');
  });

  describe('GET /report/export', () => {
    it.todo('exports CSV with correct headers');
    it.todo('exports XLSX with formatted columns');
  });
});
```

All stubs use `it.todo()` which Jest reports as "todo" (not failure) but clearly documents the behavioral contract. No implementation imports needed — these are pure contract definitions.
</action>
<verify>
<automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/backend && pnpm test -- --testPathPattern=depreciation --passWithNoTests 2>&1 | tail -10</automated>
</verify>
<done>Three spec files exist with all behavioral stubs. Running tests shows todo items. Wave 0 is complete — Plans 01 and 02 will implement against these contracts.</done>
</task>

</tasks>

<verification>
- `ls apps/backend/src/modules/depreciation/*.spec.ts` shows 3 files
- `pnpm test -- --testPathPattern=depreciation --passWithNoTests` exits 0
- Each spec file contains `describe` blocks matching the module structure
</verification>

<success_criteria>

- depreciation-engine.spec.ts exists with 12+ it.todo stubs
- depreciation-batch.spec.ts exists with 14+ it.todo stubs
- depreciation.routes.spec.ts exists with 18+ it.todo stubs
- No implementation code in any stub file
  </success_criteria>

<output>
After completion, create `.planning/phases/17-engine-de-deprecia-o/17-00-SUMMARY.md`
</output>
