---
phase: 11-devolu-o-or-amento-e-saving
plan: 06
type: execute
wave: 3
depends_on: [11-03, 11-04]
files_modified:
  - apps/frontend/src/pages/OrcamentoComprasPage.tsx
  - apps/frontend/src/pages/OrcamentoComprasPage.css
  - apps/frontend/src/pages/SavingAnalysisPage.tsx
  - apps/frontend/src/pages/SavingAnalysisPage.css
  - apps/frontend/src/hooks/usePurchaseBudgets.ts
  - apps/frontend/src/hooks/useSavingAnalysis.ts
autonomous: true
requirements: [FINC-02, FINC-03]

must_haves:
  truths:
    - 'Gerente financeiro pode ver tabela de orcamento com barras de progresso por categoria'
    - 'Gerente pode criar/editar orcamento por categoria e periodo'
    - 'Barras de progresso mostram orcado vs comprado com % e cores (verde < 80%, amarelo 80-100%, vermelho > 100%)'
    - 'Gerente pode ver saving por cotacao com detalhamento por item'
    - 'Gerente pode ver historico de preco por produto em grafico de linha Recharts'
    - 'Gerente pode ver indicadores de ciclo (% formal, % emergencial, prazo medio)'
    - 'Gerente pode ver top 10 produtos e top 5 fornecedores'
  artifacts:
    - path: 'apps/frontend/src/pages/OrcamentoComprasPage.tsx'
      provides: 'Budget execution table with progress bars'
    - path: 'apps/frontend/src/pages/SavingAnalysisPage.tsx'
      provides: 'KPI cards + Recharts charts + top tables'
    - path: 'apps/frontend/src/hooks/usePurchaseBudgets.ts'
      provides: 'API hooks for budgets'
    - path: 'apps/frontend/src/hooks/useSavingAnalysis.ts'
      provides: 'API hooks for saving analytics'
  key_links:
    - from: 'OrcamentoComprasPage.tsx'
      to: '/api/org/purchase-budgets'
      via: 'usePurchaseBudgets hook'
      pattern: 'usePurchaseBudgets'
    - from: 'SavingAnalysisPage.tsx'
      to: '/api/org/saving-analysis'
      via: 'useSavingAnalysis hook'
      pattern: 'useSavingAnalysis'
    - from: 'SavingAnalysisPage.tsx'
      to: 'recharts'
      via: 'LineChart, BarChart imports'
      pattern: "from 'recharts'"
---

<objective>
Frontend for Purchase Budgets (FINC-02) and Saving Analysis (FINC-03): two pages with budget execution tracking and procurement analytics dashboards.

Purpose: Give managers visibility into budget consumption and procurement performance with real-time data and interactive charts.
Output: OrcamentoComprasPage with editable budget table, SavingAnalysisPage with KPI cards and Recharts charts.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-devolu-o-or-amento-e-saving/11-CONTEXT.md
@.planning/phases/11-devolu-o-or-amento-e-saving/11-03-SUMMARY.md
@.planning/phases/11-devolu-o-or-amento-e-saving/11-04-SUMMARY.md

<interfaces>
From purchase-budgets backend (Plan 03):
```
GET /api/org/purchase-budgets — list budgets
POST /api/org/purchase-budgets — create budget
PUT /api/org/purchase-budgets/:id — update budget
DELETE /api/org/purchase-budgets/:id — delete budget
GET /api/org/purchase-budgets/execution — budget execution with real-time aggregation
GET /api/org/purchase-budgets/deviations — over-budget categories
```

BudgetExecutionRow: { budgetId, category, categoryLabel, farmId?, farmName?, periodStart, periodEnd, budgetedAmount, requisitado, comprado, pago, percentUsed }

From saving-analysis backend (Plan 04):

```
GET /api/org/saving-analysis/dashboard — combined analytics
GET /api/org/saving-analysis/saving — saving per quotation
GET /api/org/saving-analysis/price-history/:productId — price history
GET /api/org/saving-analysis/indicators — cycle indicators
GET /api/org/saving-analysis/top-products — top 10 products by spend
GET /api/org/saving-analysis/top-suppliers — top 5 suppliers by volume
```

SavingSummary: { totalSaving, quotationCount, avgSavingPercent, savings: QuotationSaving[] }
CycleIndicators: { percentFormal, percentEmergency, avgCycleDays, totalOrders }
PriceHistoryResult: { productId, productName, points: PriceHistoryPoint[] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OrcamentoComprasPage with budget execution table</name>
  <files>
    apps/frontend/src/hooks/usePurchaseBudgets.ts
    apps/frontend/src/pages/OrcamentoComprasPage.tsx
    apps/frontend/src/pages/OrcamentoComprasPage.css
  </files>
  <read_first>
    - apps/frontend/src/pages/StockAlertsPage.tsx (table with progress bars pattern reference)
    - apps/frontend/src/pages/GoodsReceiptsPage.tsx (page structure pattern)
    - apps/frontend/src/hooks/useGoodsReceipts.ts (hook pattern)
    - apps/frontend/src/services/api.ts (api instance)
    - apps/frontend/src/styles/tokens.css (design tokens for colors)
  </read_first>
  <action>
**usePurchaseBudgets.ts** — Create hook:
- `usePurchaseBudgets(filters)`: GET /api/org/purchase-budgets list
- `useBudgetExecution(filters)`: GET /api/org/purchase-budgets/execution with query params (farmId, periodStart, periodEnd)
- `useBudgetDeviations(filters)`: GET /api/org/purchase-budgets/deviations
- `createPurchaseBudget(input)`: POST /api/org/purchase-budgets
- `updatePurchaseBudget(id, input)`: PUT /api/org/purchase-budgets/:id
- `deletePurchaseBudget(id)`: DELETE /api/org/purchase-budgets/:id

**OrcamentoComprasPage.tsx** — Create page:

- Header: "Orçamento de Compras" title + "Novo Orçamento" primary button
- Filters row: farm select, period type dropdown (Mensal/Trimestral/Safra), date range picker
- **Two tabs**: "Execução Orçamentária" and "Desvios"

Tab 1 — Execução Orçamentária:

- Table columns: Categoria (label), Orçado (BRL), Requisitado (BRL), Comprado (BRL), Pago (BRL), % Utilizado (progress bar)
- Progress bar per row: colored div inside track div
  - < 80%: green (var(--color-primary-600))
  - 80-100%: yellow (#F9A825)
  - > 100%: red (var(--color-error-600))
  - Width = min(percentUsed, 150)% clamped visually to bar width
- Totals row at bottom: sum of all columns
- "Novo Orçamento" inline form or modal (Claude's discretion per CONTEXT.md): category select (SupplierCategory labels), period type, start date, end date, amount input. Validate category not already budgeted for overlapping period.
- Inline edit: click on "Orçado" cell to edit amount in-place. Save on blur or Enter.
- Delete: icon button per row, with ConfirmModal (medium criticality — "Tem certeza que deseja excluir este orçamento?")

Tab 2 — Desvios:

- Table of categories where percentUsed > 100
- Columns: Categoria, Orçado, Comprado, Excedente (comprado - orcado), % Excedido
- Highlight row in light red background
- Each row expandable to show the RCs/OCs that contributed to the overrun

- Empty state: "Nenhum orçamento definido para o período selecionado" + CTA
- Skeleton loading
- Mobile: cards layout for budget rows with mini progress bar
- BEM CSS: `.orcamento-compras-page`

**OrcamentoComprasPage.css** — Styles for:

- Progress bar track (height 8px, border-radius 4px, bg neutral-200) and fill (transition width 300ms)
- Inline edit input styling
- Deviation row highlight
- Totals row bold styling
- Mobile card layout
  </action>
  <verify>
  <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria> - File exists: src/hooks/usePurchaseBudgets.ts - File exists: src/pages/OrcamentoComprasPage.tsx - File exists: src/pages/OrcamentoComprasPage.css - grep "useBudgetExecution" src/hooks/usePurchaseBudgets.ts returns at least 1 match - grep "createPurchaseBudget" src/hooks/usePurchaseBudgets.ts returns at least 1 match - grep "Execu" src/pages/OrcamentoComprasPage.tsx returns at least 1 match (tab label) - grep "Desvio" src/pages/OrcamentoComprasPage.tsx returns at least 1 match (tab label) - grep "percentUsed\|progress" src/pages/OrcamentoComprasPage.tsx returns at least 1 match (progress bar) - grep "orcamento-compras-page" src/pages/OrcamentoComprasPage.css returns at least 1 match - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>OrcamentoComprasPage with budget execution table (4 columns + progress bar), deviations tab, inline edit, create/delete, filters, empty state, mobile cards. usePurchaseBudgets hook with all CRUD + execution + deviations.</done>
  </task>

<task type="auto">
  <name>Task 2: SavingAnalysisPage with KPI cards and Recharts charts</name>
  <files>
    apps/frontend/src/hooks/useSavingAnalysis.ts
    apps/frontend/src/pages/SavingAnalysisPage.tsx
    apps/frontend/src/pages/SavingAnalysisPage.css
  </files>
  <read_first>
    - apps/frontend/src/pages/FinancialDashboardPage.tsx (dashboard with Recharts pattern — lazy-loaded chart components)
    - apps/frontend/src/components/financial-dashboard/RevenueExpenseChart.tsx (Recharts BarChart pattern)
    - apps/frontend/src/hooks/useFinancialDashboard.ts (dashboard hook pattern)
    - apps/frontend/src/services/api.ts (api instance)
    - apps/frontend/src/styles/tokens.css (design tokens)
  </read_first>
  <action>
**useSavingAnalysis.ts** — Create hook:
- `useSavingDashboard(params)`: GET /api/org/saving-analysis/dashboard with startDate, endDate, category?, supplierId?
- `useSavingByQuotation(params)`: GET /api/org/saving-analysis/saving
- `usePriceHistory(productId, params)`: GET /api/org/saving-analysis/price-history/:productId
- `useCycleIndicators(params)`: GET /api/org/saving-analysis/indicators
- `useTopProducts(params)`: GET /api/org/saving-analysis/top-products
- `useTopSuppliers(params)`: GET /api/org/saving-analysis/top-suppliers

**SavingAnalysisPage.tsx** — Create page with sections:

**Header**: "Análise de Saving" title
**Filters**: date range with presets (Último Mês, Último Trimestre, Safra Atual, Último Ano), category select, supplier select

**Section 1 — KPI Cards** (top row, 4 cards):

- Saving Total: large BRL value + quotation count subtitle
- Saving Médio: avgSavingPercent + "por cotação"
- % Compras Formais: percentFormal + "com cotação"
- % Emergenciais: percentEmergency + icon color (green if < 20%, yellow 20-40%, red > 40%)
- Card styling: border-left 4px with semantic color, bg white, shadow-sm, padding 24px

**Section 2 — Saving por Cotação** (expandable table):

- Table: Cotação (sequentialNumber), Data, Fornecedores (count), Saving Total (BRL), badge with % saving
- Expandable rows: item-level detail (productName, maxPrice, winnerPrice, saving)

**Section 3 — Histórico de Preços** (interactive chart):

- Product select dropdown (search by name)
- Recharts LineChart: X axis = date, Y axis = price (BRL)
- Tooltip with price, PO number, supplier name
- Responsive via ResponsiveContainer width="100%" height={300}
- Use var(--color-primary-600) for line color
- Empty state when no product selected: "Selecione um produto para ver o histórico de preços"

**Section 4 — Indicadores de Ciclo**:

- Card row: Prazo Médio (avgCycleDays + "dias"), Total Pedidos, % Formais, % Emergenciais
- Small BarChart showing cycle time distribution if data available (or skip — Claude's discretion)

**Section 5 — Top 10 Produtos por Gasto**:

- Horizontal BarChart (Recharts): productName on Y axis, totalSpent on X axis
- Bar color: var(--color-primary-500)
- Or table: rank, productName, totalSpent (BRL), orderCount

**Section 6 — Top 5 Fornecedores por Volume**:

- Similar to Top Products: BarChart or table
- Supplier name + totalVolume + orderCount

- All sections use skeleton loading
- Empty state per section when no data in period
- Mobile: stack sections vertically, charts resize via ResponsiveContainer
- BEM CSS: `.saving-analysis-page`

**SavingAnalysisPage.css** — Styles for:

- KPI cards grid (4 cols on lg, 2 on md, 1 on sm)
- Section spacing (gap 32px between sections)
- Chart container styling
- Expandable saving table rows
- Top products/suppliers horizontal bars
  </action>
  <verify>
  <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria> - File exists: src/hooks/useSavingAnalysis.ts - File exists: src/pages/SavingAnalysisPage.tsx - File exists: src/pages/SavingAnalysisPage.css - grep "useSavingDashboard" src/hooks/useSavingAnalysis.ts returns at least 1 match - grep "usePriceHistory" src/hooks/useSavingAnalysis.ts returns at least 1 match - grep "useCycleIndicators" src/hooks/useSavingAnalysis.ts returns at least 1 match - grep "from 'recharts'" src/pages/SavingAnalysisPage.tsx returns at least 1 match - grep "LineChart\|BarChart" src/pages/SavingAnalysisPage.tsx returns at least 1 match - grep "ResponsiveContainer" src/pages/SavingAnalysisPage.tsx returns at least 1 match - grep "Saving Total\|saving-analysis-page\|Análise de Saving" src/pages/SavingAnalysisPage.tsx returns at least 1 match - grep "Top.*Produto\|top.*product" src/pages/SavingAnalysisPage.tsx returns at least 1 match (case insensitive) - grep "Top.*Fornecedor\|top.*supplier" src/pages/SavingAnalysisPage.tsx returns at least 1 match (case insensitive) - grep "saving-analysis-page" src/pages/SavingAnalysisPage.css returns at least 1 match - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>SavingAnalysisPage with 6 sections: KPI cards, saving per quotation table, price history Recharts LineChart, cycle indicators, top 10 products BarChart, top 5 suppliers. All responsive with skeleton loading and empty states.</done>
  </task>

</tasks>

<verification>
- `cd apps/frontend && npx tsc --noEmit` exits 0
- OrcamentoComprasPage renders with execution table and progress bars
- SavingAnalysisPage renders with Recharts charts and KPI cards
- Both pages responsive (mobile cards/stacked layout)
- Both hooks connect to correct API endpoints
</verification>

<success_criteria>
Both pages fully functional: OrcamentoComprasPage with editable budget table, progress bars (color-coded), deviation report. SavingAnalysisPage with KPI cards, saving per quotation, price history chart, cycle indicators, top products/suppliers. All TypeScript compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/11-devolu-o-or-amento-e-saving/11-06-SUMMARY.md`
</output>
