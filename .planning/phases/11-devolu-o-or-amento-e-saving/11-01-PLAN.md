---
phase: 11-devolu-o-or-amento-e-saving
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/prisma/schema.prisma
  - apps/backend/src/modules/goods-returns/goods-returns.types.ts
  - apps/backend/src/modules/purchase-budgets/purchase-budgets.types.ts
  - apps/backend/src/modules/saving-analysis/saving-analysis.types.ts
  - apps/backend/src/modules/stock-outputs/stock-outputs.types.ts
autonomous: true
requirements: [DEVO-01, FINC-02, FINC-03]

must_haves:
  truths:
    - 'GoodsReturn and GoodsReturnItem models exist in Prisma schema with correct fields and relations'
    - 'PurchaseBudget model exists in Prisma schema with SupplierCategory enum and period types'
    - 'StockOutputType enum includes RETURN value'
    - 'Payable model has goodsReturnId and isCredit fields'
    - 'PurchaseRequest and PurchaseOrder models have budgetExceeded boolean field'
    - 'All TypeScript type definitions compile without errors'
  artifacts:
    - path: 'apps/backend/prisma/schema.prisma'
      provides: 'GoodsReturn, GoodsReturnItem, PurchaseBudget models + enum additions'
    - path: 'apps/backend/src/modules/goods-returns/goods-returns.types.ts'
      provides: 'State machine, enums, input/output types for goods returns'
    - path: 'apps/backend/src/modules/purchase-budgets/purchase-budgets.types.ts'
      provides: 'Budget input/output types, period types'
    - path: 'apps/backend/src/modules/saving-analysis/saving-analysis.types.ts'
      provides: 'Query param and output types for analytics'
    - path: 'apps/backend/src/modules/stock-outputs/stock-outputs.types.ts'
      provides: 'Updated STOCK_OUTPUT_TYPES with RETURN'
  key_links:
    - from: 'goods-returns.types.ts'
      to: 'schema.prisma'
      via: 'Enum values must match'
      pattern: 'GR_RETURN_STATUSES.*PENDENTE.*EM_ANALISE.*APROVADA.*CONCLUIDA.*CANCELADA'
---

<objective>
Schema foundation for Phase 11: Prisma models, migration, and TypeScript type definitions for all three modules (goods returns, purchase budgets, saving analysis).

Purpose: Establish the data layer and type contracts that all subsequent plans depend on. Without this, no backend service or frontend can be built.
Output: Migrated database schema + compiled TypeScript types for 3 new modules + updated existing types.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-devolu-o-or-amento-e-saving/11-CONTEXT.md
@.planning/phases/11-devolu-o-or-amento-e-saving/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase that executor needs -->

From apps/backend/src/modules/goods-receipts/goods-receipts.types.ts:

```typescript
export const GR_STATUSES = ['RASCUNHO','AGUARDANDO_NF','AGUARDANDO_MERCADORIA','EM_CONFERENCIA','CONFIRMADO','CANCELADO'] as const;
export type GrStatus = (typeof GR_STATUSES)[number];
export const GR_VALID_TRANSITIONS: Record<string, string[]> = { ... };
export function canGrTransition(from: string, to: string): boolean;
export interface GoodsReceiptOutput { id, organizationId, sequentialNumber, purchaseOrderId?, ... }
export interface GoodsReceiptItemOutput { id, productId?, productName, unitName, orderedQty, receivedQty, unitPrice, totalPrice, batchNumber?, ... }
```

From apps/backend/src/modules/stock-outputs/stock-outputs.types.ts:

```typescript
export const STOCK_OUTPUT_TYPES = ['CONSUMPTION','MANUAL_CONSUMPTION','TRANSFER','DISPOSAL'] as const;
export type StockOutputTypeValue = (typeof STOCK_OUTPUT_TYPES)[number];
export const STOCK_OUTPUT_TYPE_LABELS: Record<StockOutputTypeValue, string> = { ... };
```

From apps/backend/prisma/schema.prisma:

```prisma
enum StockOutputType { CONSUMPTION MANUAL_CONSUMPTION TRANSFER DISPOSAL }
enum SupplierCategory { INSUMO_AGRICOLA PECUARIO PECAS COMBUSTIVEL EPI SERVICOS OUTROS }
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema additions and migration</name>
  <files>apps/backend/prisma/schema.prisma</files>
  <read_first>
    - apps/backend/prisma/schema.prisma (current schema — find StockOutputType enum, SupplierCategory enum, Payable model, PurchaseRequest model, PurchaseOrder model, GoodsReceipt model)
    - apps/backend/src/modules/goods-receipts/goods-receipts.types.ts (GR pattern reference)
    - .planning/phases/11-devolu-o-or-amento-e-saving/11-RESEARCH.md (full schema examples)
  </read_first>
  <action>
1. Add RETURN to existing `enum StockOutputType` (after DISPOSAL):
```prisma
enum StockOutputType {
  CONSUMPTION
  MANUAL_CONSUMPTION
  TRANSFER
  DISPOSAL
  RETURN
}
```

2. Add 4 new enums:

```prisma
enum GoodsReturnStatus { PENDENTE EM_ANALISE APROVADA CONCLUIDA CANCELADA }
enum GoodsReturnReason { DEFEITO VALIDADE PRODUTO_ERRADO EXCEDENTE ESPECIFICACAO_DIVERGENTE }
enum GoodsReturnAction { TROCA CREDITO ESTORNO }
enum GoodsReturnResolutionStatus { PENDING RESOLVED EXPIRED }
enum BudgetPeriodType { MENSAL TRIMESTRAL SAFRA }
```

3. Add GoodsReturn model (copy exact schema from 11-RESEARCH.md "GoodsReturn Prisma Schema" section — id cuid, organizationId, sequentialNumber, goodsReceiptId, supplierId, status GoodsReturnStatus default PENDENTE, reason GoodsReturnReason, expectedAction GoodsReturnAction, resolutionStatus GoodsReturnResolutionStatus default PENDING, resolutionDeadline DateTime?, returnInvoiceNumber String?, returnInvoiceDate DateTime?, notes String?, stockOutputId String?, creditPayableId String?, createdBy, deletedAt DateTime?, createdAt, updatedAt. Relations: Organization, GoodsReceipt, Supplier, User "GRReturnCreator", items GoodsReturnItem[]. Indexes: @@unique([organizationId, sequentialNumber]), @@index([organizationId, status]), @@index([goodsReceiptId]). @@map("goods_returns"))

4. Add GoodsReturnItem model (id cuid, goodsReturnId, productId String?, productName, unitName, returnQty Decimal @db.Decimal(12,3), unitPrice Decimal @db.Decimal(12,4), totalPrice Decimal @db.Decimal(14,2), batchNumber String?, photoUrl String?, photoFileName String?, createdAt. Relation: GoodsReturn onDelete Cascade. @@index([goodsReturnId]). @@map("goods_return_items"))

5. Add `goodsReturns GoodsReturn[]` reverse relation to GoodsReceipt model.

6. Add PurchaseBudget model (id cuid, organizationId, farmId String?, costCenterId String?, category SupplierCategory, periodType BudgetPeriodType, periodStart DateTime, periodEnd DateTime, budgetedAmount Decimal @db.Decimal(14,2), notes String?, createdBy, createdAt, updatedAt. Relations: Organization, User "PBCreator". @@index([organizationId, periodStart, periodEnd]), @@index([organizationId, category]). @@map("purchase_budgets"))

7. Add to existing Payable model:
   - `goodsReturnId String?`
   - `isCredit Boolean @default(false)`

8. Add to existing PurchaseRequest model:
   - `budgetExceeded Boolean @default(false)`

9. Add to existing PurchaseOrder model:
   - `budgetExceeded Boolean @default(false)`

10. Run migration using the project's established pattern:

```bash
cd apps/backend && npx prisma db push && npx prisma migrate dev --name add_goods_returns_budgets --create-only && npx prisma migrate resolve --applied $(ls -t prisma/migrations | head -1) && npx prisma generate
```

  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/backend && npx prisma validate && npx prisma generate</automated>
  </verify>
  <acceptance_criteria>
    - grep "RETURN" prisma/schema.prisma shows RETURN in StockOutputType enum
    - grep "GoodsReturnStatus" prisma/schema.prisma returns at least 1 match
    - grep "model GoodsReturn " prisma/schema.prisma returns 1 match
    - grep "model GoodsReturnItem " prisma/schema.prisma returns 1 match
    - grep "model PurchaseBudget " prisma/schema.prisma returns 1 match
    - grep "goodsReturnId" prisma/schema.prisma returns at least 2 matches (on Payable and GoodsReturn)
    - grep "isCredit" prisma/schema.prisma returns 1 match on Payable model
    - grep "budgetExceeded" prisma/schema.prisma returns at least 2 matches (PurchaseRequest + PurchaseOrder)
    - grep "BudgetPeriodType" prisma/schema.prisma returns at least 1 match
    - npx prisma validate exits 0
    - npx prisma generate exits 0
  </acceptance_criteria>
  <done>All 5 new enums, 3 new models, and 4 field additions exist in schema.prisma. Migration applied. Prisma client regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript type definitions for all 3 modules</name>
  <files>
    apps/backend/src/modules/goods-returns/goods-returns.types.ts
    apps/backend/src/modules/purchase-budgets/purchase-budgets.types.ts
    apps/backend/src/modules/saving-analysis/saving-analysis.types.ts
    apps/backend/src/modules/stock-outputs/stock-outputs.types.ts
  </files>
  <read_first>
    - apps/backend/src/modules/goods-receipts/goods-receipts.types.ts (pattern reference for state machine + input/output types)
    - apps/backend/src/modules/stock-outputs/stock-outputs.types.ts (current STOCK_OUTPUT_TYPES to add RETURN)
    - apps/backend/src/modules/payables/payables.types.ts (PayableOutput interface reference)
    - apps/backend/src/modules/purchase-orders/purchase-orders.types.ts (PurchaseOrder types reference)
    - .planning/phases/11-devolu-o-or-amento-e-saving/11-RESEARCH.md (type patterns)
  </read_first>
  <action>
**goods-returns.types.ts** — Create new file with:
- `GR_RETURN_STATUSES = ['PENDENTE','EM_ANALISE','APROVADA','CONCLUIDA','CANCELADA'] as const` + type `GrReturnStatus`
- `GR_RETURN_VALID_TRANSITIONS: Record<string, string[]>` mapping: PENDENTE->[EM_ANALISE,CANCELADA], EM_ANALISE->[APROVADA,CANCELADA], APROVADA->[CONCLUIDA], CONCLUIDA->[], CANCELADA->[]
- `canGrReturnTransition(from: string, to: string): boolean`
- `GR_RETURN_REASONS = ['DEFEITO','VALIDADE','PRODUTO_ERRADO','EXCEDENTE','ESPECIFICACAO_DIVERGENTE'] as const` + type
- `GR_RETURN_ACTIONS = ['TROCA','CREDITO','ESTORNO'] as const` + type
- `GR_RETURN_RESOLUTION_STATUSES = ['PENDING','RESOLVED','EXPIRED'] as const` + type
- Label maps for all enums (pt-BR): PENDENTE->"Pendente", EM_ANALISE->"Em Analise", APROVADA->"Aprovada", CONCLUIDA->"Concluida", CANCELADA->"Cancelada", DEFEITO->"Defeito", VALIDADE->"Validade vencida", PRODUTO_ERRADO->"Produto errado", EXCEDENTE->"Excedente", ESPECIFICACAO_DIVERGENTE->"Especificacao divergente", TROCA->"Troca", CREDITO->"Credito", ESTORNO->"Estorno", PENDING->"Pendente", RESOLVED->"Resolvido", EXPIRED->"Expirado"
- `GoodsReturnItemInput { productId?: string, productName: string, unitName: string, returnQty: number, unitPrice: number, batchNumber?: string }`
- `CreateGoodsReturnInput { goodsReceiptId: string, reason: GrReturnReasonValue, expectedAction: GrReturnActionValue, resolutionDeadline?: string, returnInvoiceNumber?: string, returnInvoiceDate?: string, notes?: string, items: GoodsReturnItemInput[] }`
- `TransitionGrReturnInput { status: GrReturnStatus, notes?: string }`
- `ListGoodsReturnsQuery { page?, limit?, status?, supplierId?, startDate?, endDate?, search? }`
- `GoodsReturnItemOutput { id, goodsReturnId, productId?, productName, unitName, returnQty: string, unitPrice: string, totalPrice: string, batchNumber?, photoUrl?, photoFileName?, createdAt }`
- `GoodsReturnOutput { id, organizationId, sequentialNumber, goodsReceiptId, supplierId, supplierName: string, status, reason, expectedAction, resolutionStatus, resolutionDeadline?, returnInvoiceNumber?, returnInvoiceDate?, notes?, stockOutputId?, creditPayableId?, createdBy, createdAt, updatedAt, items: GoodsReturnItemOutput[], goodsReceipt: { sequentialNumber, purchaseOrderId? } }`
- `GoodsReturnListItem { id, sequentialNumber, supplierName, status, statusLabel, reason, reasonLabel, expectedAction, actionLabel, totalValue: string, itemCount: number, createdAt }`
- `ListGoodsReturnsResult { data: GoodsReturnListItem[], total: number, page: number, limit: number }`
- `GoodsReturnError extends Error { statusCode: number }`

**purchase-budgets.types.ts** — Create new file with:

- `BUDGET_PERIOD_TYPES = ['MENSAL','TRIMESTRAL','SAFRA'] as const` + type
- `BUDGET_PERIOD_LABELS: Record<BudgetPeriodTypeValue, string>` — MENSAL->"Mensal", TRIMESTRAL->"Trimestral", SAFRA->"Safra"
- `SUPPLIER_CATEGORY_LABELS: Record<string, string>` — INSUMO_AGRICOLA->"Insumos Agricolas", PECUARIO->"Pecuario", PECAS->"Pecas e Equipamentos", COMBUSTIVEL->"Combustivel", EPI->"EPI", SERVICOS->"Servicos", OUTROS->"Outros"
- `CreatePurchaseBudgetInput { farmId?: string, costCenterId?: string, category: string, periodType: BudgetPeriodTypeValue, periodStart: string, periodEnd: string, budgetedAmount: number, notes?: string }`
- `UpdatePurchaseBudgetInput { budgetedAmount?: number, notes?: string, periodEnd?: string }`
- `ListPurchaseBudgetsQuery { page?, limit?, farmId?, category?, periodType?, periodStart?, periodEnd? }`
- `BudgetExecutionRow { budgetId: string, category: string, categoryLabel: string, farmId?: string, farmName?: string, periodStart: string, periodEnd: string, budgetedAmount: string, requisitado: string, comprado: string, pago: string, percentUsed: number }`
- `BudgetExecutionResult { rows: BudgetExecutionRow[], totals: { budgeted: string, requisitado: string, comprado: string, pago: string } }`
- `PurchaseBudgetOutput { id, organizationId, farmId?, costCenterId?, category, periodType, periodStart, periodEnd, budgetedAmount: string, notes?, createdBy, createdAt, updatedAt }`
- `ListPurchaseBudgetsResult { data: PurchaseBudgetOutput[], total: number, page: number, limit: number }`
- `BudgetCheckResult { exceeded: boolean, budgetId?: string, budgetedAmount?: string, currentSpent?: string, percentUsed?: number }`
- `PurchaseBudgetError extends Error { statusCode: number }`

**saving-analysis.types.ts** — Create new file with:

- `SavingQueryParams { startDate: string, endDate: string, category?, supplierId? }`
- `QuotationSaving { quotationId: string, sequentialNumber: string, createdAt: string, supplierCount: number, savingTotal: string, items: QuotationSavingItem[] }`
- `QuotationSavingItem { productName: string, maxPrice: string, winnerPrice: string, saving: string }`
- `SavingSummary { totalSaving: string, quotationCount: number, avgSavingPercent: string, savings: QuotationSaving[] }`
- `PriceHistoryPoint { date: string, price: string, purchaseOrderNumber: string, supplierName: string }`
- `PriceHistoryResult { productId: string, productName: string, points: PriceHistoryPoint[] }`
- `CycleIndicators { percentFormal: string, percentEmergency: string, avgCycleDays: string, totalOrders: number }`
- `TopProductBySpend { productId: string, productName: string, totalSpent: string, orderCount: number }`
- `TopSupplierByVolume { supplierId: string, supplierName: string, totalVolume: string, orderCount: number }`
- `AnalyticsDashboard { saving: SavingSummary, indicators: CycleIndicators, topProducts: TopProductBySpend[], topSuppliers: TopSupplierByVolume[] }`

**stock-outputs.types.ts** — Update existing file:

- Add `'RETURN'` to `STOCK_OUTPUT_TYPES` array (after 'DISPOSAL')
- Add `RETURN: 'Devolucao'` to `STOCK_OUTPUT_TYPE_LABELS`
  </action>
  <verify>
  <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/backend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria> - grep "RETURN" src/modules/stock-outputs/stock-outputs.types.ts shows RETURN in STOCK_OUTPUT_TYPES array and STOCK_OUTPUT_TYPE_LABELS - grep "GR_RETURN_STATUSES" src/modules/goods-returns/goods-returns.types.ts returns 1 match - grep "GR_RETURN_VALID_TRANSITIONS" src/modules/goods-returns/goods-returns.types.ts returns 1 match - grep "canGrReturnTransition" src/modules/goods-returns/goods-returns.types.ts returns 1 match - grep "CreateGoodsReturnInput" src/modules/goods-returns/goods-returns.types.ts returns 1 match - grep "GoodsReturnOutput" src/modules/goods-returns/goods-returns.types.ts returns 1 match - grep "BUDGET_PERIOD_TYPES" src/modules/purchase-budgets/purchase-budgets.types.ts returns 1 match - grep "BudgetExecutionRow" src/modules/purchase-budgets/purchase-budgets.types.ts returns 1 match - grep "BudgetCheckResult" src/modules/purchase-budgets/purchase-budgets.types.ts returns 1 match - grep "SavingSummary" src/modules/saving-analysis/saving-analysis.types.ts returns 1 match - grep "CycleIndicators" src/modules/saving-analysis/saving-analysis.types.ts returns 1 match - grep "AnalyticsDashboard" src/modules/saving-analysis/saving-analysis.types.ts returns 1 match - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All type definitions for 3 modules compile. STOCK_OUTPUT_TYPES includes RETURN. State machine, enums, input/output interfaces all defined and type-safe.</done>
  </task>

</tasks>

<verification>
- `cd apps/backend && npx prisma validate` exits 0
- `cd apps/backend && npx prisma generate` exits 0
- `cd apps/backend && npx tsc --noEmit` exits 0
- Schema contains GoodsReturn, GoodsReturnItem, PurchaseBudget models
- StockOutputType enum includes RETURN
- Payable has goodsReturnId and isCredit fields
- PurchaseRequest and PurchaseOrder have budgetExceeded field
- All 3 type definition files exist and export expected interfaces
</verification>

<success_criteria>
Database schema migrated with all new models and enum additions. TypeScript types compile cleanly. All subsequent plans can import types without schema or compilation errors.
</success_criteria>

<output>
After completion, create `.planning/phases/11-devolu-o-or-amento-e-saving/11-01-SUMMARY.md`
</output>
