---
phase: 05-concilia-o-e-fluxo-de-caixa
plan: 05
type: execute
wave: 2
depends_on: ['05-03']
files_modified:
  - apps/frontend/src/hooks/useCashflow.ts
  - apps/frontend/src/pages/CashflowPage.tsx
  - apps/frontend/src/pages/CashflowPage.css
  - apps/frontend/src/components/cashflow/CashflowChart.tsx
  - apps/frontend/src/components/cashflow/DfcTable.tsx
autonomous: true
requirements:
  - FN-13
must_haves:
  truths:
    - 'Gerente ve grafico de projecao 12 meses com 3 linhas de cenario (Realista, Otimista, Pessimista)'
    - 'Linha vermelha de referencia em y=0 indica zona de risco'
    - 'Tooltip no grafico mostra saldo, entradas, saidas e cheques pendentes'
    - 'Tabela DFC agrupa por Operacional/Investimento/Financiamento com 12 colunas mensais'
    - 'Banner de alerta aparece quando saldo negativo e projetado'
    - 'Filtro por fazenda usa dropdown local (nao FarmContext global)'
    - 'Export PDF e Excel funcionam via botoes secundarios'
  artifacts:
    - path: 'apps/frontend/src/pages/CashflowPage.tsx'
      provides: 'Cashflow projection page with chart, DFC table, alert, export'
    - path: 'apps/frontend/src/components/cashflow/CashflowChart.tsx'
      provides: 'Recharts ComposedChart with Area + 2 Lines + ReferenceLine'
    - path: 'apps/frontend/src/components/cashflow/DfcTable.tsx'
      provides: 'DFC classification table with expandable sections'
    - path: 'apps/frontend/src/hooks/useCashflow.ts'
      provides: 'Data fetching hook for cashflow projection'
      exports: ['useCashflow', 'useNegativeBalanceAlert']
  key_links:
    - from: 'CashflowPage.tsx'
      to: '/api/org/cashflow/projection'
      via: 'useCashflow hook fetch'
      pattern: 'fetch.*cashflow/projection'
    - from: 'CashflowChart.tsx'
      to: 'Recharts ComposedChart'
      via: 'import from recharts'
      pattern: 'ComposedChart.*Area.*Line.*ReferenceLine'
---

<objective>
Build the Cashflow projection frontend page with interactive Recharts chart showing 3 scenarios, DFC classification table, negative balance alert banner, farm filter, and PDF/Excel export.

Purpose: This page gives the farmer a 12-month financial outlook with scenario analysis, enabling proactive financial planning around agricultural seasonality.
Output: CashflowPage with chart, DFC table, alerts, and export functionality.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-concilia-o-e-fluxo-de-caixa/05-CONTEXT.md
@.planning/phases/05-concilia-o-e-fluxo-de-caixa/05-RESEARCH.md
@.planning/phases/05-concilia-o-e-fluxo-de-caixa/05-UI-SPEC.md
@.planning/phases/05-concilia-o-e-fluxo-de-caixa/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cashflow hook + page + chart component</name>
  <files>
    apps/frontend/src/hooks/useCashflow.ts,
    apps/frontend/src/pages/CashflowPage.tsx,
    apps/frontend/src/pages/CashflowPage.css,
    apps/frontend/src/components/cashflow/CashflowChart.tsx
  </files>
  <read_first>
    apps/frontend/src/hooks/useFinancialDashboard.ts,
    apps/frontend/src/pages/FinancialDashboardPage.tsx,
    apps/frontend/src/pages/FinancialDashboardPage.css,
    apps/frontend/src/components/financial-dashboard/RevenueExpenseChart.tsx,
    apps/frontend/src/components/animals/WeighingChartInner.tsx,
    apps/frontend/src/styles/tokens.css,
    .planning/phases/05-concilia-o-e-fluxo-de-caixa/05-UI-SPEC.md
  </read_first>
  <action>
1. Create `useCashflow.ts`:
   - `useCashflow(farmId?: string)` — fetches GET `/api/org/cashflow/projection?farmId=${farmId}`. Returns `{ projection: CashflowProjection | null, loading, error, refetch }`. Re-fetches when farmId changes.
   - `useNegativeBalanceAlert(farmId?: string)` — fetches GET `/api/org/cashflow/negative-balance-alert?farmId=${farmId}`. Returns `{ alert: {date: string, amount: number} | null, loading }`.
   - `exportCashflowPdf(farmId?: string): Promise<void>` — fetches GET `/api/org/cashflow/projection/export/pdf?farmId=${farmId}` with `Accept: application/pdf`. Triggers browser download via blob URL. Shows toast on error: "Nao foi possivel exportar. Tente novamente."
   - `exportCashflowExcel(farmId?: string): Promise<void>` — same pattern for Excel.

2. Create `CashflowPage.tsx`:
   - Page heading: h1 "Fluxo de Caixa", subtitle "Projecao de 12 meses com cenarios otimista, realista e pessimista"
   - Breadcrumb: Financeiro > Fluxo de Caixa
   - Top-right row: farm filter dropdown (local useState, "Todas as fazendas" default — same pattern as FinancialDashboardPage), "Exportar PDF" secondary button (FileDown icon), "Exportar Excel" secondary button (FileSpreadsheet icon)
   - Farm filter: `<select>` populated from useFarms() hook. On change: set local farmId state, show skeleton, refetch.
   - Negative balance alert banner (conditional): when `projection.negativeBalanceDate` is not null. Uses `role="alert"`. Background `--color-error-100`, icon AlertCircle 20px in `--color-error-500`, heading "Saldo negativo previsto", body "em {date} — saldo projetado de R$ {amount}. Revise seus compromissos financeiros." Link to scroll to chart.
   - Loading state: skeleton screen — chart skeleton (rect 100% x 320px pulse animation) + 3 DFC section skeletons (rect rows)
   - Main content: CashflowChart component + DfcTable component below
   - No data state: chart shows flat line at current balance. DFC sections show "Sem projecoes registradas" per section.
   - Export buttons: on click, show loading spinner on button (disabled), call export function. Re-enable on complete.
   - Mobile (<640px): chart hidden, show current balance card + DFC table as cards.

3. Create `CashflowPage.css`:
   - BEM pattern: `.cashflow-page`, `.cashflow-page__header`, `.cashflow-page__filters`, `.cashflow-page__alert`, `.cashflow-page__chart`, `.cashflow-page__dfc`
   - Alert banner: padding 16px, border-radius 8px, flex with icon + text
   - Filter bar: flex row, gap 16px, items center
   - Export buttons: secondary outlined style (border 1px solid neutral-300, hover neutral-200 bg)
   - Skeleton: `.skeleton-rect` with pulse animation 1500ms ease-in-out infinite, opacity 0.4->0.7
   - Spacing: all multiples of 4px
   - Mobile: `.cashflow-page__chart` display none below 640px

4. Create `CashflowChart.tsx`:
   - Props: `{ data: ProjectionPoint[] }`
   - Recharts `ComposedChart` inside `ResponsiveContainer` (width="100%" height={320})
   - `aria-label="Grafico de projecao de fluxo de caixa por cenario"` on wrapper div
   - `CartesianGrid`: strokeDasharray="3 3", stroke from `--color-neutral-200` (use `getComputedStyle` or hardcode `#E8E4DE`)
   - `ReferenceLine` y={0}: stroke `#C62828` (error-500), strokeWidth 2
   - `XAxis` dataKey="label": tick font Source Sans 3, 12px
   - `YAxis`: tickFormatter using `formatBRLCompact` function (e.g., "R$ 12k", "R$ 1,2M"). Font JetBrains Mono 12px.
   - `Area` type="monotone" dataKey="balanceRealistic": stroke `#2E7D32` (primary-600), fill `#C8E6C9` (primary-100), fillOpacity 0.4, name="Realista"
   - `Line` type="monotone" dataKey="balanceOptimistic": stroke `#2E7D32` (success-500), strokeDasharray="5 5", dot={false}, name="Otimista"
   - `Line` type="monotone" dataKey="balancePessimistic": stroke `#C62828` (error-500), strokeDasharray="5 5", dot={false}, name="Pessimista"
   - `Tooltip` with custom `CashflowTooltip` component:
     - White background card with shadow
     - Date label at top (Source Sans 3 bold)
     - 4 rows: "Saldo realista: R$ X", "Entradas previstas: R$ X", "Saidas previstas: R$ X", "Cheques pendentes: R$ X" — all values in JetBrains Mono
     - Negative saldo shown in `--color-error-500`
   - `Legend`: bottom position, text labels "Realista", "Otimista", "Pessimista"
   - `formatBRLCompact(value)`: format as "R$ 0", "R$ 1k", "R$ 12k", "R$ 123k", "R$ 1,2M" etc using Intl.NumberFormat with compact notation.
     </action>
     <verify>
     <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && pnpm --filter @protos-farm/frontend exec tsc --noEmit --pretty 2>&1 | tail -20</automated>
     </verify>
     <acceptance_criteria>
   - grep "useCashflow" apps/frontend/src/hooks/useCashflow.ts returns match
   - grep "useNegativeBalanceAlert" apps/frontend/src/hooks/useCashflow.ts returns match
   - grep "exportCashflowPdf" apps/frontend/src/hooks/useCashflow.ts returns match
   - grep "Fluxo de Caixa" apps/frontend/src/pages/CashflowPage.tsx returns match
   - grep "role=\"alert\"" apps/frontend/src/pages/CashflowPage.tsx returns match (negative balance banner)
   - grep "Saldo negativo previsto" apps/frontend/src/pages/CashflowPage.tsx returns match
   - grep "ComposedChart" apps/frontend/src/components/cashflow/CashflowChart.tsx returns match
   - grep "Area" apps/frontend/src/components/cashflow/CashflowChart.tsx returns match
   - grep "ReferenceLine" apps/frontend/src/components/cashflow/CashflowChart.tsx returns match
   - grep "balanceRealistic" apps/frontend/src/components/cashflow/CashflowChart.tsx returns match
   - grep "balanceOptimistic" apps/frontend/src/components/cashflow/CashflowChart.tsx returns match
   - grep "balancePessimistic" apps/frontend/src/components/cashflow/CashflowChart.tsx returns match
   - grep "aria-label" apps/frontend/src/components/cashflow/CashflowChart.tsx returns match
   - grep "skeleton" apps/frontend/src/pages/CashflowPage.css returns match
   - tsc --noEmit succeeds
     </acceptance_criteria>
     <done>CashflowPage renders with farm filter, negative balance alert, skeleton loading. CashflowChart displays 3-scenario projection with Area+Lines+ReferenceLine in Recharts. Custom tooltip shows saldo/entradas/saidas/cheques. Export buttons trigger PDF/Excel download.</done>
     </task>

<task type="auto">
  <name>Task 2: DFC classification table component</name>
  <files>
    apps/frontend/src/components/cashflow/DfcTable.tsx
  </files>
  <read_first>
    apps/frontend/src/pages/CashflowPage.tsx,
    apps/frontend/src/pages/CashflowPage.css,
    .planning/phases/05-concilia-o-e-fluxo-de-caixa/05-UI-SPEC.md
  </read_first>
  <action>
Create `DfcTable.tsx`:
   - Props: `{ dfc: DfcSummary }` (from CashflowProjection.dfc)
   - Three expandable sections: "Operacional", "Investimento", "Financiamento"
   - Each section header: DFC class name + net total (JetBrains Mono). Click toggles expand/collapse. Default: all expanded. Chevron icon (ChevronDown/ChevronRight from lucide-react).
   - Section content: two sub-groups "Entradas" and "Saidas"
   - Each row: category name (Source Sans 3) + 12 monthly columns (Jan-Dez values in JetBrains Mono 14px) + Total column
   - Monthly column headers: "Jan", "Fev", "Mar", "Abr", "Mai", "Jun", "Jul", "Ago", "Set", "Out", "Nov", "Dez"
   - Value formatting: Intl.NumberFormat('pt-BR', {style:'currency', currency:'BRL'}) for cells. Positive in `--color-neutral-700`, negative in `--color-error-500` (#C62828).
   - Section subtotal row: bold, background `--color-neutral-100`
   - Grand total row at bottom: net of all three sections per month.
   - `<table>` with `<caption class="visually-hidden">Demonstracao de fluxo de caixa por classificacao</caption>` and `<th scope="col">` / `<th scope="row">`.
   - Mobile (<768px): horizontal scroll with sticky first column (category name). Use `overflow-x: auto` on table wrapper. First column: `position: sticky; left: 0; background: white; z-index: 1;`.
   - Empty section: "Sem projecoes para [Operacional/Investimento/Financiamento]" in muted text.
   - Collapse/expand animation: 200ms ease-out (max-height transition or CSS grid row animation). Respect `prefers-reduced-motion`.
   - Add CSS to CashflowPage.css for DFC table: `.dfc-table`, `.dfc-table__section`, `.dfc-table__header`, `.dfc-table__row`, `.dfc-table__cell--negative`.
  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && pnpm --filter @protos-farm/frontend exec tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "DfcTable" apps/frontend/src/components/cashflow/DfcTable.tsx returns match
    - grep "Operacional" apps/frontend/src/components/cashflow/DfcTable.tsx returns match
    - grep "Investimento" apps/frontend/src/components/cashflow/DfcTable.tsx returns match
    - grep "Financiamento" apps/frontend/src/components/cashflow/DfcTable.tsx returns match
    - grep "scope=\"col\"\|scope=\"row\"" apps/frontend/src/components/cashflow/DfcTable.tsx returns match
    - grep "sticky" apps/frontend/src/pages/CashflowPage.css returns match (mobile sticky column)
    - grep "dfc-table" apps/frontend/src/pages/CashflowPage.css returns match
    - tsc --noEmit succeeds
  </acceptance_criteria>
  <done>DfcTable renders 3 expandable sections with monthly columns, negative values in red, sticky first column on mobile, accessible table structure, collapse/expand animation</done>
</task>

</tasks>

<verification>
- `tsc --noEmit` succeeds for frontend
- CashflowPage renders chart + DFC table + alert banner
- Chart shows 3 scenario lines with reference line at y=0
- DFC table has 3 expandable sections with 12 monthly columns
- Farm filter reloads data
- Export buttons trigger file download
</verification>

<success_criteria>

- Recharts ComposedChart shows Realista (area), Otimista (dashed line), Pessimista (dashed line)
- ReferenceLine at y=0 in red
- Custom tooltip shows saldo, entradas, saidas, cheques pendentes
- DFC table groups by Operacional/Investimento/Financiamento with monthly breakdowns
- Negative balance alert banner with role="alert" appears when projection has negative date
- Farm filter dropdown uses local state (not FarmContext)
- Export PDF/Excel trigger browser download
- Skeleton loading for chart and DFC sections
- Mobile: chart hidden, DFC table horizontal scroll with sticky column
  </success_criteria>

<output>
After completion, create `.planning/phases/05-concilia-o-e-fluxo-de-caixa/05-05-SUMMARY.md`
</output>
