---
phase: 16-cadastro-de-ativos
plan: 04
type: execute
wave: 3
depends_on:
  - 16-03
files_modified:
  - apps/frontend/src/components/assets/AssetDrawer.tsx
  - apps/frontend/src/components/assets/AssetDrawer.css
  - apps/frontend/src/components/assets/AssetGeneralTab.tsx
  - apps/frontend/src/components/assets/AssetDocumentsTab.tsx
  - apps/frontend/src/components/assets/AssetFuelTab.tsx
  - apps/frontend/src/components/assets/AssetReadingsTab.tsx
  - apps/frontend/src/components/assets/AssetMaintenanceTab.tsx
  - apps/frontend/src/components/assets/AssetTimelineTab.tsx
  - apps/frontend/src/hooks/useAssetDetail.ts
  - apps/frontend/src/hooks/useFuelRecords.ts
  - apps/frontend/src/hooks/useMeterReadings.ts
  - apps/frontend/src/hooks/useAssetDocuments.ts
  - apps/frontend/src/pages/AssetsPage.tsx
autonomous: true
requirements:
  - ATIV-01
  - ATIV-02
  - ATIV-06
  - OPER-01
  - OPER-03

must_haves:
  truths:
    - "Gerente clica em ativo na lista e drawer lateral abre com ficha completa em abas"
    - "Aba Geral mostra dados do ativo, fotos e dados de aquisicao"
    - "Aba Documentos lista documentos com vencimento e permite adicionar novo documento"
    - "Aba Combustivel mostra historico de abastecimentos e estatisticas com benchmarking"
    - "Aba Leituras mostra historico de horimetro/odometro e permite nova leitura com validacao anti-regressao"
    - "Aba Manutencao mostra empty state com mensagem sobre Fase 18"
    - "Documentos vencendo mostram badge visual na lista de ativos"
  artifacts:
    - path: "apps/frontend/src/components/assets/AssetDrawer.tsx"
      provides: "Drawer lateral with tabbed detail view"
      min_lines: 80
    - path: "apps/frontend/src/components/assets/AssetFuelTab.tsx"
      provides: "Fuel records list + stats + form"
      min_lines: 80
    - path: "apps/frontend/src/components/assets/AssetReadingsTab.tsx"
      provides: "Meter readings list + form with anti-regression feedback"
      min_lines: 60
    - path: "apps/frontend/src/hooks/useAssetDetail.ts"
      provides: "Hook for fetching single asset detail"
      exports: ["useAssetDetail"]
  key_links:
    - from: "AssetDrawer.tsx"
      to: "useAssetDetail"
      via: "Fetches asset by ID on open"
      pattern: "useAssetDetail"
    - from: "AssetFuelTab.tsx"
      to: "/api/org/:orgId/fuel-records"
      via: "useFuelRecords hook"
      pattern: "useFuelRecords"
    - from: "AssetReadingsTab.tsx"
      to: "/api/org/:orgId/meter-readings"
      via: "useMeterReadings hook"
      pattern: "useMeterReadings"
    - from: "AssetsPage.tsx"
      to: "AssetDrawer.tsx"
      via: "Row click opens drawer with selectedAssetId"
      pattern: "AssetDrawer"
---

<objective>
Build the asset detail drawer (ficha completa) with tabbed view: general info, documents with expiry tracking, fuel records with benchmarking, meter readings with anti-regression, maintenance placeholder, and timeline. Wire the drawer to open from the asset list.

Purpose: The drawer lateral is the primary way users interact with individual assets. It surfaces operational data (fuel, readings, documents) without navigating away from the list, following the SAP/TOTVS pattern chosen by the user.
Output: Working AssetDrawer with 6 tabs, integrated into AssetsPage via row click.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-cadastro-de-ativos/16-CONTEXT.md
@.planning/phases/16-cadastro-de-ativos/16-UI-SPEC.md
@.planning/phases/16-cadastro-de-ativos/16-RESEARCH.md

@.planning/phases/16-cadastro-de-ativos/16-01-SUMMARY.md
@.planning/phases/16-cadastro-de-ativos/16-02-SUMMARY.md
@.planning/phases/16-cadastro-de-ativos/16-03-SUMMARY.md

<interfaces>
<!-- From Plan 03 outputs -->

From apps/frontend/src/types/asset.ts:
```typescript
export interface Asset { ... }  // Full interface
export type AssetType = 'MAQUINA' | 'VEICULO' | 'IMPLEMENTO' | 'BENFEITORIA' | 'TERRA';
export type AssetDocumentType = 'CRLV' | 'SEGURO' | 'REVISAO' | 'CCIR' | 'ITR' | 'LAUDO' | 'GARANTIA' | 'OUTRO';
export const ASSET_TYPE_LABELS: Record<AssetType, string>;
export const ASSET_STATUS_LABELS: Record<AssetStatus, string>;
```

Backend API contract (from Plan 01+02):
- GET /api/org/:orgId/assets/:id — Asset with relations + _count
- GET /api/org/:orgId/fuel-records?assetId=X — { data: FuelRecord[], total, page, limit }
- GET /api/org/:orgId/fuel-records/stats/:assetId — FuelStatsResult
- POST /api/org/:orgId/fuel-records — CreateFuelRecordInput
- GET /api/org/:orgId/meter-readings?assetId=X — { data: MeterReading[], total, page, limit }
- GET /api/org/:orgId/meter-readings/latest/:assetId — { hourmeter, odometer }
- POST /api/org/:orgId/meter-readings — CreateMeterReadingInput (rejects regression)
- GET /api/org/:orgId/asset-documents?assetId=X — { data: AssetDocument[], total }
- POST /api/org/:orgId/asset-documents — CreateAssetDocumentInput
- GET /api/org/:orgId/asset-documents/expiring — { expired[], urgent[], warning[], upcoming[] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detail hooks + AssetDrawer shell + General/Documents/Maintenance/Timeline tabs</name>
  <files>
    apps/frontend/src/hooks/useAssetDetail.ts,
    apps/frontend/src/hooks/useAssetDocuments.ts,
    apps/frontend/src/components/assets/AssetDrawer.tsx,
    apps/frontend/src/components/assets/AssetDrawer.css,
    apps/frontend/src/components/assets/AssetGeneralTab.tsx,
    apps/frontend/src/components/assets/AssetDocumentsTab.tsx,
    apps/frontend/src/components/assets/AssetMaintenanceTab.tsx,
    apps/frontend/src/components/assets/AssetTimelineTab.tsx,
    apps/frontend/src/pages/AssetsPage.tsx
  </files>
  <read_first>
    apps/frontend/src/types/asset.ts,
    apps/frontend/src/hooks/useAssets.ts,
    apps/frontend/src/pages/AssetsPage.tsx,
    apps/frontend/src/pages/AnimalDetailPage.tsx,
    apps/frontend/src/styles/tokens.css,
    apps/frontend/src/components/ui/ConfirmModal.tsx
  </read_first>
  <action>
1. Create `apps/frontend/src/hooks/useAssetDetail.ts`:
   - `useAssetDetail(assetId: string | null)`:
     - State: asset (Asset | null), loading, error
     - `fetchAsset()`: GET /api/org/{orgId}/assets/{assetId} — only fetch when assetId is not null
     - useEffect on assetId change: fetch if not null, clear if null
     - `refetch()`: re-fetch current asset
     - Return { asset, loading, error, refetch }

2. Create `apps/frontend/src/hooks/useAssetDocuments.ts`:
   - `useAssetDocuments(assetId: string | null)`:
     - State: documents (AssetDocument[]), loading, error, expiringDocs
     - AssetDocument type (define inline or add to asset.ts): { id, assetId, documentType, documentName, description, expiresAt, fileUrl, createdAt }
     - `fetchDocuments()`: GET /api/org/{orgId}/asset-documents?assetId={assetId}
     - `fetchExpiring()`: GET /api/org/{orgId}/asset-documents/expiring (for the badge on main list)
     - `createDocument(input)`: POST /api/org/{orgId}/asset-documents, then refetch
     - `deleteDocument(id)`: DELETE /api/org/{orgId}/asset-documents/{id}, then refetch
     - Return { documents, expiringDocs, loading, error, createDocument, deleteDocument, fetchExpiring }

3. Create `apps/frontend/src/components/assets/AssetDrawer.tsx`:

Props: `assetId: string | null`, `isOpen: boolean`, `onClose: () => void`, `onEdit: (asset: Asset) => void`

**Drawer layout:**
- Overlay: semi-transparent backdrop, click to close
- Panel: slides in from right, 480px width on desktop, full-width on mobile (<768px)
- Animation: 300ms ease-out open, 300ms ease-in close (--duration-normal)
- Close button: X icon top-right, aria-label="Fechar"

**Header (inside drawer):**
- Asset name as `<h2>` (DM Sans 700 24px)
- Asset tag badge (JetBrains Mono 14px, --color-neutral-200 bg pill)
- Status badge (icon + text, colored)
- Edit button (Pencil icon) and Delete button (Trash2 icon) in header, 48x48px touch targets

**Tab bar** (below header):
- `role="tablist"`, horizontal
- Tabs: Geral | Documentos | Combustivel | Leituras | Manutencao | Timeline
- Each tab: `role="tab"`, `aria-selected`, `aria-controls`
- Active tab: bottom border 2px solid --color-primary-600, font-weight 600
- Tab content: `role="tabpanel"`, rendered lazily per active tab
- Tab switch animation: 200ms ease-out (--duration-fast)
- On mobile: tabs scroll horizontally if overflow

**Loading state:** Skeleton inside drawer (3 skeleton blocks matching tab content)
**Error state:** "Nao foi possivel carregar os dados deste ativo. Volte e tente novamente."

4. Create `apps/frontend/src/components/assets/AssetGeneralTab.tsx`:
Props: `asset: Asset`

Sections:
- **Dados Gerais**: name, type, classification (label from ASSET_CLASSIFICATION_LABELS), farm, status, description. Read-only display.
- **Dados de Aquisicao**: acquisitionDate (formatted), acquisitionValue (R$ formatted), supplier name, invoice number, cost center name. Show "Nao informado" for null values in muted text.
- **Dados Operacionais** (conditional by type):
  - MAQUINA: HP, fuel type, manufacturer, model, year, serial, current hourmeter
  - VEICULO: RENAVAM (mono), plate (mono), manufacturer, model, year, current odometer, fuel type
  - IMPLEMENTO: manufacturer, model, year, serial, parent machine name+tag (link)
  - BENFEITORIA: material, area m2, capacity, coordinates (if present)
  - TERRA: registration number (mono), area ha, CAR code
- **Fotos**: grid of photo thumbnails from photoUrls array. Click to enlarge (simple lightbox or new tab). "Nenhuma foto cadastrada." if empty.
- **Ativos vinculados** (only for MAQUINA with childAssets): list of linked implements with name + tag

5. Create `apps/frontend/src/components/assets/AssetDocumentsTab.tsx`:
Props: `assetId: string`

Uses useAssetDocuments hook.
- **Add document form** (compact inline form at top): documentType select (8 options from AssetDocumentType), documentName text input, expiresAt date input, "Adicionar" button
- **Documents list**: table/cards with columns: Tipo, Nome, Vencimento, Status badge
  - Expired: red badge "Vencido" + AlertTriangle icon
  - Expiring <=7 days: red badge "Vence em X dias" + AlertTriangle icon
  - Expiring <=30 days: yellow badge "Vence em X dias" + Clock icon
  - No expiry: neutral badge "Sem vencimento"
- Delete button per document (Trash2, aria-label="Excluir documento")
- Empty state: "Nenhum documento cadastrado. Adicione documentos como CRLV, seguro ou laudos."

6. Create `apps/frontend/src/components/assets/AssetMaintenanceTab.tsx`:
Props: `assetId: string`
- Empty state only: Wrench icon 48px --color-neutral-400
- "Manutencoes disponiveis na Fase 18. Por enquanto, os ativos estao sendo cadastrados."

7. Create `apps/frontend/src/components/assets/AssetTimelineTab.tsx`:
Props: `asset: Asset`
- Timeline of events derived from asset data:
  - "Ativo cadastrado" — asset.createdAt
  - "Ultima atualizacao" — asset.updatedAt (if different from createdAt)
  - Future phases will add: maintenance events, meter readings, fuel records
- Each event: date (formatted), description, icon
- Vertical timeline line with dots (--color-neutral-300 line, --color-primary-600 dots)
- If only 1 event (just creation): show it with note "Eventos futuros aparecerao aqui conforme o ativo for utilizado."

8. Modify `apps/frontend/src/pages/AssetsPage.tsx` to integrate the drawer:
- Add state: `selectedAssetId: string | null`, `drawerOpen: boolean`
- On row click (table or card): set selectedAssetId and drawerOpen=true
- On drawer close: set drawerOpen=false, selectedAssetId=null
- On drawer edit button: close drawer, open AssetModal with the asset
- Render `<AssetDrawer assetId={selectedAssetId} isOpen={drawerOpen} onClose={...} onEdit={...} />`
- Add expiry badge to asset list: if asset has documents expiring within 30 days, show yellow/red dot indicator next to asset name in the table. Use useAssetDocuments fetchExpiring on page load to get org-wide expiring docs, cross-reference by assetId.
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - useAssetDetail.ts contains `export function useAssetDetail`
    - useAssetDocuments.ts contains `export function useAssetDocuments`
    - useAssetDocuments.ts contains `fetchExpiring` function
    - AssetDrawer.tsx contains `role="tablist"` with 6 tabs
    - AssetDrawer.tsx contains `aria-selected` on tab elements
    - AssetDrawer.tsx contains `role="tabpanel"` for tab content
    - AssetDrawer.tsx renders AssetGeneralTab, AssetDocumentsTab, AssetFuelTab, AssetReadingsTab, AssetMaintenanceTab, AssetTimelineTab
    - AssetGeneralTab.tsx renders conditional sections for each AssetType
    - AssetGeneralTab.tsx renders photoUrls grid
    - AssetDocumentsTab.tsx renders inline form with documentType select (8 options)
    - AssetDocumentsTab.tsx renders expiry badges (Vencido, Vence em X dias)
    - AssetMaintenanceTab.tsx contains "Manutencoes disponiveis na Fase 18"
    - AssetTimelineTab.tsx renders timeline with createdAt event
    - AssetsPage.tsx contains `<AssetDrawer` component
    - AssetsPage.tsx contains selectedAssetId state
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>AssetDrawer opens from list row click, shows 6 tabs. GeneralTab displays all asset data by type. DocumentsTab manages documents with expiry badges. MaintenanceTab shows Phase 18 placeholder. TimelineTab shows creation event. Drawer integrated into AssetsPage.</done>
</task>

<task type="auto">
  <name>Task 2: Fuel + Readings tabs with forms and benchmarking</name>
  <files>
    apps/frontend/src/hooks/useFuelRecords.ts,
    apps/frontend/src/hooks/useMeterReadings.ts,
    apps/frontend/src/components/assets/AssetFuelTab.tsx,
    apps/frontend/src/components/assets/AssetReadingsTab.tsx
  </files>
  <read_first>
    apps/frontend/src/types/asset.ts,
    apps/frontend/src/hooks/useAssetDetail.ts,
    apps/frontend/src/components/assets/AssetDrawer.tsx,
    apps/frontend/src/services/api.ts,
    apps/frontend/src/styles/tokens.css
  </read_first>
  <action>
1. Create `apps/frontend/src/hooks/useFuelRecords.ts`:
   - Types (define inline or add to asset.ts):
     ```typescript
     export interface FuelRecord {
       id: string;
       assetId: string;
       farmId: string;
       fuelDate: string;
       liters: string;
       pricePerLiter: string;
       totalCost: string;
       hourmeterAtFuel: string | null;
       odometerAtFuel: string | null;
       notes: string | null;
       createdAt: string;
     }
     export interface FuelStats {
       assetAvgLitersPerHour: number | null;
       fleetAvgLitersPerHour: number | null;
       assetCostPerHour: number | null;
       fleetCostPerHour: number | null;
       totalLiters: string;
       totalCost: string;
       recordCount: number;
     }
     ```
   - `useFuelRecords(assetId: string | null)`:
     - State: records (FuelRecord[]), stats (FuelStats | null), loading, error
     - `fetchRecords()`: GET /api/org/{orgId}/fuel-records?assetId={assetId}
     - `fetchStats()`: GET /api/org/{orgId}/fuel-records/stats/{assetId}
     - `createRecord(input)`: POST /api/org/{orgId}/fuel-records, then refetch records + stats
     - `deleteRecord(id)`: DELETE /api/org/{orgId}/fuel-records/{id}, then refetch
     - useEffect: fetch on assetId change
     - Return { records, stats, loading, error, createRecord, deleteRecord }

2. Create `apps/frontend/src/hooks/useMeterReadings.ts`:
   - Types:
     ```typescript
     export interface MeterReading {
       id: string;
       assetId: string;
       readingDate: string;
       readingType: 'HOURMETER' | 'ODOMETER';
       value: string;
       previousValue: string | null;
       createdAt: string;
     }
     export interface LatestReadings {
       hourmeter: MeterReading | null;
       odometer: MeterReading | null;
     }
     ```
   - `useMeterReadings(assetId: string | null)`:
     - State: readings (MeterReading[]), latest (LatestReadings | null), loading, error, submitError
     - `fetchReadings()`: GET /api/org/{orgId}/meter-readings?assetId={assetId}
     - `fetchLatest()`: GET /api/org/{orgId}/meter-readings/latest/{assetId}
     - `createReading(input)`: POST /api/org/{orgId}/meter-readings. On 400 error with anti-regression message, set submitError (do NOT use generic toast — show inline error). On success, refetch readings + latest, clear submitError.
     - Return { readings, latest, loading, error, submitError, createReading }

3. Create `apps/frontend/src/components/assets/AssetFuelTab.tsx`:
Props: `assetId: string`, `assetType: AssetType`

Uses useFuelRecords hook.

Layout:
- **Stats cards** (top, 2x2 grid):
  - Card 1: "Custo/hora este ativo" — stats.assetCostPerHour formatted as R$/h (or "N/D" if null)
  - Card 2: "Custo/hora media frota" — stats.fleetCostPerHour formatted as R$/h
  - Card 3: "Total litros" — stats.totalLiters formatted
  - Card 4: "Total custo" — stats.totalCost formatted as R$
  - If assetCostPerHour > fleetCostPerHour: show red indicator "Acima da media" on card 1
  - If assetCostPerHour <= fleetCostPerHour: show green indicator "Dentro da media" on card 1

- **Add fuel record form** (compact inline):
  - Data (date input, required)
  - Litros (number input, required)
  - Preco/litro (number input with R$ prefix, required)
  - Horimetro no abastecimento (number, optional — only show for MAQUINA)
  - Odometro no abastecimento (number, optional — only show for VEICULO)
  - Observacoes (text, optional)
  - "Registrar abastecimento" button (secondary)
  - After submit: toast "Abastecimento registrado com sucesso."

- **Records list** (below form):
  - Table: Data | Litros | R$/L | Total | Horimetro/Odometro | Acoes
  - Delete button per record (Trash2, aria-label="Excluir abastecimento")
  - Sort by fuelDate desc (most recent first)

- **Empty state**: Fuel icon 48px + "Nenhum abastecimento registrado. Registre o consumo de combustivel deste ativo."

4. Create `apps/frontend/src/components/assets/AssetReadingsTab.tsx`:
Props: `assetId: string`, `assetType: AssetType`

Uses useMeterReadings hook.

Layout:
- **Current readings display** (top cards):
  - If assetType is MAQUINA: show "Horimetro atual: {latest.hourmeter.value} h" card
  - If assetType is VEICULO: show "Odometro atual: {latest.odometer.value} km" card
  - If both apply: show both cards side by side
  - If no readings: "Nenhuma leitura registrada"

- **Add reading form** (compact inline):
  - Tipo: select with options HOURMETER ("Horimetro") and ODOMETER ("Odometro"). Pre-select based on assetType (HOURMETER for MAQUINA, ODOMETER for VEICULO).
  - Data (date input, required)
  - Valor (number input, required)
  - "Registrar leitura" button (secondary)
  - **Inline error** (not toast): if submitError is set (anti-regression), show below the valor field with `role="alert"`, --color-error-500, AlertTriangle icon + message "Leitura nao pode ser menor ou igual a ultima registrada ({valor} {unidade})."
  - After successful submit: toast "Leitura registrada com sucesso."

- **Readings history** (below form):
  - Table: Data | Tipo | Valor | Anterior | Diferenca
  - Diferenca column: value - previousValue (show as "+X h" or "+X km")
  - Sort by readingDate desc

- **Empty state**: Gauge icon 48px + "Nenhuma leitura registrada. Atualize o horimetro ou odometro deste ativo."
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - useFuelRecords.ts contains `export function useFuelRecords`
    - useFuelRecords.ts contains `fetchStats` function
    - useMeterReadings.ts contains `export function useMeterReadings`
    - useMeterReadings.ts contains `submitError` state for inline anti-regression error
    - useMeterReadings.ts handles 400 error from POST by setting submitError (not generic toast)
    - AssetFuelTab.tsx renders 4 stats cards (custo/hora ativo, custo/hora frota, total litros, total custo)
    - AssetFuelTab.tsx renders benchmarking comparison ("Acima da media" or "Dentro da media")
    - AssetFuelTab.tsx renders inline form with litros, preco/litro, data fields
    - AssetFuelTab.tsx conditionally shows horimetro field for MAQUINA, odometro for VEICULO
    - AssetReadingsTab.tsx renders current reading display
    - AssetReadingsTab.tsx renders add reading form with tipo select
    - AssetReadingsTab.tsx renders inline error with `role="alert"` for anti-regression
    - AssetReadingsTab.tsx renders readings history table with diferenca column
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>AssetFuelTab shows benchmarking stats (asset vs fleet), inline form for new records, and records list. AssetReadingsTab shows current readings, add form with inline anti-regression error, and history with delta calculation. Both integrated into AssetDrawer tabs.</done>
</task>

</tasks>

<verification>
```bash
cd apps/frontend && npx tsc --noEmit
# Manual: open asset drawer, navigate all 6 tabs, add fuel record, add meter reading, verify anti-regression error
```
</verification>

<success_criteria>
- AssetDrawer opens from list row click with slide-in animation
- 6 tabs navigable with keyboard (role=tablist, aria-selected)
- GeneralTab shows type-specific data sections with photos
- DocumentsTab manages documents with expiry color coding
- FuelTab shows benchmarking stats and inline form
- ReadingsTab shows anti-regression inline error (not toast) when value <= previous
- MaintenanceTab shows Phase 18 placeholder
- TimelineTab shows creation event
- Expiry badge visible on asset list for assets with expiring documents
- All CLAUDE.md rules followed: tokens, accessibility, responsive, pt-BR voice
</success_criteria>

<output>
After completion, create `.planning/phases/16-cadastro-de-ativos/16-04-SUMMARY.md`
</output>
