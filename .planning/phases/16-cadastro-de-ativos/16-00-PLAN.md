---
phase: 16-cadastro-de-ativos
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/backend/src/modules/assets/assets.routes.spec.ts
  - apps/backend/src/modules/fuel-records/fuel-records.routes.spec.ts
  - apps/backend/src/modules/meter-readings/meter-readings.routes.spec.ts
  - apps/backend/src/modules/assets/asset-documents.routes.spec.ts
autonomous: true
requirements:
  - ATIV-01
  - ATIV-02
  - ATIV-04
  - ATIV-06
  - OPER-01
  - OPER-03

must_haves:
  truths:
    - 'All 4 spec files exist with failing stubs before any production code is written'
    - 'Running pnpm test with each testPathPattern results in test failures (RED state)'
  artifacts:
    - path: 'apps/backend/src/modules/assets/assets.routes.spec.ts'
      provides: 'Failing test stubs for asset CRUD, photo upload, geo point, export'
      min_lines: 40
    - path: 'apps/backend/src/modules/fuel-records/fuel-records.routes.spec.ts'
      provides: 'Failing test stubs for fuel record CRUD and benchmarking'
      min_lines: 20
    - path: 'apps/backend/src/modules/meter-readings/meter-readings.routes.spec.ts'
      provides: 'Failing test stubs for meter reading CRUD and anti-regression'
      min_lines: 20
    - path: 'apps/backend/src/modules/assets/asset-documents.routes.spec.ts'
      provides: 'Failing test stubs for document CRUD and expiring endpoint'
      min_lines: 20
  key_links: []
---

<objective>
Create Wave 0 test scaffolds: failing spec stubs for all backend modules in Phase 16. These files establish the RED state that Plans 01 and 02 will make GREEN.

Purpose: Nyquist compliance — every production code task has pre-existing test expectations. Executors write production code to satisfy these stubs, not invent tests after the fact.
Output: 4 spec files with failing test stubs covering ATIV-01, ATIV-02, ATIV-04, ATIV-06, OPER-01, OPER-03.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-cadastro-de-ativos/16-CONTEXT.md
@.planning/phases/16-cadastro-de-ativos/16-RESEARCH.md
@.planning/phases/16-cadastro-de-ativos/16-VALIDATION.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Executor needs the existing test pattern to replicate -->

From apps/backend/src/modules/suppliers/suppliers.routes.spec.ts (test scaffold pattern):

```typescript
import { describe, it, expect, beforeAll, afterAll } from '@jest/globals';
import request from 'supertest';
import { createApp } from '../../app';
import { prisma } from '../../shared/database/prisma';
// Setup: create org, user, auth token in beforeAll
// Teardown: clean in afterAll

describe('POST /api/org/:orgId/suppliers', () => {
  it('creates supplier with valid data', async () => {
    const res = await request(app)
      .post(`/api/org/${orgId}/suppliers`)
      .set('Authorization', `Bearer ${token}`)
      .send({ ... });
    expect(res.status).toBe(201);
  });
});
```

Backend API contract from RESEARCH.md:

- POST /api/org/:orgId/assets — creates asset with sequential PAT-NNNNN tag
- GET /api/org/:orgId/assets — paginated list with filters
- GET /api/org/:orgId/assets/:id — single asset with relations
- PATCH /api/org/:orgId/assets/:id — update
- DELETE /api/org/:orgId/assets/:id — soft delete
- POST /api/org/:orgId/assets/:id/photos — upload photo
- GET /api/org/:orgId/assets/export/csv — CSV export
- GET /api/org/:orgId/assets/export/pdf — PDF export
- POST /api/org/:orgId/fuel-records — create fuel record
- GET /api/org/:orgId/fuel-records/stats/:assetId — benchmarking stats
- POST /api/org/:orgId/meter-readings — create (rejects regression)
- GET /api/org/:orgId/meter-readings/latest/:assetId — latest readings
- POST /api/org/:orgId/asset-documents — create document
- GET /api/org/:orgId/asset-documents/expiring — grouped by urgency
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create failing spec stubs for assets, fuel-records, meter-readings, and asset-documents</name>
  <files>
    apps/backend/src/modules/assets/assets.routes.spec.ts,
    apps/backend/src/modules/fuel-records/fuel-records.routes.spec.ts,
    apps/backend/src/modules/meter-readings/meter-readings.routes.spec.ts,
    apps/backend/src/modules/assets/asset-documents.routes.spec.ts
  </files>
  <read_first>
    apps/backend/src/modules/suppliers/suppliers.routes.spec.ts,
    apps/backend/src/modules/animals/animals.routes.spec.ts,
    apps/backend/jest.config.js
  </read_first>
  <action>
Create 4 spec files with `describe` blocks and `it.todo()` stubs. Each stub describes a behavior that Plans 01 and 02 will implement. Use the existing test scaffold pattern (beforeAll setup with org/user/token, afterAll cleanup).

**IMPORTANT:** These are stubs only. Use `it.todo('description')` for each test case. The files must parse and be recognized by Jest, but every test must be in TODO state (pending). Do NOT write test bodies — Plans 01 and 02 do that.

**1. `apps/backend/src/modules/assets/assets.routes.spec.ts`:**

```typescript
import { describe, it } from '@jest/globals';

describe('Assets API', () => {
  describe('POST /api/org/:orgId/assets', () => {
    it.todo('creates asset with auto-generated PAT-00001 tag');
    it.todo('increments tag to PAT-00002 on second create');
    it.todo('creates IMPLEMENTO with parentAssetId pointing to MAQUINA');
    it.todo('rejects IMPLEMENTO with parentAssetId pointing to VEICULO (400)');
    it.todo('forces TERRA classification to NON_DEPRECIABLE_CPC27');
    it.todo('stores BENFEITORIA geoPoint as PostGIS geometry');
    it.todo('returns 400 when required fields are missing');
  });

  describe('GET /api/org/:orgId/assets', () => {
    it.todo('returns paginated list');
    it.todo('filters by assetType');
    it.todo('filters by status');
    it.todo('filters by farmId');
    it.todo('filters by minValue/maxValue on acquisitionValue');
    it.todo('searches by name, tag, and serialNumber');
  });

  describe('GET /api/org/:orgId/assets/summary', () => {
    it.todo('returns totalAssets, totalValue, inMaintenance counts');
  });

  describe('GET /api/org/:orgId/assets/:id', () => {
    it.todo('returns asset with farm, supplier, costCenter relations');
    it.todo('returns 404 for nonexistent asset');
    it.todo('returns 404 for soft-deleted asset');
  });

  describe('PATCH /api/org/:orgId/assets/:id', () => {
    it.todo('updates asset fields');
    it.todo('returns 404 for nonexistent asset');
  });

  describe('DELETE /api/org/:orgId/assets/:id', () => {
    it.todo('soft-deletes asset (sets deletedAt)');
    it.todo('soft-deleted asset excluded from GET list');
  });

  describe('POST /api/org/:orgId/assets/:id/photos', () => {
    it.todo('uploads photo and appends to photoUrls array');
  });

  describe('GET /api/org/:orgId/assets/export/csv', () => {
    it.todo('returns CSV with Content-Type text/csv');
  });

  describe('GET /api/org/:orgId/assets/export/pdf', () => {
    it.todo('returns PDF with Content-Type application/pdf');
  });
});
```

**2. `apps/backend/src/modules/fuel-records/fuel-records.routes.spec.ts`:**

```typescript
import { describe, it } from '@jest/globals';

describe('Fuel Records API', () => {
  describe('POST /api/org/:orgId/fuel-records', () => {
    it.todo('creates fuel record with correct totalCost (liters * pricePerLiter)');
    it.todo('returns 400 when required fields are missing');
  });

  describe('GET /api/org/:orgId/fuel-records', () => {
    it.todo('returns paginated list filtered by assetId');
    it.todo('sorts by fuelDate desc');
  });

  describe('GET /api/org/:orgId/fuel-records/stats/:assetId', () => {
    it.todo('returns asset average and fleet average liters/hour');
    it.todo('returns totalLiters and totalCost');
  });

  describe('DELETE /api/org/:orgId/fuel-records/:id', () => {
    it.todo('deletes fuel record');
  });
});
```

**3. `apps/backend/src/modules/meter-readings/meter-readings.routes.spec.ts`:**

```typescript
import { describe, it } from '@jest/globals';

describe('Meter Readings API', () => {
  describe('POST /api/org/:orgId/meter-readings', () => {
    it.todo('creates first reading (no previous) successfully');
    it.todo('creates reading and updates Asset.currentHourmeter');
    it.todo('rejects value less than previous (400)');
    it.todo('rejects value equal to previous (400)');
    it.todo('stores previousValue from last reading');
  });

  describe('GET /api/org/:orgId/meter-readings', () => {
    it.todo('returns paginated list filtered by assetId');
    it.todo('filters by readingType');
  });

  describe('GET /api/org/:orgId/meter-readings/latest/:assetId', () => {
    it.todo('returns latest HOURMETER and ODOMETER readings');
    it.todo('returns null for readings that do not exist');
  });
});
```

**4. `apps/backend/src/modules/assets/asset-documents.routes.spec.ts`:**

```typescript
import { describe, it } from '@jest/globals';

describe('Asset Documents API', () => {
  describe('POST /api/org/:orgId/asset-documents', () => {
    it.todo('creates document with documentType and expiresAt');
    it.todo('creates document without expiresAt');
  });

  describe('GET /api/org/:orgId/asset-documents', () => {
    it.todo('returns documents filtered by assetId');
    it.todo('filters by expiringWithinDays');
  });

  describe('GET /api/org/:orgId/asset-documents/expiring', () => {
    it.todo('returns expired, urgent, warning, upcoming groups');
    it.todo('correctly classifies documents by days until expiry');
  });

  describe('PATCH /api/org/:orgId/asset-documents/:id', () => {
    it.todo('updates document fields');
  });

  describe('DELETE /api/org/:orgId/asset-documents/:id', () => {
    it.todo('deletes document');
  });
});
```

Ensure each file has the correct Jest import and describe/it.todo structure. Do NOT add beforeAll/afterAll setup — that is added when Plans 01/02 fill in the test bodies.
</action>
<verify>
<automated>cd apps/backend && npx jest --listTests 2>&1 | grep -E "assets.routes.spec|fuel-records.routes.spec|meter-readings.routes.spec|asset-documents.routes.spec"</automated>
</verify>
<done>4 spec files exist and are recognized by Jest. All tests are in TODO (pending) state. No production code written yet.</done>
</task>

</tasks>

<verification>
```bash
cd apps/backend && npx jest --listTests 2>&1 | grep -E "assets|fuel-records|meter-readings|asset-documents"
```
</verification>

<success_criteria>

- 4 spec files exist at the expected paths
- Jest recognizes all 4 files as test suites
- All tests are it.todo() stubs (pending, not failing with errors)
- No production code created in this plan
  </success_criteria>

<output>
After completion, create `.planning/phases/16-cadastro-de-ativos/16-00-SUMMARY.md`
</output>
