---
phase: 29-ferias-afastamentos-rescisao-e-provisoes
plan: 05
type: execute
wave: 4
depends_on: ["29-02", "29-03", "29-04"]
files_modified:
  - apps/frontend/src/pages/EmployeeTerminationsPage.tsx
  - apps/frontend/src/pages/EmployeeTerminationsPage.css
  - apps/frontend/src/pages/PayrollProvisionsPage.tsx
  - apps/frontend/src/pages/PayrollProvisionsPage.css
autonomous: false
requirements:
  - FERIAS-03
  - FERIAS-04

must_haves:
  truths:
    - "EmployeeTerminationsPage renders list with all 5 type labels and status badges"
    - "Iniciar Rescisao CTA opens 3-step modal with ConfirmModal variant=danger before final submit"
    - "Termination calculation preview shows all 10+ line items with Total Liquido highlighted"
    - "TRCT PDF and GRRF PDF download buttons are available for PROCESSED/PAID terminations"
    - "PayrollProvisionsPage renders two tabs: Provisoes do Mes and Relatorio de Posicao"
    - "Calcular Provisoes CTA triggers batch calculation for selected month"
    - "Estornar action uses ConfirmModal variant=warning"
  artifacts:
    - path: "apps/frontend/src/pages/EmployeeTerminationsPage.tsx"
      provides: "Termination management page with list and 3-step wizard modal"
      min_lines: 200
    - path: "apps/frontend/src/pages/PayrollProvisionsPage.tsx"
      provides: "Provisions page with month calculation and cost center report"
      min_lines: 150
  key_links:
    - from: "EmployeeTerminationsPage.tsx"
      to: "useEmployeeTerminations.ts"
      via: "hook import"
      pattern: "useEmployeeTerminations"
    - from: "PayrollProvisionsPage.tsx"
      to: "usePayrollProvisions.ts"
      via: "hook import"
      pattern: "usePayrollProvisions"
---

<objective>
Create the EmployeeTerminationsPage and PayrollProvisionsPage frontend pages, then run a visual verification checkpoint to confirm all 4 Phase 29 pages are functional.

Purpose: Completes the frontend for termination processing (3-step wizard with calculation preview and PDF downloads) and provision management (batch calculation and cost center reporting).
Output: 2 fully functional pages + visual verification that all 4 Phase 29 features work end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-f-rias-afastamentos-rescis-o-e-provis-es/29-UI-SPEC.md
@.planning/phases/29-f-rias-afastamentos-rescis-o-e-provis-es/29-01-SUMMARY.md
@.planning/phases/29-f-rias-afastamentos-rescis-o-e-provis-es/29-02-SUMMARY.md
@.planning/phases/29-f-rias-afastamentos-rescis-o-e-provis-es/29-03-SUMMARY.md
@.planning/phases/29-f-rias-afastamentos-rescis-o-e-provis-es/29-04-SUMMARY.md

@apps/frontend/src/pages/VacationSchedulesPage.tsx
@apps/frontend/src/hooks/useEmployeeTerminations.ts
@apps/frontend/src/hooks/usePayrollProvisions.ts
@apps/frontend/src/types/termination.ts
@apps/frontend/src/types/provision.ts
@apps/frontend/src/components/ui/ConfirmModal.tsx

<interfaces>
From types/termination.ts (created in Plan 04):
```typescript
export type TerminationType = 'WITHOUT_CAUSE' | 'WITH_CAUSE' | 'VOLUNTARY' | 'SEASONAL_END' | 'MUTUAL_AGREEMENT';
export const TERMINATION_TYPE_LABELS: Record<TerminationType, string>;
export interface EmployeeTermination { ... }
export interface CreateTerminationInput { ... }
```

From types/provision.ts (created in Plan 04):
```typescript
export interface PayrollProvision { ... }
export interface ProvisionReportRow { ... }
export interface CalculateProvisionsResult { ... }
```

From hooks/useEmployeeTerminations.ts (created in Plan 04):
```typescript
export function useEmployeeTerminations(): {
  terminations, loading, error, successMessage, errorMessage,
  fetchTerminations, processTermination, confirmTermination, markAsPaid,
  getTrctPdf, getGrrfPdf, fetchExpiringDeadlines
}
```

From hooks/usePayrollProvisions.ts (created in Plan 04):
```typescript
export function usePayrollProvisions(): {
  provisions, report, loading, error, successMessage, errorMessage,
  fetchProvisions, calculateProvisions, reverseProvision, fetchReport, exportReport
}
```

UI-SPEC termination modal steps:
- Step 1: employee selector (ACTIVE only), termination type, date, notice period type
- Step 2: read-only breakdown table (Rubrica | Valor) with 10+ rows, Total Liquido highlighted
- Step 3: ConfirmModal danger inline + generate TRCT

UI-SPEC provisions layout:
- Tab 1 "Provisoes do Mes": table grouped by employee
- Tab 2 "Relatorio de Posicao": summary by cost center + provision type
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EmployeeTerminationsPage — list, 3-step wizard modal, PDF downloads</name>
  <files>
    apps/frontend/src/pages/EmployeeTerminationsPage.tsx,
    apps/frontend/src/pages/EmployeeTerminationsPage.css
  </files>
  <read_first>
    - apps/frontend/src/pages/VacationSchedulesPage.tsx (just created — page structure, tab, modal, breadcrumb patterns)
    - apps/frontend/src/pages/VacationSchedulesPage.css (CSS patterns)
    - apps/frontend/src/hooks/useEmployeeTerminations.ts (available functions, return types)
    - apps/frontend/src/types/termination.ts (EmployeeTermination, TERMINATION_TYPE_LABELS)
    - apps/frontend/src/components/ui/ConfirmModal.tsx (ConfirmModal props — isOpen, onClose, onConfirm, variant, title, message)
    - .planning/phases/29-f-rias-afastamentos-rescis-o-e-provis-es/29-UI-SPEC.md (EmployeeTerminationsPage + EmployeeTerminationModal sections)
  </read_first>
  <action>
    **EmployeeTerminationsPage.tsx:**

    1. **Page layout:** breadcrumb "RH > Rescisoes", page title "Rescisoes" (DM Sans 700 20px), primary CTA "Iniciar Rescisao" (UserMinus icon, primary green, one per screen)

    2. **Filter bar:**
       - Employee search: text input, min 2 chars, 300ms debounce
       - Termination type: select with TERMINATION_TYPE_LABELS options
       - Status: select (DRAFT, PROCESSED, PAID)
       - All filters apply instantly (no submit button)

    3. **Table:**
       - Columns: Colaborador, Tipo Rescisao, Data Rescisao, Aviso Previo, Saldo Salario, Total Bruto, Total Liquido, Prazo Pagamento, Status, Acoes
       - Type column: TERMINATION_TYPE_LABELS
       - Monetary columns: right-aligned, JetBrains Mono 14px, formatCurrency
       - Aviso Previo: "X dias" text
       - Status badges: DRAFT→neutral "RASCUNHO", PROCESSED→info-blue "PROCESSADO", PAID→success-green "PAGO"
       - Payment deadline alert: warning badge "Pagar ate [data]" in warning-amber when within 3 days; error-red when past deadline
       - Actions:
         - DRAFT: "Confirmar" (secondary, triggers confirmTermination), "Editar" (secondary)
         - PROCESSED: "Pagar" (secondary), "TRCT" (FileText icon, downloads TRCT PDF), "GRRF" (Download icon, downloads GRRF PDF)
         - PAID: "TRCT" + "GRRF" download buttons only
       - PDF download buttons: aria-label="Baixar TRCT de [nome do colaborador]" per UI-SPEC accessibility

    4. **EmployeeTerminationModal (3-step wizard):**
       - **Step 1 "Dados da Rescisao":**
         - Employee selector (only ACTIVE status, not AFASTADO with stability)
         - Termination type selector (5 options from TERMINATION_TYPE_LABELS)
         - Termination date picker
         - Notice period type selector (Trabalhado, Indenizado, Dispensado)
         - "Proximo" button
       - **Step 2 "Conferir Calculo":**
         - Read-only breakdown table: two columns (Rubrica | Valor)
         - Rows rendered from processTermination response:
           - Saldo de Salario
           - Aviso Previo (show "X dias" in Rubrica)
           - 13o Proporcional
           - Ferias Vencidas
           - Ferias Proporcionais
           - 1/3 Ferias
           - Multa FGTS (label adapts: "Multa FGTS 40%" / "Multa FGTS 20%" / "Sem multa FGTS")
           - **separator line (border-top)**
           - Total Bruto
           - INSS (negative)
           - IRRF (negative)
           - **Total Liquido (bold, 16px, border-top separator)**
         - All amounts: JetBrains Mono, right-aligned
         - Null/missing values: em dash "---"
         - "Voltar" + "Confirmar Rescisao" buttons
       - **Step 3 "Confirmar e Gerar TRCT":**
         - ConfirmModal variant="danger" (inline, not separate modal)
         - Confirmation text: "Iniciar a rescisao bloqueia o contrato do colaborador. Confirme apenas apos revisar todos os dados."
         - On confirm: calls confirmTermination, generates TRCT, shows toast "Rescisao processada. TRCT disponivel para download."

    5. **Destructive confirmation:** ConfirmModal variant="danger" (medium criticality --- NOT ConfirmDeleteModal with name typing, per UI-SPEC). Uses ConfirmModal from `@/components/ui/ConfirmModal`.

    6. **Empty state:** UserMinus icon 48px + "Nenhuma rescisao registrada" + "Ao iniciar uma rescisao, o sistema calcula automaticamente todos os direitos trabalhistas e gera o TRCT." + "Iniciar Rescisao" CTA

    7. **Skeleton loading:** 5 rows. Accessibility: semantic table, aria-labels, focus visible, prefers-reduced-motion.

    **EmployeeTerminationsPage.css:** Same patterns as VacationSchedulesPage.css --- table, filter, badges, modal, wizard steps. Calculation preview table: compact, no hover, JetBrains Mono for values. Total Liquido row: font-weight 700, font-size 16px, border-top 1px solid --color-neutral-200.
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - EmployeeTerminationsPage.tsx contains `Iniciar Rescisao` CTA button text
    - EmployeeTerminationsPage.tsx contains `useEmployeeTerminations` hook import
    - EmployeeTerminationsPage.tsx contains `TERMINATION_TYPE_LABELS` import
    - EmployeeTerminationsPage.tsx contains `ConfirmModal` import
    - EmployeeTerminationsPage.tsx contains `variant="danger"` or `variant='danger'`
    - EmployeeTerminationsPage.tsx contains `Conferir Calculo` step label
    - EmployeeTerminationsPage.tsx contains `Total Liquido` text
    - EmployeeTerminationsPage.tsx contains `Multa FGTS` text
    - EmployeeTerminationsPage.tsx contains `aria-label` with `Baixar TRCT` pattern
    - EmployeeTerminationsPage.tsx contains `scope="col"` on table headers
    - EmployeeTerminationsPage.tsx contains `Pagar ate` deadline warning
    - EmployeeTerminationsPage.css exists with calculation preview styles
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>EmployeeTerminationsPage fully implements UI-SPEC with 3-step wizard, calculation preview, PDF downloads, deadline alerts, and ConfirmModal danger confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: PayrollProvisionsPage — tabs, month calculation, cost center report</name>
  <files>
    apps/frontend/src/pages/PayrollProvisionsPage.tsx,
    apps/frontend/src/pages/PayrollProvisionsPage.css
  </files>
  <read_first>
    - apps/frontend/src/pages/VacationSchedulesPage.tsx (tab pattern reference)
    - apps/frontend/src/pages/EmployeeTerminationsPage.tsx (just created --- page structure reference)
    - apps/frontend/src/hooks/usePayrollProvisions.ts (available functions)
    - apps/frontend/src/types/provision.ts (PayrollProvision, ProvisionReportRow, CalculateProvisionsResult)
    - apps/frontend/src/components/ui/ConfirmModal.tsx (for estorno confirmation)
    - .planning/phases/29-f-rias-afastamentos-rescis-o-e-provis-es/29-UI-SPEC.md (PayrollProvisionsPage section)
  </read_first>
  <action>
    **PayrollProvisionsPage.tsx:**

    1. **Page layout:** breadcrumb "RH > Provisoes", page title "Provisoes" (DM Sans 700 20px), primary CTA "Calcular Provisoes" (BarChart3 icon, primary green, one per screen)

    2. **Month selector:** select input with month/year options (e.g., "Mar/2026") --- same format as PayrollRunsPage getMonthLabel. Controls which month's data is shown.

    3. **Two tabs:** "Provisoes do Mes" | "Relatorio de Posicao"

    4. **Tab "Provisoes do Mes":**
       - Table grouped by employee (employee name as group header row)
       - Columns: Colaborador, Mes Referencia, Tipo, Salario Base, Provisao, Encargos, Total, Centro de Custo, Acoes
       - Tipo column: "Ferias" or "Decimo Terceiro"
       - Monetary columns: right-aligned, JetBrains Mono 14px, formatCurrency
       - Centro de Custo: show name or em dash "---" if null
       - Reversed rows: strikethrough text + "ESTORNADO" badge in neutral gray
       - Actions: "Estornar" button (secondary, triggers ConfirmModal variant="warning" with text "Estornar a provisao de [tipo] de [mes]? Os valores serao removidos do passivo apurado.")
       - Summary row at bottom: totals for Provisao, Encargos, Total columns (bold, border-top)

    5. **Tab "Relatorio de Posicao":**
       - Summary table by cost center
       - Columns: Centro de Custo, Total Ferias, Total 13o, Total Encargos, Total Geral
       - All monetary: right-aligned, JetBrains Mono, formatCurrency
       - Grand total row at bottom (bold, border-top)
       - Export button: "Exportar CSV" (secondary, Download icon) --- calls exportReport

    6. **"Calcular Provisoes" CTA action:**
       - Triggers calculateProvisions(referenceMonth) via hook
       - On success: shows toast "Provisoes de [mes/ano] calculadas para [N] colaboradores."
       - On conflict (409): shows error toast "Provisoes de [mes/ano] ja foram calculadas. Estorne as existentes antes de recalcular."

    7. **Empty state:** BarChart3 icon 48px + "Provisoes nao calculadas para este mes" + "Clique em Calcular Provisoes para apurar ferias e 13o de todos os colaboradores ativos." + "Calcular Provisoes" CTA

    8. **Skeleton loading:** 5 rows. Accessibility: semantic table, aria-labels, focus visible, prefers-reduced-motion.

    **PayrollProvisionsPage.css:** Same patterns --- table, tabs, badges, summary rows. Strikethrough for reversed: `text-decoration: line-through; opacity: 0.6;`. Summary row: font-weight 700, border-top.
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - PayrollProvisionsPage.tsx contains `Calcular Provisoes` CTA button text
    - PayrollProvisionsPage.tsx contains `usePayrollProvisions` hook import
    - PayrollProvisionsPage.tsx contains `Provisoes do Mes` tab label
    - PayrollProvisionsPage.tsx contains `Relatorio de Posicao` tab label
    - PayrollProvisionsPage.tsx contains `Estornar` action button text
    - PayrollProvisionsPage.tsx contains `ConfirmModal` import
    - PayrollProvisionsPage.tsx contains `variant="warning"` or `variant='warning'`
    - PayrollProvisionsPage.tsx contains `Exportar CSV` button text
    - PayrollProvisionsPage.tsx contains `Decimo Terceiro` provision type label
    - PayrollProvisionsPage.tsx contains `scope="col"` on table headers
    - PayrollProvisionsPage.tsx contains JetBrains Mono or font-mono class for monetary values
    - PayrollProvisionsPage.css exists with summary row and strikethrough styles
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PayrollProvisionsPage fully implements UI-SPEC with tabs, month calculation trigger, cost center report, reversal with ConfirmModal warning, and CSV export.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all 4 Phase 29 pages</name>
  <files>none</files>
  <action>
    All 4 Phase 29 pages have been implemented. Start backend and frontend, then verify each page renders correctly with proper data flow.
  </action>
  <what-built>
    All 4 Phase 29 pages: VacationSchedulesPage (ferias scheduling + calendar), EmployeeAbsencesPage (absence registration + payroll impact), EmployeeTerminationsPage (rescisao wizard + TRCT/GRRF PDF), PayrollProvisionsPage (monthly provisions + cost center report). Plus 4 backend modules with full CRUD, calculation, and tests.
  </what-built>
  <how-to-verify>
    1. Start backend and frontend: `cd apps/backend && pnpm dev` + `cd apps/frontend && pnpm dev`
    2. Navigate to sidebar — confirm RH group shows 4 new items: Ferias, Afastamentos, Rescisoes, Provisoes
    3. **Ferias** (/vacation-schedules):
       - Verify two tabs render (Periodos Aquisitivos / Agendamentos)
       - Click "Agendar Ferias" — verify 3-step modal opens
       - Check calendar toggle within Agendamentos tab
    4. **Afastamentos** (/employee-absences):
       - Verify filter bar (type, date range, search)
       - Click "Registrar Afastamento" — verify modal with type selector
       - Select "Acidente de Trabalho" — verify CAT number field appears
    5. **Rescisoes** (/employee-terminations):
       - Click "Iniciar Rescisao" — verify 3-step wizard
       - Step 2: verify calculation preview shows all line items
       - Step 3: verify ConfirmModal with danger variant appears
    6. **Provisoes** (/payroll-provisions):
       - Verify two tabs (Provisoes do Mes / Relatorio de Posicao)
       - Click "Calcular Provisoes" — verify it triggers batch calculation
       - Check "Estornar" button triggers warning confirmation
    7. Run backend tests: `cd apps/backend && npx jest --testPathPattern="vacations|absences|terminations|provisions" --no-coverage -q` — all pass
  </how-to-verify>
  <verify>User confirms all 4 pages render and function correctly</verify>
  <done>All 4 Phase 29 pages verified by user as functional and visually correct</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd apps/frontend && npx tsc --noEmit` — no type errors
- `cd apps/backend && npx jest --testPathPattern="vacation-schedules|employee-absences|employee-terminations|payroll-provisions" --no-coverage -q` — all pass
- All 4 pages render without console errors
- Sidebar shows all 4 new navigation items
</verification>

<success_criteria>
1. EmployeeTerminationsPage has 3-step wizard with calculation preview showing all line items
2. PayrollProvisionsPage has month calculation, cost center report tab, and reversal confirmation
3. Both pages follow UI-SPEC for colors, typography, spacing, and accessibility
4. All 4 Phase 29 pages are accessible via sidebar and render correctly
5. Full backend test suite for Phase 29 passes
</success_criteria>

<output>
After completion, create `.planning/phases/29-f-rias-afastamentos-rescis-o-e-provis-es/29-05-SUMMARY.md`
</output>
