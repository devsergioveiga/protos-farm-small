---
phase: 20-alienacao-baixa-ativos
plan: '02'
type: execute
wave: 2
depends_on: ['20-00']
files_modified:
  - apps/backend/src/modules/asset-farm-transfers/asset-farm-transfers.service.ts
  - apps/backend/src/modules/asset-farm-transfers/asset-farm-transfers.routes.ts
  - apps/backend/src/modules/asset-farm-transfers/asset-farm-transfers.routes.spec.ts
  - apps/backend/src/modules/asset-inventory/asset-inventory.service.ts
  - apps/backend/src/modules/asset-inventory/asset-inventory.routes.ts
  - apps/backend/src/modules/asset-inventory/asset-inventory.routes.spec.ts
  - apps/backend/src/app.ts
autonomous: true
requirements:
  - DISP-04
  - DISP-05

must_haves:
  truths:
    - 'Gerente pode transferir ativo entre fazendas da mesma org com historico'
    - 'Transferencia rejeita fazenda destino de outra organizacao'
    - 'Ativo alienado nao pode ser transferido'
    - 'Contador pode criar inventario, contar itens e reconciliar'
    - 'Inventario segue fluxo DRAFT -> COUNTING -> RECONCILED'
  artifacts:
    - path: 'apps/backend/src/modules/asset-farm-transfers/asset-farm-transfers.service.ts'
      provides: 'createTransfer, listTransfers service'
      exports: ['createTransfer', 'listTransfers']
    - path: 'apps/backend/src/modules/asset-farm-transfers/asset-farm-transfers.routes.ts'
      provides: 'POST transfer, GET transfer history'
      exports: ['assetFarmTransfersRouter']
    - path: 'apps/backend/src/modules/asset-inventory/asset-inventory.service.ts'
      provides: 'createInventory, countItems, reconcileInventory, listInventories'
      exports:
        ['createInventory', 'countItems', 'reconcileInventory', 'listInventories', 'getInventory']
    - path: 'apps/backend/src/modules/asset-inventory/asset-inventory.routes.ts'
      provides: 'CRUD + reconcile routes'
      exports: ['assetInventoryRouter']
  key_links:
    - from: 'asset-farm-transfers.service.ts'
      to: 'tx.farm.findFirst'
      via: 'same-org guard on destination farm'
      pattern: "organizationId.*ctx\\.organizationId"
    - from: 'asset-inventory.service.ts'
      to: 'tx.assetInventoryItem'
      via: 'DRAFT->COUNTING->RECONCILED state machine'
      pattern: 'status.*COUNTING|RECONCILED'
---

<objective>
Asset farm transfer and inventory reconciliation backend: two modules with service, routes, and integration tests.

Purpose: DISP-04 (transfer between farms with history) and DISP-05 (physical inventory count + reconciliation).
Output: Working endpoints for farm transfers and inventory management.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-alienacao-baixa-ativos/20-RESEARCH.md
@.planning/phases/20-alienacao-baixa-ativos/20-00-SUMMARY.md
</context>

<interfaces>
From apps/backend/src/modules/asset-farm-transfers/asset-farm-transfers.types.ts (Plan 00):
```typescript
export class AssetTransferError extends Error { ... }
export interface CreateTransferInput {
  toFarmId: string;
  transferDate: string;
  toCostCenterId?: string;
  notes?: string;
}
export interface TransferOutput { id, assetId, assetTag, assetName, fromFarmId, fromFarmName, toFarmId, toFarmName, ... }
export interface ListTransfersQuery { page?, limit? }
```

From apps/backend/src/modules/asset-inventory/asset-inventory.types.ts (Plan 00):

```typescript
export class AssetInventoryError extends Error { ... }
export const PHYSICAL_STATUSES = ['ENCONTRADO', 'NAO_ENCONTRADO', 'AVARIADO', 'DESCARTADO'] as const;
export type PhysicalStatus = (typeof PHYSICAL_STATUSES)[number];
export interface CreateInventoryInput { farmId?, notes? }
export interface CountItemInput { assetId, physicalStatus, notes? }
export interface ReconcileInventoryInput { items: { assetId, action? }[] }
export interface InventoryOutput { ... }
```

From apps/backend/src/modules/stock-inventories/stock-inventories.service.ts (reference for inventory flow):

```typescript
// DRAFT -> count items -> RECONCILED flow
// createInventory loads all products with balance into items
// reconcileInventory generates adjustments
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Asset farm transfers service + routes + tests</name>
  <files>
    apps/backend/src/modules/asset-farm-transfers/asset-farm-transfers.service.ts,
    apps/backend/src/modules/asset-farm-transfers/asset-farm-transfers.routes.ts,
    apps/backend/src/modules/asset-farm-transfers/asset-farm-transfers.routes.spec.ts
  </files>
  <read_first>
    apps/backend/src/modules/asset-farm-transfers/asset-farm-transfers.types.ts,
    apps/backend/src/modules/assets/assets.service.ts,
    apps/backend/src/modules/asset-acquisitions/asset-acquisitions.routes.ts,
    apps/backend/src/modules/asset-acquisitions/asset-acquisitions.routes.spec.ts
  </read_first>
  <behavior>
    - Test 1: POST /org/:orgId/asset-farm-transfers/:assetId/transfer creates transfer record + updates asset.farmId — returns 201
    - Test 2: Transfer also updates asset.costCenterId when toCostCenterId provided
    - Test 3: Transfer rejects when destination farm belongs to different org — returns 400
    - Test 4: Transfer rejects ALIENADO asset — returns 400
    - Test 5: Transfer rejects non-existent asset — returns 404
    - Test 6: GET /org/:orgId/asset-farm-transfers/:assetId/transfers returns transfer history array ordered by transferDate desc
    - Test 7: Transfer history is empty for asset with no transfers — returns 200 with empty array
  </behavior>
  <action>
1. Create `asset-farm-transfers.service.ts`:

**`createTransfer(ctx: RlsContext, assetId: string, input: CreateTransferInput, userId: string): Promise<TransferOutput>`**

- Use `prisma.$transaction` directly (same pattern as asset-acquisitions).
- Guard: find asset by id + organizationId + deletedAt null. 404 if not found. 400 if ALIENADO.
- Guard: find destination farm by toFarmId + organizationId. 400 with "Fazenda destino nao encontrada na organizacao" if not found.
- Guard: asset.farmId !== input.toFarmId (cannot transfer to same farm).
- Create AssetFarmTransfer record with fromFarmId=asset.farmId, fromCostCenterId=asset.costCenterId.
- Update asset: farmId=input.toFarmId, costCenterId=input.toCostCenterId ?? asset.costCenterId.
- Return TransferOutput with farm names included (query fromFarm and toFarm names).

**`listTransfers(ctx: RlsContext, assetId: string, query: ListTransfersQuery): Promise<{ data: TransferOutput[]; total: number }>`**

- Paginated query on AssetFarmTransfer where assetId + organizationId, ordered by transferDate desc.
- Include fromFarm.name, toFarm.name, asset.assetTag, asset.name.

2. Create `asset-farm-transfers.routes.ts`:
   - `export const assetFarmTransfersRouter = Router();`
   - `POST /org/:orgId/asset-farm-transfers/:assetId/transfer` — authenticate + checkPermission('assets:update')
   - `GET /org/:orgId/asset-farm-transfers/:assetId/transfers` — authenticate + checkPermission('assets:read')
   - Standard error handling pattern.

3. Create `asset-farm-transfers.routes.spec.ts`:
   - Setup: create org, 2 farms (same org), 1 farm (different org), user, asset.
   - Write all 7 tests from behavior block.
     </action>
     <verify>
     <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && pnpm --filter backend test -- --testPathPattern="asset-farm-transfers" --no-coverage</automated>
     </verify>
     <acceptance_criteria>
   - asset-farm-transfers.service.ts contains `prisma.$transaction`
   - asset-farm-transfers.service.ts contains `organizationId: ctx.organizationId` in farm lookup
   - asset-farm-transfers.service.ts contains `status === 'ALIENADO'` guard
   - asset-farm-transfers.routes.ts contains `checkPermission('assets:update')`
   - asset-farm-transfers.routes.spec.ts contains at least 6 `it(` or `test(` blocks
   - All tests pass with exit code 0
     </acceptance_criteria>
     <done>Transfer updates farmId + costCenterId atomically. Same-org guard enforced. Transfer history queryable. All 7 tests pass.</done>
     </task>

<task type="auto" tdd="true">
  <name>Task 2: Asset inventory service + routes + tests + app.ts wiring for both modules</name>
  <files>
    apps/backend/src/modules/asset-inventory/asset-inventory.service.ts,
    apps/backend/src/modules/asset-inventory/asset-inventory.routes.ts,
    apps/backend/src/modules/asset-inventory/asset-inventory.routes.spec.ts,
    apps/backend/src/app.ts
  </files>
  <read_first>
    apps/backend/src/modules/asset-inventory/asset-inventory.types.ts,
    apps/backend/src/modules/stock-inventories/stock-inventories.service.ts,
    apps/backend/src/modules/stock-inventories/stock-inventories.routes.ts,
    apps/backend/src/modules/stock-inventories/stock-inventories.routes.spec.ts,
    apps/backend/src/app.ts
  </read_first>
  <behavior>
    - Test 1: POST /org/:orgId/asset-inventories creates inventory in DRAFT status, auto-loads all ATIVO assets as items — returns 201
    - Test 2: POST with farmId filters assets to that farm only
    - Test 3: PATCH /org/:orgId/asset-inventories/:id/count updates items physicalStatus, transitions to COUNTING — returns 200
    - Test 4: Count rejects inventory in RECONCILED status — returns 400
    - Test 5: POST /org/:orgId/asset-inventories/:id/reconcile transitions to RECONCILED, sets reconciledAt — returns 200
    - Test 6: Reconcile rejects inventory in DRAFT status (must count first) — returns 400
    - Test 7: GET /org/:orgId/asset-inventories/:id returns full inventory with items
    - Test 8: GET /org/:orgId/asset-inventories returns paginated list
    - Test 9: Inventory divergenceCount counts items where physicalStatus != 'ENCONTRADO'
  </behavior>
  <action>
1. Create `asset-inventory.service.ts`:

**`createInventory(ctx: RlsContext, input: CreateInventoryInput, userId: string): Promise<InventoryOutput>`**

- Create AssetInventory record in DRAFT status.
- Query all assets where organizationId + status in ['ATIVO', 'INATIVO', 'EM_MANUTENCAO'] + deletedAt null + (farmId filter if provided).
- Create AssetInventoryItem for each asset with registeredStatus = asset.status, physicalStatus = null.
- Return full InventoryOutput.

**`countItems(ctx: RlsContext, inventoryId: string, items: CountItemInput[]): Promise<InventoryOutput>`**

- Guard: inventory must be DRAFT or COUNTING. 400 if RECONCILED or CANCELLED.
- Update each item's physicalStatus and notes.
- If status is DRAFT, transition to COUNTING.
- Return updated InventoryOutput.

**`reconcileInventory(ctx: RlsContext, inventoryId: string, userId: string): Promise<InventoryOutput>`**

- Guard: inventory must be COUNTING. 400 if DRAFT ("Realize a contagem antes de conciliar") or RECONCILED.
- Set status=RECONCILED, reconciledAt=now(), reconciledBy=userId.
- Return final InventoryOutput.

**`getInventory(ctx: RlsContext, inventoryId: string): Promise<InventoryOutput>`**

- Find by id + organizationId. Include items with asset details.
- Compute itemCount, countedCount (items where physicalStatus is not null), divergenceCount (items where physicalStatus != 'ENCONTRADO' and physicalStatus is not null).
- Map status labels and physicalStatus labels.

**`listInventories(ctx: RlsContext, query: ListInventoriesQuery): Promise<{ data: InventoryOutput[]; total: number }>`**

- Paginated query with optional status and farmId filters. Include \_count of items.

2. Create `asset-inventory.routes.ts`:
   - `export const assetInventoryRouter = Router();`
   - `POST /org/:orgId/asset-inventories` — authenticate + checkPermission('assets:update') — creates inventory
   - `GET /org/:orgId/asset-inventories` — authenticate + checkPermission('assets:read') — list
   - `GET /org/:orgId/asset-inventories/:id` — authenticate + checkPermission('assets:read') — get detail
   - `PATCH /org/:orgId/asset-inventories/:id/count` — authenticate + checkPermission('assets:update') — count items
   - `POST /org/:orgId/asset-inventories/:id/reconcile` — authenticate + checkPermission('assets:update') — reconcile

3. Create `asset-inventory.routes.spec.ts`:
   - Setup: create org, farm, user, 3 assets (2 ATIVO, 1 ALIENADO — alienado should be excluded).
   - Write all 9 tests from behavior block.

4. Wire BOTH routers in `app.ts`:
   - Add import: `import { assetFarmTransfersRouter } from './modules/asset-farm-transfers/asset-farm-transfers.routes';`
   - Add import: `import { assetInventoryRouter } from './modules/asset-inventory/asset-inventory.routes';`
   - Add: `app.use('/api', assetFarmTransfersRouter);` and `app.use('/api', assetInventoryRouter);` after assetDisposalsRouter (or assetAcquisitionsRouter if disposals not yet wired)
     </action>
     <verify>
     <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && pnpm --filter backend test -- --testPathPattern="asset-inventory" --no-coverage</automated>
     </verify>
     <acceptance_criteria>
   - asset-inventory.service.ts contains `status: { in: ['ATIVO', 'INATIVO', 'EM_MANUTENCAO']` for asset loading
   - asset-inventory.service.ts contains `status: 'COUNTING'` and `status: 'RECONCILED'` transitions
   - asset-inventory.routes.ts contains 5 route definitions (POST, GET list, GET detail, PATCH count, POST reconcile)
   - asset-inventory.routes.spec.ts contains at least 8 `it(` or `test(` blocks
   - app.ts contains `assetFarmTransfersRouter` and `assetInventoryRouter`
   - All tests pass with exit code 0
     </acceptance_criteria>
     <done>Inventory DRAFT->COUNTING->RECONCILED flow working. Auto-loads assets on creation. Count updates physical status. Reconcile finalizes. Both new routers wired in app.ts. All 9+ tests pass.</done>
     </task>

</tasks>

<verification>
- `pnpm --filter backend test -- --testPathPattern="asset-farm-transfers|asset-inventory" --no-coverage` passes
- Transfer enforces same-org guard
- Inventory follows DRAFT -> COUNTING -> RECONCILED flow
</verification>

<success_criteria>
Farm transfer and inventory reconciliation backends complete with all guards, state machine, and tests passing. Both routers wired in app.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/20-alienacao-baixa-ativos/20-02-SUMMARY.md`
</output>
