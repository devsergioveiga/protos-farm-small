---
phase: 20-alienacao-baixa-ativos
plan: '04'
type: execute
wave: 3
depends_on: ['20-01', '20-02', '20-03']
files_modified:
  - apps/frontend/src/types/asset.ts
  - apps/frontend/src/hooks/useAssetDisposal.ts
  - apps/frontend/src/hooks/useAssetTransfer.ts
  - apps/frontend/src/hooks/useAssetInventory.ts
  - apps/frontend/src/hooks/usePatrimonyDashboard.ts
  - apps/frontend/src/components/assets/AssetDisposalModal.tsx
  - apps/frontend/src/components/assets/AssetDisposalModal.css
  - apps/frontend/src/components/assets/AssetTransferModal.tsx
  - apps/frontend/src/components/assets/AssetTransferModal.css
  - apps/frontend/src/components/assets/AssetInventoryModal.tsx
  - apps/frontend/src/components/assets/AssetInventoryModal.css
  - apps/frontend/src/pages/AssetInventoryPage.tsx
  - apps/frontend/src/pages/AssetInventoryPage.css
  - apps/frontend/src/pages/PatrimonyDashboardPage.tsx
  - apps/frontend/src/pages/PatrimonyDashboardPage.css
  - apps/frontend/src/components/layout/Sidebar.tsx
  - apps/frontend/src/App.tsx
  - apps/frontend/src/pages/AssetsPage.tsx
  - apps/frontend/src/components/assets/AssetDrawer.tsx
autonomous: false
requirements:
  - DISP-01
  - DISP-02
  - DISP-03
  - DISP-04
  - DISP-05
  - DISP-06

must_haves:
  truths:
    - 'Gerente pode abrir modal de alienacao a partir da ficha do ativo e registrar venda/descarte/sinistro'
    - 'Modal de alienacao mostra preview de ganho/perda antes de confirmar'
    - 'Gerente pode transferir ativo entre fazendas via modal'
    - 'Contador pode criar e conciliar inventario patrimonial'
    - 'Dashboard patrimonial mostra KPIs de valor total, depreciacao, aquisicoes/baixas'
    - 'Sidebar tem links para inventario patrimonial e dashboard'
  artifacts:
    - path: 'apps/frontend/src/components/assets/AssetDisposalModal.tsx'
      provides: 'Disposal form modal with type selector, gain/loss preview, installment config'
    - path: 'apps/frontend/src/components/assets/AssetTransferModal.tsx'
      provides: 'Transfer form modal with destination farm selector'
    - path: 'apps/frontend/src/pages/AssetInventoryPage.tsx'
      provides: 'Inventory list + detail with count form and reconcile action'
    - path: 'apps/frontend/src/pages/PatrimonyDashboardPage.tsx'
      provides: 'KPI cards + charts for patrimony overview'
  key_links:
    - from: 'AssetDisposalModal.tsx'
      to: '/api/org/:orgId/asset-disposals/:assetId/dispose'
      via: 'useAssetDisposal hook POST'
      pattern: 'asset-disposals.*dispose'
    - from: 'AssetTransferModal.tsx'
      to: '/api/org/:orgId/asset-farm-transfers/:assetId/transfer'
      via: 'useAssetTransfer hook POST'
      pattern: 'asset-farm-transfers.*transfer'
    - from: 'PatrimonyDashboardPage.tsx'
      to: '/api/org/financial-dashboard/patrimony'
      via: 'usePatrimonyDashboard hook GET'
      pattern: 'financial-dashboard/patrimony'
---

<objective>
Frontend: disposal modal, transfer modal, inventory page, patrimony dashboard, hooks, types, sidebar/routing wiring.

Purpose: Complete UI for all 6 requirements (DISP-01 through DISP-06).
Output: All modals, pages, hooks, and navigation wired for asset lifecycle close operations.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-alienacao-baixa-ativos/20-RESEARCH.md
@.planning/phases/20-alienacao-baixa-ativos/20-01-SUMMARY.md
@.planning/phases/20-alienacao-baixa-ativos/20-02-SUMMARY.md
@.planning/phases/20-alienacao-baixa-ativos/20-03-SUMMARY.md
</context>

<interfaces>
From apps/frontend/src/types/asset.ts:
```typescript
export type AssetStatus = 'ATIVO' | 'INATIVO' | 'EM_MANUTENCAO' | 'ALIENADO' | 'EM_ANDAMENTO';
export interface Asset { id, organizationId, farmId, assetType, status, name, assetTag, acquisitionValue, ... }
```

From apps/frontend/src/hooks/useAssetAcquisition.ts (pattern reference):

```typescript
// Uses apiClient.post for mutations, useState for loading/error
```

From apps/frontend/src/components/assets/AssetModal.tsx (pattern reference):

```typescript
// Modal with isOpen, onClose, onSuccess props
// Form sections in CSS with .modal-section
```

From apps/frontend/src/components/assets/AssetDrawer.tsx:

```typescript
// Drawer with tabs: General, Fuel, Readings, Documents, Maintenance, Timeline
// Action buttons in header area
```

From backend types (Plan 00):

```typescript
// DisposalType: 'VENDA' | 'DESCARTE' | 'SINISTRO' | 'OBSOLESCENCIA'
// CreateDisposalInput: { disposalType, disposalDate, saleValue?, buyerName?, motivation?, dueDate?, installmentCount?, firstDueDate? }
// CreateTransferInput: { toFarmId, transferDate, toCostCenterId?, notes? }
// PhysicalStatus: 'ENCONTRADO' | 'NAO_ENCONTRADO' | 'AVARIADO' | 'DESCARTADO'
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Frontend types, hooks, disposal + transfer modals, AssetDrawer wiring</name>
  <files>
    apps/frontend/src/types/asset.ts,
    apps/frontend/src/hooks/useAssetDisposal.ts,
    apps/frontend/src/hooks/useAssetTransfer.ts,
    apps/frontend/src/components/assets/AssetDisposalModal.tsx,
    apps/frontend/src/components/assets/AssetDisposalModal.css,
    apps/frontend/src/components/assets/AssetTransferModal.tsx,
    apps/frontend/src/components/assets/AssetTransferModal.css,
    apps/frontend/src/components/assets/AssetDrawer.tsx,
    apps/frontend/src/pages/AssetsPage.tsx
  </files>
  <read_first>
    apps/frontend/src/types/asset.ts,
    apps/frontend/src/hooks/useAssetAcquisition.ts,
    apps/frontend/src/hooks/useAssets.ts,
    apps/frontend/src/components/assets/AssetModal.tsx,
    apps/frontend/src/components/assets/AssetModal.css,
    apps/frontend/src/components/assets/AssetDrawer.tsx,
    apps/frontend/src/components/assets/AssetDrawer.css,
    apps/frontend/src/components/assets/InstallmentPreviewTable.tsx,
    apps/frontend/src/pages/AssetsPage.tsx,
    apps/frontend/src/components/ui/ConfirmModal.tsx,
    apps/frontend/src/styles/tokens.css
  </read_first>
  <action>
1. **Update `types/asset.ts`** — Add new types at the bottom:
   ```typescript
   // ─── Disposal Types ──────────────────────────────────────────────────
   export type DisposalType = 'VENDA' | 'DESCARTE' | 'SINISTRO' | 'OBSOLESCENCIA';

export const DISPOSAL_TYPE_LABELS: Record<DisposalType, string> = {
VENDA: 'Venda',
DESCARTE: 'Descarte',
SINISTRO: 'Sinistro',
OBSOLESCENCIA: 'Obsolescencia',
};

export interface CreateDisposalInput {
disposalType: DisposalType;
disposalDate: string;
saleValue?: number;
buyerName?: string;
motivation?: string;
documentUrl?: string;
dueDate?: string;
installmentCount?: number;
firstDueDate?: string;
}

export interface DisposalOutput {
id: string;
assetId: string;
assetTag: string;
assetName: string;
disposalType: DisposalType;
disposalTypeLabel: string;
disposalDate: string;
saleValue: number | null;
netBookValue: number;
gainLoss: number;
buyerName: string | null;
motivation: string | null;
receivableId: string | null;
installmentCount: number;
cancelledDepreciationCount: number;
createdAt: string;
}

// ─── Transfer Types ──────────────────────────────────────────────────
export interface CreateTransferInput {
toFarmId: string;
transferDate: string;
toCostCenterId?: string;
notes?: string;
}

export interface TransferOutput {
id: string;
assetId: string;
fromFarmId: string;
fromFarmName: string;
toFarmId: string;
toFarmName: string;
transferDate: string;
notes: string | null;
createdAt: string;
}

// ─── Inventory Types ─────────────────────────────────────────────────
export type PhysicalStatus = 'ENCONTRADO' | 'NAO_ENCONTRADO' | 'AVARIADO' | 'DESCARTADO';

export const PHYSICAL_STATUS_LABELS: Record<PhysicalStatus, string> = {
ENCONTRADO: 'Encontrado',
NAO_ENCONTRADO: 'Nao encontrado',
AVARIADO: 'Avariado',
DESCARTADO: 'Descartado',
};

export interface InventoryItemOutput {
id: string;
assetId: string;
assetTag: string;
assetName: string;
assetType: string;
registeredStatus: string;
physicalStatus: string | null;
physicalStatusLabel: string | null;
notes: string | null;
}

export interface InventoryOutput {
id: string;
farmId: string | null;
farmName: string | null;
status: string;
statusLabel: string;
notes: string | null;
items: InventoryItemOutput[];
itemCount: number;
countedCount: number;
divergenceCount: number;
createdAt: string;
}

// ─── Patrimony Dashboard Types ───────────────────────────────────────
export interface PatrimonyDashboardOutput {
totalActiveValue: number;
accumulatedDepreciation: number;
netBookValue: number;
acquisitionsInPeriod: { count: number; totalValue: number };
disposalsInPeriod: { count: number; totalSaleValue: number; totalGainLoss: number };
assetCountByType: Array<{ assetType: string; count: number }>;
assetCountByStatus: Array<{ status: string; count: number }>;
}

````

2. **Create `hooks/useAssetDisposal.ts`**:
- `createDisposal(orgId, assetId, data)`: POST to `/api/org/${orgId}/asset-disposals/${assetId}/dispose`
- `getDisposal(orgId, assetId)`: GET from `/api/org/${orgId}/asset-disposals/${assetId}/disposal`
- Return `{ createDisposal, getDisposal, loading, error }`.
- Follow same pattern as `useAssetAcquisition.ts`.

3. **Create `hooks/useAssetTransfer.ts`**:
- `createTransfer(orgId, assetId, data)`: POST to `/api/org/${orgId}/asset-farm-transfers/${assetId}/transfer`
- `listTransfers(orgId, assetId)`: GET from `/api/org/${orgId}/asset-farm-transfers/${assetId}/transfers`
- Return `{ createTransfer, listTransfers, transfers, loading, error }`.

4. **Create `AssetDisposalModal.tsx` + `.css`**:
- Props: `isOpen: boolean, onClose: () => void, onSuccess: () => void, asset: Asset`
- Form sections:
  a. **Tipo de alienacao**: Radio buttons for VENDA, DESCARTE, SINISTRO, OBSOLESCENCIA with labels
  b. **Data**: date input for disposalDate (default today)
  c. **Venda section** (visible only when VENDA selected): saleValue (BRL currency input), buyerName, dueDate, installmentCount (1-120, default 1), firstDueDate (visible when installmentCount > 1)
  d. **Baixa section** (visible for DESCARTE/SINISTRO/OBSOLESCENCIA): motivation (textarea, required), documentUrl (optional text input for laudo URL)
  e. **Preview card**: Show netBookValue (from asset.acquisitionValue as fallback — note: exact NBV from backend on submit), estimated gainLoss for VENDA = saleValue - acquisitionValue (approximate), or "Perda total do valor contabil" for write-offs
  f. **Existing InstallmentPreviewTable** reused when installmentCount > 1 (import from `@/components/assets/InstallmentPreviewTable`)
- Submit: calls `createDisposal`, on success shows toast "Ativo alienado com sucesso", calls onSuccess, closes modal.
- On error: shows inline error message with `role="alert"`.
- CSS: Follow AssetModal.css patterns — `.disposal-modal`, `.disposal-section`, `.disposal-preview-card` with token colors. Preview card uses `var(--color-neutral-50)` background, gain in green text (`var(--color-primary-600)`), loss in red (`var(--color-error-500)`).
- Destructive action: Use ConfirmModal wrapping the submit — "Tem certeza? Esta acao nao pode ser desfeita. O ativo sera marcado como alienado."

5. **Create `AssetTransferModal.tsx` + `.css`**:
- Props: `isOpen: boolean, onClose: () => void, onSuccess: () => void, asset: Asset, farms: Farm[]`
- Form: destination farm select (filtered to exclude current farm, same org only — filtering done by API), transferDate (default today), toCostCenterId (optional select from cost centers), notes textarea.
- Submit: calls `createTransfer`. Toast: "Ativo transferido para {farmName} com sucesso". Calls onSuccess.
- CSS: Minimal — follow AssetModal.css patterns.

6. **Wire modals in `AssetDrawer.tsx`**:
- Add "Alienar" and "Transferir" action buttons in the drawer header/actions area (using Lucide icons: `PackageMinus` for alienar, `ArrowRightLeft` for transfer).
- Buttons disabled when asset.status === 'ALIENADO'.
- State: `showDisposalModal` and `showTransferModal` booleans.
- Render `<AssetDisposalModal>` and `<AssetTransferModal>` conditionally.
- On success: refetch asset detail + close drawer or refresh data.

7. **Wire modals also from `AssetsPage.tsx`** — add context menu or action dropdown on each asset row with "Alienar" and "Transferir" options (only for non-ALIENADO assets).
</action>
<verify>
 <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && npx tsc --noEmit --project apps/frontend/tsconfig.json 2>&1 | head -30</automated>
</verify>
<acceptance_criteria>
 - types/asset.ts contains `export type DisposalType`
 - types/asset.ts contains `export interface PatrimonyDashboardOutput`
 - useAssetDisposal.ts contains `asset-disposals` in API path
 - useAssetTransfer.ts contains `asset-farm-transfers` in API path
 - AssetDisposalModal.tsx contains `disposalType` form field
 - AssetDisposalModal.tsx contains `ConfirmModal` import
 - AssetDisposalModal.tsx contains `InstallmentPreviewTable` import
 - AssetTransferModal.tsx contains `toFarmId` form field
 - AssetDrawer.tsx contains `AssetDisposalModal` import
 - AssetDrawer.tsx contains `AssetTransferModal` import
 - AssetsPage.tsx contains `Alienar` or `alienar` text
 - TypeScript compilation exits 0 or has only pre-existing errors
</acceptance_criteria>
<done>Disposal modal with type selector, gain/loss preview, installment support. Transfer modal with farm selector. Both wired from AssetDrawer and AssetsPage. All types defined.</done>
</task>

<task type="auto">
<name>Task 2: Inventory page, patrimony dashboard page, hooks, sidebar + routing</name>
<files>
 apps/frontend/src/hooks/useAssetInventory.ts,
 apps/frontend/src/hooks/usePatrimonyDashboard.ts,
 apps/frontend/src/components/assets/AssetInventoryModal.tsx,
 apps/frontend/src/components/assets/AssetInventoryModal.css,
 apps/frontend/src/pages/AssetInventoryPage.tsx,
 apps/frontend/src/pages/AssetInventoryPage.css,
 apps/frontend/src/pages/PatrimonyDashboardPage.tsx,
 apps/frontend/src/pages/PatrimonyDashboardPage.css,
 apps/frontend/src/components/layout/Sidebar.tsx,
 apps/frontend/src/App.tsx
</files>
<read_first>
 apps/frontend/src/types/asset.ts,
 apps/frontend/src/pages/AssetsPage.tsx,
 apps/frontend/src/pages/DepreciationPage.tsx,
 apps/frontend/src/pages/FinancialDashboardPage.tsx,
 apps/frontend/src/hooks/useAssets.ts,
 apps/frontend/src/components/layout/Sidebar.tsx,
 apps/frontend/src/App.tsx,
 apps/frontend/src/styles/tokens.css
</read_first>
<action>
1. **Create `hooks/useAssetInventory.ts`**:
- `createInventory(orgId, data)`: POST to `/api/org/${orgId}/asset-inventories`
- `listInventories(orgId, query)`: GET from `/api/org/${orgId}/asset-inventories`
- `getInventory(orgId, id)`: GET from `/api/org/${orgId}/asset-inventories/${id}`
- `countItems(orgId, id, items)`: PATCH to `/api/org/${orgId}/asset-inventories/${id}/count`
- `reconcileInventory(orgId, id)`: POST to `/api/org/${orgId}/asset-inventories/${id}/reconcile`
- Return `{ inventories, inventory, createInventory, countItems, reconcileInventory, loading, error, refetch }`.

2. **Create `hooks/usePatrimonyDashboard.ts`**:
- `fetchDashboard(orgId, query)`: GET from `/api/org/${orgId}/financial-dashboard/patrimony?year=${y}&month=${m}&farmId=${f}`
- Return `{ data: PatrimonyDashboardOutput | null, loading, error, refetch }`.
- Auto-fetch on mount with current year/month.

3. **Create `AssetInventoryModal.tsx` + `.css`** (for creating new inventory):
- Props: `isOpen, onClose, onSuccess`
- Form: farmId select (optional — "Todas as fazendas" default), notes textarea.
- Submit: calls createInventory. Toast: "Inventario criado com {N} ativos". Calls onSuccess.

4. **Create `AssetInventoryPage.tsx` + `.css`**:
- Two sections: **lista de inventarios** (table with status badge, item counts, date) + **detalhe inline** (when an inventory is selected).
- List: columns = Data, Fazenda, Status (badge), Total Ativos, Contados, Divergencias. Click opens detail below.
- Detail section: table of inventory items showing assetTag, assetName, assetType, registeredStatus, physicalStatus (editable select dropdown), notes (editable).
- Physical status selects: options ENCONTRADO, NAO_ENCONTRADO, AVARIADO, DESCARTADO with labels.
- Buttons:
  - "Novo Inventario" (top) — opens AssetInventoryModal
  - "Salvar Contagem" — calls countItems with modified items, transitions to COUNTING
  - "Conciliar" — appears when status is COUNTING, calls reconcileInventory (use ConfirmModal: "Deseja conciliar este inventario? Os resultados serao finalizados.")
- Status badge colors: DRAFT=`var(--color-neutral-300)`, COUNTING=`var(--color-warning-500)` (or use amber/yellow token), RECONCILED=`var(--color-primary-600)`, CANCELLED=`var(--color-error-500)`.
- Empty state for no inventories: Calendar icon + "Nenhum inventario realizado" + "Crie o primeiro inventario para conciliar o patrimonio fisico com o contabil" + CTA button.
- Mobile: table transforms to cards (per CLAUDE.md rules).

5. **Create `PatrimonyDashboardPage.tsx` + `.css`**:
- KPI cards row (4 cards):
  a. "Valor Total Ativos" — totalActiveValue formatted as BRL
  b. "Depreciacao Acumulada" — accumulatedDepreciation formatted as BRL
  c. "Valor Contabil Liquido" — netBookValue formatted as BRL
  d. "Ganho/Perda Periodo" — disposalsInPeriod.totalGainLoss (green if positive, red if negative)
- Period section: acquisitionsInPeriod (count + value), disposalsInPeriod (count + saleValue).
- Charts: assetCountByType (horizontal bar or donut), assetCountByStatus (donut/pie).
- Use Recharts (already installed) for charts — PieChart for type distribution, BarChart for status.
- Year/month selector at top (same pattern as FinancialDashboardPage).
- FarmId filter (optional select).
- CSS: Follow FinancialDashboardPage.css patterns — `.patrimony-dashboard`, KPI cards in grid, responsive.

6. **Update `Sidebar.tsx`**:
- Add to PATRIMONIO group after existing items:
  ```
  { to: '/asset-inventories', icon: ClipboardCheck, label: 'Inventario Patrimonial' },
  { to: '/patrimony-dashboard', icon: BarChart3, label: 'Dashboard Patrimonial' },
  ```
- Import `ClipboardCheck` and `BarChart3` from `lucide-react`.

7. **Update `App.tsx`**:
- Add lazy imports:
  ```typescript
  const AssetInventoryPage = lazy(() => import('@/pages/AssetInventoryPage'));
  const PatrimonyDashboardPage = lazy(() => import('@/pages/PatrimonyDashboardPage'));
  ```
- Add routes after `/depreciation`:
  ```tsx
  <Route path="/asset-inventories" element={<AssetInventoryPage />} />
  <Route path="/patrimony-dashboard" element={<PatrimonyDashboardPage />} />
  ```
</action>
<verify>
 <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && npx tsc --noEmit --project apps/frontend/tsconfig.json 2>&1 | head -30</automated>
</verify>
<acceptance_criteria>
 - useAssetInventory.ts contains `asset-inventories` in API paths
 - usePatrimonyDashboard.ts contains `financial-dashboard/patrimony` in API path
 - AssetInventoryPage.tsx contains `physicalStatus` or `ENCONTRADO`
 - AssetInventoryPage.tsx contains `Conciliar` button text
 - AssetInventoryPage.tsx contains `ConfirmModal` import
 - PatrimonyDashboardPage.tsx contains `totalActiveValue`
 - PatrimonyDashboardPage.tsx contains Recharts import (`PieChart` or `BarChart`)
 - Sidebar.tsx contains `asset-inventories` route
 - Sidebar.tsx contains `patrimony-dashboard` route
 - App.tsx contains `AssetInventoryPage` lazy import
 - App.tsx contains `PatrimonyDashboardPage` lazy import
 - App.tsx contains `path="/asset-inventories"` route
 - App.tsx contains `path="/patrimony-dashboard"` route
 - TypeScript compilation exits 0 or has only pre-existing errors
</acceptance_criteria>
<done>Inventory page with DRAFT->COUNTING->RECONCILED flow. Patrimony dashboard with KPIs and charts. Sidebar links and routes wired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Task 3: Visual verification of disposal, transfer, inventory, and dashboard</name>
<files>apps/frontend/src/components/assets/AssetDisposalModal.tsx, apps/frontend/src/pages/AssetInventoryPage.tsx, apps/frontend/src/pages/PatrimonyDashboardPage.tsx</files>
<action>
Human visually verifies the complete Phase 20 frontend:
- AssetDisposalModal (4 disposal types, gain/loss preview, installment)
- AssetTransferModal (farm selector)
- AssetInventoryPage (create, count, reconcile)
- PatrimonyDashboardPage (KPIs, charts)
- Sidebar links

Steps:
1. Start dev servers: pnpm dev (both backend and frontend)
2. Navigate to /assets, click on any asset to open the drawer
3. Verify "Alienar" and "Transferir" buttons appear in drawer header
4. Click "Alienar" — verify modal opens with disposal type radio buttons
5. Select "Venda", fill saleValue, buyerName, dueDate — verify gain/loss preview updates
6. Change installmentCount to 3 — verify InstallmentPreviewTable appears
7. Cancel modal, click "Transferir" — verify farm selector shows only org farms
8. Navigate to /asset-inventories — verify page loads with empty state
9. Click "Novo Inventario" — verify modal, create one — verify items auto-loaded
10. Set physical status on some items, click "Salvar Contagem"
11. Click "Conciliar" — verify confirm modal appears
12. Navigate to /patrimony-dashboard — verify KPI cards and charts render
13. Verify sidebar shows "Inventario Patrimonial" and "Dashboard Patrimonial" links
</action>
<verify>Human confirms all 13 verification steps pass. Type "approved" or describe issues.</verify>
<done>All disposal, transfer, inventory, and dashboard UI verified as working correctly by human.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes
- All modals render correctly with proper form validation
- Inventory page follows DRAFT -> COUNTING -> RECONCILED flow
- Dashboard shows KPIs and charts
- Sidebar and routes work
</verification>

<success_criteria>
Complete frontend for all 6 DISP requirements. Disposal modal with gain/loss preview and installment support. Transfer modal with farm selector. Inventory page with state machine. Patrimony dashboard with KPIs. All navigation wired.
</success_criteria>

<output>
After completion, create `.planning/phases/20-alienacao-baixa-ativos/20-04-SUMMARY.md`
</output>
````
