---
phase: 21-controle-operacional
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(app)/meter-reading.tsx
  - apps/mobile/app/(app)/(tabs)/more.tsx
autonomous: true
requirements:
  - OPER-03

must_haves:
  truths:
    - 'Operator can submit hourmeter or odometer reading from mobile with anti-regression validated server-side'
    - 'Mobile meter reading screen is accessible from the More tab quick actions'
    - 'Screen works offline by queuing the reading for sync'
  artifacts:
    - path: 'apps/mobile/app/(app)/meter-reading.tsx'
      provides: 'Mobile meter reading form screen'
      min_lines: 100
  key_links:
    - from: 'apps/mobile/app/(app)/meter-reading.tsx'
      to: '/api/org/:orgId/meter-readings'
      via: 'api.post() or offline queue'
      pattern: 'meter-readings'
    - from: 'apps/mobile/app/(app)/(tabs)/more.tsx'
      to: 'apps/mobile/app/(app)/meter-reading.tsx'
      via: "router.push('/meter-reading')"
      pattern: 'meter-reading'
---

<objective>
Create a dedicated mobile screen for meter reading submission (OPER-03). The backend endpoint already exists with anti-regression validation. This plan adds the mobile UI following the same pattern as maintenance-request.tsx.

Purpose: Enable field operators to quickly update hourmeter/odometer from their phone with offline support.
Output: New meter-reading.tsx screen, wired from More tab quick actions.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-controle-operacional/21-RESEARCH.md

<interfaces>
<!-- Existing backend endpoint for meter readings -->

From apps/backend/src/modules/meter-readings/meter-readings.routes.ts:

```typescript
// POST /org/:orgId/meter-readings
// Body: { assetId: string, readingType: 'HOURMETER' | 'ODOMETER', readingValue: number, readingDate: string, notes?: string }
// Anti-regression: server validates newValue > lastValue in transaction, returns 409 if regression
// Response 201: { id, assetId, readingType, readingValue, readingDate, notes, createdAt }
```

From apps/mobile/app/(app)/maintenance-request.tsx (reference pattern):

```typescript
// Pattern: SafeAreaView > KeyboardAvoidingView > ScrollView
// State management: useState for form fields, useRef for text inputs
// Asset picker: FlatList with search, cached assets from SQLite
// Submit: api.post() when online, createOfflineQueue().enqueue() when offline
// Haptics: success notification on submit, warning on error
// Navigation: useRouter().back() on success
// Imports: expo-haptics, expo-location, lucide-react-native, @protos-farm/shared tokens
```

From apps/mobile/app/(app)/(tabs)/more.tsx:

```typescript
// Quick action list pattern: array of { icon, label, route, color } rendered as Pressable cards
// Navigation: router.push(route)
```

From apps/backend/src/modules/meter-readings/meter-readings.types.ts:

```typescript
export class MeterReadingError extends Error {
  statusCode: number;
  // 409 for anti-regression: "Leitura deve ser maior que a última registrada (X)"
}
export interface CreateMeterReadingInput {
  assetId: string;
  readingType: 'HOURMETER' | 'ODOMETER';
  readingValue: number;
  readingDate: string;
  notes?: string;
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mobile meter-reading.tsx screen</name>
  <files>
    apps/mobile/app/(app)/meter-reading.tsx
  </files>
  <read_first>
    apps/mobile/app/(app)/maintenance-request.tsx,
    apps/mobile/app/(app)/_layout.tsx,
    apps/backend/src/modules/meter-readings/meter-readings.routes.ts,
    apps/backend/src/modules/meter-readings/meter-readings.service.ts,
    apps/backend/src/modules/meter-readings/meter-readings.types.ts,
    apps/mobile/services/api.ts,
    apps/mobile/services/offline-queue.ts,
    apps/mobile/stores/ConnectivityContext.tsx,
    apps/mobile/stores/FarmContext.tsx
  </read_first>
  <action>
    Create `apps/mobile/app/(app)/meter-reading.tsx` following the maintenance-request.tsx pattern exactly:

    **Imports:**
    - `{ useState, useEffect, useCallback, useRef }` from react
    - `{ View, Text, TextInput, Pressable, ScrollView, KeyboardAvoidingView, Platform, Alert, FlatList }` from react-native
    - `{ SafeAreaView }` from react-native-safe-area-context
    - `{ useRouter, useLocalSearchParams }` from expo-router — support optional `assetId` param for pre-selection
    - `{ useSQLiteContext }` from expo-sqlite
    - `* as Haptics` from expo-haptics
    - `{ ArrowLeft, Gauge, Search, WifiOff, Check, Clock }` from lucide-react-native
    - `{ spacing, fontSize }` from @protos-farm/shared
    - `{ useTheme }` from @/stores/ThemeContext
    - `{ useThemedStyles }` from @/hooks/useThemedStyles
    - `{ useFarmContext }` from @/stores/FarmContext
    - `{ useConnectivity }` from @/stores/ConnectivityContext
    - `{ useSyncContext }` from @/stores/SyncContext
    - `{ createOfflineQueue }` from @/services/offline-queue
    - `{ api }` from @/services/api

    **Types:**
    - `type ReadingType = 'HOURMETER' | 'ODOMETER';`
    - `interface CachedAsset { id: string; name: string; assetTag?: string; currentHourmeter?: number; currentOdometer?: number; }`

    **State:**
    - `selectedAsset: CachedAsset | null` (pre-populated from params if assetId provided)
    - `readingType: ReadingType` (default 'HOURMETER')
    - `readingValue: string` (text input — parse to number on submit)
    - `readingDate: string` (default today ISO)
    - `notes: string`
    - `submitting: boolean`
    - `assetSearch: string`
    - `showAssetPicker: boolean`
    - `assets: CachedAsset[]`
    - `error: string | null`

    **Asset loading:**
    - On mount, query SQLite for cached assets (same pattern as maintenance-request.tsx `loadAssets`).
    - If `assetId` search param provided, pre-select that asset.
    - Filter assets by search term on name or assetTag.

    **Reading type selector:**
    - Two toggle buttons side by side: "Horímetro" (HOURMETER) and "Odômetro" (ODOMETER).
    - Active button: `backgroundColor: colors.primary[600]`, text white.
    - Inactive button: `backgroundColor: colors.neutral[100]`, text neutral[700].
    - Minimum touch target 48x48px.
    - `accessibilityRole="radio"`, `accessibilityState={{ selected: readingType === type }}`.

    **Value input:**
    - Label: "Valor da Leitura *" (always visible, not just placeholder).
    - TextInput with `keyboardType="decimal-pad"`.
    - Suffix text: "horas" for HOURMETER, "km" for ODOMETER.
    - If selectedAsset has currentHourmeter/currentOdometer, show hint below input: `"Última leitura: ${currentValue} ${unit}"` in muted color.
    - Inline validation: if parsed value <= currentValue, show error `"Leitura deve ser maior que ${currentValue} ${unit}"` with `role="alert"`.

    **Date input:**
    - Label: "Data da Leitura *".
    - Default: today. Simple TextInput with date mask (DD/MM/YYYY) or use date string.

    **Notes input:**
    - Label: "Observações".
    - TextInput multiline, 3 lines height.

    **Submit button:**
    - Label: "Registrar Leitura".
    - Disabled when: no selectedAsset, no readingValue, value <= 0, submitting, or anti-regression fails.
    - Primary button style: `backgroundColor: colors.primary[600]`, white text, minHeight 48px, borderRadius 8px.
    - `accessibilityRole="button"`, `accessibilityLabel="Registrar leitura"`.

    **Submit handler:**
    - Parse `readingValue` to number. Validate > 0.
    - Build payload: `{ assetId: selectedAsset.id, readingType, readingValue: parsedValue, readingDate, notes: notes || undefined }`.
    - If online (`isConnected`):
      - `await api.post(`/api/org/${farmContext.organizationId}/meter-readings`, payload)`.
      - On success: `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)`, Alert "Leitura registrada com sucesso", `router.back()`.
      - On 409 error (anti-regression): `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Warning)`, set error from response message.
      - On other error: set generic error "Não foi possível registrar. Tente novamente."
    - If offline:
      - Enqueue via `createOfflineQueue('meter-readings').enqueue({ ...payload, localId: generateLocalId() })`.
      - `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)`.
      - Alert "Leitura salva offline. Será enviada ao reconectar."
      - `router.back()`.

    **Offline banner:**
    - If `!isConnected`, show banner at top: `WifiOff` icon + "Sem conexão. A leitura será enfileirada."

    **Layout:**
    - `SafeAreaView` > `KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : 'height'}` > `ScrollView`.
    - Header: back button (`ArrowLeft` 24px) + title "Atualizar Leitura" (DM Sans 700).
    - All spacing uses `spacing[N]` tokens from shared package.
    - All text uses `fontSize[N]` tokens.
    - All colors from `useTheme()`.

    **Styles (createStyles pattern):**
    - Follow the exact `const createStyles = (c: ThemeColors) => ({...})` pattern from maintenance-request.tsx.
    - `safeArea: { flex: 1, backgroundColor: c.neutral[50] }`
    - `header: { flexDirection: 'row', alignItems: 'center', padding: spacing[4], gap: spacing[3] }`
    - `form: { padding: spacing[4], gap: spacing[5] }`
    - `label: { fontFamily: 'SourceSans3_600SemiBold', fontSize: fontSize.sm, color: c.neutral[700], marginBottom: spacing[1] }`
    - `input: { borderWidth: 1, borderColor: c.neutral[300], borderRadius: 8, padding: spacing[3], minHeight: 48, fontFamily: 'SourceSans3_400Regular', fontSize: fontSize.base, color: c.neutral[800] }`
    - `submitButton: { backgroundColor: c.primary[600], borderRadius: 8, padding: spacing[3], minHeight: 48, alignItems: 'center', justifyContent: 'center' }`
    - `submitButtonDisabled: { backgroundColor: c.neutral[300] }`
    - `submitButtonText: { fontFamily: 'DMSans_700Bold', fontSize: fontSize.base, color: c.neutral[0] }`
    - `errorText: { fontFamily: 'SourceSans3_400Regular', fontSize: fontSize.sm, color: c.error[500], marginTop: spacing[1] }`
    - `hintText: { fontFamily: 'SourceSans3_400Regular', fontSize: fontSize.xs, color: c.neutral[500], marginTop: spacing[1] }`

  </action>
  <verify>
    <automated>test -f /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/mobile/app/\(app\)/meter-reading.tsx && echo "FILE EXISTS" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `meter-reading.tsx` exists at `apps/mobile/app/(app)/meter-reading.tsx`
    - `meter-reading.tsx` contains `SafeAreaView` (mandatory per mobile CLAUDE.md)
    - `meter-reading.tsx` contains `KeyboardAvoidingView` (mandatory for form screens)
    - `meter-reading.tsx` contains `Haptics.notificationAsync` (success and warning feedback)
    - `meter-reading.tsx` contains `meter-readings` (API endpoint path)
    - `meter-reading.tsx` contains `HOURMETER` and `ODOMETER`
    - `meter-reading.tsx` contains `accessibilityLabel` or `accessibilityRole` (at least 3 occurrences)
    - `meter-reading.tsx` contains `createOfflineQueue` (offline support)
    - `meter-reading.tsx` contains `WifiOff` (offline banner icon)
    - `meter-reading.tsx` contains `spacing` import from @protos-farm/shared
    - `meter-reading.tsx` does NOT contain `TouchableOpacity` (use Pressable per mobile CLAUDE.md)
  </acceptance_criteria>
  <done>Mobile meter-reading screen renders with asset picker, reading type toggle, value input with anti-regression hint, date input, notes, and submit with offline queue support.</done>
</task>

<task type="auto">
  <name>Task 2: Wire meter-reading into More tab quick actions</name>
  <files>
    apps/mobile/app/(app)/(tabs)/more.tsx
  </files>
  <read_first>
    apps/mobile/app/(app)/(tabs)/more.tsx,
    apps/mobile/app/(app)/(tabs)/_layout.tsx
  </read_first>
  <action>
    1. Read `more.tsx` to understand the existing quick actions structure.
    2. Add a new quick action entry for meter reading:
       - Icon: `Gauge` from `lucide-react-native` (import it at the top).
       - Label: `"Atualizar Horímetro"`
       - Route: `"/meter-reading"` (Expo Router resolves to `app/(app)/meter-reading.tsx`)
       - Color: `colors.primary[600]` (green — same as other asset-related actions)
    3. Place the new entry logically near other asset/equipment-related actions (after maintenance request if it exists, or at a sensible position in the list).
    4. Ensure the Pressable for this action has:
       - `accessibilityLabel="Atualizar horímetro ou odômetro"`
       - `accessibilityRole="button"`
       - Minimum touch target 48x48px (already handled by the card style if following existing pattern)
  </action>
  <verify>
    <automated>grep -c "meter-reading" /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/mobile/app/\(app\)/\(tabs\)/more.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `more.tsx` contains `Gauge` import from lucide-react-native
    - `more.tsx` contains `meter-reading` (the route path)
    - `more.tsx` contains `Atualizar Horímetro` or `Horímetro` (the label)
    - grep for `meter-reading` returns at least 1 match
  </acceptance_criteria>
  <done>More tab shows "Atualizar Horímetro" quick action. Tapping it navigates to the meter-reading screen.</done>
</task>

</tasks>

<verification>
1. File exists: `ls apps/mobile/app/(app)/meter-reading.tsx`
2. Wired in more tab: `grep "meter-reading" apps/mobile/app/(app)/(tabs)/more.tsx`
3. Offline support: `grep "createOfflineQueue" apps/mobile/app/(app)/meter-reading.tsx`
4. Accessibility: `grep -c "accessibilityLabel" apps/mobile/app/(app)/meter-reading.tsx` returns >= 3
</verification>

<success_criteria>

- Operator can open meter-reading screen from More tab
- Screen shows asset picker, reading type toggle (Hourmeter/Odometer), value input with last-reading hint
- Anti-regression shown inline before submit
- Submit works online (API call) and offline (queue)
- Haptic feedback on success and error
- Screen follows all mobile CLAUDE.md requirements (SafeAreaView, KeyboardAvoidingView, Pressable, accessibility, tokens)
  </success_criteria>

<output>
After completion, create `.planning/phases/21-controle-operacional/21-03-SUMMARY.md`
</output>
