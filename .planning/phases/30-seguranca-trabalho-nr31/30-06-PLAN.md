---
phase: 30-seguranca-trabalho-nr31
plan: 06
type: execute
wave: 3
depends_on: ["30-03", "30-04"]
files_modified:
  - apps/frontend/src/hooks/useTrainingTypes.ts
  - apps/frontend/src/hooks/useTrainingRecords.ts
  - apps/frontend/src/hooks/useMedicalExams.ts
  - apps/frontend/src/pages/TrainingTypesPage.tsx
  - apps/frontend/src/pages/TrainingTypesPage.css
  - apps/frontend/src/components/training-types/TrainingTypeModal.tsx
  - apps/frontend/src/components/training-types/PositionTrainingRequirementsModal.tsx
  - apps/frontend/src/pages/TrainingRecordsPage.tsx
  - apps/frontend/src/pages/TrainingRecordsPage.css
  - apps/frontend/src/components/training-records/TrainingRecordModal.tsx
  - apps/frontend/src/pages/MedicalExamsPage.tsx
  - apps/frontend/src/pages/MedicalExamsPage.css
  - apps/frontend/src/components/medical-exams/MedicalExamModal.tsx
autonomous: true
requirements:
  - SEGUR-02
  - SEGUR-03

must_haves:
  truths:
    - "TrainingTypesPage shows system types as read-only and custom types as editable"
    - "TrainingRecordsPage allows registering collective training with employee multi-select"
    - "MedicalExamsPage shows ASO list with result badges and expiry status"
    - "Certificate PDF downloadable per participant"
    - "ASO modal auto-calculates nextExamDate"
  artifacts:
    - path: "apps/frontend/src/pages/TrainingTypesPage.tsx"
      provides: "Training type list with tabs"
      min_lines: 100
    - path: "apps/frontend/src/pages/TrainingRecordsPage.tsx"
      provides: "Training record list with participant expand"
      min_lines: 100
    - path: "apps/frontend/src/pages/MedicalExamsPage.tsx"
      provides: "ASO list with result and expiry badges"
      min_lines: 100
  key_links:
    - from: "TrainingRecordModal.tsx"
      to: "/api/training-records"
      via: "useTrainingRecords hook"
      pattern: "useTrainingRecords"
    - from: "MedicalExamModal.tsx"
      to: "/api/medical-exams"
      via: "useMedicalExams hook"
      pattern: "useMedicalExams"
---

<objective>
Frontend training and ASO pages: TrainingTypesPage, TrainingRecordsPage, MedicalExamsPage with all modals and hooks.

Purpose: Implement SEGUR-02 and SEGUR-03 frontend — training type management with system type protection, training session recording with multi-step modal, and ASO management with result badges and expiry alerts.
Output: 3 fully functional pages with modals per UI-SPEC contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-seguranca-trabalho-nr31/30-CONTEXT.md
@.planning/phases/30-seguranca-trabalho-nr31/30-UI-SPEC.md
@.planning/phases/30-seguranca-trabalho-nr31/30-03-SUMMARY.md
@.planning/phases/30-seguranca-trabalho-nr31/30-04-SUMMARY.md
@.planning/phases/30-seguranca-trabalho-nr31/30-05-SUMMARY.md
@apps/frontend/src/types/training.ts
@apps/frontend/src/types/medical-exam.ts
@apps/frontend/src/pages/EpiProductsPage.tsx
@apps/frontend/src/pages/EpiDeliveriesPage.tsx
@apps/frontend/src/components/shared/ComplianceStatusBadge.tsx
@apps/frontend/src/hooks/useEpiProducts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hooks + TrainingTypesPage + TrainingRecordsPage with modals</name>
  <files>
    apps/frontend/src/hooks/useTrainingTypes.ts,
    apps/frontend/src/hooks/useTrainingRecords.ts,
    apps/frontend/src/pages/TrainingTypesPage.tsx,
    apps/frontend/src/pages/TrainingTypesPage.css,
    apps/frontend/src/components/training-types/TrainingTypeModal.tsx,
    apps/frontend/src/components/training-types/PositionTrainingRequirementsModal.tsx,
    apps/frontend/src/pages/TrainingRecordsPage.tsx,
    apps/frontend/src/pages/TrainingRecordsPage.css,
    apps/frontend/src/components/training-records/TrainingRecordModal.tsx
  </files>
  <read_first>
    - apps/frontend/src/types/training.ts (interfaces from Plan 05)
    - apps/frontend/src/hooks/useEpiProducts.ts (hook pattern from Plan 05)
    - apps/frontend/src/pages/EpiProductsPage.tsx (page with tabs pattern from Plan 05)
    - apps/frontend/src/pages/EpiProductsPage.css (CSS pattern)
    - apps/frontend/src/components/epi-products/EpiProductModal.tsx (modal pattern)
    - apps/frontend/src/components/epi-products/PositionEpiRequirementsModal.tsx (requirements modal pattern)
    - .planning/phases/30-seguranca-trabalho-nr31/30-UI-SPEC.md (Pages 3 and 4 layout contracts)
  </read_first>
  <action>
**hooks/useTrainingTypes.ts:** Follow useEpiProducts pattern:
- fetchTrainingTypes, createTrainingType, updateTrainingType, deleteTrainingType
- seedNr31Types (POST /training-types/seed)
- fetchPositionRequirements, createPositionRequirement, deletePositionRequirement
- API paths: `/training-types/...`, `/training-types/position-requirements/...`

**hooks/useTrainingRecords.ts:**
- fetchTrainingRecords (with query params: trainingTypeId, instructorType, dateFrom, dateTo, farmId)
- createTrainingRecord, deleteTrainingRecord
- downloadCertificatePdf(trainingRecordId, employeeId) — use api.getBlob, open in new tab
- API paths: `/training-records/...`, `/training-records/:id/employees/:employeeId/certificate`

**TrainingTypesPage.tsx** — Per UI-SPEC page 3:
- Page header: GraduationCap icon + "Tipos de Treinamento" title + "Novo Tipo de Treinamento" primary CTA
- Tab strip (2 tabs): "Tipos de Treinamento" / "Requisitos por Cargo"
- **Tab 1:**
  - Table: Nome, Referencia NR, Carga horaria minima (format: `{N}h`), Validade padrao (format: `{N} meses`), Sistema badge, Acoes
  - System rows (isSystem=true): read-only, show "Sistema" badge with sky-100 bg / sky-500 text color, tooltip "Treinamento obrigatorio NR-31 — nao editavel", no edit/delete icons
  - Custom rows: edit (pencil icon) + delete (trash icon) actions
  - Empty state: GraduationCap icon 48px + "Nenhum tipo de treinamento cadastrado" + CTA
- **Tab 2 — Requisitos por Cargo:**
  - Same layout as EpiProductsPage Tab 2 but for training types
  - Global types (isGlobal=true): show "Todos os cargos" chip in neutral-200/neutral-600
  - "Configurar" opens PositionTrainingRequirementsModal

**TrainingTypesPage.css:** Follow EpiProductsPage.css pattern. Add `.training-type-badge--system` with background var(--color-sky-100) and color var(--color-sky-500) (or use neutral-200/neutral-600 if sky tokens don't exist).

**TrainingTypeModal.tsx:**
- Fields: Nome (required), Descricao (textarea optional), Carga horaria minima (number, required, min 1), Validade padrao em meses (number, required, min 1), Referencia NR (text optional), Global (checkbox, label: "Obrigatorio para todos os cargos")
- isSystem types cannot open in edit mode — button should not be rendered
- Validate on blur, required fields with *

**PositionTrainingRequirementsModal.tsx:**
- Same pattern as PositionEpiRequirementsModal but for training types
- Shows global training types with "Todos os cargos" label (non-removable from position level)
- Add: training type picker + add button
- Remove: delete icon + ConfirmModal warning

**TrainingRecordsPage.tsx** — Per UI-SPEC page 4:
- Page header: ClipboardList icon + "Registros de Treinamento" title + "Registrar Treinamento" primary CTA
- Filter bar: training type filter (select), instructor type filter (radio: INTERNO/EXTERNO/Todos), date range, farm filter (FarmContext)
- Table: Data (dd/MM/yyyy), Tipo de Treinamento, Instrutor, Tipo Instrutor (INSTRUCTOR_TYPE_LABELS), CH realizada (`{N}h`), Local, Participantes (count badge), Acoes (expand/certificate/delete)
- Row expand: participant list showing employee name + ComplianceStatusBadge (based on expiresAt) + "Baixar Certificado" link per participant
- Pagination
- Empty state: "Nenhum treinamento registrado" + "Registre o primeiro treinamento realizado para acompanhar a conformidade." + CTA

**TrainingRecordsPage.css:** Follow pattern.

**TrainingRecordModal.tsx** — Multi-step modal per UI-SPEC:
- **Step 1 — Dados do Treinamento:** tipo de treinamento (select from training types), data (date, not future), instrutor nome (text required), tipo instrutor (radio: INTERNO/EXTERNO), registro CRM/CREA (text, shown when EXTERNO, optional), carga horaria realizada (number, required), local (text optional), observacoes (textarea optional), upload lista presenca (file upload optional)
  - Warning if effectiveHours < trainingType.minHours: "Carga horaria abaixo do minimo exigido ({N}h) para este tipo de treinamento." (warning, not error — still allows proceeding)
  - "Proximo" button validates step 1 before advancing
- **Step 2 — Participantes:** Employee multi-select with search (checkbox list or transfer-list pattern). Each selected employee shows calculated expiresAt below name: `Validade ate: {date + validityMonths formatted dd/MM/yyyy}`
  - "Registrar" button submits the full payload
- On success: toast "Treinamento registrado. Certificados disponiveis para download.", close, refetch
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - useTrainingTypes.ts contains `fetchTrainingTypes` and `seedNr31Types`
    - useTrainingRecords.ts contains `downloadCertificatePdf`
    - TrainingTypesPage.tsx contains `Sistema` badge text
    - TrainingTypesPage.tsx contains `Requisitos por Cargo` tab
    - TrainingTypesPage.tsx contains `nao editavel` tooltip text (or equivalent)
    - TrainingRecordsPage.tsx contains `Registrar Treinamento`
    - TrainingRecordsPage.tsx contains `ComplianceStatusBadge`
    - TrainingRecordModal.tsx contains `Proximo` or step navigation
    - TrainingRecordModal.tsx contains `employeeIds`
    - TrainingRecordModal.tsx contains `Carga horaria abaixo` warning
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TrainingTypesPage and TrainingRecordsPage fully implemented with modals, system type protection, multi-step training registration, and certificate PDF download.</done>
</task>

<task type="auto">
  <name>Task 2: MedicalExamsPage + MedicalExamModal + hook</name>
  <files>
    apps/frontend/src/hooks/useMedicalExams.ts,
    apps/frontend/src/pages/MedicalExamsPage.tsx,
    apps/frontend/src/pages/MedicalExamsPage.css,
    apps/frontend/src/components/medical-exams/MedicalExamModal.tsx
  </files>
  <read_first>
    - apps/frontend/src/types/medical-exam.ts (interfaces from Plan 05)
    - apps/frontend/src/hooks/useEpiProducts.ts (hook pattern)
    - apps/frontend/src/pages/EpiProductsPage.tsx (page pattern)
    - apps/frontend/src/components/shared/ComplianceStatusBadge.tsx (badge component)
    - .planning/phases/30-seguranca-trabalho-nr31/30-UI-SPEC.md (Page 5 layout contract, MedicalExamModal spec, result badge mapping)
  </read_first>
  <action>
**hooks/useMedicalExams.ts:**
- fetchMedicalExams (with query params: employeeId, type, result, expiryStatus, farmId, page, limit)
- createMedicalExam, updateMedicalExam, deleteMedicalExam
- fetchEmployeeExams(employeeId)
- API paths: `/medical-exams/...`

**MedicalExamsPage.tsx** — Per UI-SPEC page 5:
- Page header: Stethoscope icon + "ASOs" title + "Registrar ASO" primary CTA
- Filter bar: employee search (debounce 300ms), ASO type filter (select from ASO_TYPE_LABELS), result filter (APTO/INAPTO/APTO_COM_RESTRICAO), expiry status filter (all/vencendo/vencido), farm filter (FarmContext)
- Table columns: Colaborador, Funcao, Tipo ASO (ASO_TYPE_LABELS), Data (dd/MM/yyyy), Medico (CRM in JetBrains Mono), Resultado (result badge), Proximo exame (ComplianceStatusBadge based on expiryStatus), Acoes (view/delete/download ASO)
- **Result badge mapping** (per UI-SPEC):
  - APTO: success-100/success-500 + CheckCircle icon
  - INAPTO: error-100/error-500 + XCircle icon
  - APTO_COM_RESTRICAO: warning-100/warning-500 + AlertTriangle icon
  Create a small inline component or use a config object similar to ComplianceStatusBadge for ASO results.
- Pagination
- Download ASO: if documentUrl present, open in new tab; if not, disable button
- Delete: ConfirmModal variant warning, message: "Excluir ASO: O historico do exame sera removido permanentemente. Confirmar exclusao?"
- Empty state: Stethoscope icon 48px + "Nenhum ASO registrado" + "Registre o ASO admissional dos colaboradores para iniciar o controle." + CTA

**MedicalExamsPage.css:** Follow established pattern.

**MedicalExamModal.tsx:**
- Fields:
  - Colaborador (employee picker with search, required *)
  - Tipo ASO (select: ADMISSIONAL, PERIODICO, RETORNO_TRABALHO, MUDANCA_RISCO, DEMISSIONAL — use ASO_TYPE_LABELS for display)
  - Data do exame (date, required, cannot be future — error: "A data nao pode ser no futuro")
  - Nome medico (text, required *)
  - CRM (text, pattern hint "CRM/UF 00000", error: "Informe o CRM no formato CRM/UF 00000")
  - Resultado (radio: Apto / Inapto / Apto com restricao)
  - Restricoes (textarea, ONLY shown when result = APTO_COM_RESTRICAO, required when shown)
  - Data proximo exame (date, auto-calculated based on Position.asoPeriodicityMonths, but user-editable — show pre-filled value; error if before exam date: "A data do proximo exame deve ser apos a data do exame")
  - Observacoes (textarea, optional)
  - Upload ASO digitalizado (file upload, optional, accept application/pdf,image/jpeg,image/png)
- **INAPTO banner:** When result = INAPTO, show inline info banner inside modal: info-100 bg + info icon + "Colaborador inapto para trabalho. O gestor sera notificado via dashboard de conformidade."
- **Auto-calculate nextExamDate:** When employee is selected and exam date is entered, fetch employee's position asoPeriodicityMonths (or default 12), compute nextExamDate = date + months. Pre-fill the field. User can override.
- Validate on blur. Required fields with *.
- On save: createMedicalExam, toast "ASO registrado com sucesso.", close, refetch
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - useMedicalExams.ts contains `fetchMedicalExams` and `createMedicalExam`
    - MedicalExamsPage.tsx contains `Registrar ASO`
    - MedicalExamsPage.tsx contains `ComplianceStatusBadge`
    - MedicalExamsPage.tsx contains `ASO_TYPE_LABELS` or equivalent type display
    - MedicalExamsPage.tsx contains `Nenhum ASO registrado` (empty state)
    - MedicalExamModal.tsx contains `APTO_COM_RESTRICAO`
    - MedicalExamModal.tsx contains `inapto para trabalho` (INAPTO banner)
    - MedicalExamModal.tsx contains `asoPeriodicityMonths` or equivalent auto-calculation
    - MedicalExamModal.tsx contains `data do proximo exame deve ser apos` (validation)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MedicalExamsPage fully implemented with ASO result badges, expiry status, INAPTO warning banner, and auto-calculated nextExamDate per UI-SPEC contract.</done>
</task>

</tasks>

<verification>
- `cd apps/frontend && npx tsc --noEmit` compiles clean
- TrainingTypesPage has system/custom type differentiation
- TrainingRecordsPage has multi-step modal with participant selection
- MedicalExamsPage has result badges and expiry status
- All modals follow form validation rules from UI-SPEC
</verification>

<success_criteria>
SEGUR-02 and SEGUR-03 frontend complete: Training type management, training session recording with certificate download, and ASO management with result badges and auto-calculated expiry dates.
</success_criteria>

<output>
After completion, create `.planning/phases/30-seguranca-trabalho-nr31/30-06-SUMMARY.md`
</output>
