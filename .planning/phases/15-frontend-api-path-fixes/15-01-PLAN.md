---
phase: 15-frontend-api-path-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/hooks/usePurchasingKanban.ts
  - apps/frontend/src/hooks/usePurchasingKanban.spec.ts
  - apps/frontend/src/hooks/useNotificationPreferences.ts
  - apps/frontend/src/hooks/useNotificationPreferences.spec.ts
  - apps/frontend/src/hooks/useNotifications.ts
  - apps/frontend/src/hooks/useNotifications.spec.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-03
user_setup: []

must_haves:
  truths:
    - "Kanban DnD RC_PENDENTE to RC_APROVADA calls POST /org/purchase-requests/:id/transition with body {action:'APPROVE'}"
    - 'Kanban DnD RC_APROVADA to EM_COTACAO calls POST /org/quotations (no orgId segment)'
    - 'Kanban DnD OC_EMITIDA to AGUARDANDO_ENTREGA calls PATCH /org/purchase-orders/:id/transition (no orgId segment)'
    - 'Notification preferences GET and PUT include orgId in path segment'
    - 'DAILY_DIGEST appears in NotificationType union and NOTIFICATION_LABELS map'
  artifacts:
    - path: 'apps/frontend/src/hooks/usePurchasingKanban.ts'
      provides: 'Corrected DnD API paths without orgId for action endpoints'
      contains: '/org/purchase-requests/'
    - path: 'apps/frontend/src/hooks/useNotificationPreferences.ts'
      provides: 'orgId-injected notification preferences paths'
      contains: 'useAuth'
    - path: 'apps/frontend/src/hooks/useNotifications.ts'
      provides: 'DAILY_DIGEST in type union and label map'
      contains: 'DAILY_DIGEST'
    - path: 'apps/frontend/src/hooks/useNotificationPreferences.spec.ts'
      provides: 'Tests for notification preferences orgId path'
    - path: 'apps/frontend/src/hooks/useNotifications.spec.ts'
      provides: 'Tests for DAILY_DIGEST label presence'
  key_links:
    - from: 'apps/frontend/src/hooks/usePurchasingKanban.ts'
      to: 'apps/backend/src/modules/purchase-requests/purchase-requests.routes.ts'
      via: 'POST /org/purchase-requests/:id/transition'
      pattern: "api\\.post.*purchase-requests.*transition"
    - from: 'apps/frontend/src/hooks/useNotificationPreferences.ts'
      to: 'apps/backend/src/modules/notification-preferences/notification-preferences.routes.ts'
      via: 'GET/PUT /org/:orgId/notification-preferences'
      pattern: "org/\\$\\{orgId\\}/notification-preferences"
---

<objective>
Fix 5 frontend API path mismatches that cause 404s in Kanban DnD transitions and notification preferences, and add missing DAILY_DIGEST notification type label.

Purpose: Restore full Kanban drag-and-drop interactivity and notification preference management that were broken by orgId path convention mismatch between kanban (path-param) and action endpoints (JWT convention).

Output: 3 corrected hook files + 2 new spec files + 1 updated spec file, all tests green.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-frontend-api-path-fixes/15-RESEARCH.md

<interfaces>
<!-- From apps/frontend/src/stores/AuthContext.tsx -->
```typescript
interface User {
  userId: string;
  email: string;
  role: string;
  organizationId: string | null;
}

function useAuth(): AuthContextValue; // returns { user, isAuthenticated, ... }
// Import: import { useAuth } from '../stores/AuthContext';

````

<!-- From apps/frontend/src/services/api.ts -->
```typescript
// api.get<T>(url: string): Promise<T>
// api.post<T>(url: string, body?: unknown): Promise<T>
// api.put<T>(url: string, body?: unknown): Promise<T>
// api.patch<T>(url: string, body?: unknown): Promise<T>
// Import: import { api } from '@/services/api';
````

<!-- Backend route contracts (read-only reference) -->

```
POST   /org/purchase-requests/:id/transition  body: { action: 'APPROVE' | 'REJECT' | ... }
POST   /org/quotations                        body: { purchaseRequestId: string }
PATCH  /org/purchase-orders/:id/transition     body: { status: 'EM_TRANSITO' }
GET    /org/:orgId/notification-preferences
PUT    /org/:orgId/notification-preferences    body: { eventType, channel, enabled }
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create and update test specs for all 5 fixes</name>
  <files>
    apps/frontend/src/hooks/usePurchasingKanban.spec.ts,
    apps/frontend/src/hooks/useNotificationPreferences.spec.ts,
    apps/frontend/src/hooks/useNotifications.spec.ts
  </files>
  <read_first>
    apps/frontend/src/hooks/usePurchasingKanban.spec.ts,
    apps/frontend/src/hooks/usePurchasingKanban.ts,
    apps/frontend/src/hooks/useNotificationPreferences.ts,
    apps/frontend/src/hooks/useNotifications.ts,
    apps/frontend/src/stores/AuthContext.tsx
  </read_first>
  <behavior>
    usePurchasingKanban.spec.ts (ADD to existing file — keep all 4 existing tests):
    - Test: RC_PENDENTE->RC_APROVADA calls api.post with path containing '/org/purchase-requests/card-id/transition' (NOT '/approve') and body { action: 'APPROVE' }
    - Test: RC_APROVADA->EM_COTACAO calls api.post with path '/org/quotations' (no orgId segment like '/org/org-1/quotations')
    - Test: OC_EMITIDA->AGUARDANDO_ENTREGA calls api.patch with path '/org/purchase-orders/card-id/transition' (no orgId segment)

    useNotificationPreferences.spec.ts (NEW file):
    - Test: fetchPreferences calls api.get with path matching '/org/{orgId}/notification-preferences' (must contain orgId segment)
    - Test: togglePreference calls api.put with path matching '/org/{orgId}/notification-preferences'
    - Mock useAuth to return { user: { organizationId: 'org-123' } }

    useNotifications.spec.ts (NEW file):
    - Test: DAILY_DIGEST exists as key in NOTIFICATION_LABELS
    - Test: NOTIFICATION_LABELS.DAILY_DIGEST equals 'Resumo diario'

  </behavior>
  <action>
    1. UPDATE usePurchasingKanban.spec.ts — add 3 new tests to the existing describe block. The mock board must include RC_PENDENTE and RC_APROVADA columns with cards. Add to MOCK_BOARD_RESPONSE.columns:
       - { id: 'RC_PENDENTE', label: 'RC Pendente', count: 1, cards: [{ id: 'card-rc', entityType: 'RC', sequentialNumber: 'RC-010', urgency: 'NORMAL', requesterName: 'Ana', totalValue: 500, daysInStage: 1, isOverdue: false, farmId: 'farm-1', farmName: 'Fazenda Sol' }] }
       - { id: 'RC_APROVADA', label: 'RC Aprovada', count: 1, cards: [{ id: 'card-rc2', entityType: 'RC', sequentialNumber: 'RC-011', urgency: 'NORMAL', requesterName: 'Pedro', totalValue: 800, daysInStage: 3, isOverdue: false, farmId: 'farm-1', farmName: 'Fazenda Sol' }] }
       New tests:
       a) 'RC_PENDENTE->RC_APROVADA calls api.post /org/purchase-requests/:id/transition with {action: APPROVE}':
          - await moveCard('card-rc', 'RC_PENDENTE', 'RC_APROVADA')
          - expect(mockPost).toHaveBeenCalledWith('/org/purchase-requests/card-rc/transition', { action: 'APPROVE' })
       b) 'RC_APROVADA->EM_COTACAO calls api.post /org/quotations without orgId':
          - await moveCard('card-rc2', 'RC_APROVADA', 'EM_COTACAO')
          - expect(mockPost).toHaveBeenCalledWith('/org/quotations', { purchaseRequestId: 'card-rc2' })
       c) Update the existing 'OC_EMITIDA->AGUARDANDO_ENTREGA' test assertion from `expect.stringMatching(...)` to exact path: expect(mockPatch).toHaveBeenCalledWith('/org/purchase-orders/card-2/transition', { status: 'EM_TRANSITO' })

    2. CREATE useNotificationPreferences.spec.ts:
       - vi.mock('../services/api') with mockGet, mockPut
       - vi.mock('../stores/AuthContext') returning { useAuth: () => ({ user: { organizationId: 'org-123' } }) }
       - Test fetchPreferences: renderHook, waitFor loading=false, expect mockGet called with '/org/org-123/notification-preferences'
       - Test togglePreference: call togglePreference('RC_APPROVED', 'badge', true), expect mockPut called with '/org/org-123/notification-preferences' and body { eventType: 'RC_APPROVED', channel: 'badge', enabled: true }

    3. CREATE useNotifications.spec.ts:
       - Import NOTIFICATION_LABELS and NotificationType from './useNotifications'
       - Test: expect(NOTIFICATION_LABELS).toHaveProperty('DAILY_DIGEST')
       - Test: expect(NOTIFICATION_LABELS.DAILY_DIGEST).toBe('Resumo diario')

    4. Run all tests — they MUST FAIL (RED phase). The kanban path tests will fail because current code uses wrong paths. The preferences tests will fail because useAuth is not imported. The DAILY_DIGEST test will fail because the key does not exist.

  </action>
  <verify>
    <automated>cd apps/frontend && pnpm test -- --run usePurchasingKanban.spec useNotificationPreferences.spec useNotifications.spec 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - usePurchasingKanban.spec.ts contains 'RC_PENDENTE' and 'RC_APROVADA' test cases
    - usePurchasingKanban.spec.ts contains string '/org/purchase-requests/card-rc/transition'
    - usePurchasingKanban.spec.ts contains { action: 'APPROVE' }
    - usePurchasingKanban.spec.ts contains "'/org/quotations'" (exact path, no orgId)
    - usePurchasingKanban.spec.ts contains "'/org/purchase-orders/card-2/transition'" (exact path, no orgId)
    - useNotificationPreferences.spec.ts exists and contains '/org/org-123/notification-preferences'
    - useNotificationPreferences.spec.ts contains vi.mock('../stores/AuthContext')
    - useNotifications.spec.ts exists and contains 'DAILY_DIGEST'
    - All 3 spec files parse without syntax errors (vitest can discover them)
  </acceptance_criteria>
  <done>All spec files created/updated with assertions for the 5 corrected behaviors. Tests run and fail (RED) because production code has not been fixed yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix 3 hook files to pass all tests (GREEN)</name>
  <files>
    apps/frontend/src/hooks/usePurchasingKanban.ts,
    apps/frontend/src/hooks/useNotificationPreferences.ts,
    apps/frontend/src/hooks/useNotifications.ts
  </files>
  <read_first>
    apps/frontend/src/hooks/usePurchasingKanban.ts,
    apps/frontend/src/hooks/useNotificationPreferences.ts,
    apps/frontend/src/hooks/useNotifications.ts,
    apps/frontend/src/stores/AuthContext.tsx
  </read_first>
  <behavior>
    - usePurchasingKanban.ts moveCard: 3 path fixes
    - useNotificationPreferences.ts: useAuth import + orgId injection
    - useNotifications.ts: DAILY_DIGEST addition
  </behavior>
  <action>
    1. EDIT usePurchasingKanban.ts — change 3 lines in the moveCard function (lines 116-126):

       Line 117 BEFORE: await api.post(`/org/${orgId}/purchase-requests/${cardId}/approve`);
       Line 117 AFTER:  await api.post(`/org/purchase-requests/${cardId}/transition`, { action: 'APPROVE' });

       Line 119 BEFORE: await api.post(`/org/${orgId}/quotations`, { purchaseRequestId: cardId });
       Line 119 AFTER:  await api.post('/org/quotations', { purchaseRequestId: cardId });

       Line 124 BEFORE: await api.patch(`/org/${orgId}/purchase-orders/${cardId}/transition`, {
       Line 124 AFTER:  await api.patch(`/org/purchase-orders/${cardId}/transition`, {

       IMPORTANT: Do NOT change line 65 (`/org/${orgId}/purchasing/kanban`) — that path is correct and must keep orgId.

    2. EDIT useNotificationPreferences.ts — 3 changes:

       a) Add import after line 2:
          import { useAuth } from '../stores/AuthContext';

       b) Add at the top of useNotificationPreferences function body, before useState declarations (after line 24):
          const { user } = useAuth();
          const orgId = user?.organizationId ?? '';

       c) Line 35 BEFORE: const data = await api.get<NotificationPreference[]>('/org/notification-preferences');
          Line 35 AFTER:  const data = await api.get<NotificationPreference[]>(`/org/${orgId}/notification-preferences`);

       d) Line 56 BEFORE: await api.put('/org/notification-preferences', {
          Line 56 AFTER:  await api.put(`/org/${orgId}/notification-preferences`, {

    3. EDIT useNotifications.ts — 2 changes:

       a) Add to NotificationType union (after line 18, before the semicolon):
          | 'DAILY_DIGEST'

       b) Add to NOTIFICATION_LABELS Record (after line 34, before closing brace):
          DAILY_DIGEST: 'Resumo diario',

    4. Run all tests — they MUST PASS (GREEN phase).
    5. Run full frontend test suite to confirm no regressions.

  </action>
  <verify>
    <automated>cd apps/frontend && pnpm test -- --run usePurchasingKanban.spec useNotificationPreferences.spec useNotifications.spec 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - usePurchasingKanban.ts line 117 contains "/org/purchase-requests/${cardId}/transition" (no orgId, no /approve)
    - usePurchasingKanban.ts line 117 contains { action: 'APPROVE' }
    - usePurchasingKanban.ts line 119 contains "'/org/quotations'" (no orgId template)
    - usePurchasingKanban.ts line 124 contains "/org/purchase-orders/${cardId}/transition" (no orgId)
    - usePurchasingKanban.ts line 65 still contains "/org/${orgId}/purchasing/kanban" (unchanged)
    - useNotificationPreferences.ts contains "import { useAuth } from '../stores/AuthContext'"
    - useNotificationPreferences.ts contains "const { user } = useAuth()"
    - useNotificationPreferences.ts contains "/org/${orgId}/notification-preferences" in both GET and PUT
    - useNotifications.ts NotificationType union contains 'DAILY_DIGEST'
    - useNotifications.ts NOTIFICATION_LABELS contains key DAILY_DIGEST with value 'Resumo diario'
    - All spec files pass (exit code 0)
    - Full frontend test suite passes: cd apps/frontend && pnpm test -- --run
  </acceptance_criteria>
  <done>All 5 API path fixes applied, all tests green, no regressions in full frontend suite.</done>
</task>

</tasks>

<verification>
1. Run targeted specs: `cd apps/frontend && pnpm test -- --run usePurchasingKanban.spec useNotificationPreferences.spec useNotifications.spec`
2. Run full frontend test suite: `cd apps/frontend && pnpm test -- --run`
3. Grep confirmation — no orgId in action paths:
   - `grep -n 'orgId.*purchase-requests' apps/frontend/src/hooks/usePurchasingKanban.ts` returns 0 matches
   - `grep -n 'orgId.*purchase-orders' apps/frontend/src/hooks/usePurchasingKanban.ts` returns 0 matches
   - `grep -n 'orgId.*quotations' apps/frontend/src/hooks/usePurchasingKanban.ts` returns 0 matches
   - `grep -n 'orgId.*purchasing/kanban' apps/frontend/src/hooks/usePurchasingKanban.ts` returns 1 match (the correct fetchBoard path)
4. Grep confirmation — orgId present in notification preferences:
   - `grep -n 'orgId.*notification-preferences' apps/frontend/src/hooks/useNotificationPreferences.ts` returns 2 matches (GET + PUT)
5. Grep confirmation — DAILY_DIGEST present:
   - `grep 'DAILY_DIGEST' apps/frontend/src/hooks/useNotifications.ts` returns 2 matches (union + label)
</verification>

<success_criteria>

- All 3 DnD transitions call correct backend paths without orgId in URL
- RC_PENDENTE->RC_APROVADA sends { action: 'APPROVE' } body to /transition endpoint
- Notification preferences GET and PUT include orgId segment from useAuth()
- DAILY_DIGEST notification type has a label in the frontend bell
- All existing tests continue to pass (no regressions)
- 8+ new/updated test assertions all green
  </success_criteria>

<output>
After completion, create `.planning/phases/15-frontend-api-path-fixes/15-01-SUMMARY.md`
</output>
