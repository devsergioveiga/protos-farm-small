---
phase: 19-integrao-financeira-aquisio
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - apps/frontend/src/types/asset.ts
  - apps/frontend/src/hooks/useAssetAcquisition.ts
  - apps/frontend/src/components/assets/InstallmentPreviewTable.tsx
  - apps/frontend/src/components/assets/InstallmentPreviewTable.css
  - apps/frontend/src/components/assets/AssetModal.tsx
  - apps/frontend/src/components/assets/AssetModal.css
  - apps/frontend/src/hooks/usePayables.ts
  - apps/frontend/src/pages/PayablesPage.tsx
  - apps/frontend/src/components/payables/PayableModal.tsx
autonomous: true
requirements:
  - AQUI-01
  - AQUI-02
  - AQUI-07

must_haves:
  truths:
    - "AssetModal shows collapsible 'Dados Financeiros' section when acquisitionValue > 0"
    - "User can select AVISTA or FINANCIADO payment type via radio card UI"
    - "FINANCIADO selection shows installment fields and live InstallmentPreviewTable"
    - "Saving asset with financial data calls POST /asset-acquisitions and creates CP automatically"
    - "Info banner confirms CP will be auto-generated before save"
    - "InstallmentPreviewTable shows first 6 rows with expand for remaining"
  artifacts:
    - path: "apps/frontend/src/components/assets/InstallmentPreviewTable.tsx"
      provides: "Read-only installment schedule preview table"
      min_lines: 80
    - path: "apps/frontend/src/hooks/useAssetAcquisition.ts"
      provides: "Hook for asset acquisition API calls and form state"
      exports: ["useAssetAcquisition"]
  key_links:
    - from: "AssetModal.tsx"
      to: "/api/org/:orgId/asset-acquisitions"
      via: "useAssetAcquisition hook"
      pattern: "asset-acquisitions"
    - from: "InstallmentPreviewTable.tsx"
      to: "generateInstallments"
      via: "client-side preview calculation"
      pattern: "generateInstallments|installments"
---

<objective>
Extend AssetModal with "Dados Financeiros" collapsible section for cash and financed purchases, plus InstallmentPreviewTable component for financing preview.

Purpose: Enable the user to register asset acquisitions with automatic CP generation directly from the asset creation flow, covering AQUI-01 (auto CP), AQUI-02 (financed with installments), and AQUI-07 (cost center + classification linkage).
Output: Extended AssetModal with financial data section, new InstallmentPreviewTable component, useAssetAcquisition hook, and PayableCategory label updates.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-integrao-financeira-aquisio/19-RESEARCH.md
@.planning/phases/19-integrao-financeira-aquisio/19-UI-SPEC.md
@.planning/phases/19-integrao-financeira-aquisio/19-01-SUMMARY.md

<interfaces>
<!-- API endpoints from Plan 01 -->
POST /api/org/:orgId/asset-acquisitions
  Body: { name, type, classification, farmId, supplierId, acquisitionValue, acquisitionDate,
          invoiceNumber, costCenterId, costCenterMode, paymentType: 'AVISTA'|'FINANCIADO',
          dueDate?, installmentCount?, firstDueDate?, interestRate?, ...assetFields }
  Response 201: { asset: { id, assetTag, name }, payableId, installmentCount }

POST /api/org/:orgId/asset-acquisitions/parse-nfe
  Body: FormData with file field (XML)
  Response 200: NfeParsedData

POST /api/org/:orgId/asset-acquisitions/from-nfe
  Body: CreateFromNfeInput
  Response 201: NfeAcquisitionOutput
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, hook, InstallmentPreviewTable, and PayableCategory frontend updates</name>
  <files>
    apps/frontend/src/types/asset.ts,
    apps/frontend/src/hooks/useAssetAcquisition.ts,
    apps/frontend/src/components/assets/InstallmentPreviewTable.tsx,
    apps/frontend/src/components/assets/InstallmentPreviewTable.css,
    apps/frontend/src/hooks/usePayables.ts,
    apps/frontend/src/pages/PayablesPage.tsx,
    apps/frontend/src/components/payables/PayableModal.tsx
  </files>
  <read_first>
    apps/frontend/src/types/asset.ts,
    apps/frontend/src/hooks/useAssets.ts,
    apps/frontend/src/hooks/useAssetForm.ts,
    apps/frontend/src/hooks/usePayables.ts,
    apps/frontend/src/pages/PayablesPage.tsx,
    apps/frontend/src/components/payables/PayableModal.tsx,
    apps/frontend/src/styles/tokens.css
  </read_first>
  <action>
    1. Update `apps/frontend/src/types/asset.ts`:
       - Add `PaymentType` type: `export type PaymentType = 'AVISTA' | 'FINANCIADO';`
       - Add `AssetAcquisitionInput` interface:
         ```typescript
         export interface AssetAcquisitionInput extends CreateAssetInput {
           paymentType: PaymentType;
           dueDate?: string;
           installmentCount?: number;
           firstDueDate?: string;
           interestRate?: number;
         }
         ```
       - Add `AssetAcquisitionOutput` interface:
         ```typescript
         export interface AssetAcquisitionOutput {
           asset: { id: string; assetTag: string; name: string };
           payableId: string | null;
           installmentCount: number;
         }
         ```
       - Add `InstallmentPreview` interface:
         ```typescript
         export interface InstallmentPreview {
           number: number;
           dueDate: Date;
           amount: number;
         }
         ```

    2. Create `apps/frontend/src/hooks/useAssetAcquisition.ts`:
       - Import `apiClient` pattern from existing hooks (e.g., useAssets.ts)
       - Export `useAssetAcquisition()` hook returning:
         - `createAcquisition(input: AssetAcquisitionInput): Promise<AssetAcquisitionOutput>` — POST to `/api/org/${orgId}/asset-acquisitions`
         - `parseNfe(file: File): Promise<NfeParsedData>` — POST FormData to `/api/org/${orgId}/asset-acquisitions/parse-nfe`
         - `createFromNfe(nfeParsed: NfeParsedData, input: CreateFromNfeInput): Promise<NfeAcquisitionOutput>` — POST to `/api/org/${orgId}/asset-acquisitions/from-nfe`
         - `isLoading: boolean`, `error: string | null`
       - Use `useOrganization()` or equivalent to get orgId from context
       - Error handling: catch and return user-friendly pt-BR error messages

    3. Create `apps/frontend/src/components/assets/InstallmentPreviewTable.tsx`:
       - Props: `{ installments: InstallmentPreview[]; totalAmount: number; isLoading?: boolean }`
       - Render `<table>` with `<caption>Parcelas geradas</caption>`
       - Column headers with `<th scope="col">`: `#` | `Vencimento` | `Valor`
       - `#` column: right-aligned, JetBrains Mono 14px (`font-family: var(--font-mono)`)
       - `Vencimento` column: formatted pt-BR `dd/mm/aaaa` using `Intl.DateTimeFormat('pt-BR')`
       - `Valor` column: right-aligned, JetBrains Mono 14px, formatted BRL using `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })`
       - Show first 6 rows. If `installments.length > 6`: render 6 rows + `<button>` below table "Ver todas as {N} parcelas" / "Recolher" toggle. Expanded state: `display:block`, collapsed: `display:none` on remaining rows (no animation per STATE.md locked decision).
       - Footer row always visible: "Total" label | em-dash | sum formatted BRL, JetBrains Mono 14px bold
       - Loading state: when `isLoading=true`, render 3 skeleton rows with CSS class `installment-table__skeleton-row` (opacity 0.4-0.7 pulse, 1.5s infinite)
       - Minimum row height 48px for touch target compliance

    4. Create `apps/frontend/src/components/assets/InstallmentPreviewTable.css`:
       - Use CSS custom properties from tokens.css exclusively (no hardcoded colors/sizes)
       - `.installment-table` — border-collapse, width 100%
       - `.installment-table__header` — background `var(--color-neutral-100)`, font 14px `var(--font-body)`, weight 600
       - `.installment-table__cell--number`, `.installment-table__cell--amount` — text-align right, font-family `var(--font-mono)`
       - `.installment-table__footer` — font-weight 700, border-top 2px solid `var(--color-neutral-200)`
       - `.installment-table__skeleton-row` — animation: skeleton-pulse 1.5s ease-in-out infinite (opacity 0.4 to 0.7)
       - `.installment-table__expand-btn` — background none, border none, color `var(--color-primary-600)`, cursor pointer, min-height 48px, font 14px `var(--font-body)`, padding `var(--space-2) 0`

    5. Update `apps/frontend/src/hooks/usePayables.ts`:
       - Add `'ASSET_ACQUISITION'` to the `PayableCategory` type union

    6. Update `apps/frontend/src/pages/PayablesPage.tsx`:
       - Add `ASSET_ACQUISITION: 'Aquisicao de Ativo'` to `CATEGORY_LABELS` Record

    7. Update `apps/frontend/src/components/payables/PayableModal.tsx`:
       - Add `'ASSET_ACQUISITION'` to the local `PayableCategory` type
       - Add `ASSET_ACQUISITION: 'Aquisicao de Ativo'` to `CATEGORY_LABELS`
       - Exclude `ASSET_ACQUISITION` from the category `<select>` dropdown options (asset CPs are generated-only, not manually created): filter it out in the `.map()` that renders `<option>` elements
  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && npx tsc --noEmit --project apps/frontend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - types/asset.ts exports PaymentType, AssetAcquisitionInput, AssetAcquisitionOutput, InstallmentPreview
    - useAssetAcquisition.ts exports useAssetAcquisition hook with createAcquisition, parseNfe, createFromNfe methods
    - InstallmentPreviewTable.tsx renders table with caption "Parcelas geradas", th scope="col" headers, JetBrains Mono for amounts
    - InstallmentPreviewTable.tsx shows max 6 rows with expand/collapse button
    - InstallmentPreviewTable.css uses only var(--*) tokens, includes skeleton-pulse animation
    - usePayables.ts PayableCategory type includes 'ASSET_ACQUISITION'
    - PayablesPage.tsx CATEGORY_LABELS includes ASSET_ACQUISITION
    - PayableModal.tsx excludes ASSET_ACQUISITION from category select dropdown
    - TypeScript compilation succeeds without errors
  </acceptance_criteria>
  <done>Frontend types, hook, installment table, and payable category updates are complete. All files compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Extend AssetModal with "Dados Financeiros" collapsible section</name>
  <files>
    apps/frontend/src/components/assets/AssetModal.tsx,
    apps/frontend/src/components/assets/AssetModal.css
  </files>
  <read_first>
    apps/frontend/src/components/assets/AssetModal.tsx,
    apps/frontend/src/components/assets/AssetModal.css,
    apps/frontend/src/components/assets/InstallmentPreviewTable.tsx,
    apps/frontend/src/hooks/useAssetAcquisition.ts,
    apps/frontend/src/types/asset.ts,
    apps/frontend/src/styles/tokens.css,
    .planning/phases/19-integrao-financeira-aquisio/19-UI-SPEC.md
  </read_first>
  <action>
    1. Modify `apps/frontend/src/components/assets/AssetModal.tsx`:

       **Add imports:**
       - `import { ChevronDown, ChevronUp, Info } from 'lucide-react';`
       - `import InstallmentPreviewTable from './InstallmentPreviewTable';`
       - `import { useAssetAcquisition } from '@/hooks/useAssetAcquisition';`
       - `import type { PaymentType, InstallmentPreview } from '@/types/asset';`

       **Add state for financial section:**
       ```typescript
       const [financialExpanded, setFinancialExpanded] = useState(true);
       const [paymentType, setPaymentType] = useState<PaymentType | ''>('');
       const [dueDate, setDueDate] = useState('');
       const [installmentCount, setInstallmentCount] = useState<number | ''>('');
       const [firstDueDate, setFirstDueDate] = useState('');
       const [interestRate, setInterestRate] = useState('');
       const [installmentPreviews, setInstallmentPreviews] = useState<InstallmentPreview[]>([]);
       const [previewLoading, setPreviewLoading] = useState(false);
       ```

       **Add installment preview calculation (on blur of financing fields):**
       - Function `recalculateInstallments()`: when paymentType=FINANCIADO and installmentCount>=2 and firstDueDate is set:
         - Parse acquisitionValue from form state
         - Compute installments client-side: divide total by count, distribute dates monthly from firstDueDate, assign residual to first installment
         - Set `installmentPreviews` state
       - Call on `onBlur` of installmentCount, firstDueDate, interestRate fields

       **Add "Dados Financeiros" collapsible section after existing "Dados de Aquisicao" section:**
       - Only render when `acquisitionValue > 0` (check form state for acquisitionValue field)
       - Section header: clickable div with "Dados Financeiros" label + ChevronDown/ChevronUp icon (16px)
         - `aria-label` toggling between "Expandir Dados Financeiros" and "Recolher Dados Financeiros"
         - Background `var(--color-neutral-100)` on hover
         - `onClick`: toggle `financialExpanded` state
       - Section body: `display: financialExpanded ? 'block' : 'none'` (zero animation per STATE.md)

       **Inside section body:**
       a. **Payment type radio cards** wrapped in `<fieldset>` + `<legend>Tipo de pagamento</legend>`:
          - Two radio cards: "A Vista" and "Financiado"
          - Each card: `<label>` wrapping hidden `<input type="radio">` + card content
          - Card styling:
            - Unselected: white background, `var(--color-neutral-200)` border, `var(--radius-md)`
            - "A Vista" selected: `var(--color-neutral-100)` background, `var(--color-primary-600)` 2px border
            - "Financiado" selected: `var(--color-sky-100)` background, `var(--color-primary-600)` 2px border
          - Radio inputs: `aria-required="true"` when acquisitionValue > 0
          - Card content: label text (14px Source Sans 3 600) + description (14px Source Sans 3 400)
            - "A Vista": "Pagamento em parcela unica"
            - "Financiado": "Pagamento parcelado com financiamento"

       b. **Due date field** (shown when paymentType=AVISTA, `display:none` otherwise):
          - `<label htmlFor="acq-due-date">Data de vencimento *</label>`
          - `<input type="date" id="acq-due-date" aria-label="Data de vencimento" required />`

       c. **Financing block** (shown when paymentType=FINANCIADO, `display:none` otherwise):
          - Field: "Numero de parcelas *" — `<input type="number" min="2" max="360" />`
            - Inline validation on blur: if < 2, show error "Minimo 2 parcelas para financiamento"
          - Field: "Vencimento da 1a parcela *" — `<input type="date" />`
          - Field: "Taxa de juros a.m. (%)" — optional, `<input type="text" placeholder="0,00 — deixe em branco para parcelas iguais" />`
            - Accept comma as decimal separator, format on blur to "X,XX%"
          - `<InstallmentPreviewTable installments={installmentPreviews} totalAmount={acquisitionValue} isLoading={previewLoading} />`

       d. **Cost center selector** (shown if not already visible in base form): pre-populate from asset's costCenterId

       e. **Classification chip**: read-only display of asset's `classification` using ASSET_CLASSIFICATION_LABELS, styled as informational chip: `var(--color-neutral-100)` background, 14px Source Sans 3, `var(--radius-sm)` border-radius, `var(--space-2)` padding

       f. **CP generation info banner** (shown when acquisitionValue > 0 AND supplierId is set AND payment type selected):
          - `role="status"` (informational, not error)
          - `var(--color-info-100)` background, `var(--color-info-500)` left border 4px, padding 12px 16px
          - `Info` icon 16px + "Uma conta a pagar sera criada automaticamente ao salvar este ativo."
          - Dismissible with X button (`aria-label="Fechar aviso"`)

       **Modify form submission:**
       - When financial data is filled (paymentType set), call `useAssetAcquisition().createAcquisition()` instead of the existing `useAssetForm().handleSubmit()`
       - Build AssetAcquisitionInput from form state: merge all existing asset fields + paymentType, dueDate/firstDueDate, installmentCount, interestRate
       - On success: show toast "Ativo registrado. Conta a pagar criada automaticamente." (when payableId returned), call `onSuccess()`
       - On error: show error toast with user-friendly message
       - When no financial data (paymentType empty): use existing asset creation flow unchanged

       **Update modal footer CTA text:**
       - When financial data present: button text "Registrar Ativo" (instead of existing "Salvar")
       - Otherwise: keep existing button text

    2. Update `apps/frontend/src/components/assets/AssetModal.css`:
       - `.asset-modal__financial-section` — margin-top `var(--space-6)`, border-top 1px solid `var(--color-neutral-200)`
       - `.asset-modal__financial-header` — display flex, justify-content space-between, align-items center, padding `var(--space-3) var(--space-4)`, cursor pointer, min-height 48px, background `var(--color-neutral-50)`, border-radius `var(--radius-md)`, hover background `var(--color-neutral-100)`
       - `.asset-modal__financial-body` — padding `var(--space-4)`
       - `.asset-modal__payment-cards` — display flex, gap `var(--space-4)`, margin-bottom `var(--space-4)`
       - `.asset-modal__payment-card` — flex 1, border 2px solid `var(--color-neutral-200)`, border-radius `var(--radius-md)`, padding `var(--space-4)`, cursor pointer, min-height 48px, transition none
       - `.asset-modal__payment-card--selected-avista` — background `var(--color-neutral-100)`, border-color `var(--color-primary-600)`
       - `.asset-modal__payment-card--selected-financiado` — background `var(--color-sky-100)`, border-color `var(--color-primary-600)`
       - `.asset-modal__payment-card input[type="radio"]` — position absolute, opacity 0, width 0, height 0
       - `.asset-modal__payment-card:focus-within` — outline 2px solid `var(--color-primary-500)`, outline-offset 2px
       - `.asset-modal__financing-fields` — display grid, grid-template-columns 1fr 1fr, gap `var(--space-4)`, margin-bottom `var(--space-4)`
       - `.asset-modal__info-banner` — display flex, align-items center, gap `var(--space-3)`, background `var(--color-info-100)`, border-left 4px solid `var(--color-info-500)`, padding `var(--space-3) var(--space-4)`, border-radius `var(--radius-md)`, margin-top `var(--space-4)`, font-size 14px
       - `.asset-modal__classification-chip` — display inline-flex, background `var(--color-neutral-100)`, padding `var(--space-1) var(--space-2)`, border-radius `var(--radius-sm)`, font-size 14px, font-family `var(--font-body)`
       - Responsive: `@media (max-width: 639px)` — payment cards stack vertically (flex-direction column), financing fields single column (grid-template-columns 1fr)
  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && npx tsc --noEmit --project apps/frontend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - AssetModal.tsx imports InstallmentPreviewTable and useAssetAcquisition
    - AssetModal.tsx contains fieldset with legend "Tipo de pagamento"
    - AssetModal.tsx contains radio inputs for AVISTA and FINANCIADO
    - AssetModal.tsx contains display:none/display:block conditional rendering for financing fields (no CSS animation)
    - AssetModal.tsx contains role="status" info banner with text "Uma conta a pagar sera criada automaticamente ao salvar este ativo."
    - AssetModal.tsx calls createAcquisition when paymentType is set
    - AssetModal.css uses only var(--*) token references (no hardcoded hex colors or pixel values outside 4px multiples)
    - AssetModal.css has .asset-modal__payment-card with min-height 48px
    - AssetModal.css has responsive media query for max-width 639px
    - TypeScript compilation succeeds without errors
  </acceptance_criteria>
  <done>AssetModal has complete "Dados Financeiros" section: payment type radio cards, conditional financing fields with live installment preview, CP auto-generation info banner, and submission routing to asset-acquisitions API endpoint.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project apps/frontend/tsconfig.json` — no type errors
- AssetModal renders financial section when acquisitionValue field has value > 0
- InstallmentPreviewTable renders table with caption, th headers, JetBrains Mono amounts
- PayablesPage and PayableModal include ASSET_ACQUISITION category label
- PayableModal excludes ASSET_ACQUISITION from manual category dropdown
</verification>

<success_criteria>
- AssetModal shows "Dados Financeiros" collapsible section with AVISTA/FINANCIADO radio cards
- FINANCIADO displays installmentCount, firstDueDate, interestRate fields + InstallmentPreviewTable
- AVISTA displays single dueDate field
- Form submission with financial data calls POST /asset-acquisitions
- Info banner confirms automatic CP creation
- InstallmentPreviewTable shows first 6 rows with expand link
- All CSS uses design tokens exclusively
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/19-integrao-financeira-aquisio/19-02-SUMMARY.md`
</output>
