---
phase: 26-par-metros-de-folha-e-motor-de-c-lculo
plan: '04'
type: execute
wave: 3
depends_on: ['26-03']
files_modified:
  - apps/frontend/src/types/payroll.ts
  - apps/frontend/src/hooks/usePayrollRubricas.ts
  - apps/frontend/src/hooks/usePayrollTables.ts
  - apps/frontend/src/pages/PayrollParametersPage.tsx
  - apps/frontend/src/pages/PayrollParametersPage.css
  - apps/frontend/src/components/payroll/PayrollRubricaModal.tsx
  - apps/frontend/src/components/payroll/PayrollLegalTableModal.tsx
  - apps/frontend/src/App.tsx
  - apps/frontend/src/components/layout/Sidebar.tsx
autonomous: false
requirements:
  - FOLHA-01

must_haves:
  truths:
    - 'Contador can see list of rubricas organized by type (proventos, descontos) with system rubricas visually distinguished'
    - 'Contador can create a custom rubrica via modal with name, type, formula type, and rate/formula field'
    - 'Contador can edit custom rubricas but cannot edit system rubricas (edit button disabled with lock icon)'
    - 'Contador can deactivate custom rubricas via confirmation modal'
    - 'Contador can see legal tables (INSS, IRRF, SALARY_FAMILY, MINIMUM_WAGE, FUNRURAL) with effective dates and bracket values'
    - 'Contador can add a new legal table version with effective date and bracket rows'
    - 'Page loads with skeleton, then shows real data'
  artifacts:
    - path: 'apps/frontend/src/pages/PayrollParametersPage.tsx'
      provides: 'Main payroll parameters page with tabs'
      contains: 'PayrollParametersPage'
    - path: 'apps/frontend/src/components/payroll/PayrollRubricaModal.tsx'
      provides: 'Modal for create/edit rubrica'
      contains: 'PayrollRubricaModal'
    - path: 'apps/frontend/src/components/payroll/PayrollLegalTableModal.tsx'
      provides: 'Modal for adding new legal table version'
      contains: 'PayrollLegalTableModal'
    - path: 'apps/frontend/src/types/payroll.ts'
      provides: 'Frontend types matching backend API'
      contains: 'PayrollRubrica'
    - path: 'apps/frontend/src/hooks/usePayrollRubricas.ts'
      provides: 'Hook for rubricas data fetching and mutations'
      contains: 'usePayrollRubricas'
    - path: 'apps/frontend/src/hooks/usePayrollTables.ts'
      provides: 'Hook for legal tables data fetching and mutations'
      contains: 'usePayrollTables'
  key_links:
    - from: 'apps/frontend/src/pages/PayrollParametersPage.tsx'
      to: 'apps/frontend/src/hooks/usePayrollRubricas.ts'
      via: 'import { usePayrollRubricas }'
      pattern: 'usePayrollRubricas'
    - from: 'apps/frontend/src/pages/PayrollParametersPage.tsx'
      to: 'apps/frontend/src/hooks/usePayrollTables.ts'
      via: 'import { usePayrollTables }'
      pattern: 'usePayrollTables'
    - from: 'apps/frontend/src/hooks/usePayrollRubricas.ts'
      to: '/api/org/:orgId/payroll-rubricas'
      via: 'fetch'
      pattern: 'payroll-rubricas'
    - from: 'apps/frontend/src/App.tsx'
      to: 'apps/frontend/src/pages/PayrollParametersPage.tsx'
      via: 'Route path'
      pattern: 'payroll-parameters'
---

<objective>
Build the frontend page for payroll parameter configuration — rubricas list with CRUD modals and legal tables display with update modal.

Purpose: Provide the Contador with a web interface to configure rubricas and update legal tables without needing database access or code deploys.
Output: PayrollParametersPage with 2 tabs (Rubricas / Tabelas Legais), 2 modals (RubricaModal / LegalTableModal), types, hooks, routing, and sidebar entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-par-metros-de-folha-e-motor-de-c-lculo/26-UI-SPEC.md
@.planning/phases/26-par-metros-de-folha-e-motor-de-c-lculo/26-03-SUMMARY.md

<interfaces>
<!-- Backend API endpoints from Plan 03 -->

Endpoints (all under /api/org/:orgId/):

- GET /payroll-rubricas?rubricaType=&isActive=&search=&page=&limit= → { data: RubricaOutput[], total, page, limit }
- GET /payroll-rubricas/:id → RubricaOutput
- POST /payroll-rubricas → RubricaOutput (201)
- PUT /payroll-rubricas/:id → RubricaOutput
- PATCH /payroll-rubricas/:id/deactivate → RubricaOutput
- GET /payroll-tables?tableType=&effectiveAt= → LegalTableOutput[]
- GET /payroll-tables/:id → LegalTableOutput
- POST /payroll-tables → LegalTableOutput (201)
- GET /payroll-tables/effective?tableType=&competenceDate= → LegalTableOutput

<!-- Existing patterns from Phase 25 frontend -->

From apps/frontend/src/hooks/useEmployees.ts:

- useState + useCallback pattern (no SWR)
- fetchData on mount via useEffect
- api.get/post/put/patch helpers with error toast via sonner

From apps/frontend/src/pages/EmployeesPage.tsx:

- Page + tab layout pattern
- Skeleton loading rows
- Modal for create/edit triggered by state
- ConfirmModal for destructive actions

From apps/frontend/src/components/layout/Sidebar.tsx:

- Sidebar group pattern for section headers (e.g., "RH" group)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend types, hooks, and PayrollParametersPage with Rubricas tab</name>
  <files>apps/frontend/src/types/payroll.ts, apps/frontend/src/hooks/usePayrollRubricas.ts, apps/frontend/src/hooks/usePayrollTables.ts, apps/frontend/src/pages/PayrollParametersPage.tsx, apps/frontend/src/pages/PayrollParametersPage.css, apps/frontend/src/components/payroll/PayrollRubricaModal.tsx, apps/frontend/src/App.tsx, apps/frontend/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - .planning/phases/26-par-metros-de-folha-e-motor-de-c-lculo/26-UI-SPEC.md (full UI contract — tabs, badges, copy, interaction states, accessibility)
    - apps/frontend/src/types/employee.ts (pattern reference for type file)
    - apps/frontend/src/hooks/useEmployees.ts (pattern reference for hook with useState+useCallback)
    - apps/frontend/src/pages/EmployeesPage.tsx (pattern reference for page layout with tabs + skeleton + modal)
    - apps/frontend/src/pages/EmployeesPage.css (pattern reference for page CSS)
    - apps/frontend/src/components/employees/EmployeeModal.tsx (pattern reference for form modal)
    - apps/frontend/src/components/ui/ConfirmModal.tsx (for deactivate confirmation)
    - apps/frontend/src/App.tsx (current routes — add /payroll-parameters)
    - apps/frontend/src/components/layout/Sidebar.tsx (add "Folha de Pagamento" group or add under existing "RH" group)
    - apps/frontend/src/styles/tokens.css (CSS custom properties for colors, spacing, fonts)
  </read_first>
  <action>
**Create `apps/frontend/src/types/payroll.ts`:**

```typescript
export type RubricaType = 'PROVENTO' | 'DESCONTO' | 'INFORMATIVO';
export type CalculationType = 'FIXED_VALUE' | 'PERCENTAGE' | 'FORMULA' | 'SYSTEM';
export type SystemFormulaType =
  | 'SYSTEM_INSS'
  | 'SYSTEM_IRRF'
  | 'SYSTEM_FGTS'
  | 'SYSTEM_SALARY_FAMILY'
  | 'SYSTEM_FUNRURAL';
export type LegalTableType = 'INSS' | 'IRRF' | 'SALARY_FAMILY' | 'MINIMUM_WAGE' | 'FUNRURAL';

export interface PayrollRubrica {
  id: string;
  code: string;
  name: string;
  rubricaType: RubricaType;
  calculationType: CalculationType;
  formulaType: SystemFormulaType | null;
  baseFormula: string | null;
  rate: string | null;
  fixedValue: string | null;
  incideINSS: boolean;
  incideFGTS: boolean;
  incideIRRF: boolean;
  isSystem: boolean;
  isActive: boolean;
  eSocialCode: string | null;
  createdAt: string;
  updatedAt: string;
}

export interface CreateRubricaInput {
  code: string;
  name: string;
  rubricaType: RubricaType;
  calculationType: CalculationType;
  formulaType?: SystemFormulaType;
  baseFormula?: string;
  rate?: number;
  fixedValue?: number;
  incideINSS?: boolean;
  incideFGTS?: boolean;
  incideIRRF?: boolean;
  eSocialCode?: string;
}

export interface PayrollLegalTable {
  id: string;
  organizationId: string | null;
  tableType: LegalTableType;
  stateCode: string | null;
  effectiveFrom: string;
  notes: string | null;
  createdBy: string;
  createdAt: string;
  brackets: PayrollTableBracket[];
  scalarValues: PayrollTableScalar[];
}

export interface PayrollTableBracket {
  id: string;
  fromValue: string;
  upTo: string | null;
  rate: string;
  deduction: string | null;
  order: number;
}

export interface PayrollTableScalar {
  id: string;
  key: string;
  value: string;
}

export interface CreateLegalTableInput {
  tableType: LegalTableType;
  stateCode?: string;
  effectiveFrom: string;
  notes?: string;
  brackets?: {
    fromValue: number;
    upTo?: number | null;
    rate: number;
    deduction?: number;
    order: number;
  }[];
  scalarValues?: { key: string; value: number }[];
}
```

**Create `apps/frontend/src/hooks/usePayrollRubricas.ts`:**

Follow useState+useCallback pattern from useEmployees:

- `rubricas: PayrollRubrica[]`, `loading: boolean`, `error: string | null`
- `fetchRubricas(orgId)` — GET /api/org/:orgId/payroll-rubricas?isActive=true
- `createRubrica(orgId, data)` — POST, then refetch, toast "Rubrica criada com sucesso"
- `updateRubrica(orgId, id, data)` — PUT, then refetch, toast "Rubrica atualizada com sucesso"
- `deactivateRubrica(orgId, id)` — PATCH /deactivate, then refetch, toast "Rubrica desativada com sucesso"
- Error handling: toast via sonner with human-friendly message "Nao foi possivel salvar. Verifique sua conexao e tente novamente."

**Create `apps/frontend/src/hooks/usePayrollTables.ts`:**

- `tables: PayrollLegalTable[]`, `loading: boolean`
- `fetchTables(orgId, tableType?)` — GET /api/org/:orgId/payroll-tables
- `createTable(orgId, data)` — POST, then refetch, toast "Tabela cadastrada com sucesso"

**Create `apps/frontend/src/pages/PayrollParametersPage.tsx`:**

Follow EmployeesPage pattern for structure:

1. **Breadcrumb:** RH > Parametros de Folha
2. **Page title:** "Parametros de Folha" (DM Sans 24px bold)
3. **Tabs:** "Rubricas" | "Tabelas Legais" — tab underline uses `--color-primary-600`
4. **Loading state:** 3 skeleton rows per tab

**Rubricas Tab:**

- Header row with "Nova Rubrica" primary CTA button (only primary button on page, `--color-primary-600` background)
- Table with columns: Rubrica (name), Tipo (badge), Calculo (badge), Taxa/Formula, Acoes
- **Type badges per UI-SPEC:**
  - PROVENTO: `--color-success-100` bg, `--color-success-500` text, TrendingUp icon 16px + "Provento"
  - DESCONTO: `--color-error-100` bg, `--color-error-500` text, TrendingDown icon 16px + "Desconto"
- **Formula type badges per UI-SPEC:**
  - SYSTEM: `--color-warning-100` bg, `--color-warning-500` text, Lock icon 16px + "Sistema"
  - PERCENTAGE: `--color-neutral-100` bg, `--color-neutral-700` text, "Percentual"
  - FORMULA: `--color-info-100` bg, `--color-info-500` text, Code icon 16px + "Formula"
- **Row actions per UI-SPEC:**
  - Custom rubrica: "Editar" button (Pencil icon + "Editar" text) + "Desativar" button (Slash icon + "Desativar" text) — compact secondary buttons with text labels
  - System rubrica: Lock icon in disabled state with tooltip `aria-label="Rubrica protegida por lei, nao editavel"` — no "Desativar" button
  - System rubrica row: subtle `--color-neutral-100` background
- **Empty state per UI-SPEC:** heading "Nenhuma rubrica configurada", body "As rubricas definem como proventos e descontos sao calculados na folha. Comece adicionando uma rubrica.", CTA "Nova Rubrica"
- **Deactivate:** uses `ConfirmModal` variant danger with copy: title "Desativar rubrica?", body "A rubrica '{nome}' deixara de aparecer no processamento de novas folhas. Folhas ja fechadas nao sao afetadas.", confirm "Desativar Rubrica"

**Create `apps/frontend/src/components/payroll/PayrollRubricaModal.tsx`:**

Modal form for create/edit (follows EmployeeModal pattern):

- Fields:
  - `name`: text input, required, max 100 chars, label "Nome da Rubrica"
  - `code`: text input, required, uppercase, label "Codigo" (hidden on edit)
  - `rubricaType`: radio group PROVENTO/DESCONTO, `<fieldset>` + `<legend>` "Tipo", required
  - `calculationType`: radio group PERCENTUAL/FORMULA (SISTEMA not selectable by user), `<fieldset>` + `<legend>` "Tipo de Calculo"
  - `rate`: number input, visible only when calculationType=PERCENTAGE, label "Taxa (%)", range 0.01-100, step 0.01
  - `baseFormula`: text input with `font-family: var(--font-mono)`, visible only when calculationType=FORMULA, label "Formula", placeholder "SALARIO_BASE \* 0.05"
  - Below formula field: read-only variable reference panel with available variables as `<code>` tags: SALARIO_BASE, HORA_NORMAL, HORAS_EXTRAS_50, HORAS_EXTRAS_100, SALARIO_MINIMO, PISO_REGIONAL, DIAS_TRABALHADOS, DIAS_UTEIS_MES
  - Incidence checkboxes: "Incide INSS", "Incide FGTS", "Incide IRRF"
- Submit disabled until required fields filled
- OnBlur validation per UI-SPEC
- Error display with `role="alert"`
- Primary CTA: "Salvar Rubrica" (create) or "Salvar Rubrica" (edit)
- Modal focus management: focus on first field on open, Escape closes, focus returns to trigger

**Create `apps/frontend/src/pages/PayrollParametersPage.css`:**

CSS following tokens.css variables:

- Tab styles with active underline
- Badge styles for rubrica type and formula type
- Table row styles with hover
- System rubrica row background
- Responsive: on mobile, table transforms to stacked cards (per CLAUDE.md rule)

**Update `apps/frontend/src/App.tsx`:**

Add route: `<Route path="/payroll-parameters" element={<PayrollParametersPage />} />`

**Update `apps/frontend/src/components/layout/Sidebar.tsx`:**

Add under the "RH" group (or create "Folha" subgroup):

- Link: "Parametros de Folha" with icon `Settings` from lucide-react, path `/payroll-parameters`
  </action>
  <verify>
  <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria> - types/payroll.ts contains `export interface PayrollRubrica` - types/payroll.ts contains `export type RubricaType` - types/payroll.ts contains `export interface PayrollLegalTable` - hooks/usePayrollRubricas.ts contains `export function usePayrollRubricas` or `export const usePayrollRubricas` - hooks/usePayrollTables.ts contains `export function usePayrollTables` or `export const usePayrollTables` - PayrollParametersPage.tsx contains `PayrollParametersPage` - PayrollParametersPage.tsx contains `Rubricas` and `Tabelas Legais` (tab labels) - PayrollParametersPage.tsx contains `Nova Rubrica` (primary CTA) - PayrollParametersPage.tsx contains `ConfirmModal` (for deactivate) - PayrollParametersPage.tsx contains `aria-label` (accessibility) - PayrollRubricaModal.tsx contains `PayrollRubricaModal` - PayrollRubricaModal.tsx contains `fieldset` and `legend` (radio group accessibility) - PayrollRubricaModal.tsx contains `role="alert"` (error messages) - PayrollRubricaModal.tsx contains `font-mono` or `--font-mono` (formula field) - App.tsx contains `payroll-parameters` - Sidebar.tsx contains `payroll-parameters` or `Parametros de Folha` - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Rubricas tab fully functional with badges, system rubrica protection, create/edit modal, deactivate confirmation, sidebar link, route registered</done>
  </task>

<task type="auto">
  <name>Task 2: Legal Tables tab with table display and update modal</name>
  <files>apps/frontend/src/pages/PayrollParametersPage.tsx, apps/frontend/src/components/payroll/PayrollLegalTableModal.tsx</files>
  <read_first>
    - .planning/phases/26-par-metros-de-folha-e-motor-de-c-lculo/26-UI-SPEC.md (Legal Table section, Bracket Table pattern, Interaction States, Copywriting)
    - apps/frontend/src/pages/PayrollParametersPage.tsx (just created — add Tabelas Legais tab content)
    - apps/frontend/src/hooks/usePayrollTables.ts (hook to use)
    - apps/frontend/src/types/payroll.ts (PayrollLegalTable, PayrollTableBracket types)
    - apps/frontend/src/styles/tokens.css (color/font tokens)
  </read_first>
  <action>
**Update `PayrollParametersPage.tsx` — Tabelas Legais tab content:**

Display legal tables grouped by type. For each type (INSS, IRRF, SALARY_FAMILY, MINIMUM_WAGE, FUNRURAL):

1. **Group header:** Type name as heading (e.g., "INSS", "IRRF") — DM Sans 20px bold
2. **Current effective table:** Show with badge:
   - Effective (effectiveFrom <= today): `--color-success-100` bg, `--color-success-500` text, CheckCircle icon 16px + "Vigente"
   - Future (effectiveFrom > today): `--color-info-100` bg, `--color-info-500` text, Calendar icon 16px + "Agendada"
3. **Bracket table (for INSS, IRRF):** `<table>` with `<th scope="col">` headers:
   - "De (R$)", "Ate (R$)", "Aliquota (%)", "Deduzir (R$)" (IRRF only)
   - Monetary cells: `font-family: var(--font-mono)`, right-aligned
   - Last bracket "Ate" column shows "Sem limite" in regular font
   - Format monetary values with Brazilian locale: `1.621,00`
4. **Scalar values (for SALARY_FAMILY, MINIMUM_WAGE):** Key-value display with monetary formatting
5. **History toggle:** "Ver historico" / "Ocultar historico" collapsible section showing previous table versions
   - Historical tables get badge: `--color-neutral-100` bg, `--color-neutral-500` text, "Historico"
   - Collapse animation: 200ms ease-out
6. **"Atualizar Tabela" button** per table type — opens PayrollLegalTableModal

**Empty state per UI-SPEC:** heading "Tabelas legais nao configuradas", body "Cadastre as tabelas de INSS, IRRF e salario-familia para que o motor de calculo funcione corretamente.", CTA "Cadastrar Tabela INSS"

**Create `apps/frontend/src/components/payroll/PayrollLegalTableModal.tsx`:**

Modal for adding a new table version:

- Props: `tableType: LegalTableType`, `onSave`, `onClose`, `isOpen`
- Fields:
  - `tableType`: display-only (locked, shown as label)
  - `effectiveFrom`: date picker, limited to first day of month (input type="month" or date with day forced to 01), label "Vigencia a partir de"
  - **For bracket types (INSS, IRRF):**
    - Dynamic bracket rows with fields: "De (R$)", "Ate (R$)", "Aliquota (%)", "Deduzir (R$)" (IRRF only)
    - Each field: `<input type="number" step="0.01">` with `aria-label="De (R$) - Faixa {N}"`
    - Last row "Ate" field disabled (unbounded / "Sem limite")
    - "Adicionar faixa" button to add new bracket row
    - Remove button on each row except last
    - Rate fields: 0-100 range, 2 decimal places
    - Deduction fields: monetary, min 0, 2 decimal places
  - **For scalar types (SALARY_FAMILY, MINIMUM_WAGE, FUNRURAL):**
    - Key-value inputs for each scalar (dynamic list based on table type)
    - For SALARY_FAMILY: "Valor por filho (R$)" + "Limite de renda (R$)"
    - For MINIMUM_WAGE: "Salario Minimo Federal (R$)"
    - For FUNRURAL: "PF Social (%)","PF RAT (%)", "PF SENAR (%)", "PF Total (%)", "PJ FUNRURAL (%)", "PJ SENAR (%)", "PJ Total (%)"
  - `notes`: text input, optional, label "Observacoes"
- Validation:
  - effectiveFrom required
  - At least 1 bracket or scalar required
  - Error on duplicate effectiveFrom: "Ja existe uma tabela com vigencia neste mes. Escolha uma data diferente." with `role="alert"`
- Primary CTA: "Confirmar Tabela"
- Modal focus and keyboard management per UI-SPEC accessibility rules

**Accessibility for bracket table:**

- Tab order: left-to-right within row, then next row, then "Adicionar faixa" at end
- `aria-label` on each input following pattern "De (R$) - Faixa 1"
- Table headers: `<th scope="col">`
  </action>
  <verify>
  <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria> - PayrollParametersPage.tsx contains `Tabelas Legais` (tab label) - PayrollParametersPage.tsx contains `Vigente` (effective badge text) - PayrollParametersPage.tsx contains `Agendada` (future badge text) - PayrollParametersPage.tsx contains `Sem limite` (last bracket display) - PayrollParametersPage.tsx contains `Ver historico` (history toggle) - PayrollParametersPage.tsx contains `Atualizar Tabela` (update button) - PayrollParametersPage.tsx contains `font-mono` or `--font-mono` (monetary cells) - PayrollLegalTableModal.tsx contains `PayrollLegalTableModal` - PayrollLegalTableModal.tsx contains `Confirmar Tabela` (submit CTA) - PayrollLegalTableModal.tsx contains `Adicionar faixa` (add bracket button) - PayrollLegalTableModal.tsx contains `aria-label` (bracket input accessibility) - PayrollLegalTableModal.tsx contains `role="alert"` (error display) - PayrollLegalTableModal.tsx contains `effectiveFrom` (date field) - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Legal tables tab shows all table types with brackets, scalars, effective badges, history toggle, and update modal with dynamic bracket editor</done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of PayrollParametersPage</name>
  <files>apps/frontend/src/pages/PayrollParametersPage.tsx</files>
  <action>User visually verifies the complete PayrollParametersPage including rubricas CRUD and legal tables display</action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>User approved the visual and functional behavior of the payroll parameters page</done>
  <what-built>Complete payroll parameters configuration page with rubricas CRUD and legal tables display/update</what-built>
  <how-to-verify>
    1. Start backend: `cd apps/backend && pnpm dev`
    2. Start frontend: `cd apps/frontend && pnpm dev`
    3. Login as a MANAGER user
    4. Navigate to RH > Parametros de Folha (or click sidebar link)
    5. Verify Rubricas tab:
       - System rubricas are listed with "Sistema" badge and lock icon on edit
       - Click "Nova Rubrica" — modal opens with form fields
       - Create a custom rubrica with tipo PROVENTO, calculo PERCENTUAL, rate 5%
       - Verify the new rubrica appears in the list with correct badges
       - Click "Editar" on the custom rubrica — modal pre-fills values
       - Click "Desativar" — confirmation modal appears with correct copy
       - Verify system rubricas have no edit/deactivate buttons
    6. Verify Tabelas Legais tab:
       - INSS table shows 4 brackets with correct 2026 values
       - IRRF table shows 5 brackets with deduction column
       - Monetary values display in JetBrains Mono, right-aligned
       - "Vigente" badge appears on current tables
       - Click "Ver historico" — collapsible section toggles
       - Click "Atualizar Tabela" — modal opens with bracket editor
       - Verify "Adicionar faixa" adds a new row
    7. Check responsive: resize to mobile width — table should transform to cards
    8. Check accessibility: tab through the page — focus visible, labels present
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Frontend compiles without TypeScript errors: `npx tsc --noEmit`
- Page loads and displays rubricas and legal tables
- CRUD operations work via the modals
- System rubricas are protected from editing
- Legal tables display with correct badges and bracket values
</verification>

<success_criteria>

- PayrollParametersPage accessible at /payroll-parameters
- Sidebar link under RH group
- Rubricas tab: list with badges, create/edit modal, deactivate confirmation
- Tabelas Legais tab: grouped display, bracket table, effective badges, history, update modal
- All accessibility requirements from UI-SPEC met
- Responsive design works on mobile
- Visual verification approved by user
  </success_criteria>

<output>
After completion, create `.planning/phases/26-par-metros-de-folha-e-motor-de-c-lculo/26-04-SUMMARY.md`
</output>
