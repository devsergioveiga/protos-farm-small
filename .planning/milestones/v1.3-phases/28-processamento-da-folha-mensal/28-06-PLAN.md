---
phase: 28-processamento-da-folha-mensal
plan: 06
type: execute
wave: 4
depends_on: ['28-04', '28-05']
files_modified: []
autonomous: false
requirements: [FOLHA-02, FOLHA-03, FOLHA-04, FOLHA-05]

must_haves:
  truths:
    - 'Full payroll processing cycle works end-to-end: wizard -> process -> preview -> close -> revert'
    - 'Salary advance flow works: register individual/batch -> CP created -> deducted in folha'
    - 'Payslip PDF downloads correctly from both runs page and employee ficha'
    - 'All backend tests pass'
---

<objective>
Visual and functional verification of the complete payroll processing flow.

Purpose: Human confirms that the 4-step wizard, payroll run detail, salary advances, payslip PDF, and employee ficha tab all work correctly together.
Output: Verified working payroll processing module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-processamento-da-folha-mensal/28-CONTEXT.md
@.planning/phases/28-processamento-da-folha-mensal/28-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and functional verification of complete payroll module</name>
  <files></files>
  <action>
Human verifies the complete payroll processing module built in Plans 01-05. This is a checkpoint task — no code changes, only verification.

What was built:

- Backend: PayrollRun state machine (PENDING->PROCESSING->CALCULATED->COMPLETED->REVERTED), per-employee calculation with INSS/IRRF/FGTS/DSR/noturno, salary advances with CP integration, payslip PDF, email distribution, ZIP download, 13th salary two-parcel calculation
- Frontend: PayrollRunsPage with Rodadas/Adiantamentos tabs, 4-step wizard, PayrollRunDetailModal with close/revert, PayrollRunStatusBadge, SalaryAdvanceModal (individual+batch), PayslipTab on employee ficha

How to verify:

1. Start backend: `cd apps/backend && pnpm dev`
2. Start frontend: `cd apps/frontend && pnpm dev`
3. Navigate to Folha de Pagamento in sidebar

Wizard flow (FOLHA-02): 4. Click "Iniciar Folha" -> wizard opens 5. Step 1: Select month/year, select "Mensal" type, click "Proximo" 6. Step 2: Verify employee list shows with timesheet status chips 7. Step 3: Verify eligible employees pre-checked, ineligible greyed out 8. Step 4: Verify processing animation, auto-advances to completion

Run detail + close/revert (FOLHA-02): 9. Click Eye icon on the new run -> detail modal opens 10. Verify items table shows employees with calculated values 11. Click "Fechar Folha" -> confirm -> verify status changes to COMPLETED 12. Click "Estornar Folha" -> verify ConfirmDeleteModal with typing

Salary advances (FOLHA-03): 13. Switch to "Adiantamentos" tab, register individual and batch

Payslip PDF (FOLHA-04): 14. Download ZIP, open PDF, verify tabular layout

Employee ficha (FOLHA-04): 15. Navigate to employee detail -> "Holerites" tab -> download

13th salary (FOLHA-05): 16. Process 1a Parcela (no deductions), then 2a Parcela (with INSS/IRRF)

Resume signal: Type "approved" if everything works, or describe any issues found
</action>
<verify>
<automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/backend && pnpm test --no-coverage</automated>
</verify>
<done>Human confirms all flows work end-to-end. No blocking UI issues. PDF layout acceptable.</done>
</task>

</tasks>

<verification>
- All backend tests pass: `cd apps/backend && pnpm test --no-coverage`
- Full payroll lifecycle functional
- PDF layout matches D-04 classic tabular format
- Wizard follows D-01 4-step flow
- Estorno follows D-03 with ConfirmDeleteModal
</verification>

<success_criteria>
Human confirms all flows work end-to-end. No blocking UI issues. PDF layout is acceptable.
</success_criteria>

<output>
After completion, create `.planning/phases/28-processamento-da-folha-mensal/28-06-SUMMARY.md`
</output>
