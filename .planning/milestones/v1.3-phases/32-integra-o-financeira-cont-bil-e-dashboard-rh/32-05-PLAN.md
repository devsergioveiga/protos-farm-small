---
phase: 32-integra-o-financeira-cont-bil-e-dashboard-rh
plan: 05
type: execute
wave: 3
depends_on: ['32-03']
files_modified:
  - apps/frontend/src/types/hr-dashboard.ts
  - apps/frontend/src/hooks/useHrDashboard.ts
  - apps/frontend/src/components/hr-dashboard/PayrollCostTrendChart.tsx
  - apps/frontend/src/components/hr-dashboard/PayrollCompositionChart.tsx
  - apps/frontend/src/pages/HrDashboardPage.tsx
  - apps/frontend/src/pages/HrDashboardPage.css
  - apps/frontend/src/App.tsx
  - apps/frontend/src/components/layout/Sidebar.tsx
autonomous: true
requirements: [INTEGR-03]
must_haves:
  truths:
    - 'HrDashboardPage shows 4 KPI cards with trend arrows'
    - '12-month BarChart renders with 3 stacked series (Bruto/Liquido/Encargos)'
    - 'PieChart shows folha composition breakdown with percentages'
    - 'Cost by activity table sorted by cost descending'
    - 'Upcoming contract expirations shown in 30/60/90 day cards'
    - 'Alerts section shows overdue payables, pending timesheets, expired contracts'
    - 'farmId and year/month filters apply to all sections'
    - 'Charts lazy-loaded with React.lazy + Suspense'
  artifacts:
    - path: 'apps/frontend/src/pages/HrDashboardPage.tsx'
      provides: 'Full HR KPI dashboard page'
    - path: 'apps/frontend/src/components/hr-dashboard/PayrollCostTrendChart.tsx'
      provides: '12-month stacked BarChart'
    - path: 'apps/frontend/src/components/hr-dashboard/PayrollCompositionChart.tsx'
      provides: 'PieChart composition breakdown'
    - path: 'apps/frontend/src/hooks/useHrDashboard.ts'
      provides: 'Hook to fetch HR dashboard data'
  key_links:
    - from: 'HrDashboardPage'
      to: 'GET /org/hr-dashboard'
      via: 'useHrDashboard hook'
      pattern: "api\\.get.*hr-dashboard"
    - from: 'HrDashboardPage'
      to: 'PayrollCostTrendChart'
      via: 'React.lazy + Suspense'
      pattern: "React\\.lazy.*PayrollCostTrendChart"
    - from: 'HrDashboardPage'
      to: 'PayrollCompositionChart'
      via: 'React.lazy + Suspense'
      pattern: "React\\.lazy.*PayrollCompositionChart"
---

<objective>
Build the HrDashboardPage (INTEGR-03 frontend). Full KPI dashboard with 4 metric cards, 12-month BarChart, PieChart composition, cost-by-activity table, contract expiration cards, alerts section, and farm/date filters.

Purpose: Gives the manager a consolidated view of HR cost KPIs -- headcount, payroll cost trends, composition, turnover, and upcoming contract events.
Output: HrDashboardPage with all visualizations, lazy-loaded charts, route registration, sidebar entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-integra-o-financeira-cont-bil-e-dashboard-rh/32-RESEARCH.md
@.planning/phases/32-integra-o-financeira-cont-bil-e-dashboard-rh/32-UI-SPEC.md
@.planning/phases/32-integra-o-financeira-cont-bil-e-dashboard-rh/32-03-SUMMARY.md
@apps/frontend/src/pages/FinancialDashboardPage.tsx
@apps/frontend/src/hooks/useFinancialDashboard.ts
@apps/frontend/src/components/shared/SafetyKpiCard.tsx
@apps/frontend/src/App.tsx
@apps/frontend/src/components/layout/Sidebar.tsx

<interfaces>
<!-- Backend contract from Plan 03 -->

```typescript
// GET /org/hr-dashboard?farmId=&year=&month=
interface HrDashboardResponse {
  headcount: {
    total: number;
    byStatus: Record<string, number>;
    byContractType: Record<string, number>;
  };
  currentMonthCost: {
    gross: number;
    net: number;
    charges: number;
    avgPerEmployee: number;
    costPerHectare: number | null;
  };
  trend12Months: Array<{ yearMonth: string; gross: number; net: number; charges: number }>;
  composition: Array<{ label: string; amount: number; percentage: number }>;
  costByActivity: Array<{ activityType: string; totalCost: number }>;
  turnover: { last12MonthsRate: number; terminationsLast12: number; admissionsLast12: number };
  upcomingContractExpirations: Array<{
    days: number;
    count: number;
    employees: Array<{ id: string; name: string; endDate: string; contractType: string }>;
  }>;
  alerts: { overduePayablesPayroll: number; pendingTimesheets: number; expiredContracts: number };
}
```

Existing frontend patterns:

- FinancialDashboardPage layout: breadcrumb + filter bar + KPI cards + charts + tables
- SafetyKpiCard: reusable KPI card component at @/components/shared/
- useFinancialDashboard: useState + useCallback hook pattern to replicate
- TopCategoriesChart.tsx: Recharts PieChart pattern with COLORS array and ResponsiveContainer
- Recharts already installed v3.7.0
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Types + hook + chart components</name>
  <files>
    apps/frontend/src/types/hr-dashboard.ts,
    apps/frontend/src/hooks/useHrDashboard.ts,
    apps/frontend/src/components/hr-dashboard/PayrollCostTrendChart.tsx,
    apps/frontend/src/components/hr-dashboard/PayrollCompositionChart.tsx
  </files>
  <action>
    1. Create types/hr-dashboard.ts:
       - HrDashboardQuery interface: { farmId?: string; year: number; month: number }
       - HrDashboardResponse interface (mirrors backend exactly)
       - Helper type aliases for sub-sections

    2. Create hooks/useHrDashboard.ts following useFinancialDashboard pattern:
       - `useHrDashboard(params: HrDashboardQuery)` with useState + useCallback
       - Calls GET `/org/${orgId}/hr-dashboard?farmId=&year=&month=` via api.get
       - Returns { data: HrDashboardResponse | null, isLoading, error, refetch }
       - useEffect triggers refetch on params change
       - Uses useAuth internally for orgId (per Phase 28 pattern)

    3. Create PayrollCostTrendChart.tsx:
       - Props: `{ data: Array<{ yearMonth: string; gross: number; net: number; charges: number }> }`
       - Recharts stacked BarChart inside ResponsiveContainer (height 320)
       - 3 stacked Bar series: "Bruto" (primary-600), "Liquido" (primary-400), "Encargos" (sky-500)
       - X-axis: month labels abbreviated pt-BR ("Jan", "Fev", "Mar", etc.)
       - Y-axis: formatted as "R$ Xk" for thousands
       - Tooltip: full BRL currency format
       - Legend at bottom
       - Use CSS custom properties for colors (per UI-SPEC chart palette)
       - Export as default for React.lazy compatibility

    4. Create PayrollCompositionChart.tsx:
       - Props: `{ data: Array<{ label: string; amount: number; percentage: number }> }`
       - Recharts PieChart inside ResponsiveContainer (height 280)
       - Color palette per UI-SPEC: primary-500, sun-500, sky-500, earth-500, neutral-400
       - Cell fill from COLORS array
       - Tooltip: BRL currency format
       - Legend with percentage labels
       - Export as default for React.lazy compatibility

    ANTI-PATTERNS:
    - Do NOT import Recharts at top level -- these components will be React.lazy loaded
    - Do NOT use custom SVG -- use Recharts library (per RESEARCH.md Don't Hand-Roll)

  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - HrDashboardResponse type mirrors backend
    - useHrDashboard hook fetches and manages state
    - PayrollCostTrendChart renders stacked BarChart with 3 series
    - PayrollCompositionChart renders PieChart with color palette
    - Both charts default-exported for React.lazy
  </done>
</task>

<task type="auto">
  <name>Task 2: HrDashboardPage + route + sidebar</name>
  <files>
    apps/frontend/src/pages/HrDashboardPage.tsx,
    apps/frontend/src/pages/HrDashboardPage.css,
    apps/frontend/src/App.tsx,
    apps/frontend/src/components/layout/Sidebar.tsx
  </files>
  <action>
    1. Create HrDashboardPage.tsx per UI-SPEC Screen 3. Layout mirrors FinancialDashboardPage:

       **Filter bar:** fazenda select + year/month pickers. Auto-applies on change (no submit button). Default: current month, no farm filter.

       **KPI card row** (4 cards, responsive grid: 2x2 mobile, 4x1 desktop using CSS grid):
       - "Total de Colaboradores" -- headcount.total, trend arrow comparing to previous month
       - "Custo Bruto da Folha" -- currentMonthCost.gross formatted BRL, trend arrow
       - "Custo por Hectare" -- currentMonthCost.costPerHectare formatted BRL (or "--" if null)
       - "Turnover 12 meses" -- turnover.last12MonthsRate + "%" (green=low, red=high)
       - Use SafetyKpiCard component (reuse structure per UI-SPEC) or create similar card with big number (JetBrains Mono) + label (Source Sans 3) + trend arrow

       **Charts section** (2-column grid, stacks on mobile):
       - Left: `React.lazy(() => import('../components/hr-dashboard/PayrollCostTrendChart'))` wrapped in `<Suspense fallback={<SkeletonChart />}>`
       - Right: `React.lazy(() => import('../components/hr-dashboard/PayrollCompositionChart'))` wrapped in `<Suspense fallback={<SkeletonChart />}>`

       **Cost by activity table:**
       - Columns: ATIVIDADE | CUSTO TOTAL | % DO TOTAL (ALL CAPS headers)
       - Sorted by cost descending
       - Max 8 rows + "Ver mais" expand button
       - Monetary values: JetBrains Mono, right-aligned

       **Upcoming expirations** (3-column card group):
       - 30 dias card: warning-500 accent, count badge + employee list
       - 60 dias card: sun-500 accent, count badge + employee list
       - 90 dias card: neutral-400 accent, count badge + employee list
       - Each employee: name + endDate + contractType pill

       **Alerts section** (3 compact rows):
       - Overdue payables: error color, count badge, "Ver" link to /payables?filter=overdue
       - Pending timesheets: warning color, count badge, "Ver" link to /timesheets
       - Expired contracts: warning color, count badge

       **Loading state:** 4 skeleton KPI cards (rectangles with pulse) + skeleton chart placeholders. Never spinner.

       **Empty state:** "Sem dados de folha para o periodo" + body + CTA "Processar Folha" linking to /payroll-runs

    2. Create HrDashboardPage.css:
       - KPI card grid: 20px gap, responsive 2x2 to 4x1
       - Chart grid: 2 columns above 1024px, 1 column below
       - Activity table styles
       - Expiration card colors (30d warning, 60d sun, 90d neutral)
       - Alert row compact styles
       - Skeleton pulse animation (reuse existing pattern)
       - @media (prefers-reduced-motion: reduce) -- disable animations

    3. Register route in App.tsx: `<Route path="/hr-dashboard" element={<HrDashboardPage />} />`

    4. Add sidebar entry in Sidebar.tsx under "RH" group:
       - `{ to: '/hr-dashboard', icon: BarChart3, label: 'Dashboard RH' }`
       - Use BarChart3 icon from lucide-react
       - Position after existing RH items

    DESIGN SYSTEM:
    - Spacing: 4px multiples only (20px gap for KPI cards per UI-SPEC exception)
    - Touch targets 48x48px minimum on all interactive elements
    - DM Sans 600 for headings, Source Sans 3 for body, JetBrains Mono for data
    - Colors per UI-SPEC color contract
    - Mobile-first: base styles for mobile, enhance for desktop
    - Focus visible: outline 2px primary-500
    - Semantic HTML: <main>, <section>, <table>, <button>
    - aria-label on icon-only buttons
    - Never center text blocks >2 lines
    - Minimum font 12px

  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - HrDashboardPage renders with 4 KPI cards, charts, tables, expirations, alerts
    - Filters apply to all sections
    - Charts lazy-loaded with skeleton fallback
    - Mobile responsive layout
    - Empty state with CTA
    - Route at /hr-dashboard
    - Sidebar entry visible under RH
    - Accessible and WCAG AA compliant
    - pt-BR copy throughout
  </done>
</task>

</tasks>

<verification>
- `cd apps/frontend && npx tsc --noEmit` -- no type errors
- `cd apps/frontend && npx vitest run` -- all tests pass
- Manual: navigate to /hr-dashboard, verify KPI cards render
- Manual: verify charts load lazily with skeleton fallback
- Manual: test farm filter changes data
- Manual: verify mobile layout at 375px width
</verification>

<success_criteria>

- Full HR dashboard with all INTEGR-03 KPIs visible
- 4 KPI cards with trend arrows
- 12-month BarChart and composition PieChart lazy-loaded
- Cost by activity table sorted correctly
- Contract expirations in 30/60/90 day buckets
- Alerts with counts and links
- Farm/date filters work
- Responsive mobile layout
- Accessible markup
  </success_criteria>

<output>
After completion, create `.planning/phases/32-integra-o-financeira-cont-bil-e-dashboard-rh/32-05-SUMMARY.md`
</output>
