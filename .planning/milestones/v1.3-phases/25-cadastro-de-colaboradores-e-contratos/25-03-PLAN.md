---
phase: 25-cadastro-de-colaboradores-e-contratos
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
  - 25-02
files_modified:
  - apps/frontend/src/types/employee.ts
  - apps/frontend/src/types/employee-contract.ts
  - apps/frontend/src/types/position.ts
  - apps/frontend/src/types/work-schedule.ts
  - apps/frontend/src/hooks/useEmployees.ts
  - apps/frontend/src/hooks/usePositions.ts
  - apps/frontend/src/hooks/useWorkSchedules.ts
  - apps/frontend/src/hooks/useEmployeeContracts.ts
  - apps/frontend/src/hooks/useEmployeeMovements.ts
  - apps/frontend/src/pages/EmployeesPage.tsx
  - apps/frontend/src/pages/EmployeesPage.css
  - apps/frontend/src/pages/PositionsPage.tsx
  - apps/frontend/src/pages/PositionsPage.css
  - apps/frontend/src/pages/WorkSchedulesPage.tsx
  - apps/frontend/src/pages/WorkSchedulesPage.css
  - apps/frontend/src/components/employees/CreateEmployeeModal.tsx
  - apps/frontend/src/components/employees/EmployeeStatusBadge.tsx
  - apps/frontend/src/components/employees/EmployeeStatusModal.tsx
  - apps/frontend/src/components/employees/EmployeeFarmAssocModal.tsx
  - apps/frontend/src/components/employee-contracts/CreateContractModal.tsx
  - apps/frontend/src/components/employee-contracts/ContractAmendmentModal.tsx
  - apps/frontend/src/components/positions/CreatePositionModal.tsx
  - apps/frontend/src/components/positions/SalaryBandModal.tsx
  - apps/frontend/src/components/work-schedules/CreateWorkScheduleModal.tsx
  - apps/frontend/src/components/layout/Sidebar.tsx
  - apps/frontend/src/App.tsx
autonomous: true
requirements:
  - COLAB-01
  - COLAB-02
  - COLAB-03

must_haves:
  truths:
    - "EmployeesPage lists employees with status badges, filters, search, and pagination"
    - "CreateEmployeeModal is multi-step (4 steps) with CPF validation onBlur"
    - "PositionsPage shows positions with salary bands and staffing view"
    - "WorkSchedulesPage lists schedules with template indicator"
    - "All forms are in modals, never dedicated pages"
    - "Sidebar has RH group with Colaboradores, Cargos, Escalas"
    - "Status badges use correct colors and icons per UI-SPEC"
  artifacts:
    - path: "apps/frontend/src/pages/EmployeesPage.tsx"
      provides: "Employee listing with filters, search, status badges"
      min_lines: 100
    - path: "apps/frontend/src/pages/PositionsPage.tsx"
      provides: "Position listing with salary bands and staffing view"
      min_lines: 80
    - path: "apps/frontend/src/pages/WorkSchedulesPage.tsx"
      provides: "Work schedule listing with templates"
      min_lines: 60
    - path: "apps/frontend/src/components/employees/CreateEmployeeModal.tsx"
      provides: "Multi-step employee creation form"
      min_lines: 150
    - path: "apps/frontend/src/components/layout/Sidebar.tsx"
      provides: "RH navigation group"
      contains: "Colaboradores"
  key_links:
    - from: "EmployeesPage.tsx"
      to: "useEmployees hook"
      via: "import and call"
      pattern: "useEmployees"
    - from: "Sidebar.tsx"
      to: "/employees"
      via: "navigation link"
      pattern: "/employees"
    - from: "App.tsx"
      to: "EmployeesPage"
      via: "route registration"
      pattern: "EmployeesPage"
---

<objective>
Build all frontend pages (EmployeesPage, PositionsPage, WorkSchedulesPage), types, hooks, modals, sidebar navigation, and route registration for the employees, contracts, positions, and work schedules modules.

Purpose: The user-facing interface for COLAB-01, COLAB-02, and COLAB-03 listing and creation flows.
Output: 3 pages, 9 modals, 5 hooks, 4 type files, sidebar RH group, all routes registered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cadastro-de-colaboradores-e-contratos/25-CONTEXT.md
@.planning/phases/25-cadastro-de-colaboradores-e-contratos/25-RESEARCH.md
@.planning/phases/25-cadastro-de-colaboradores-e-contratos/25-UI-SPEC.md
@apps/frontend/src/pages/AnimalsPage.tsx
@apps/frontend/src/components/layout/Sidebar.tsx
@apps/frontend/src/App.tsx
@apps/frontend/src/styles/tokens.css
@apps/frontend/src/types/animal.ts
@apps/frontend/src/hooks/useAnimals.ts

<interfaces>
<!-- Backend API endpoints created in Plans 01 and 02 -->

Employees API:
GET    /api/org/:orgId/employees                    → { data: Employee[], total, page, limit }
POST   /api/org/:orgId/employees                    → { employee: Employee, warnings: string[] }
GET    /api/org/:orgId/employees/:id                → Employee (full detail)
PUT    /api/org/:orgId/employees/:id                → Employee
PATCH  /api/org/:orgId/employees/:id/status         → { status: EmployeeStatus }
POST   /api/org/:orgId/employees/:id/dependents     → EmployeeDependent
DELETE /api/org/:orgId/employees/:id/dependents/:depId → 204
POST   /api/org/:orgId/employees/:id/farms          → EmployeeFarm
PATCH  /api/org/:orgId/employees/:id/farms/:farmAssocId → EmployeeFarm
POST   /api/org/:orgId/employees/:id/documents      → EmployeeDocument (multipart)
DELETE /api/org/:orgId/employees/:id/documents/:docId → 204
GET    /api/org/:orgId/employees/:id/salary-history  → SalaryHistoryPoint[]

Contracts API:
GET    /api/org/:orgId/employee-contracts              → { data: EmployeeContract[], total }
POST   /api/org/:orgId/employee-contracts              → EmployeeContract
GET    /api/org/:orgId/employee-contracts/:id           → EmployeeContract (with amendments)
POST   /api/org/:orgId/employee-contracts/:id/amendments → ContractAmendment
GET    /api/org/:orgId/employee-contracts/:id/pdf       → PDF blob

Positions API:
GET    /api/org/:orgId/positions                     → { data: Position[], total }
POST   /api/org/:orgId/positions                     → Position
GET    /api/org/:orgId/positions/:id                 → Position (with salary bands)
PUT    /api/org/:orgId/positions/:id                 → Position
PUT    /api/org/:orgId/positions/:id/salary-bands    → SalaryBand[]
GET    /api/org/:orgId/positions/staffing-view       → StaffingViewItem[]

Work Schedules API:
GET    /api/org/:orgId/work-schedules                → { data: WorkSchedule[], total }
POST   /api/org/:orgId/work-schedules                → WorkSchedule
PUT    /api/org/:orgId/work-schedules/:id            → WorkSchedule
DELETE /api/org/:orgId/work-schedules/:id            → 204
POST   /api/org/:orgId/work-schedules/seed-templates → WorkSchedule[]

Movements API:
GET    /api/org/:orgId/employee-movements                → { data: EmployeeMovement[], total }
POST   /api/org/:orgId/employee-movements                → EmployeeMovement
GET    /api/org/:orgId/employee-movements/timeline/:employeeId → TimelineEntry[]
POST   /api/org/:orgId/employees/bulk-salary-adjustment  → { updated: number, errors: string[] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend types, hooks, sidebar RH group, route registration</name>
  <files>
    apps/frontend/src/types/employee.ts,
    apps/frontend/src/types/employee-contract.ts,
    apps/frontend/src/types/position.ts,
    apps/frontend/src/types/work-schedule.ts,
    apps/frontend/src/hooks/useEmployees.ts,
    apps/frontend/src/hooks/usePositions.ts,
    apps/frontend/src/hooks/useWorkSchedules.ts,
    apps/frontend/src/hooks/useEmployeeContracts.ts,
    apps/frontend/src/hooks/useEmployeeMovements.ts,
    apps/frontend/src/components/layout/Sidebar.tsx,
    apps/frontend/src/App.tsx
  </files>
  <read_first>
    apps/frontend/src/types/animal.ts,
    apps/frontend/src/hooks/useAnimals.ts,
    apps/frontend/src/components/layout/Sidebar.tsx,
    apps/frontend/src/App.tsx,
    apps/frontend/src/styles/tokens.css,
    .planning/phases/25-cadastro-de-colaboradores-e-contratos/25-UI-SPEC.md,
    .planning/phases/25-cadastro-de-colaboradores-e-contratos/25-RESEARCH.md
  </read_first>
  <action>
**1. Create `types/employee.ts`:**

```typescript
export type EmployeeStatus = 'ATIVO' | 'AFASTADO' | 'FERIAS' | 'DESLIGADO';
export type BankAccountType = 'CORRENTE' | 'POUPANCA';
export type DocumentType = 'RG' | 'CPF' | 'CTPS' | 'ASO' | 'CONTRATO' | 'OUTRO';

export interface Employee {
  id: string;
  organizationId: string;
  userId?: string;
  name: string;
  cpf: string;
  rg?: string;
  rgIssuer?: string;
  rgUf?: string;
  pisPassep?: string;
  ctpsNumber?: string;
  ctpsSeries?: string;
  ctpsUf?: string;
  birthDate: string;
  motherName?: string;
  fatherName?: string;
  educationLevel?: string;
  maritalStatus?: string;
  nationality: string;
  bloodType?: string;
  hasDisability: boolean;
  disabilityType?: string;
  phone?: string;
  email?: string;
  zipCode?: string;
  street?: string;
  number?: string;
  complement?: string;
  neighborhood?: string;
  city?: string;
  state?: string;
  bankCode?: string;
  bankAgency?: string;
  bankAccount?: string;
  bankAccountType?: BankAccountType;
  bankAccountDigit?: string;
  initialVacationBalance?: number;
  initialHourBankBalance?: number;
  status: EmployeeStatus;
  photoUrl?: string;
  notes?: string;
  admissionDate: string;
  terminationDate?: string;
  createdAt: string;
  updatedAt: string;
  // Relations (populated in detail)
  farms?: EmployeeFarm[];
  dependents?: EmployeeDependent[];
  documents?: EmployeeDocument[];
  contracts?: EmployeeContract[];
}

export interface EmployeeDependent {
  id: string;
  name: string;
  cpf?: string;
  birthDate: string;
  relationship: string;
  irrf: boolean;
  salaryFamily: boolean;
}

export interface EmployeeFarm {
  id: string;
  farmId: string;
  positionId?: string;
  startDate: string;
  endDate?: string;
  status: string;
  farm?: { name: string };
  position?: { name: string };
}

export interface EmployeeDocument {
  id: string;
  documentType: DocumentType;
  fileName: string;
  filePath: string;
  uploadedBy: string;
  uploadedAt: string;
}

export interface EmployeeStatusHistory {
  id: string;
  fromStatus: EmployeeStatus;
  toStatus: EmployeeStatus;
  reason: string;
  changedBy: string;
  effectiveAt: string;
}

export interface SalaryHistoryPoint {
  effectiveAt: string;
  salary: number;
  movementType: string;
  reason?: string;
}

// Input types for forms
export interface CreateEmployeeInput {
  name: string;
  cpf: string;
  rg?: string;
  rgIssuer?: string;
  rgUf?: string;
  pisPassep?: string;
  ctpsNumber?: string;
  ctpsSeries?: string;
  ctpsUf?: string;
  birthDate: string;
  motherName?: string;
  fatherName?: string;
  educationLevel?: string;
  maritalStatus?: string;
  nationality?: string;
  bloodType?: string;
  hasDisability?: boolean;
  disabilityType?: string;
  phone?: string;
  email?: string;
  zipCode?: string;
  street?: string;
  number?: string;
  complement?: string;
  neighborhood?: string;
  city?: string;
  state?: string;
  bankCode?: string;
  bankAgency?: string;
  bankAccount?: string;
  bankAccountType?: BankAccountType;
  bankAccountDigit?: string;
  admissionDate: string;
  notes?: string;
  dependents?: Array<{
    name: string;
    cpf?: string;
    birthDate: string;
    relationship: string;
    irrf?: boolean;
    salaryFamily?: boolean;
  }>;
  farmId?: string;
  positionId?: string;
}
```

Also import/re-export EmployeeContract from employee-contract.ts.

**2. Create `types/employee-contract.ts`:**

```typescript
export type ContractType = 'CLT_INDETERMINATE' | 'CLT_DETERMINATE' | 'SEASONAL' | 'INTERMITTENT' | 'TRIAL' | 'APPRENTICE';

export interface EmployeeContract {
  id: string;
  employeeId: string;
  positionId: string;
  workScheduleId?: string;
  contractType: ContractType;
  startDate: string;
  endDate?: string;
  salary: number;
  weeklyHours: number;
  union?: string;
  costCenterId?: string;
  notes?: string;
  isActive: boolean;
  position?: { name: string };
  workSchedule?: { name: string };
  amendments?: ContractAmendment[];
}

export interface ContractAmendment {
  id: string;
  description: string;
  effectiveAt: string;
  changes: Record<string, { from: unknown; to: unknown }>;
  createdBy: string;
  createdAt: string;
}

export const CONTRACT_TYPE_LABELS: Record<ContractType, string> = {
  CLT_INDETERMINATE: 'CLT — Prazo indeterminado',
  CLT_DETERMINATE: 'CLT — Prazo determinado',
  SEASONAL: 'Safra',
  INTERMITTENT: 'Intermitente',
  TRIAL: 'Experiência',
  APPRENTICE: 'Aprendiz',
};
```

**3. Create `types/position.ts`:**

```typescript
export type SalaryBandLevel = 'JUNIOR' | 'PLENO' | 'SENIOR';

export interface Position {
  id: string;
  organizationId: string;
  name: string;
  cbo?: string;
  description?: string;
  additionalTypes: string[];
  isActive: boolean;
  salaryBands?: SalaryBand[];
  _count?: { employeeFarms: number };
}

export interface SalaryBand {
  id: string;
  positionId: string;
  level: SalaryBandLevel;
  minSalary: number;
  maxSalary: number;
}

export interface StaffingViewItem {
  positionId: string;
  positionName: string;
  cbo: string | null;
  totalEmployees: number;
  byFarm: Array<{ farmId: string; farmName: string; count: number }>;
}

export const SALARY_BAND_LABELS: Record<SalaryBandLevel, string> = {
  JUNIOR: 'Junior',
  PLENO: 'Pleno',
  SENIOR: 'Senior',
};
```

**4. Create `types/work-schedule.ts`:**

```typescript
export type WorkScheduleType = 'FIXED' | 'SHIFT' | 'CUSTOM';

export interface WorkSchedule {
  id: string;
  organizationId: string;
  name: string;
  type: WorkScheduleType;
  workDays: number[];
  startTime: string;
  endTime: string;
  breakMinutes: number;
  isTemplate: boolean;
  notes?: string;
  _count?: { contracts: number };
}

export const WORK_SCHEDULE_TYPE_LABELS: Record<WorkScheduleType, string> = {
  FIXED: 'Fixo',
  SHIFT: 'Turno',
  CUSTOM: 'Personalizado',
};

export const DAY_LABELS = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sab'];
```

**5. Create hooks** following useAnimals.ts pattern (SWR or React Query — match existing pattern):
- `useEmployees(params)` — GET /api/org/:orgId/employees with query params, returns { employees, total, isLoading, mutate }
- `useEmployee(id)` — GET /api/org/:orgId/employees/:id, returns { employee, isLoading, mutate }
- `usePositions(params)` — GET /api/org/:orgId/positions
- `useWorkSchedules(params)` — GET /api/org/:orgId/work-schedules
- `useEmployeeContracts(params)` — GET /api/org/:orgId/employee-contracts
- `useEmployeeMovements(params)` — GET /api/org/:orgId/employee-movements

Each hook should follow the exact data-fetching pattern used by useAnimals.ts (likely using `useSWR` or custom fetch wrapper).

**6. Add RH group to `Sidebar.tsx`:**

Following the exact pattern of existing groups (ESTOQUE, OPERACOES, etc.):

```tsx
// New group — add after existing groups
{
  label: 'RH',
  items: [
    { label: 'Colaboradores', path: '/employees', icon: UserRound },
    { label: 'Cargos', path: '/positions', icon: Briefcase },
    { label: 'Escalas', path: '/work-schedules', icon: Calendar },
  ],
}
```

Import icons from lucide-react: `UserRound`, `Briefcase`, `Calendar`.
Group label "RH" in ALL CAPS per UI-SPEC sidebar section.

**7. Register routes in `App.tsx`:**

Add 4 routes following existing pattern:
```tsx
<Route path="/employees" element={<EmployeesPage />} />
<Route path="/employees/:employeeId" element={<EmployeeDetailPage />} />
<Route path="/positions" element={<PositionsPage />} />
<Route path="/work-schedules" element={<WorkSchedulesPage />} />
```

Import pages lazily if existing pattern uses React.lazy. EmployeeDetailPage will be created in Plan 04 — register the route now, the page file comes later.
  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - types/employee.ts contains `export type EmployeeStatus`
    - types/employee.ts contains `export interface Employee {`
    - types/employee-contract.ts contains `export const CONTRACT_TYPE_LABELS`
    - types/position.ts contains `export interface StaffingViewItem`
    - types/work-schedule.ts contains `export const DAY_LABELS`
    - hooks/useEmployees.ts contains `useEmployees`
    - hooks/usePositions.ts contains `usePositions`
    - hooks/useWorkSchedules.ts contains `useWorkSchedules`
    - Sidebar.tsx contains `Colaboradores` and `/employees`
    - Sidebar.tsx contains `Cargos` and `/positions`
    - Sidebar.tsx contains `Escalas` and `/work-schedules`
    - App.tsx contains `/employees` route
    - App.tsx contains `/positions` route
    - App.tsx contains `/work-schedules` route
  </acceptance_criteria>
  <done>All frontend types mirror backend, hooks fetch data, sidebar has RH group, routes registered.</done>
</task>

<task type="auto">
  <name>Task 2: EmployeesPage + employee modals (Create, Status, FarmAssoc)</name>
  <files>
    apps/frontend/src/pages/EmployeesPage.tsx,
    apps/frontend/src/pages/EmployeesPage.css,
    apps/frontend/src/components/employees/CreateEmployeeModal.tsx,
    apps/frontend/src/components/employees/EmployeeStatusBadge.tsx,
    apps/frontend/src/components/employees/EmployeeStatusModal.tsx,
    apps/frontend/src/components/employees/EmployeeFarmAssocModal.tsx
  </files>
  <read_first>
    apps/frontend/src/pages/AnimalsPage.tsx,
    apps/frontend/src/components/animals/CreateAnimalModal.tsx,
    apps/frontend/src/components/ui/ConfirmModal.tsx,
    apps/frontend/src/styles/tokens.css,
    apps/frontend/src/types/employee.ts,
    apps/frontend/src/hooks/useEmployees.ts,
    .planning/phases/25-cadastro-de-colaboradores-e-contratos/25-UI-SPEC.md
  </read_first>
  <action>
**1. Create `EmployeeStatusBadge.tsx`:**

Per UI-SPEC Interaction States section:
```tsx
// Status → color token, icon, label:
// ATIVO   → --color-success-500 (#2E7D32), CheckCircle 16px, "Ativo"
// AFASTADO → --color-warning-500 (#F57F17), Clock 16px, "Afastado"
// FERIAS  → --color-info-500 (#0277BD), Umbrella 16px, "Ferias"
// DESLIGADO → --color-neutral-500 (#7A7267), XCircle 16px, "Desligado"
```

Badge component with colored background (light tint), colored text, icon + text together (never color as sole indicator). Use `aria-hidden="true"` on icons since text is present.

**2. Create `EmployeesPage.tsx`** following AnimalsPage.tsx pattern:

Per UI-SPEC EmployeesPage interaction contract:
- **Header:** Title "Colaboradores" (DM Sans 20px/700), primary button "Cadastrar colaborador" (accent green, top right, maximum 1 per page)
- **Filters:** Status multiselect chips, contract type dropdown, farm dropdown (if multi-farm), position autocomplete
- **Search:** Full-text by name, CPF (auto-mask when typing), matricula
- **Sort:** Name A-Z default, admission date, position
- **Table:** Columns — Nome (with avatar placeholder), CPF (JetBrains Mono 14px), Cargo, Fazenda, Status (EmployeeStatusBadge), Admissao
- **Table <768px:** Transform to cards with nome, cargo, status badge, avatar — per UI-SPEC responsive rules
- **Pagination:** 20 per page
- **Loading:** 8 skeleton rows, never spinner full-page
- **Empty state:** Icon UserRound 48px, heading "Nenhum colaborador cadastrado", body "Cadastre o primeiro colaborador da fazenda ou importe uma lista em massa.", CTA "Cadastrar colaborador" (accent button) + secondary "Importar" button — per UI-SPEC copywriting
- **Row click:** Navigate to `/employees/:employeeId`
- **CPF display:** Format as `###.###.###-##` using JetBrains Mono font

**3. Create `CreateEmployeeModal.tsx`** as multi-step modal:

Per UI-SPEC CreateEmployeeModal contract — 4 steps:
- **Step 1 — Dados Pessoais:** name (required), CPF (required, validate onBlur with `isValidCPF` — show error "CPF invalido. Verifique os digitos e tente novamente." with `role="alert"` and `aria-describedby`), birthDate (required), sexo, PIS/PASEP (validate onBlur — warning in yellow if invalid "PIS/PASEP parece invalido. Voce pode salvar e corrigir depois."), RG, CTPS
- **Step 2 — Contrato:** contract type (radio cards with icon per type), admission date (required), position (autocomplete from positions API), salary (required), work schedule (dropdown from work-schedules API)
- **Step 3 — Dados Bancarios e Saude:** bank code, agency, account, account type (CORRENTE/POUPANCA), blood type, health plan
- **Step 4 — Revisao:** Summary cards showing all entered data, primary button "Cadastrar colaborador"

Stepper in modal header: numbered circles with step labels. `aria-current="step"` on active step.
Required fields marked with `*`.
Validation inline onBlur per UI-SPEC, not just on submit.
Modal uses existing modal component pattern from the project.

Use CPF validation from a client-side implementation matching the backend algorithm (sum of products mod 11). PIS validation can be client-side or just submit and show warning from response.

**4. Create `EmployeeStatusModal.tsx`:**

Modal for changing employee status. Shows current status, target status selector (only valid transitions per state machine), reason text field (required), optional return date for AFASTADO/FERIAS.
- Transition to DESLIGADO: Use `ConfirmDeleteModal` pattern — requires typing employee full name. Message: "Esta acao e irreversivel. O colaborador sera marcado como DESLIGADO e nao podera ser reativado. Confirme digitando o nome completo."
- Other transitions: Use `ConfirmModal` with variant="warning". Messages per UI-SPEC copywriting contract.

**5. Create `EmployeeFarmAssocModal.tsx`:**

Simple modal with farm selector (dropdown of user's farms), position selector (autocomplete from positions API), start date. Submits to POST /employees/:id/farms.

**6. Create `EmployeesPage.css`:**
- Use CSS custom properties from tokens.css
- Table styles matching existing patterns
- Card layout for mobile breakpoint (<768px)
- Skeleton animation using opacity 0.4-0.7 pulse per UI-SPEC
  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - EmployeesPage.tsx contains `Cadastrar colaborador` (primary CTA copy)
    - EmployeesPage.tsx contains `Nenhum colaborador cadastrado` (empty state)
    - EmployeesPage.tsx contains skeleton loading pattern (not spinner)
    - EmployeeStatusBadge.tsx contains `CheckCircle` and `Clock` and `Umbrella` and `XCircle`
    - EmployeeStatusBadge.tsx contains `#2E7D32` or `--color-success-500`
    - CreateEmployeeModal.tsx contains at least 4 steps
    - CreateEmployeeModal.tsx contains `aria-current` (stepper accessibility)
    - CreateEmployeeModal.tsx contains CPF validation (`isValidCPF` or equivalent)
    - EmployeeStatusModal.tsx contains `ConfirmDeleteModal` or equivalent for DESLIGADO
    - EmployeeFarmAssocModal.tsx exists and contains form with farm selector
    - `npx tsc --noEmit` exits 0 or has no errors related to employee files
  </acceptance_criteria>
  <done>EmployeesPage lists employees with filters, search, status badges, pagination, skeleton loading, and empty state. CreateEmployeeModal has 4-step form with CPF validation. Status modal handles all transitions with proper confirmation. Farm association modal works.</done>
</task>

<task type="auto">
  <name>Task 3: PositionsPage + WorkSchedulesPage + contract/position/schedule modals</name>
  <files>
    apps/frontend/src/pages/PositionsPage.tsx,
    apps/frontend/src/pages/PositionsPage.css,
    apps/frontend/src/pages/WorkSchedulesPage.tsx,
    apps/frontend/src/pages/WorkSchedulesPage.css,
    apps/frontend/src/components/employee-contracts/CreateContractModal.tsx,
    apps/frontend/src/components/employee-contracts/ContractAmendmentModal.tsx,
    apps/frontend/src/components/positions/CreatePositionModal.tsx,
    apps/frontend/src/components/positions/SalaryBandModal.tsx,
    apps/frontend/src/components/work-schedules/CreateWorkScheduleModal.tsx
  </files>
  <read_first>
    apps/frontend/src/pages/AnimalsPage.tsx,
    apps/frontend/src/pages/EmployeesPage.tsx,
    apps/frontend/src/styles/tokens.css,
    apps/frontend/src/types/position.ts,
    apps/frontend/src/types/work-schedule.ts,
    apps/frontend/src/types/employee-contract.ts,
    apps/frontend/src/hooks/usePositions.ts,
    apps/frontend/src/hooks/useWorkSchedules.ts,
    .planning/phases/25-cadastro-de-colaboradores-e-contratos/25-UI-SPEC.md
  </read_first>
  <action>
**1. Create `PositionsPage.tsx`:**

Following AnimalsPage.tsx list pattern:
- **Header:** Title "Cargos" (DM Sans 20px/700), primary button "Cadastrar cargo" (accent green)
- **Table:** Columns — Nome, CBO (JetBrains Mono 14px), Adicionais (chips), Colaboradores (count from _count.employeeFarms), Faixas Salariais (count)
- **Row actions:** "Faixas" button (opens SalaryBandModal), Edit
- **Staffing view:** Tab or section below showing aggregate grid — position x farm with employee counts (from staffing-view API). Display as "Quadro de Lotacao" heading.
- **Empty state:** Heading "Nenhum cargo cadastrado", body "Crie os cargos antes de cadastrar colaboradores.", CTA "Cadastrar cargo"
- **Search:** by name
- **Pagination:** 20 per page

**2. Create `WorkSchedulesPage.tsx`:**

Following same list pattern:
- **Header:** Title "Escalas de Trabalho" (DM Sans 20px/700), primary button "Cadastrar escala" (accent green), secondary button "Gerar Templates"
- **Table:** Columns — Nome, Tipo (FIXED/SHIFT/CUSTOM label), Dias (chip display of workDays using DAY_LABELS), Horario (startTime - endTime), Intervalo (breakMinutes + "min"), Template (boolean badge), Em uso (contract count)
- **"Gerar Templates" button:** Calls POST /work-schedules/seed-templates to create the 4 pre-configured rural schedules
- **Empty state:** Heading "Nenhuma escala cadastrada", body "Configure as escalas de trabalho para vincular aos contratos.", CTA "Cadastrar escala"

**3. Create `CreateContractModal.tsx`:**

Modal with fields conditional on contractType (per D-05):
- contractType: radio cards (6 options with labels from CONTRACT_TYPE_LABELS)
- positionId: autocomplete (from positions API)
- startDate: date input (required)
- endDate: date input — REQUIRED for CLT_DETERMINATE, SEASONAL, TRIAL, APPRENTICE. HIDDEN for CLT_INDETERMINATE, INTERMITTENT.
- salary: currency input (required), JetBrains Mono font
- weeklyHours: number input (default 44)
- workScheduleId: dropdown (from work-schedules API)
- union: text input
- costCenterId: dropdown (from cost-centers API)
- notes: textarea

When contractType changes, show/hide endDate and validate accordingly.

**4. Create `ContractAmendmentModal.tsx`:**

Simple modal:
- description: text input (required)
- effectiveAt: date input (required)
- changes: Dynamic key-value form. Preset keys: "salary" (with from/to currency inputs), "position" (from/to position dropdowns), "schedule" (from/to schedule dropdowns). Store as JSON.

**5. Create `CreatePositionModal.tsx`:**

Modal with:
- name: text input (required)
- cbo: text input with mask (6 digits, validated)
- description: textarea
- additionalTypes: multi-select chips from ["INSALUBRIDADE", "PERICULOSIDADE", "NOTURNO"]

**6. Create `SalaryBandModal.tsx`:**

Modal with inline editable table per UI-SPEC:
- 3 rows (JUNIOR, PLENO, SENIOR)
- Each row: Level (label), Min Salary (currency input), Max Salary (currency input)
- Validate: minSalary <= maxSalary for each level
- Validate: JUNIOR max <= PLENO min, PLENO max <= SENIOR min (band ordering)
- Save submits PUT /positions/:id/salary-bands with array of 3 bands

**7. Create `CreateWorkScheduleModal.tsx`:**

Modal with:
- name: text input (required)
- type: radio (FIXED/SHIFT/CUSTOM with labels from WORK_SCHEDULE_TYPE_LABELS)
- workDays: checkbox grid with day labels (Dom Seg Ter Qua Qui Sex Sab) — visual day picker
- startTime: time input HH:mm (required)
- endTime: time input HH:mm (required)
- breakMinutes: number input (default 60)
- isTemplate: checkbox
- notes: textarea

**8. CSS files** for PositionsPage.css and WorkSchedulesPage.css following same patterns as EmployeesPage.css — tokens-based styling.
  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - PositionsPage.tsx contains `Cadastrar cargo` (primary CTA)
    - PositionsPage.tsx contains `Quadro de Lotacao` or `staffing`
    - PositionsPage.tsx contains `Nenhum cargo cadastrado` (empty state)
    - WorkSchedulesPage.tsx contains `Cadastrar escala` (primary CTA)
    - WorkSchedulesPage.tsx contains `seed-templates` or `Gerar Templates`
    - WorkSchedulesPage.tsx contains `Nenhuma escala cadastrada` (empty state)
    - CreateContractModal.tsx contains `CLT_INDETERMINATE` and `endDate` conditional logic
    - CreateContractModal.tsx contains `CONTRACT_TYPE_LABELS`
    - CreatePositionModal.tsx contains `cbo` validation
    - SalaryBandModal.tsx contains `JUNIOR` and `PLENO` and `SENIOR`
    - CreateWorkScheduleModal.tsx contains `workDays` day picker
    - `npx tsc --noEmit` exits 0 or has no errors related to these files
  </acceptance_criteria>
  <done>PositionsPage shows positions with salary bands and staffing view. WorkSchedulesPage lists schedules with template generation. All contract/position/schedule modals work with proper validation and conditional fields.</done>
</task>

</tasks>

<verification>
- `cd apps/frontend && npx tsc --noEmit` exits 0
- Sidebar shows RH group with 3 items
- Routes /employees, /positions, /work-schedules registered
- All pages have empty states with correct copy per UI-SPEC
- CreateEmployeeModal has 4 steps with CPF validation onBlur
</verification>

<success_criteria>
- 3 frontend pages (EmployeesPage, PositionsPage, WorkSchedulesPage) with correct UI-SPEC compliance
- 9 modals (CreateEmployee, EmployeeStatus, EmployeeFarmAssoc, CreateContract, ContractAmendment, CreatePosition, SalaryBand, CreateWorkSchedule)
- 5 hooks fetching data from backend APIs
- 4 type files mirroring backend types
- Sidebar RH group with 3 navigation items
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-cadastro-de-colaboradores-e-contratos/25-03-SUMMARY.md`
</output>
