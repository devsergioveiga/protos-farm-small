---
phase: 25-cadastro-de-colaboradores-e-contratos
plan: 04
type: execute
wave: 3
depends_on:
  - 25-01
  - 25-02
  - 25-03
files_modified:
  - apps/backend/src/modules/employees/employee-file-parser.ts
  - apps/backend/src/modules/employees/employee-bulk-import.service.ts
  - apps/backend/src/modules/employees/employees.routes.ts
  - apps/backend/src/modules/employees/employees.routes.spec.ts
  - apps/frontend/src/pages/EmployeeDetailPage.tsx
  - apps/frontend/src/pages/EmployeeDetailPage.css
  - apps/frontend/src/components/employees/tabs/PersonalDataTab.tsx
  - apps/frontend/src/components/employees/tabs/ContractTab.tsx
  - apps/frontend/src/components/employees/tabs/EvolutionTab.tsx
  - apps/frontend/src/components/employees/tabs/DocumentsTab.tsx
  - apps/frontend/src/components/employees/tabs/HistoryTab.tsx
  - apps/frontend/src/components/employees/SalaryEvolutionChart.tsx
  - apps/frontend/src/components/employee-bulk-import/EmployeeBulkImportModal.tsx
autonomous: false
requirements:
  - COLAB-04
  - COLAB-05

must_haves:
  truths:
    - "Manager can import employees via CSV/Excel with template download, preview with validation, and confirmation"
    - "Employee detail page shows all data in tabbed layout with fixed header"
    - "Salary evolution chart renders with Recharts LineChart"
    - "Document tab supports upload and delete of files"
    - "History tab shows merged timeline of status changes and movements"
    - "Import preview highlights CPF errors in red and PIS warnings in yellow"
  artifacts:
    - path: "apps/backend/src/modules/employees/employee-file-parser.ts"
      provides: "CSV/Excel parser for employee import"
      exports: ["parseEmployeeFile"]
    - path: "apps/backend/src/modules/employees/employee-bulk-import.service.ts"
      provides: "Bulk import with preview and confirm"
      exports: ["previewBulkImport", "confirmBulkImport", "generateTemplate"]
    - path: "apps/frontend/src/pages/EmployeeDetailPage.tsx"
      provides: "Employee detail with 5 tabs"
      min_lines: 100
    - path: "apps/frontend/src/components/employees/SalaryEvolutionChart.tsx"
      provides: "Recharts salary evolution chart"
      contains: "LineChart"
    - path: "apps/frontend/src/components/employee-bulk-import/EmployeeBulkImportModal.tsx"
      provides: "4-step import modal"
      min_lines: 100
  key_links:
    - from: "employee-file-parser.ts"
      to: "animal-file-parser.ts"
      via: "Pattern reuse"
      pattern: "parseEmployeeFile"
    - from: "EmployeeDetailPage.tsx"
      to: "/api/org/:orgId/employees/:id"
      via: "useEmployee hook"
      pattern: "useEmployee"
    - from: "SalaryEvolutionChart.tsx"
      to: "recharts"
      via: "LineChart import"
      pattern: "LineChart.*recharts"
---

<objective>
Implement bulk employee import (backend parser + 3 endpoints + frontend modal) and the employee detail page with all 5 tabs including salary evolution chart.

Purpose: Complete COLAB-04 (import) and COLAB-05 (ficha completa) — the final two requirements of Phase 25.
Output: Working bulk import flow, complete employee detail page with tabs and chart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cadastro-de-colaboradores-e-contratos/25-CONTEXT.md
@.planning/phases/25-cadastro-de-colaboradores-e-contratos/25-RESEARCH.md
@.planning/phases/25-cadastro-de-colaboradores-e-contratos/25-UI-SPEC.md
@.planning/phases/25-cadastro-de-colaboradores-e-contratos/25-01-SUMMARY.md
@.planning/phases/25-cadastro-de-colaboradores-e-contratos/25-02-SUMMARY.md
@.planning/phases/25-cadastro-de-colaboradores-e-contratos/25-03-SUMMARY.md
@apps/backend/src/modules/animals/animal-file-parser.ts
@apps/backend/src/modules/assets/asset-bulk-import.service.ts
@apps/frontend/src/pages/AnimalDetailPage.tsx
@apps/frontend/src/components/animals/AnimalBulkImportModal.tsx
@apps/frontend/src/components/cashflow/CashflowChart.tsx
@apps/frontend/src/types/employee.ts
@apps/frontend/src/hooks/useEmployees.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend bulk import — parser, preview, confirm, template endpoints</name>
  <files>
    apps/backend/src/modules/employees/employee-file-parser.ts,
    apps/backend/src/modules/employees/employee-bulk-import.service.ts,
    apps/backend/src/modules/employees/employees.routes.ts,
    apps/backend/src/modules/employees/employees.routes.spec.ts
  </files>
  <read_first>
    apps/backend/src/modules/animals/animal-file-parser.ts,
    apps/backend/src/modules/assets/asset-bulk-import.service.ts,
    apps/backend/src/modules/employees/employees.routes.ts,
    apps/backend/src/modules/employees/employees.service.ts,
    apps/backend/src/shared/utils/document-validator.ts,
    .planning/phases/25-cadastro-de-colaboradores-e-contratos/25-RESEARCH.md
  </read_first>
  <action>
**1. Create `employee-file-parser.ts`** based on `animal-file-parser.ts`:

- `parseEmployeeFile(buffer: Buffer, mimeType: string)`: Parse CSV or XLSX file. Return `{ headers: string[], rows: Record<string, string>[] }`.
- Handle BOM, latin1 encoding, auto-detect separator (`,` or `;`) — reuse exact patterns from animal-file-parser.
- Expected columns (for auto-mapping): nome, cpf, rg, pis_pasep, data_nascimento, data_admissao, cargo, salario, telefone, email, banco, agencia, conta, tipo_conta, saldo_ferias, banco_horas
- `MAX_BULK_EMPLOYEE_ROWS = 500` (same limit as animals)

**2. Create `employee-bulk-import.service.ts`** based on `asset-bulk-import.service.ts`:

- `uploadAndParse(ctx, file)`: Call parseEmployeeFile. Return `{ columnHeaders, sampleRows (first 5), totalRows }`.

- `previewBulkImport(ctx, data)`: Accept `{ rows, columnMapping }`. For each row:
  - Validate CPF via `isValidCPF` — if invalid, mark row as ERROR with message "CPF invalido"
  - Validate PIS via `isValidPIS` — if invalid, mark row as WARNING with message "PIS/PASEP parece invalido"
  - Check CPF uniqueness within org — if duplicate, mark as ERROR "CPF ja cadastrado"
  - Check CPF uniqueness within import batch — if duplicate, mark as ERROR "CPF duplicado no arquivo"
  - Validate required fields: nome, cpf, data_nascimento, data_admissao
  - Return `{ validRows, errorRows, warningRows, totalRows }` with per-row status and messages.

- `confirmBulkImport(ctx, data)`: Accept validated rows. Create employees in batch within a single `prisma.$transaction`. For each row:
  - Create Employee with mapped fields
  - If saldo_ferias provided, set initialVacationBalance
  - If banco_horas provided, set initialHourBankBalance
  - Create initial EmployeeSalaryHistory entry if salary is present
  Return `{ created: number, errors: Array<{ row: number, message: string }> }`.

- `generateTemplate()`: Use ExcelJS to create an XLSX template with:
  - Header row with all expected column names (nome, cpf, rg, pis_pasep, data_nascimento, data_admissao, cargo, salario, telefone, email, banco, agencia, conta, tipo_conta, saldo_ferias, banco_horas)
  - One sample row with example data
  - Column widths set appropriately
  Return buffer.

**3. Add 4 endpoints to existing `employees.routes.ts`:**

```
POST /org/:orgId/employees/bulk/upload    → uploadAndParse (multer single file)
POST /org/:orgId/employees/bulk/preview   → previewBulkImport
POST /org/:orgId/employees/bulk/confirm   → confirmBulkImport
GET  /org/:orgId/employees/bulk/template  → generateTemplate (Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet)
```

IMPORTANT: Place these routes BEFORE the `/:id` route to avoid route conflicts.

**4. Add tests to `employees.routes.spec.ts`:**
- GET template — returns 200 with xlsx content-type
- POST upload — parses CSV file, returns headers and rows
- POST preview — validates CPF, detects duplicates, returns error/warning/valid counts
- POST preview with invalid CPF — row marked as error
- POST preview with invalid PIS — row marked as warning (not error)
- POST confirm — creates employees, returns created count
  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/backend && npx jest --testPathPattern="employees.routes.spec" --bail</automated>
  </verify>
  <acceptance_criteria>
    - employee-file-parser.ts contains `parseEmployeeFile`
    - employee-file-parser.ts contains `MAX_BULK_EMPLOYEE_ROWS`
    - employee-bulk-import.service.ts contains `previewBulkImport`
    - employee-bulk-import.service.ts contains `confirmBulkImport`
    - employee-bulk-import.service.ts contains `generateTemplate`
    - employee-bulk-import.service.ts contains `isValidCPF` and `isValidPIS`
    - employees.routes.ts contains `/bulk/upload`
    - employees.routes.ts contains `/bulk/preview`
    - employees.routes.ts contains `/bulk/confirm`
    - employees.routes.ts contains `/bulk/template`
    - employees.routes.spec.ts contains test cases for bulk operations
    - `npx jest --testPathPattern="employees.routes.spec" --bail` exits 0
  </acceptance_criteria>
  <done>Bulk import works end-to-end: upload parses CSV/XLSX, preview validates CPF/PIS with error/warning differentiation, confirm creates employees in batch, template downloadable as XLSX.</done>
</task>

<task type="auto">
  <name>Task 2: EmployeeDetailPage with 5 tabs + SalaryEvolutionChart + BulkImportModal</name>
  <files>
    apps/frontend/src/pages/EmployeeDetailPage.tsx,
    apps/frontend/src/pages/EmployeeDetailPage.css,
    apps/frontend/src/components/employees/tabs/PersonalDataTab.tsx,
    apps/frontend/src/components/employees/tabs/ContractTab.tsx,
    apps/frontend/src/components/employees/tabs/EvolutionTab.tsx,
    apps/frontend/src/components/employees/tabs/DocumentsTab.tsx,
    apps/frontend/src/components/employees/tabs/HistoryTab.tsx,
    apps/frontend/src/components/employees/SalaryEvolutionChart.tsx,
    apps/frontend/src/components/employee-bulk-import/EmployeeBulkImportModal.tsx
  </files>
  <read_first>
    apps/frontend/src/pages/AnimalDetailPage.tsx,
    apps/frontend/src/components/animals/AnimalBulkImportModal.tsx,
    apps/frontend/src/components/cashflow/CashflowChart.tsx,
    apps/frontend/src/types/employee.ts,
    apps/frontend/src/types/employee-contract.ts,
    apps/frontend/src/hooks/useEmployees.ts,
    apps/frontend/src/hooks/useEmployeeContracts.ts,
    apps/frontend/src/hooks/useEmployeeMovements.ts,
    apps/frontend/src/styles/tokens.css,
    .planning/phases/25-cadastro-de-colaboradores-e-contratos/25-UI-SPEC.md
  </read_first>
  <action>
**1. Create `EmployeeDetailPage.tsx`** following AnimalDetailPage.tsx tab pattern:

Per UI-SPEC EmployeeDetailPage contract:
- **Breadcrumb:** "Colaboradores > [Nome do colaborador]"
- **Fixed header:** Avatar 80x80px (placeholder `UserRound` 48px if no photo, `aria-label="Foto nao disponivel"`), name in Display 24px/700 DM Sans, current position in Label 14px/700 Source Sans 3, EmployeeStatusBadge, buttons "Editar" and "Mudar Status"
- **Tabs:** WAI-ARIA Tabs pattern — `role="tablist"`, `role="tab"` with `aria-selected`, `role="tabpanel"` with `aria-controls`. Tab labels: "Dados Pessoais", "Contrato", "Evolucao", "Documentos", "Historico"
- **Active tab indicator:** underline 2px `--color-primary-600`, not background color
- **Mobile:** Avatar 56px, name truncated after 2 lines, tabs scroll horizontal with fade overflow indicator
- **Tab order:** breadcrumb -> tabs -> content -> footer actions

Fetch employee detail via `useEmployee(employeeId)` hook. Extract employeeId from URL params.

**2. Create `PersonalDataTab.tsx`:**
- Display personal data in a 2-column grid (desktop), 1-column (mobile)
- CPF, RG, PIS/PASEP displayed in JetBrains Mono 14px
- Dependents section: accordion or collapsible list, each showing name, CPF, relationship, birthDate, IRRF/salario-familia badges
- Farm associations: list with farm name, position, start/end dates, status
- Action buttons: "Adicionar dependente", "Associar fazenda" (open respective modals)
- Nationality, marital status, education as read-only fields

**3. Create `ContractTab.tsx`:**
- Current contract (isActive=true) highlighted at top with all fields
- Contract type displayed using CONTRACT_TYPE_LABELS
- Salary in JetBrains Mono font, formatted as BRL
- Amendments list: chronological, each showing description, effective date, changes (from->to)
- Action buttons: "Novo contrato" (opens CreateContractModal), "Aditivo" (opens ContractAmendmentModal), "Baixar PDF" (downloads contract PDF)
- Historical contracts below (isActive=false), collapsed by default

**4. Create `EvolutionTab.tsx`:**
- SalaryEvolutionChart component at top
- Below chart: timeline of EmployeeMovement records (from employee-movements/timeline/:id API)
- Each timeline entry: icon by type (TrendingUp for SALARY_ADJUSTMENT, Award for PROMOTION, ArrowRightLeft for TRANSFER, Briefcase for POSITION_CHANGE), date, description, from->to values
- Empty state: icon TrendingUp 48px, "Nenhum reajuste registrado. O historico aparecera aqui."

**5. Create `SalaryEvolutionChart.tsx`** per UI-SPEC Recharts contract:

```tsx
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts';
```

- Data from GET /employees/:id/salary-history
- Line color: `var(--color-primary-600)` (#2E7D32)
- Grid color: `var(--color-neutral-200)` (#E8E4DD)
- XAxis: dates formatted as "mm/aaaa" using `Intl.DateTimeFormat('pt-BR', { month: '2-digit', year: 'numeric' })`
- YAxis: values formatted as BRL using `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })`
- Custom tooltip: white background, neutral border, shadow, shows value (JetBrains Mono 14px) + date + reason
- Dots: `r={4}`, activeDot `r={6}`, fill primary-600
- ResponsiveContainer width="100%" height={240} (180px on mobile per UI-SPEC)
- Loading: skeleton rectangle 100% x 240px with pulse
- Empty state (no data points): TrendingUp 48px icon + "Nenhum reajuste registrado. O historico aparecera aqui."

**6. Create `DocumentsTab.tsx`:**
- List of documents: icon by type (FileText for all), fileName, documentType label, upload date, uploaded by
- Upload button: `<input type="file" accept=".pdf,.jpg,.jpeg,.png">` with associated label
- Delete button: opens ConfirmModal variant="warning" with message "O arquivo sera excluido permanentemente. Tem certeza?"
- Download: link to `/api/uploads/employees/{orgId}/{employeeId}/{fileName}`
- Empty state: "Nenhum documento anexado. Faca upload do RG, CTPS ou ASO."

**7. Create `HistoryTab.tsx`:**
- Merged timeline of EmployeeStatusHistory + EmployeeMovement (from timeline API)
- Each entry: date, icon+color by type, description
- Status changes: colored by target status (green for ATIVO, yellow for AFASTADO, etc.)
- Empty state: "Nenhuma movimentacao registrada ainda."

**8. Create `EmployeeBulkImportModal.tsx`** following AnimalBulkImportModal.tsx pattern:

Per UI-SPEC EmployeeBulkImportModal contract — 4 steps:
- **Step 1 — Upload:** Drag-and-drop zone + alternative file button, accepts .xlsx/.csv. "Baixar template" link calls GET /employees/bulk/template. Calls POST /employees/bulk/upload on file select.
- **Step 2 — Mapeamento:** Table showing detected columns vs expected fields. Dropdowns to map columns. Follow exact pattern from AnimalBulkImportModal.
- **Step 3 — Preview:** Table with all rows. Error rows highlighted red with AlertCircle icon and message. Warning rows highlighted yellow with AlertTriangle icon. Valid rows normal. Per-row error/warning messages visible. Summary counts at top. Calls POST /employees/bulk/preview.
- **Step 4 — Relatorio:** After POST /employees/bulk/confirm. Shows created count, error count. "Baixar relatorio de erros" button for CSV download of error rows. Close button.

**9. CSS files** — EmployeeDetailPage.css:
- Fixed header styling with avatar
- Tab underline indicator (2px --color-primary-600)
- Responsive: avatar 80px desktop / 56px mobile, tabs scroll horizontal on mobile
- Timeline styling for HistoryTab
- Chart container responsive
  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small/apps/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - EmployeeDetailPage.tsx contains `role="tablist"` (ARIA tabs)
    - EmployeeDetailPage.tsx contains 5 tab labels: `Dados Pessoais`, `Contrato`, `Evolucao` or `Evolução`, `Documentos`, `Historico` or `Histórico`
    - EmployeeDetailPage.tsx contains breadcrumb `Colaboradores`
    - SalaryEvolutionChart.tsx contains `LineChart` import from recharts
    - SalaryEvolutionChart.tsx contains `ResponsiveContainer`
    - SalaryEvolutionChart.tsx contains `pt-BR` formatting
    - PersonalDataTab.tsx contains dependent section
    - ContractTab.tsx contains `CONTRACT_TYPE_LABELS`
    - DocumentsTab.tsx contains `accept=".pdf,.jpg,.jpeg,.png"` or file upload input
    - DocumentsTab.tsx contains `Nenhum documento anexado`
    - HistoryTab.tsx contains `Nenhuma movimentacao registrada` or equivalent
    - EmployeeBulkImportModal.tsx contains 4 steps
    - EmployeeBulkImportModal.tsx contains `/bulk/upload` and `/bulk/preview` and `/bulk/confirm`
    - `npx tsc --noEmit` exits 0 or has no errors related to these files
  </acceptance_criteria>
  <done>EmployeeDetailPage shows all 5 tabs with proper WAI-ARIA, chart renders salary evolution, document upload/delete works, history shows merged timeline. BulkImportModal has 4-step flow with validation preview.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete employee management system</name>
  <action>Human verification of complete Phase 25 implementation.</action>
  <verify>Visual inspection by user</verify>
  <done>User approves the complete employee management system</done>
  <what-built>Complete employee management system: listing with filters and status badges, multi-step creation form with CPF validation, detail page with 5 tabs (personal data, contract, salary evolution chart, documents, history), positions with salary bands and staffing view, work schedules with rural templates, bulk import with Excel template and validation preview, contract management with amendments and PDF, and sidebar RH navigation group.</what-built>
  <how-to-verify>
    1. Navigate to the app, verify "RH" group appears in sidebar with Colaboradores, Cargos, Escalas
    2. Click "Cargos" — verify PositionsPage loads with empty state, create a position with CBO
    3. Click "Escalas" — verify WorkSchedulesPage loads, click "Gerar Templates" to create 4 rural schedules
    4. Click "Colaboradores" — verify EmployeesPage loads with empty state
    5. Click "Cadastrar colaborador" — verify 4-step modal: enter CPF (test with invalid — should show error), complete all steps, submit
    6. Verify employee appears in list with correct status badge
    7. Click employee row — verify EmployeeDetailPage loads with tabs
    8. Test each tab: Dados Pessoais (shows data), Contrato (shows contract), Evolucao (shows chart or empty state), Documentos (upload a file), Historico (shows timeline)
    9. Test status change: click "Mudar Status" — change to AFASTADO, verify badge updates
    10. Test bulk import: click "Importar", download template, upload template with test data, verify preview shows validation
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd apps/backend && npx jest --testPathPattern="employees" --bail` exits 0
- `cd apps/frontend && npx tsc --noEmit` exits 0
- All 5 COLAB requirements implemented
- Employee can be created, viewed in detail, status changed, documents uploaded
- Bulk import works with validation preview
- Salary chart renders with Recharts
</verification>

<success_criteria>
- Bulk import: upload -> parse -> preview (CPF errors red, PIS warnings yellow) -> confirm -> report
- Employee detail: 5 tabs with proper WAI-ARIA, fixed header, breadcrumb
- Salary evolution chart: Recharts LineChart with pt-BR formatting
- Documents: upload/download/delete with file persistence
- History: merged timeline of status changes and movements
- Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/25-cadastro-de-colaboradores-e-contratos/25-04-SUMMARY.md`
</output>
