---
phase: 36-lan-amentos-manuais-raz-o-e-saldo-de-abertura
plan: 05
type: execute
wave: 3
depends_on: [36-03]
files_modified:
  - apps/frontend/src/hooks/useLedger.ts
  - apps/frontend/src/pages/LedgerPage.tsx
  - apps/frontend/src/pages/LedgerPage.css
  - apps/frontend/src/pages/TrialBalancePage.tsx
  - apps/frontend/src/pages/TrialBalancePage.css
  - apps/frontend/src/components/layout/Sidebar.tsx
  - apps/frontend/src/App.tsx
autonomous: true
requirements: [RAZAO-01, RAZAO-02, RAZAO-03]

must_haves:
  truths:
    - 'Ledger page shows running balance for a selected account and period'
    - 'Clicking an entry row in ledger opens a read-only detail view (drill-down)'
    - 'Trial balance page shows 3-column table with balance validation bar'
    - 'Daily book is accessible as a section/tab with chronological entry listing'
    - 'Export buttons (CSV, PDF, XLSX) trigger downloads'
    - 'Sidebar CONTABILIDADE group has links to Razao and Balancete'
  artifacts:
    - path: 'apps/frontend/src/pages/LedgerPage.tsx'
      provides: 'Ledger page with account selector, period filter, running balance table, drill-down, exports'
    - path: 'apps/frontend/src/pages/TrialBalancePage.tsx'
      provides: 'Trial balance 3-column table with group totals, balance validation, exports, daily book tab'
  key_links:
    - from: 'LedgerPage.tsx'
      to: '/api/org/:orgId/ledger/razao'
      via: 'useLedger hook fetch'
      pattern: 'useLedger|ledger/razao'
    - from: 'TrialBalancePage.tsx'
      to: '/api/org/:orgId/ledger/balancete'
      via: 'useLedger hook fetch'
      pattern: 'useTrialBalance|ledger/balancete'
    - from: 'Sidebar.tsx'
      to: 'LedgerPage, TrialBalancePage'
      via: 'route links in CONTABILIDADE group'
      pattern: '/ledger|/trial-balance'
---

<objective>
Build the frontend for accounting reports: Ledger page (razao contabil), Trial Balance page (balancete de verificacao) with daily book tab, sidebar navigation, and export buttons.

Purpose: These reports let accountants verify accounting integrity — the ledger shows per-account detail, the trial balance shows system-wide balance, and the daily book provides the legal record.
Output: LedgerPage, TrialBalancePage (with daily book), useLedger hook, sidebar updates, route registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-lan-amentos-manuais-raz-o-e-saldo-de-abertura/36-UI-SPEC.md
@.planning/phases/36-lan-amentos-manuais-raz-o-e-saldo-de-abertura/36-RESEARCH.md
@.planning/phases/35-plano-de-contas-e-periodos-fiscais/35-04-SUMMARY.md

<interfaces>
From apps/frontend/src/types/journal-entries.ts (created by Plan 04):
```typescript
export interface LedgerLine { entryId, entryNumber, entryDate, description, side, amount, runningBalance }
export interface LedgerOutput { accountId, accountCode, accountName, nature, periodStart, periodEnd, previousBalance, lines, finalBalance }
export interface TrialBalanceRow { accountId, accountCode, accountName, accountType, nature, level, isSynthetic, previousBalance, debitMovement, creditMovement, currentBalance }
export interface TrialBalanceOutput { periodId, periodMonth, periodYear, rows, grandTotals, isBalanced }
```

From apps/frontend/src/hooks/useChartOfAccounts.ts:

```typescript
export function useChartOfAccounts(orgId) — { accounts, loading, error, refetch }
```

From apps/frontend/src/hooks/useFiscalPeriods.ts:

```typescript
export function useFiscalPeriods(orgId) — { fiscalYears, loading, error, refetch }
```

Sidebar CONTABILIDADE group (line ~295-301 in Sidebar.tsx):

```typescript
{
  title: 'CONTABILIDADE',
  items: [
    { to: '/chart-of-accounts', icon: GitBranch, label: 'Plano de Contas' },
    { to: '/fiscal-periods', icon: Calendar, label: 'Periodos Fiscais' },
    { to: '/accounting-entries', icon: BookOpen, label: 'Lancamentos Contabeis' },
  ],
},
```

Backend API (from Plan 03):

- GET /api/org/:orgId/ledger/razao?accountId=&startDate=&endDate=
- GET /api/org/:orgId/ledger/razao/export/csv?...
- GET /api/org/:orgId/ledger/razao/export/pdf?...
- GET /api/org/:orgId/ledger/balancete?fiscalYearId=&month=
- GET /api/org/:orgId/ledger/balancete/export/pdf?...
- GET /api/org/:orgId/ledger/balancete/export/xlsx?...
- GET /api/org/:orgId/ledger/diario?startDate=&endDate=
- GET /api/org/:orgId/ledger/diario/export/pdf?...
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: useLedger hook + LedgerPage + LedgerPage.css</name>
  <files>
    apps/frontend/src/hooks/useLedger.ts,
    apps/frontend/src/pages/LedgerPage.tsx,
    apps/frontend/src/pages/LedgerPage.css
  </files>
  <read_first>
    apps/frontend/src/types/journal-entries.ts,
    apps/frontend/src/hooks/useChartOfAccounts.ts,
    apps/frontend/src/hooks/useFiscalPeriods.ts,
    apps/frontend/src/pages/ChartOfAccountsPage.tsx,
    apps/frontend/src/pages/ChartOfAccountsPage.css,
    apps/frontend/src/services/api.ts,
    apps/frontend/src/styles/tokens.css,
    .planning/phases/36-lan-amentos-manuais-raz-o-e-saldo-de-abertura/36-UI-SPEC.md
  </read_first>
  <action>
    **Hook** (`hooks/useLedger.ts`):

    - `useLedger(orgId, accountId, startDate, endDate, costCenterId?)` — GET `/api/org/${orgId}/ledger/razao` with query params; returns `{ ledger: LedgerOutput | null, loading, error, refetch }`
    - `useTrialBalance(orgId, fiscalYearId, month, comparePreviousPeriod?)` — GET `/api/org/${orgId}/ledger/balancete`; returns `{ trialBalance: TrialBalanceOutput | null, loading, error, refetch }`
    - `useDailyBook(orgId, startDate, endDate, entryType?)` — GET `/api/org/${orgId}/ledger/diario`; returns `{ dailyBook, loading, error, refetch }`
    - `exportLedgerCsv(orgId, filters)` — triggers download via `window.open` or fetch + blob
    - `exportLedgerPdf(orgId, filters)` — triggers download
    - `exportTrialBalancePdf(orgId, filters)` — triggers download
    - `exportTrialBalanceXlsx(orgId, filters)` — triggers download
    - `exportDailyBookPdf(orgId, filters)` — triggers download

    **LedgerPage.tsx** — Per UI-SPEC:

    - Page heading: "Razao Contabil" (DM Sans 700, `--text-lg`)
    - Breadcrumb: Inicio > Contabilidade > Razao Contabil
    - Account selector: searchable combobox from `useChartOfAccounts`, filtered to analytic accounts only (`!isSynthetic`). Placeholder: "Selecione uma conta analitica"
    - Period selector: fiscal year + month dropdowns from `useFiscalPeriods`. Default: current open period.
    - Empty state (no account selected): "Selecione uma conta para visualizar o razao." (centered, icon Search 48px)
    - Empty state (account selected, no data): "Nenhum lancamento nesta conta para o periodo selecionado."
    - Error state: "Nao foi possivel carregar o razao. Tente novamente."
    - Loading: 8-row skeleton with 5 columns

    - Ledger table with `<table>` + `<th scope="col">` + `<caption>`:
      - First row: "Saldo Anterior" — visually distinct (`--color-neutral-200` bg, `--font-bold`), amount in JetBrains Mono
      - Columns: Data | Numero | Historico | Debito | Credito | Saldo Progressivo
      - Amount cells: JetBrains Mono 14px, right-aligned
      - DEBITO amounts: `--color-error-500` text
      - CREDITO amounts: `--color-success-500` text
      - Running balance (Saldo Progressivo): `--color-neutral-700` for positive/zero, `--color-error-500` for negative
      - Drill-down: clicking entry number row opens a read-only inline detail (expand row or small modal showing all lines of the entry)

    - Export buttons (secondary): "Exportar CSV" and "Exportar PDF"
      - During export: spinner icon replaces normal icon (18px), button disabled
      - Position: right side of filter bar

    - Responsive <768px: hide running balance column; expand row for details as card
    - `prefers-reduced-motion: reduce` on all hover/transition effects

    **LedgerPage.css**: Use only `var(--*)` tokens. Implement:
    - `.ledger-table` with proper spacing (12px cell padding)
    - `.saldo-anterior-row` with `--color-neutral-200` background
    - `.amount-debit` with `--color-error-500` color
    - `.amount-credit` with `--color-success-500` color
    - `.amount-mono` with `font-family: 'JetBrains Mono', monospace`
    - Responsive: `@media (max-width: 767px)` hide running balance column
    - Hover on row: 100ms ease-out per UI-SPEC

  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useLedger.ts contains `export function useLedger(`
    - hooks/useLedger.ts contains `export function useTrialBalance(`
    - hooks/useLedger.ts contains `export function useDailyBook(`
    - hooks/useLedger.ts contains export functions for CSV, PDF, XLSX downloads
    - LedgerPage.tsx contains "Razao Contabil" heading
    - LedgerPage.tsx contains "Selecione uma conta analitica" placeholder
    - LedgerPage.tsx contains "Saldo Anterior" row text
    - LedgerPage.tsx contains "Saldo Progressivo" column header
    - LedgerPage.tsx contains "Exportar CSV" and "Exportar PDF" button text
    - LedgerPage.tsx contains `role="dialog"` or detail expansion for drill-down
    - LedgerPage.tsx uses Skeleton for loading
    - LedgerPage.css contains `var(--color-error-500)` for debit amounts
    - LedgerPage.css contains `var(--color-success-500)` for credit amounts
    - LedgerPage.css contains `JetBrains Mono` font reference
    - LedgerPage.css has no hardcoded hex values
    - `npx tsc --noEmit` exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>
    Ledger page shows account selector, period filter, running balance table with drill-down, saldo anterior row, and export buttons. All styling via tokens, responsive behavior, and accessibility requirements met.
  </done>
</task>

<task type="auto">
  <name>Task 2: TrialBalancePage (with daily book tab) + sidebar + routing</name>
  <files>
    apps/frontend/src/pages/TrialBalancePage.tsx,
    apps/frontend/src/pages/TrialBalancePage.css,
    apps/frontend/src/components/layout/Sidebar.tsx,
    apps/frontend/src/App.tsx
  </files>
  <read_first>
    apps/frontend/src/hooks/useLedger.ts,
    apps/frontend/src/types/journal-entries.ts,
    apps/frontend/src/pages/LedgerPage.tsx,
    apps/frontend/src/components/layout/Sidebar.tsx,
    apps/frontend/src/App.tsx,
    apps/frontend/src/styles/tokens.css,
    .planning/phases/36-lan-amentos-manuais-raz-o-e-saldo-de-abertura/36-UI-SPEC.md
  </read_first>
  <action>
    **TrialBalancePage.tsx** — Per UI-SPEC:

    - Page heading: "Balancete de Verificacao" (DM Sans 700, `--text-lg`)
    - Breadcrumb: Inicio > Contabilidade > Balancete de Verificacao
    - Period selector: fiscal year dropdown + month dropdown from `useFiscalPeriods`
    - "Comparativo com periodo anterior" toggle: adds second set of columns; off by default

    - **Balance validation bar** (full-width strip above table, always visible):
      - Balanced: `--color-success-100` background, `--color-success-500` text + CheckCircle icon, "Balancete equilibrado"
      - Unbalanced: `--color-error-100` background, `--color-error-500` text + XCircle icon, "Atencao: debitos e creditos divergem em R$ {valor}"

    - Table with `<table>` + `<th scope="col">`:
      - Columns: Codigo | Conta | Saldo Anterior (D/C) | Movimento Debito | Movimento Credito | Saldo Atual (D/C)
      - Group rows (synthetic accounts): `--font-bold` text + `--color-neutral-100` background
      - Analytic account rows: normal weight, alternating stripe with `--color-neutral-50`
      - Total rows: DM Sans `--font-bold`, top border `--color-neutral-300`
      - All amount cells: JetBrains Mono 14px, right-aligned
      - DEBITO column header and amounts: `--color-error-500`
      - CREDITO column header and amounts: `--color-success-500`

    - Empty state: "Nenhum movimento no periodo selecionado." (centered, icon BarChart3 48px)
    - Loading: 10-row skeleton matching 3-column layout

    - Export buttons (secondary): "Exportar XLSX" + "Exportar PDF"
      - During export: spinner icon, button disabled

    - **Daily Book section** (below trial balance or as a tab):
      - Section heading: "Livro Diario" (DM Sans 700, `--text-md`)
      - Date range filter (same as current period by default)
      - Entry list: grouped by date, each entry shows entryNumber, description, lines table
      - Export: "Exportar PDF" button
      - Empty state: "Nenhum lancamento no periodo selecionado."

    - Responsive <768px: horizontal scroll within table; sticky account code column
    - `prefers-reduced-motion: reduce` on all transitions

    **TrialBalancePage.css**: Use only `var(--*)` tokens:
    - `.validation-bar` with appropriate background colors
    - `.group-row` with `--color-neutral-100` background and bold
    - `.total-row` with top border and bold
    - `.amount-mono` with JetBrains Mono
    - Responsive: `@media (max-width: 767px)` horizontal scroll, sticky first column

    **Sidebar.tsx updates**: Add two new items to the CONTABILIDADE group:

    ```typescript
    { to: '/ledger', icon: BookOpen, label: 'Razao Contabil' },
    { to: '/trial-balance', icon: BarChart3, label: 'Balancete' },
    ```

    Import `BarChart3` from `lucide-react`. Place after "Lancamentos Contabeis" entry. The existing "Lancamentos Contabeis" link at `/accounting-entries` remains unchanged.

    **App.tsx updates**: Add lazy imports and routes:

    ```typescript
    const LedgerPage = lazy(() => import('@/pages/LedgerPage'));
    const TrialBalancePage = lazy(() => import('@/pages/TrialBalancePage'));
    ```

    Add routes inside the authenticated layout:
    ```tsx
    <Route path="/ledger" element={<LedgerPage />} />
    <Route path="/trial-balance" element={<TrialBalancePage />} />
    ```

  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TrialBalancePage.tsx contains "Balancete de Verificacao" heading
    - TrialBalancePage.tsx contains "Balancete equilibrado" balanced state text
    - TrialBalancePage.tsx contains "debitos e creditos divergem" unbalanced state text
    - TrialBalancePage.tsx contains "Exportar XLSX" and "Exportar PDF" buttons
    - TrialBalancePage.tsx contains "Livro Diario" section heading
    - TrialBalancePage.tsx uses Skeleton for loading
    - TrialBalancePage.tsx contains `<table>` with `<th scope="col">`
    - TrialBalancePage.css contains `var(--color-neutral-100)` for group rows
    - TrialBalancePage.css contains `JetBrains Mono` font reference
    - TrialBalancePage.css has no hardcoded hex values
    - Sidebar.tsx contains `'/ledger'` link
    - Sidebar.tsx contains `'/trial-balance'` link
    - Sidebar.tsx contains `BarChart3` icon import
    - App.tsx contains `lazy(() => import('@/pages/LedgerPage'))`
    - App.tsx contains `lazy(() => import('@/pages/TrialBalancePage'))`
    - App.tsx contains `path="/ledger"` route
    - App.tsx contains `path="/trial-balance"` route
    - `npx tsc --noEmit` exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>
    Trial balance page shows 3-column table with balance validation bar, group totals, and export buttons. Daily book section shows chronological entries with PDF export. Sidebar has links to Razao and Balancete. Routes registered in App.tsx with code splitting.
  </done>
</task>

</tasks>

<verification>
cd apps/frontend && npx tsc --noEmit
</verification>

<success_criteria>

- LedgerPage shows running balance per account with drill-down
- TrialBalancePage shows 3-column trial balance with validation bar
- Daily book section lists entries chronologically
- Export buttons trigger CSV/PDF/XLSX downloads
- Sidebar CONTABILIDADE group has Razao and Balancete links
- All pages follow UI-SPEC design contract
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/36-lan-amentos-manuais-raz-o-e-saldo-de-abertura/36-05-SUMMARY.md`
</output>
