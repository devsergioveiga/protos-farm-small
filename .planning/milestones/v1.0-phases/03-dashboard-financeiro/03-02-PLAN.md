---
phase: 03-dashboard-financeiro
plan: 02
type: execute
wave: 2
depends_on: ['03-01']
files_modified:
  - apps/frontend/src/hooks/useFinancialDashboard.ts
  - apps/frontend/src/pages/FinancialDashboardPage.tsx
  - apps/frontend/src/pages/FinancialDashboardPage.css
  - apps/frontend/src/components/financial-dashboard/RevenueExpenseChart.tsx
  - apps/frontend/src/components/financial-dashboard/TopCategoriesChart.tsx
  - apps/frontend/src/components/layout/Sidebar.tsx
  - apps/frontend/src/App.tsx
autonomous: false
requirements: [FN-15]

must_haves:
  truths:
    - 'User sees 4 KPI cards at top: Saldo bancario real, CP 30d, CR 30d, Resultado do mes'
    - 'Each KPI card shows YoY % variation with arrow or em-dash when no prior data'
    - 'Bar chart shows receitas vs despesas by month using Recharts'
    - 'Pie chart shows top 5 expense categories with percentages'
    - 'Top 5 despesas por categoria and top 5 receitas por cliente displayed side by side on desktop'
    - 'Farm dropdown defaults to Todas as fazendas and uses local state (not FarmContext.selectedFarmId)'
    - 'Period dropdown defaults to Mes atual with options Mes anterior and Ultimo trimestre'
    - 'Alerts panel shows overdue CP count/total and projected negative balance warning'
    - 'Dashboard is the first item in FINANCEIRO sidebar group at route /financial-dashboard'
    - 'Skeleton loading shown during fetch, error state with retry button on failure'
  artifacts:
    - path: 'apps/frontend/src/hooks/useFinancialDashboard.ts'
      provides: 'Hook with local filter state and fetch from /api/org/financial-dashboard'
      exports: ['useFinancialDashboard']
    - path: 'apps/frontend/src/pages/FinancialDashboardPage.tsx'
      provides: 'Main dashboard page with KPIs, charts, top 5, alerts'
      min_lines: 150
    - path: 'apps/frontend/src/pages/FinancialDashboardPage.css'
      provides: 'CSS module with fin-dashboard BEM classes'
      contains: 'fin-dashboard'
    - path: 'apps/frontend/src/components/financial-dashboard/RevenueExpenseChart.tsx'
      provides: 'Recharts BarChart component (lazy-loaded)'
      exports: ['default']
    - path: 'apps/frontend/src/components/financial-dashboard/TopCategoriesChart.tsx'
      provides: 'Recharts PieChart component (lazy-loaded)'
      exports: ['default']
  key_links:
    - from: 'apps/frontend/src/pages/FinancialDashboardPage.tsx'
      to: '/api/org/financial-dashboard'
      via: 'useFinancialDashboard hook fetch'
      pattern: 'useFinancialDashboard'
    - from: 'apps/frontend/src/App.tsx'
      to: 'FinancialDashboardPage'
      via: 'lazy import + Route'
      pattern: 'financial-dashboard'
    - from: 'apps/frontend/src/components/layout/Sidebar.tsx'
      to: '/financial-dashboard'
      via: 'sidebar menu item'
      pattern: 'financial-dashboard'
---

<objective>
Create the Financial Dashboard frontend page with all visual components.

Purpose: Give the proprietario/gerente a consolidated financial health view — 4 KPI cards, revenue vs expense bar chart, top categories pie chart, top 5 ranked lists, and alerts — as the home screen of the FINANCEIRO module.

Output: FinancialDashboardPage with hook, chart components, CSS module, sidebar entry, and route registration.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-financeiro/03-CONTEXT.md
@.planning/phases/03-dashboard-financeiro/03-RESEARCH.md
@.planning/phases/03-dashboard-financeiro/03-UI-SPEC.md
@.planning/phases/03-dashboard-financeiro/03-01-SUMMARY.md

<interfaces>
<!-- Backend endpoint created in Plan 01 -->
From apps/backend/src/modules/financial-dashboard/financial-dashboard.types.ts:
```typescript
export interface FinancialDashboardOutput {
  totalBankBalance: number;
  totalBankBalancePrevYear: number | null;
  payablesDue30d: number;
  payablesDue30dPrevYear: number | null;
  receivablesDue30d: number;
  receivablesDue30dPrevYear: number | null;
  monthResult: number;
  monthResultPrevYear: number | null;
  monthlyTrend: Array<{ yearMonth: string; revenues: number; expenses: number }>;
  topExpenseCategories: Array<{ category: string; categoryLabel: string; total: number; percentage: number }>;
  topPayablesByCategory: Array<{ rank: number; category: string; categoryLabel: string; total: number; relativePercent: number }>;
  topReceivablesByClient: Array<{ rank: number; clientName: string; total: number; relativePercent: number }>;
  alerts: { overduePayablesCount: number; overduePayablesTotal: number; projectedBalanceNegative: boolean };
}
```

From apps/frontend/src/components/layout/Sidebar.tsx (FINANCEIRO group):

```typescript
{
  title: 'FINANCEIRO',
  items: [
    { to: '/bank-accounts', icon: Building2, label: 'Contas bancárias' },
    { to: '/payables', icon: Receipt, label: 'Contas a pagar' },
    { to: '/receivables', icon: ReceiptText, label: 'Contas a receber' },
  ],
}
```

From apps/frontend/src/App.tsx (route pattern):

```typescript
const BankAccountsPage = lazy(() => import('@/pages/BankAccountsPage'));
// ...
<Route path="/bank-accounts" element={<BankAccountsPage />} />
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hook, page, CSS, chart components, sidebar entry, and route</name>
  <files>
    apps/frontend/src/hooks/useFinancialDashboard.ts
    apps/frontend/src/pages/FinancialDashboardPage.tsx
    apps/frontend/src/pages/FinancialDashboardPage.css
    apps/frontend/src/components/financial-dashboard/RevenueExpenseChart.tsx
    apps/frontend/src/components/financial-dashboard/TopCategoriesChart.tsx
    apps/frontend/src/components/layout/Sidebar.tsx
    apps/frontend/src/App.tsx
  </files>
  <read_first>
    apps/frontend/src/hooks/useMilkDashboard.ts
    apps/frontend/src/pages/MilkDashboardPage.tsx
    apps/frontend/src/pages/MilkDashboardPage.css
    apps/frontend/src/components/cultivars/CultivarProductivity.tsx
    apps/frontend/src/components/animals/WeighingChartInner.tsx
    apps/frontend/src/components/layout/Sidebar.tsx
    apps/frontend/src/App.tsx
    apps/frontend/src/styles/tokens.css
    apps/frontend/src/contexts/FarmContext.tsx
    apps/frontend/src/services/api.ts
    .planning/phases/03-dashboard-financeiro/03-UI-SPEC.md
    .planning/phases/03-dashboard-financeiro/03-RESEARCH.md
  </read_first>
  <action>
**useFinancialDashboard.ts** — Follow the `useMilkDashboard` pattern exactly:

```typescript
import { useState, useEffect, useCallback } from 'react';
import { api } from '../services/api';

export interface FinancialDashboardData {
  totalBankBalance: number;
  totalBankBalancePrevYear: number | null;
  payablesDue30d: number;
  payablesDue30dPrevYear: number | null;
  receivablesDue30d: number;
  receivablesDue30dPrevYear: number | null;
  monthResult: number;
  monthResultPrevYear: number | null;
  monthlyTrend: Array<{ yearMonth: string; revenues: number; expenses: number }>;
  topExpenseCategories: Array<{
    category: string;
    categoryLabel: string;
    total: number;
    percentage: number;
  }>;
  topPayablesByCategory: Array<{
    rank: number;
    category: string;
    categoryLabel: string;
    total: number;
    relativePercent: number;
  }>;
  topReceivablesByClient: Array<{
    rank: number;
    clientName: string;
    total: number;
    relativePercent: number;
  }>;
  alerts: {
    overduePayablesCount: number;
    overduePayablesTotal: number;
    projectedBalanceNegative: boolean;
  };
}

export type FinancialDashboardPeriod = { year: number; month: number };

export function useFinancialDashboard(params: {
  farmId: string | null;
  period: FinancialDashboardPeriod;
}) {
  // States: data, isLoading, error
  // useCallback for fetch: GET /api/org/financial-dashboard?year=X&month=Y&farmId=Z
  // useEffect on [farmId, period.year, period.month]
  // Return { data, isLoading, error, refetch }
}
```

Query params: `year`, `month`, and optionally `farmId` (omit if null). Fetch from `api.get('/org/financial-dashboard', { params })`.

**FinancialDashboardPage.tsx** — Main page component. Structure follows 03-UI-SPEC.md Layout Structure section exactly:

1. **Header:** `<h1>Dashboard Financeiro</h1>` + `<p>Posicao financeira consolidada da fazenda</p>`
2. **Filters bar:** Two `<select>` elements with visible `<label htmlFor>`:
   - Farm filter: reads `farms` from `useFarmContext()` for options, but stores selection in LOCAL `useState<string | null>(null)` (defaults to "Todas as fazendas"). Does NOT use `FarmContext.selectedFarmId`.
   - Period filter: dropdown with 3 options: "Mes atual" (current), "Mes anterior" (previous), "Ultimo trimestre" (quarter). Maps to `{ year, month }` for the hook.

   Period resolution logic:

   ```typescript
   const PERIOD_OPTIONS = [
     { label: 'Mes atual', value: 'current' },
     { label: 'Mes anterior', value: 'previous' },
     { label: 'Ultimo trimestre', value: 'quarter' },
   ];

   function resolvePeriod(value: string): FinancialDashboardPeriod {
     const now = new Date();
     switch (value) {
       case 'previous': {
         const d = new Date(Date.UTC(now.getFullYear(), now.getMonth() - 1, 1));
         return { year: d.getUTCFullYear(), month: d.getUTCMonth() + 1 };
       }
       case 'quarter': {
         const d = new Date(Date.UTC(now.getFullYear(), now.getMonth() - 2, 1));
         return { year: d.getUTCFullYear(), month: d.getUTCMonth() + 1 };
       }
       default:
         return { year: now.getFullYear(), month: now.getMonth() + 1 };
     }
   }
   ```

3. **KPI grid:** 4 `<article>` cards with BEM class `fin-dashboard__kpi-card`:
   - Each card: icon in 32x32 circle (`--color-primary-50` bg, `--color-primary-600` icon, 20px size), label (Source Sans 3 14px `--color-neutral-500`), value (JetBrains Mono 28px bold `--color-neutral-800`; if negative use `--color-error-500`), YoY badge below.
   - Icons from lucide-react: Building2 (saldo), TrendingDown (CP), TrendingUp (CR), BarChart2 (resultado).
   - KPI 1 label: "Saldo bancario real" with micro-badge "SALDO REAL" (10px uppercase, `--color-primary-100` bg, `--color-primary-700` text).
   - KPI 2 label: "A pagar — proximos 30 dias"
   - KPI 3 label: "A receber — proximos 30 dias"
   - KPI 4 label: "Resultado do mes" (changes to "Resultado do trimestre" when quarter selected). Below value: "Lucro no periodo" if positive, "Deficit no periodo" (`--color-error-500`) if negative.
   - YoY badge component `YoyBadge({ current, prevYear })`:
     - If `prevYear === null`: render `<span aria-label="Sem dados do ano anterior">—</span>` with class `fin-dashboard__yoy--neutral`
     - If positive change: green arrow up `fin-dashboard__yoy--up` with `aria-label`
     - If negative change: red arrow down `fin-dashboard__yoy--down` with `aria-label`
     - Compute: `pct = ((current - prevYear) / Math.abs(prevYear) * 100).toFixed(1)`
   - Format monetary values: `value.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' })`

4. **Endividamento placeholder:** Info banner below KPI grid: `<div class="fin-dashboard__debt-placeholder" role="note">` with Info icon (16px, `--color-info-500`) + "Endividamento: credito rural disponivel na Fase 6" (12px, `--color-neutral-500`). Background `--color-info-100`, padding 8px, border-radius `--radius-md`.

5. **Charts row:** Two side-by-side cards on desktop (>=768px), stacked on mobile:
   - Left: RevenueExpenseChart (lazy loaded via `React.lazy(() => import(...))`)
   - Right: TopCategoriesChart (lazy loaded)
   - Each in a white card with 24px padding, `--shadow-sm`, `--radius-lg`.
   - Title "Receitas vs Despesas" / "Top 5 Categorias de Despesa" in DM Sans 18px bold.
   - Subtitle "Valores realizados (baixados) por mes" below bar chart title.

6. **Top 5 ranked lists:** Two side-by-side panels on desktop, stacked on mobile:
   - Left: "Maiores despesas por categoria" — renders `topPayablesByCategory` array
   - Right: "Maiores receitas por cliente" — renders `topReceivablesByClient` array
   - Each row: rank badge (DM Sans 14px bold, min-width 28px) + label (Source Sans 3 14px) + value (JetBrains Mono 14px, right-aligned) + progress bar (4px height, `--color-primary-400` fill at `relativePercent`%).
   - Row gap: 12px vertical.

7. **Alerts panel:** `<section aria-label="Alertas financeiros">` with role="status":
   - No alerts: CheckCircle icon (24px, `--color-success-500`) + "Nenhum alerta financeiro no momento."
   - Overdue CP: AlertCircle icon (20px, `--color-error-500`) + "N conta(s) a pagar vencida(s), totalizando R$ X,XX." with 4px left border `--color-error-500`.
   - Projected negative: AlertTriangle icon (20px, `--color-warning-500`) + "O saldo bancario pode ficar negativo nos proximos 30 dias." with 4px left border `--color-warning-500`.

8. **Loading state:** Skeleton screens (not spinners). KPI card skeleton: rect 100%x96px. BarChart skeleton: rect 100%x288px. PieChart skeleton: rect 100%x248px. Top 5 skeleton: 5 rects 100%x32px. CSS animation: opacity 0.4 to 0.7, 1.5s infinite ease-in-out. Respect `prefers-reduced-motion: reduce` (freeze at 0.6 opacity).

9. **Error state:** Full-width card with AlertCircle 48px `--color-error-500`, heading "Nao foi possivel carregar o dashboard" (DM Sans 18px bold), body "Verifique sua conexao e tente novamente." (14px `--color-neutral-600`), `<button>` "Tentar novamente" (secondary style, 48px height). Button calls `refetch()`.

10. **Empty state:** When `data` has all zeros/empty arrays: heading "Nenhum dado financeiro ainda", body "Cadastre contas bancarias, contas a pagar e a receber para ver o dashboard.", CTA `<a>` styled as button linking to `/bank-accounts`.

**RevenueExpenseChart.tsx:**

```tsx
import {
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
  Legend,
} from 'recharts';

// Props: { data: Array<{ yearMonth: string; revenues: number; expenses: number }> }
// ResponsiveContainer width="100%" height={240} (200 on mobile)
// Two bars: Receitas fill="var(--color-primary-600)", Despesas fill="var(--color-neutral-400)"
// XAxis: month abbreviation (transform "2026-03" to "Mar"), Source Sans 3 12px
// YAxis: JetBrains Mono 12px
// Tooltip: format as R$ with pt-BR locale
// Legend below chart
// Outer div: aria-label="Grafico de barras: receitas e despesas por mes"
```

**TopCategoriesChart.tsx:**

```tsx
import { PieChart, Pie, Cell, Tooltip, Legend, ResponsiveContainer } from 'recharts';

// Props: { data: Array<{ category: string; categoryLabel: string; total: number; percentage: number }> }
// 5 color palette: var(--color-primary-400), var(--color-sun-500), var(--color-sky-500), var(--color-earth-500), var(--color-neutral-400)
// PieChart width/height: 200px desktop, 160px mobile
// Labels: percentage inside slice if >= 10%, otherwise in legend only
// Legend below chart on all breakpoints
// Outer div: aria-label="Grafico de pizza: top 5 categorias de despesa"
```

**FinancialDashboardPage.css** — CSS module with BEM classes. All values from tokens.css custom properties:

- `.fin-dashboard` — max-width 1280px, margin auto, padding `var(--space-6)`
- `.fin-dashboard__header h1` — DM Sans, `var(--text-2xl)`, font-weight 700, `var(--color-neutral-800)`
- `.fin-dashboard__header p` — Source Sans 3, `var(--text-sm)`, `var(--color-neutral-500)`
- `.fin-dashboard__filters` — display flex, gap 12px, align-items end, margin-bottom `var(--space-6)`
- `.fin-dashboard__filters select` — min-height 48px, padding 12px 16px, border 1px solid `var(--color-neutral-200)`, border-radius `var(--radius-md)`, background `var(--color-neutral-0)`, font-family Source Sans 3, font-size 14px
- `.fin-dashboard__kpis` — display grid, grid-template-columns 1fr 1fr, gap 16px. At >=1024px: grid-template-columns repeat(4, 1fr), gap 24px.
- `.fin-dashboard__kpi-card` — background `var(--color-neutral-0)`, border 1px solid `var(--color-neutral-200)`, border-radius `var(--radius-lg)` (12px), box-shadow `var(--shadow-sm)`, padding 24px. Hover: box-shadow `var(--shadow-lg)`, transition box-shadow 200ms ease-out.
- `.fin-dashboard__kpi-icon` — width 32px, height 32px, border-radius 50%, background `var(--color-primary-50)`, display flex, align-items center, justify-content center
- `.fin-dashboard__kpi-value` — font-family JetBrains Mono, font-size 28px, font-weight 700, color `var(--color-neutral-800)`, line-height 1.2
- `.fin-dashboard__kpi-value--negative` — color `var(--color-error-500)`
- `.fin-dashboard__kpi-label` — font-family Source Sans 3, font-size 14px, color `var(--color-neutral-500)`
- `.fin-dashboard__yoy--up` — color `var(--color-success-500)`, font-size 12px
- `.fin-dashboard__yoy--down` — color `var(--color-error-500)`, font-size 12px
- `.fin-dashboard__yoy--neutral` — color `var(--color-neutral-400)`, font-size 12px
- `.fin-dashboard__saldo-badge` — font-size 10px, text-transform uppercase, letter-spacing 0.5px, background `var(--color-primary-100)`, color `var(--color-primary-700)`, padding 2px 6px, border-radius 4px
- `.fin-dashboard__debt-placeholder` — background `var(--color-info-100)`, padding 8px, border-radius `var(--radius-md)`, margin-top `var(--space-4)`, margin-bottom `var(--space-8)`, display flex, align-items center, gap 8px, font-size 12px, color `var(--color-neutral-500)`
- `.fin-dashboard__charts` — display grid, grid-template-columns 1fr, gap `var(--space-6)`. At >=768px: grid-template-columns 1fr 1fr.
- `.fin-dashboard__chart-card` — background `var(--color-neutral-0)`, padding 24px, border-radius `var(--radius-lg)`, box-shadow `var(--shadow-sm)`, border 1px solid `var(--color-neutral-200)`, margin-bottom `var(--space-8)`
- `.fin-dashboard__chart-title` — font-family DM Sans, font-size 18px, font-weight 700, color `var(--color-neutral-800)`, margin-bottom `var(--space-2)`
- `.fin-dashboard__chart-subtitle` — font-size 14px, color `var(--color-neutral-500)`, margin-bottom `var(--space-4)`
- `.fin-dashboard__top5` — display grid, grid-template-columns 1fr, gap `var(--space-6)`. At >=768px: grid-template-columns 1fr 1fr.
- `.fin-dashboard__top5-panel` — background `var(--color-neutral-0)`, padding 24px, border-radius `var(--radius-lg)`, box-shadow `var(--shadow-sm)`, border 1px solid `var(--color-neutral-200)`
- `.fin-dashboard__top5-row` — display flex, align-items center, gap 8px, margin-bottom 12px
- `.fin-dashboard__top5-rank` — font-family DM Sans, font-size 14px, font-weight 700, min-width 28px, color `var(--color-neutral-700)`
- `.fin-dashboard__top5-value` — font-family JetBrains Mono, font-size 14px, color `var(--color-neutral-800)`, margin-left auto
- `.fin-dashboard__top5-bar` — width 100%, height 4px, background `var(--color-neutral-100)`, border-radius 2px, margin-top 4px
- `.fin-dashboard__top5-bar-fill` — height 100%, background `var(--color-primary-400)`, border-radius 2px
- `.fin-dashboard__alerts` — margin-top `var(--space-8)`
- `.fin-dashboard__alert-row` — display flex, align-items center, gap 8px, padding 12px, border-left 4px solid, margin-bottom 8px, border-radius `var(--radius-md)`, background `var(--color-neutral-0)`
- `.fin-dashboard__skeleton` — background `var(--color-neutral-100)`, border-radius `var(--radius-lg)`, animation pulse 1.5s infinite ease-in-out
- `@keyframes pulse` — 0% opacity 0.4, 50% opacity 0.7, 100% opacity 0.4
- `@media (prefers-reduced-motion: reduce)` — `.fin-dashboard__skeleton` animation none, opacity 0.6
- `.fin-dashboard__error` — text-align center, padding `var(--space-12)`, background `var(--color-neutral-0)`, border-radius `var(--radius-lg)`
- `.fin-dashboard__error button` — min-height 48px, min-width 48px, padding 12px 24px, border 1px solid `var(--color-neutral-200)`, border-radius `var(--radius-md)`, background `var(--color-neutral-0)`, cursor pointer

**Sidebar.tsx** — Add Dashboard as the FIRST item in the FINANCEIRO group:

```typescript
// Change from:
items: [
  { to: '/bank-accounts', icon: Building2, label: 'Contas bancarias' },
  ...
]
// To:
items: [
  { to: '/financial-dashboard', icon: LayoutDashboard, label: 'Dashboard' },
  { to: '/bank-accounts', icon: Building2, label: 'Contas bancarias' },
  ...
]
```

Import `LayoutDashboard` from lucide-react.

**App.tsx** — Add lazy import and route:

```typescript
const FinancialDashboardPage = lazy(() => import('@/pages/FinancialDashboardPage'));
// ...
<Route path="/financial-dashboard" element={<FinancialDashboardPage />} />
```

Place the route near the other financial routes (bank-accounts, payables, receivables).
</action>
<verify>
<automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && pnpm --filter @protos-farm/frontend exec tsc --noEmit 2>&1 | head -20</automated>
</verify>
<acceptance_criteria> - apps/frontend/src/hooks/useFinancialDashboard.ts contains `export function useFinancialDashboard` - apps/frontend/src/hooks/useFinancialDashboard.ts contains `/org/financial-dashboard` - apps/frontend/src/pages/FinancialDashboardPage.tsx contains `Dashboard Financeiro` - apps/frontend/src/pages/FinancialDashboardPage.tsx contains `fin-dashboard__kpi-card` - apps/frontend/src/pages/FinancialDashboardPage.tsx contains `Saldo banc` - apps/frontend/src/pages/FinancialDashboardPage.tsx contains `aria-label` - apps/frontend/src/pages/FinancialDashboardPage.tsx contains `SALDO REAL` - apps/frontend/src/pages/FinancialDashboardPage.tsx contains `useState` for local farmId (not FarmContext.selectedFarmId) - apps/frontend/src/pages/FinancialDashboardPage.tsx contains `Endividamento` - apps/frontend/src/pages/FinancialDashboardPage.css contains `fin-dashboard__kpis` - apps/frontend/src/pages/FinancialDashboardPage.css contains `prefers-reduced-motion` - apps/frontend/src/pages/FinancialDashboardPage.css contains `--color-primary-50` - apps/frontend/src/components/financial-dashboard/RevenueExpenseChart.tsx contains `BarChart` - apps/frontend/src/components/financial-dashboard/RevenueExpenseChart.tsx contains `ResponsiveContainer` - apps/frontend/src/components/financial-dashboard/TopCategoriesChart.tsx contains `PieChart` - apps/frontend/src/components/layout/Sidebar.tsx contains `financial-dashboard` - apps/frontend/src/components/layout/Sidebar.tsx contains `LayoutDashboard` - apps/frontend/src/App.tsx contains `FinancialDashboardPage` - apps/frontend/src/App.tsx contains `financial-dashboard` - TypeScript compiles without errors
</acceptance_criteria>
<done>Financial Dashboard page renders with all components, wired to backend endpoint via hook, accessible at /financial-dashboard, listed first in FINANCEIRO sidebar group. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Financial Dashboard visually</name>
  <files>apps/frontend/src/pages/FinancialDashboardPage.tsx</files>
  <action>
    Complete Financial Dashboard page built in Task 1: 4 KPI cards, revenue vs expense bar chart, top categories pie chart, top 5 ranked lists (despesas + receitas), alerts panel, farm/period filters, skeleton loading, and error state. Dashboard is at /financial-dashboard as the first FINANCEIRO sidebar item. User verifies the full page end-to-end.
  </action>
  <what-built>Complete Financial Dashboard page with 4 KPI cards, revenue vs expense bar chart, top categories pie chart, top 5 ranked lists (despesas + receitas), alerts panel, farm/period filters, skeleton loading, and error state. Dashboard is at /financial-dashboard as the first FINANCEIRO sidebar item.</what-built>
  <how-to-verify>
    1. Start frontend dev server: `cd apps/frontend \&\& pnpm dev`
    2. Start backend dev server: `cd apps/backend \&\& pnpm dev`
    3. Navigate to http://localhost:5173/financial-dashboard
    4. Verify 4 KPI cards are visible at top (Saldo bancario real, A pagar 30d, A receber 30d, Resultado do mes)
    5. Verify each KPI shows a "SALDO REAL" badge on the first card
    6. Verify YoY badges show arrows or em-dash
    7. Verify bar chart shows receitas (green) vs despesas (neutral gray) by month
    8. Verify pie chart shows top 5 expense categories
    9. Verify Top 5 ranked lists show despesas by category and receitas by client
    10. Verify alerts panel at bottom
    11. Change farm dropdown — data should refetch
    12. Change period dropdown — data should refetch
    13. Verify sidebar shows "Dashboard" as first item in FINANCEIRO group
    14. Resize browser to mobile width (<768px) — verify KPI cards stack 2x2, charts stack vertically, top 5 lists stack
    15. Check skeleton loading appears briefly during fetch
  </how-to-verify>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small \&\& pnpm --filter @protos-farm/frontend exec tsc --noEmit 2>\&1 | head -5</automated>
  </verify>
  <done>User visually approves the Financial Dashboard page with all components rendering correctly, responsive layout working, and filters functional.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @protos-farm/frontend exec tsc --noEmit` compiles without errors
- Page loads at /financial-dashboard without console errors
- Sidebar shows Dashboard as first FINANCEIRO item
- All 4 KPI cards render with correct labels and formatting
- Charts render with correct data
- Farm and period filters work
- Mobile responsive layout correct
</verification>

<success_criteria>

- Financial Dashboard page is the landing page of the FINANCEIRO module
- 4 KPI cards show saldo total, CP 30d, CR 30d, and resultado do mes with YoY comparison
- Bar chart shows receitas vs despesas by month using Recharts
- Pie chart shows top 5 expense categories
- Top 5 ranked lists show despesas by category and receitas by client
- Farm dropdown uses local state (not FarmContext.selectedFarmId)
- Period defaults to current month
- Skeleton loading during fetch, error state with retry button
- Alerts show overdue CP and projected negative balance
- WCAG AA accessible: semantic HTML, aria-labels, focus visible, prefers-reduced-motion
- Human-verify checkpoint approved
  </success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-financeiro/03-02-SUMMARY.md`
</output>
