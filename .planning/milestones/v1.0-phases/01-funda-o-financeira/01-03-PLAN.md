---
phase: 01-funda-o-financeira
plan: 03
type: execute
wave: 3
depends_on: ['01-02']
files_modified:
  - apps/frontend/src/pages/BankAccountsPage.tsx
  - apps/frontend/src/pages/BankAccountsPage.css
  - apps/frontend/src/components/bank-accounts/BankAccountModal.tsx
  - apps/frontend/src/components/bank-accounts/BankAccountModal.css
  - apps/frontend/src/hooks/useBankAccounts.ts
  - apps/frontend/src/components/layout/Sidebar.tsx
  - apps/frontend/src/App.tsx
autonomous: false
requirements:
  - FN-01
  - FN-03

must_haves:
  truths:
    - "User sees FINANCEIRO group in sidebar with 'Contas bancarias' link"
    - 'User sees bank accounts as cards with bank name, type, balance'
    - 'User sees totalisation bar with total balance and totals by account type'
    - 'User can filter accounts by farm, type, and bank'
    - 'User can create account via modal with all required fields including optional producerId and multiple farms'
    - 'User can edit and soft-delete accounts'
    - 'User can view statement for an account filtered by period'
    - 'User can export statement as PDF, Excel, or CSV'
    - 'Empty state shows illustration + CTA when no accounts exist'
  artifacts:
    - path: 'apps/frontend/src/pages/BankAccountsPage.tsx'
      provides: 'Dashboard page with cards, filters, totals bar, statement view'
      min_lines: 150
    - path: 'apps/frontend/src/components/bank-accounts/BankAccountModal.tsx'
      provides: 'Create/edit modal for bank accounts'
      min_lines: 100
    - path: 'apps/frontend/src/hooks/useBankAccounts.ts'
      provides: 'Data fetching hook for bank accounts'
      exports: ['useBankAccounts']
    - path: 'apps/frontend/src/components/layout/Sidebar.tsx'
      provides: 'Updated sidebar with FINANCEIRO group'
      contains: 'FINANCEIRO'
  key_links:
    - from: 'apps/frontend/src/pages/BankAccountsPage.tsx'
      to: '/api/org/bank-accounts'
      via: 'useBankAccounts hook'
      pattern: "api\\.get.*bank-accounts"
    - from: 'apps/frontend/src/components/bank-accounts/BankAccountModal.tsx'
      to: '/api/org/bank-accounts'
      via: 'api.post / api.patch'
      pattern: "api\\.(post|patch).*bank-accounts"
    - from: 'apps/frontend/src/App.tsx'
      to: 'apps/frontend/src/pages/BankAccountsPage.tsx'
      via: 'lazy route /bank-accounts'
      pattern: 'bank-accounts'
    - from: 'apps/frontend/src/components/layout/Sidebar.tsx'
      to: '/bank-accounts'
      via: 'NAV_GROUPS item'
      pattern: 'bank-accounts'
---

<objective>
Build the frontend for bank accounts: dashboard page with account cards, filters, totalization bar, statement view with export, create/edit modal, and sidebar integration.

Purpose: Delivers the user-facing interface for FN-01 and FN-03 — the gerente can manage bank accounts and view balances.
Output: Working frontend page accessible via sidebar, with CRUD modal and statement export.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-financeira/01-CONTEXT.md
@.planning/phases/01-funda-o-financeira/01-RESEARCH.md
@.planning/phases/01-funda-o-financeira/01-02-SUMMARY.md

<interfaces>
<!-- Backend API endpoints from Plan 02 -->

```
GET    /api/org/bank-accounts                       -> { data: BankAccountOutput[] }
POST   /api/org/bank-accounts                       -> BankAccountOutput (201)
GET    /api/org/bank-accounts/dashboard              -> { totalBalance, accountCount, byType[] }
GET    /api/org/bank-accounts/:id                    -> BankAccountOutput
PATCH  /api/org/bank-accounts/:id                    -> BankAccountOutput
DELETE /api/org/bank-accounts/:id                    -> 204
GET    /api/org/bank-accounts/:id/statement          -> { data: Transaction[] }
GET    /api/org/bank-accounts/:id/statement/export   -> Buffer (PDF/XLSX/CSV)
```

BankAccountOutput shape:

```typescript
{
  id: string;
  name: string;
  type: 'CHECKING' | 'SAVINGS' | 'INVESTMENT' | 'RURAL_CREDIT';
  bankCode: string;
  bankName: string;
  agency: string;
  agencyDigit?: string;
  accountNumber: string;
  accountDigit?: string;
  producerId?: string;
  producer?: { id: string; name: string };
  farms: { id: string; name: string }[];
  balance: { initialBalance: number; currentBalance: number };
  notes?: string;
  isActive: boolean;
}
```

From packages/shared:

```typescript
import { FEBRABAN_BANKS } from '@protos-farm/shared';
// Array of { code, name, shortName } for bank dropdown
```

Sidebar pattern (from Sidebar.tsx):

```typescript
// NAV_GROUPS array of { title: string, items: { to, icon, label }[] }
// Add FINANCEIRO group with Building2 icon
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BankAccountsPage, BankAccountModal, hook, and routing</name>
  <files>
    apps/frontend/src/pages/BankAccountsPage.tsx,
    apps/frontend/src/pages/BankAccountsPage.css,
    apps/frontend/src/components/bank-accounts/BankAccountModal.tsx,
    apps/frontend/src/components/bank-accounts/BankAccountModal.css,
    apps/frontend/src/hooks/useBankAccounts.ts,
    apps/frontend/src/components/layout/Sidebar.tsx,
    apps/frontend/src/App.tsx
  </files>
  <action>
    1. **useBankAccounts hook** (`hooks/useBankAccounts.ts`):
       - Follow the useState + useEffect + useCallback pattern (NOT React Query — per project convention)
       - `useBankAccounts(filters: { farmId?, type?, bankCode? })` returns `{ accounts, loading, error, refetch }`
       - `useBankAccountDashboard()` returns `{ dashboard, loading, error, refetch }`
       - `useBankAccountStatement(accountId, query: { from?, to?, type? })` returns `{ transactions, loading, error }`
       - All use `api.get()` from `@/services/api`

    2. **BankAccountsPage** (`pages/BankAccountsPage.tsx`):
       - **Layout per user decision:** cards per account + totalization bar at top
       - **Totalization bar:** sticky top bar showing: Saldo Total (formatted BRL), and breakdown by type (Corrente: R$ X, Investimento: R$ Y, Credito Rural: R$ Z). Use dashboard endpoint data.
       - **Filters:** sidebar/toolbar with dropdowns for: Fazenda (from org farms), Tipo (CHECKING/SAVINGS/INVESTMENT/RURAL_CREDIT with pt-BR labels), Banco (from FEBRABAN_BANKS shortName). Flat list, no fixed grouping per user decision.
       - **Account cards:** Each card shows:
         - Bank icon (Building2 from lucide-react) + bank shortName + account type badge
         - Account name (bold, heading style)
         - Agency/Account number
         - Current balance (large, formatted as BRL)
         - Saldo projetado: "--" with tooltip "Disponivel apos cadastrar contas a pagar e receber" (per CONTEXT.md: prepare interface for future projection data)
         - Linked farms as chips/tags
         - Producer name if present
         - Edit and View Statement action buttons
       - **Empty state:** illustration + "Nenhuma conta bancaria cadastrada" + "Cadastre sua primeira conta para acompanhar saldos e extratos" + CTA button "Nova conta bancaria" (per CLAUDE.md empty state pattern)
       - **Skeleton loading:** card skeletons while loading (per CLAUDE.md: never spinner full-page)
       - **Statement view:** When user clicks "Ver extrato" on a card, show inline expanded section (or tab) with:
         - Period filter (date range picker)
         - Type filter (Entrada/Saida/Todos)
         - Transaction list (date, description, type badge, amount in BRL)
         - Export buttons: PDF, Excel, CSV — each triggers download via GET /statement/export?format=X
       - **FAB / Header button:** "Nova conta bancaria" button opens BankAccountModal

    3. **BankAccountModal** (`components/bank-accounts/BankAccountModal.tsx`):
       - Props: `isOpen, onClose, onSuccess, accountId?` (edit mode if accountId provided)
       - Per CLAUDE.md: CRUD always in modal, never dedicated page
       - Form fields:
         - Nome da conta* (text input)
         - Tipo* (select: Conta corrente / Poupanca / Investimento / Credito rural)
         - Banco* (searchable select from FEBRABAN_BANKS — show shortName + code, search by name/shortName/code)
         - Agencia* (text) + Digito (text, optional)
         - Numero da conta* (text) + Digito (text, optional)
         - Saldo inicial* (currency input — format as BRL on blur, store as number)
         - Produtor rural (optional select — fetch producers from existing API endpoint)
         - Fazendas (multi-select — fetch farms from existing API endpoint)
         - Observacoes (textarea, optional)
       - Validation: inline onBlur per CLAUDE.md. Required fields marked with *. BankCode validated against FEBRABAN_BANK_MAP.
       - On submit: POST for create, PATCH for edit. Show success toast ("Conta bancaria cadastrada com sucesso" / "Conta bancaria atualizada com sucesso"). Call onSuccess callback.
       - Accessible: aria-required on required fields, label always visible (never placeholder-only), focus first field on open.

    4. **Sidebar update** (`components/layout/Sidebar.tsx`):
       - Add FINANCEIRO group to NAV_GROUPS array, positioned BEFORE the CONFIGURACAO group (after ESTOQUE)
       - Items: `{ to: '/bank-accounts', icon: Building2, label: 'Contas bancarias' }`
       - Import `Building2` from `lucide-react`

    5. **App.tsx route** (`App.tsx`):
       - Add lazy-loaded route: `const BankAccountsPage = React.lazy(() => import('./pages/BankAccountsPage'));`
       - Add Route path="/bank-accounts" element=BankAccountsPage inside the protected layout

    6. **Styling:**
       - Use Tailwind CSS per project convention (tokens in tailwind config)
       - Cards: shadow, hover state, rounded-lg, proper spacing (4px scale)
       - Touch targets: min 48x48px on all buttons
       - Mobile responsive: cards stack vertically on small screens (grid cols-1 sm:cols-2 lg:cols-3)
       - Totalization bar: sticky, subtle background, clear typography hierarchy
       - BRL formatting: use `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })`
       - Account type labels in pt-BR: { CHECKING: 'Conta corrente', SAVINGS: 'Poupanca', INVESTMENT: 'Investimento', RURAL_CREDIT: 'Credito rural' }

  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && pnpm --filter @protos-farm/frontend build</automated>
  </verify>
  <done>Frontend builds without errors. BankAccountsPage renders with cards + totalization. Modal creates/edits accounts. Statement view with export works. Sidebar shows FINANCEIRO group. Route /bank-accounts accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete bank accounts UI end-to-end</name>
  <files>N/A</files>
  <action>
    Human verifies the complete bank accounts interface by following the steps below. No code changes needed — this is a visual and functional verification checkpoint.
  </action>
  <verify>
    Steps:
    1. Start dev servers: `pnpm dev:backend` and `pnpm dev:frontend`
    2. Login and check sidebar — FINANCEIRO group with "Contas bancarias" link visible
    3. Click "Contas bancarias" — empty state shows illustration + CTA
    4. Click "Nova conta bancaria" — modal opens with all fields
    5. Fill: Nome="Conta BB Principal", Tipo=Conta corrente, Banco=search "Brasil" select "BB (001)", Agencia="1234", Conta="56789-0", Saldo inicial="10000", select a farm — submit — success toast + card appears
    6. Create second account (Investimento type) — totalization bar shows total and breakdown by type
    7. Test filters: by Tipo, by Fazenda — cards filter correctly
    8. Click "Ver extrato" — statement shows OPENING_BALANCE transaction
    9. Export PDF — downloads with correct BRL formatting and header
    10. Export Excel and CSV — files download correctly
    11. Edit account — modal pre-fills, update works
    12. Delete account — disappears from list
    13. Resize browser narrow — cards stack vertically (responsive)
  </verify>
  <done>User confirms all 13 verification steps pass. Bank accounts interface is functional and visually correct.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @protos-farm/frontend build` succeeds
- Sidebar shows FINANCEIRO group
- Account CRUD works end-to-end
- Statement export generates correct files
- Totalization bar shows correct sums
</verification>

<success_criteria>

- Gerente can create bank accounts with FEBRABAN bank, link to farms, set initial balance
- Dashboard shows all accounts as cards with current balance and totalization bar
- Filters by farm/type/bank work
- Statement view shows transactions with period filter
- PDF/Excel/CSV export works with BRL formatting
- Interface prepared for future saldo projetado (placeholder shown)
- No float arithmetic anywhere — all BRL display uses Intl.NumberFormat
  </success_criteria>

<output>
After completion, create `.planning/phases/01-funda-o-financeira/01-03-SUMMARY.md`
</output>
