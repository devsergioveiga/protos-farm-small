---
phase: 02-n-cleo-ap-ar
plan: 05
type: execute
wave: 4
depends_on: ['02-02', '02-03', '02-04']
files_modified:
  - apps/frontend/src/pages/PayablesPage.tsx
  - apps/frontend/src/pages/PayablesPage.css
  - apps/frontend/src/components/payables/PayableModal.tsx
  - apps/frontend/src/components/payables/PayableModal.css
  - apps/frontend/src/components/payables/PaymentModal.tsx
  - apps/frontend/src/components/payables/BatchPaymentModal.tsx
  - apps/frontend/src/components/payables/CnabRetornoModal.tsx
  - apps/frontend/src/hooks/usePayables.ts
autonomous: true
requirements:
  - FN-07
  - FN-08
  - FN-10

must_haves:
  truths:
    - 'Gerente ve lista de CP com filtros por status, categoria, periodo e busca'
    - 'Gerente cria CP em modal com parcelamento editavel e rateio por centro de custo'
    - 'Gerente da baixa via modal com campos de juros/multa/desconto e selecao de conta'
    - 'Aging mostra tabela de faixas clicavel que filtra a lista'
    - 'Calendario mensal mostra dots nos dias com vencimento'
    - 'Badge de vencidos aparece no sidebar'
  artifacts:
    - path: 'apps/frontend/src/pages/PayablesPage.tsx'
      provides: 'CP page with list, aging table, calendar, filters'
      min_lines: 300
    - path: 'apps/frontend/src/components/payables/PayableModal.tsx'
      provides: 'Create/edit CP modal with installments and rateio'
      min_lines: 200
    - path: 'apps/frontend/src/hooks/usePayables.ts'
      provides: 'Data fetching hooks for payables, aging, calendar'
      exports: ['usePayables', 'usePayablesAging', 'usePayableCalendar', 'useOverdueCount']
  key_links:
    - from: 'apps/frontend/src/pages/PayablesPage.tsx'
      to: '/api/org/payables'
      via: 'usePayables hook fetch'
      pattern: 'fetch.*payables'
    - from: 'apps/frontend/src/hooks/usePayables.ts'
      to: '/api/org/payables-aging'
      via: 'aging and calendar endpoints'
      pattern: 'payables-aging'
---

<objective>
Build the PayablesPage frontend with full CP management UI: list with filters, create/edit modal with parcelamento and rateio, payment modal, batch payment modal, CNAB retorno import modal, aging table, calendar view, and sidebar badge.

Purpose: FN-07 (CP list and CRUD UI), FN-08 (settlement and CNAB retorno UI), FN-10 (aging, calendar, alerts UI). Complete CP user experience.
Output: PayablesPage, 4 modals, hooks, sidebar integration.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-n-cleo-ap-ar/02-CONTEXT.md
@.planning/phases/02-n-cleo-ap-ar/02-02-SUMMARY.md
@.planning/phases/02-n-cleo-ap-ar/02-04-SUMMARY.md
@apps/frontend/src/pages/BankAccountsPage.tsx
@apps/frontend/src/components/bank-accounts/BankAccountModal.tsx

<interfaces>
<!-- Backend API endpoints consumed by this plan -->
// GET  /api/org/payables?farmId=&status=&category=&startDate=&endDate=&search=&page=&limit=
// POST /api/org/payables { supplierName, category, description, totalAmount, dueDate, farmId, installmentCount, costCenterItems[], ... }
// PUT  /api/org/payables/:id { ... }
// DELETE /api/org/payables/:id
// POST /api/org/payables/:id/settle { paidAt, amount, bankAccountId, interestAmount?, fineAmount?, discountAmount? }
// POST /api/org/payables/batch-settle { bankAccountId, paidAt, items[] }
// POST /api/org/payables/:id/reverse
// POST /api/org/payables/cnab/remessa { bankAccountId, format, payableIds[] }
// POST /api/org/payables/cnab/retorno/preview (multipart file)
// POST /api/org/payables/cnab/retorno/confirm { records[] }
// GET  /api/org/payables-aging
// GET  /api/org/payables-aging/bucket/:bucket
// GET  /api/org/payables-aging/overdue-count
// GET  /api/org/payables-aging/calendar?year=&month=&farmId=

<!-- Existing frontend patterns -->

// BankAccountsPage: card grid, filters, skeleton loading, empty state
// BankAccountModal: isOpen + onClose + onSuccess + optional editId pattern
// Sidebar.tsx: FINANCEIRO group with icon and items (already has "Contas bancarias")
// Hooks: useState + useEffect + useCallback pattern (not React Query)
// Currency: Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PayablesPage with list, aging, calendar, and hooks</name>
  <files>apps/frontend/src/pages/PayablesPage.tsx, apps/frontend/src/pages/PayablesPage.css, apps/frontend/src/hooks/usePayables.ts</files>
  <action>
    **usePayables.ts:**
    Create 4 hooks following useBankAccounts pattern (useState + useEffect + useCallback):
    - `usePayables(query)` — fetch GET /api/org/payables with filters, returns { payables, loading, error, refetch }
    - `usePayablesAging(farmId?)` — fetch GET /api/org/payables-aging, returns { aging, loading }
    - `usePayableCalendar(year, month, farmId?)` — fetch GET /api/org/payables-aging/calendar, returns { days, loading }
    - `useOverdueCount()` — fetch GET /api/org/payables-aging/overdue-count, returns { count, loading }

    **PayablesPage.tsx:**
    Page layout with 3 tabs/views within the page (not separate routes — stays within 3-level nav depth):

    **Tab 1: "Titulos" (default)**
    - Filter toolbar: status dropdown (Todos/Pendente/Pago/Vencido/Cancelado), category dropdown, farm selector, date range (inicio/fim), search input
    - Table with columns: Fornecedor, Descricao, Valor (BRL formatted, JetBrains Mono), Vencimento, Status (badge colored), Parcela (e.g. "2/3"), Acoes (dropdown: Editar, Baixar, Estornar, Excluir)
    - Mobile: transform table rows into cards empilhados (per CLAUDE.md)
    - Pagination at bottom
    - "Nova Conta a Pagar" primary button (only 1 per screen)
    - Bulk selection checkboxes for batch actions: "Baixar Selecionados" and "Gerar CNAB"
    - Empty state with CTA "Registre sua primeira conta a pagar"
    - Skeleton loading (never spinner)

    **Tab 2: "Aging"**
    - Aging table: 7 rows (Vencidas, 7 dias, 15 dias, 30 dias, 60 dias, 90 dias, >90 dias), columns: Faixa, Qtd, Total (BRL). Row for Vencidas highlighted in error color (#C62828). Each row clickable — clicking filters Tab 1 to that bucket.
    - Grand total row at bottom
    - Overdue summary at top: "X titulos vencidos totalizando R$ Y"

    **Tab 3: "Calendario"**
    - Month/year selector (prev/next month arrows)
    - Calendar grid showing each day of the month. Days with payable due dates show a green dot (#2E7D32) and the count. Click a day to show a popover/tooltip with the payables due that day (supplier, amount, status).
    - Custom implementation (no external library — simple CSS grid of 7 columns x 5-6 rows)

    **PayablesPage.css:**
    Styles following BankAccountsPage.css patterns: 4px spacing scale, DM Sans headlines, Source Sans 3 body, JetBrains Mono for amounts, skeleton animations, responsive cards for mobile.

  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && npx --prefix apps/frontend tsc --noEmit -p apps/frontend/tsconfig.json 2>&1 | head -10</automated>
  </verify>
  <done>PayablesPage renders with 3 tabs (Titulos/Aging/Calendario), list has filters and pagination, aging table shows 7 buckets, calendar shows month grid with dots, hooks fetch from backend, tsc clean</done>
</task>

<task type="auto">
  <name>Task 2: PayableModal, PaymentModal, BatchPaymentModal, CnabRetornoModal</name>
  <files>apps/frontend/src/components/payables/PayableModal.tsx, apps/frontend/src/components/payables/PayableModal.css, apps/frontend/src/components/payables/PaymentModal.tsx, apps/frontend/src/components/payables/BatchPaymentModal.tsx, apps/frontend/src/components/payables/CnabRetornoModal.tsx</files>
  <action>
    **PayableModal.tsx:**
    Create/edit modal following BankAccountModal pattern (isOpen, onClose, onSuccess, editId?).
    - Fields: fornecedor (text), categoria (dropdown PayableCategory), descricao (textarea), valor total (BRL currency input), vencimento (date), num documento (optional text), fazenda (dropdown), produtor (optional dropdown), conta bancaria (optional dropdown), notas (optional textarea)
    - Installment section: "Parcelas" input (number, min 1). When > 1, show generated installment table below: columns Parcela (#), Valor (editable BRL input), Vencimento (editable date). Pre-filled by dividing total with first-installment residual. User can edit individual values — but sum must match total (show warning if mismatch).
    - Cost center rateio section: mode toggle (Percentual | Valor Fixo). Add rows: centro de custo (dropdown), fazenda (dropdown), percentage (%) or valor fixo (BRL). "Adicionar centro de custo" button. Validation: sum of percentages = 100% or sum of valores = total. Show inline error if invalid.
    - Recurrence section (collapsed by default): frequencia (Mensal/Quinzenal/Semanal), data fim (optional date). Only on create, not edit.
    - Validation: all required fields marked with *, inline validation onBlur, aria-required. Label always visible (never placeholder-only). Error with icon + message per CLAUDE.md.
    - Save: POST /api/org/payables (create) or PUT /api/org/payables/:id (edit)

    **PaymentModal.tsx:**
    Settlement modal. Props: payableId, payable data (for display), onClose, onSuccess.
    - Display: fornecedor, valor original, vencimento (read-only)
    - Editable fields: data pagamento (date, default today), valor pago (BRL, default = original), conta bancaria (dropdown required), juros (BRL optional), multa (BRL optional), desconto (BRL optional)
    - Calculated display: "Valor efetivo: R$ X" = valor pago + juros + multa - desconto (updates live)
    - Submit: POST /api/org/payables/:id/settle

    **BatchPaymentModal.tsx:**
    Bordero modal. Props: payableIds[], payables data[], onClose, onSuccess.
    - Display list of selected payables (supplier, amount, due date) with individual juros/multa/desconto inputs per item
    - Common fields: data pagamento (date), conta bancaria (dropdown — must be same for all, per user decision)
    - Total display at bottom: sum of all effective amounts
    - Submit: POST /api/org/payables/batch-settle

    **CnabRetornoModal.tsx:**
    Upload + preview + confirm flow (Research Pattern 6).
    - Step 1: File upload zone (drag & drop or click to select .ret/.txt file). Submit to POST /cnab/retorno/preview.
    - Step 2: Preview table showing parsed records: nosso numero, status (Liquidado/Devolvido/Rejeitado), valor pago, data liquidacao, match status (Encontrado/Nao encontrado). Checkboxes to select which matched records to process. "Confirmar Baixa" button.
    - Step 3: POST /cnab/retorno/confirm with selected records. Show success/error count.
    - Use existing multer upload pattern.

    **PayableModal.css:**
    Styles for all modals: 4px spacing scale, proper touch targets (48x48 minimum), form fields with 12px vertical / 16px horizontal padding, DM Sans for modal titles, Source Sans 3 for form labels.

  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && npx --prefix apps/frontend tsc --noEmit -p apps/frontend/tsconfig.json 2>&1 | head -10</automated>
  </verify>
  <done>All 4 modals render correctly: PayableModal with installment editing and rateio, PaymentModal with effective amount calculation, BatchPaymentModal for bordero, CnabRetornoModal with 3-step flow, tsc clean</done>
</task>

</tasks>

<verification>
- tsc --noEmit passes for frontend
- PayablesPage has 3 functional tabs
- PayableModal generates editable installment rows when count > 1
- PaymentModal calculates effective amount from juros/multa/desconto
- CnabRetornoModal shows upload -> preview -> confirm steps
</verification>

<success_criteria>

- PayablesPage with list, aging table (7 faixas), and calendar (month grid with dots)
- PayableModal with installment editing and cost center rateio (% and fixed modes)
- PaymentModal with juros/multa/desconto and effective amount calculation
- BatchPaymentModal for bulk settlement with common bank account
- CnabRetornoModal with upload/preview/confirm workflow
- All CLAUDE.md design rules followed (spacing, fonts, accessibility, mobile responsiveness)
- tsc clean
  </success_criteria>

<output>
After completion, create `.planning/phases/02-n-cleo-ap-ar/02-05-SUMMARY.md`
</output>
