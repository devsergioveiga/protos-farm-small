---
phase: 02-n-cleo-ap-ar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/prisma/schema.prisma
  - apps/backend/prisma/migrations/20260401100000_add_payables_receivables/migration.sql
  - apps/backend/prisma/migrations/20260401200000_bank_account_cnab_fields/migration.sql
  - packages/shared/src/utils/installments.ts
  - packages/shared/src/utils/__tests__/installments.spec.ts
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - FN-07
  - FN-11

must_haves:
  truths:
    - 'Prisma schema has Payable, PayableInstallment, PayableCostCenterItem, Receivable, ReceivableInstallment, ReceivableCostCenterItem models'
    - 'BankAccount has optional convenioCode, carteira, variacao fields'
    - 'Installment generator puts cent residual on FIRST installment per user decision'
    - 'Sum of generated installments always equals total amount exactly'
  artifacts:
    - path: 'apps/backend/prisma/schema.prisma'
      provides: 'All AP/AR models with enums and indexes'
      contains: 'model Payable'
    - path: 'packages/shared/src/utils/installments.ts'
      provides: 'generateInstallments and validateCostCenterItems'
      exports: ['generateInstallments', 'validateCostCenterItems']
    - path: 'packages/shared/src/utils/__tests__/installments.spec.ts'
      provides: 'Tests for installment and rateio logic'
      min_lines: 50
  key_links:
    - from: 'packages/shared/src/utils/installments.ts'
      to: 'packages/shared/src/types/money.ts'
      via: 'import Money'
      pattern: 'import.*Money.*from'
---

<objective>
Create the database schema for all AP/AR models (Payable, Receivable, installments, cost center items) with enums and indexes, add CNAB convenience fields to BankAccount, and implement shared utility functions for installment generation and cost center rateio validation.

Purpose: This is the foundation for all subsequent AP/AR plans. Every backend service depends on these models and utilities.
Output: Two applied migrations, shared installment/rateio utilities with tests, updated Prisma client.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-n-cleo-ap-ar/02-RESEARCH.md

<interfaces>
<!-- From packages/shared/src/types/money.ts -->
export interface IMoney {
  add(other: IMoney): IMoney;
  subtract(other: IMoney): IMoney;
  multiply(factor: number): IMoney;
  divide(divisor: number): IMoney;
  toNumber(): number;
  toDecimal(): Decimal;
  toBRL(): string;
  equals(other: IMoney): boolean;
  isZero(): boolean;
  isNegative(): boolean;
}
export const Money: MoneyFactory; // Money(100), Money.zero(), Money.fromPrismaDecimal()

<!-- From apps/backend/prisma/schema.prisma — existing BankAccount model -->

model BankAccount {
id, organizationId, name, type, bankCode, agency, agencyDigit?, accountNumber, accountDigit?, producerId?, notes?, isActive, createdAt, updatedAt
// relations: organization, producer, farms[], balance?, transactions[]
@@index([organizationId])
@@index([organizationId, type])
@@map("bank_accounts")
}

<!-- From apps/backend/prisma/schema.prisma — existing CostCenter model -->

model CostCenter {
id, farmId, code, name, description?, isActive, createdAt, updatedAt
// relation: farm
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared installment generator and cost center rateio validator</name>
  <files>packages/shared/src/utils/installments.ts, packages/shared/src/utils/__tests__/installments.spec.ts, packages/shared/src/index.ts</files>
  <behavior>
    - generateInstallments(Money(1000), 3, date) => 3 items, first = R$333.34, rest = R$333.33
    - generateInstallments(Money(100), 1, date) => 1 item with full amount
    - generateInstallments(Money(100), 4, date) => [25.00, 25.00, 25.00, 25.00] (no residual)
    - sum of all installments always equals totalAmount exactly
    - installment dates increment by frequencyMonths (default 1)
    - validateCostCenterItems in PERCENTAGE mode: items summing to 100% passes, 99% throws
    - validateCostCenterItems in FIXED_VALUE mode: items summing to totalAmount passes, mismatch throws
    - validateCostCenterItems with empty array throws
    - validateCostCenterItems with mixed modes throws
  </behavior>
  <action>
    Create `packages/shared/src/utils/installments.ts` with two exported functions:

    1. `generateInstallments(totalAmount: IMoney, count: number, firstDueDate: Date, frequencyMonths?: number)` — divides totalAmount by count using Money arithmetic, applies cent residual to FIRST installment (i === 0) per user decision. Returns `{ number: number; amount: IMoney; dueDate: Date }[]`. Use simple date arithmetic: `new Date(firstDueDate)` then `setMonth(getMonth() + i * frequencyMonths)`.

    2. `validateCostCenterItems(totalAmount: IMoney, items: CostCenterItemInput[])` — validates that all items use the same allocMode. In PERCENTAGE mode, sum of percentages must be 100 (tolerance 0.01). In FIXED_VALUE mode, sum of fixedAmounts must equal totalAmount exactly using Money.equals(). Throws descriptive error on failure.

    Export `CostCenterItemInput` interface: `{ costCenterId: string; farmId: string; allocMode: 'PERCENTAGE' | 'FIXED_VALUE'; percentage?: number; fixedAmount?: number }`.

    Add exports to `packages/shared/src/index.ts`.

    Write tests in `packages/shared/src/utils/__tests__/installments.spec.ts` FIRST (red), then implement (green).

  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && pnpm --filter @protos-farm/shared test -- --testPathPattern=installments</automated>
  </verify>
  <done>generateInstallments correctly puts residual on first installment, validateCostCenterItems rejects invalid rateio, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Prisma schema and migrations for AP/AR models + CNAB fields</name>
  <files>apps/backend/prisma/schema.prisma, apps/backend/prisma/migrations/20260401100000_add_payables_receivables/migration.sql, apps/backend/prisma/migrations/20260401200000_bank_account_cnab_fields/migration.sql</files>
  <action>
    Add to `apps/backend/prisma/schema.prisma`:

    **Enums:** PayableStatus (PENDING, PAID, PARTIAL, OVERDUE, CANCELLED), ReceivableStatus (PENDING, RECEIVED, PARTIAL, OVERDUE, CANCELLED, RENEGOTIATED), RecurrenceFrequency (WEEKLY, BIWEEKLY, MONTHLY), PayableCategory (INPUTS, MAINTENANCE, PAYROLL, RENT, SERVICES, TAXES, FINANCING, OTHER), ReceivableCategory (GRAIN_SALE, CATTLE_SALE, MILK_SALE, LEASE, SERVICES, OTHER), CostCenterAllocMode (PERCENTAGE, FIXED_VALUE).

    **Models** (follow exact schema from 02-RESEARCH.md Prisma Schema Additions section):
    - Payable with all fields including recurrence, installmentCount, settlement fields. Relations to Organization, Farm, Producer?, BankAccount?, PayableInstallment[], PayableCostCenterItem[], self-referencing Payable[] for recurrence. Composite index @@index([organizationId, status, dueDate]) CRITICAL for aging. @@map("payables").
    - PayableInstallment with payableId, number, amount, dueDate, status, paidAt, amountPaid. @@map("payable_installments").
    - PayableCostCenterItem with payableId, costCenterId, farmId, allocMode, percentage?, fixedAmount?. @@map("payable_cost_center_items").
    - Receivable — symmetric to Payable with: clientName instead of supplierName, nfeKey (String?, 44 chars), funruralRate (Decimal? @db.Decimal(5,4)), funruralAmount (Decimal? @db.Decimal(15,2)), ReceivableStatus, ReceivableCategory. @@map("receivables").
    - ReceivableInstallment — symmetric to PayableInstallment. @@map("receivable_installments").
    - ReceivableCostCenterItem — symmetric to PayableCostCenterItem. @@map("receivable_cost_center_items").

    Add reverse relations on Organization, Farm, Producer, BankAccount, CostCenter models.

    **CNAB fields on BankAccount:** Add `convenioCode String?`, `carteira String?`, `variacao String?` — all optional.

    **Migrations:** Create two migration directories manually (use `db push` + `migrate resolve` pattern from Phase 1 if `migrate dev` fails due to shadow DB issue):
    1. `20260401100000_add_payables_receivables` — all AP/AR tables, enums, indexes
    2. `20260401200000_bank_account_cnab_fields` — ALTER TABLE bank_accounts ADD COLUMN for 3 CNAB fields

    Run `prisma generate` after migration to update client types.

  </action>
  <verify>
    <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && npx --prefix apps/backend prisma generate && npx --prefix apps/backend tsc --noEmit -p apps/backend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>All 6 new models and 6 enums exist in Prisma client, BankAccount has CNAB fields, migrations applied, tsc compiles cleanly</done>
</task>

</tasks>

<verification>
- `prisma generate` produces types for all new models without errors
- `tsc --noEmit` passes (no type errors from new schema)
- Installment tests pass with residual on FIRST installment
- Sum verification: generateInstallments always sums to exact total
</verification>

<success_criteria>

- 6 new Prisma models with correct indexes and relations
- 6 new enums for status, category, frequency, alloc mode
- BankAccount extended with 3 optional CNAB fields
- generateInstallments utility with first-installment residual
- validateCostCenterItems utility with percentage and fixed-value modes
- All tests pass, tsc clean
  </success_criteria>

<output>
After completion, create `.planning/phases/02-n-cleo-ap-ar/02-01-SUMMARY.md`
</output>
