---
phase: 05-concilia-o-e-fluxo-de-caixa
plan: 06
type: execute
wave: 4
depends_on: ['05-04', '05-05']
files_modified:
  - apps/frontend/src/App.tsx
  - apps/frontend/src/components/layout/Sidebar.tsx
  - apps/frontend/src/pages/FinancialDashboardPage.tsx
  - apps/frontend/src/pages/FinancialDashboardPage.css
  - apps/frontend/src/hooks/useFinancialDashboard.ts
  - apps/backend/src/modules/financial-dashboard/financial-dashboard.service.ts
  - apps/backend/src/modules/financial-dashboard/financial-dashboard.routes.ts
autonomous: false
requirements:
  - FN-06
  - FN-13
must_haves:
  truths:
    - 'Sidebar FINANCEIRO group includes Conciliacao and Fluxo de Caixa entries'
    - 'Lazy routes /reconciliation and /cashflow are registered in App.tsx'
    - 'Financial Dashboard shows alert card when negative balance is projected'
    - 'Dashboard alert card links to /cashflow page'
  artifacts:
    - path: 'apps/frontend/src/App.tsx'
      provides: 'Lazy routes for ReconciliationPage and CashflowPage'
      contains: 'ReconciliationPage'
    - path: 'apps/frontend/src/components/layout/Sidebar.tsx'
      provides: 'Conciliacao and Fluxo de Caixa entries in FINANCEIRO group'
    - path: 'apps/frontend/src/pages/FinancialDashboardPage.tsx'
      provides: 'Negative balance alert card in dashboard alerts section'
  key_links:
    - from: 'Sidebar.tsx'
      to: '/reconciliation and /cashflow routes'
      via: 'nav items in FINANCEIRO group'
      pattern: 'reconciliation|cashflow'
    - from: 'FinancialDashboardPage.tsx'
      to: '/api/org/cashflow/negative-balance-alert'
      via: 'useNegativeBalanceAlert hook'
      pattern: 'negative-balance-alert'
---

<objective>
Wire ReconciliationPage and CashflowPage into the application: sidebar entries, lazy routes, and dashboard negative balance alert card. Human-verify the complete flow.

Purpose: Integration makes the new pages discoverable and the dashboard alert provides proactive financial risk notification.
Output: Full sidebar integration, lazy routes, dashboard alert, end-to-end human verification.
</objective>

<execution_context>
@/Users/sergiosoares/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergiosoares/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-concilia-o-e-fluxo-de-caixa/05-CONTEXT.md
@.planning/phases/05-concilia-o-e-fluxo-de-caixa/05-UI-SPEC.md
@.planning/phases/05-concilia-o-e-fluxo-de-caixa/05-04-SUMMARY.md
@.planning/phases/05-concilia-o-e-fluxo-de-caixa/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar entries + lazy routes + dashboard negative balance alert</name>
  <files>
    apps/frontend/src/App.tsx,
    apps/frontend/src/components/layout/Sidebar.tsx,
    apps/frontend/src/pages/FinancialDashboardPage.tsx,
    apps/frontend/src/pages/FinancialDashboardPage.css,
    apps/frontend/src/hooks/useFinancialDashboard.ts,
    apps/backend/src/modules/financial-dashboard/financial-dashboard.service.ts,
    apps/backend/src/modules/financial-dashboard/financial-dashboard.routes.ts
  </files>
  <read_first>
    apps/frontend/src/App.tsx,
    apps/frontend/src/components/layout/Sidebar.tsx,
    apps/frontend/src/pages/FinancialDashboardPage.tsx,
    apps/frontend/src/pages/FinancialDashboardPage.css,
    apps/frontend/src/hooks/useFinancialDashboard.ts,
    apps/backend/src/modules/financial-dashboard/financial-dashboard.service.ts,
    apps/backend/src/modules/financial-dashboard/financial-dashboard.routes.ts
  </read_first>
  <action>
1. **App.tsx** — Add lazy imports and routes:
   ```typescript
   const ReconciliationPage = lazy(() => import('@/pages/ReconciliationPage'));
   const CashflowPage = lazy(() => import('@/pages/CashflowPage'));
   ```
   Add `<Route path="/reconciliation" element={<ReconciliationPage />} />` and `<Route path="/cashflow" element={<CashflowPage />} />` inside the authenticated AppLayout routes, after the existing financial routes (after /checks).

2. **Sidebar.tsx** — Add 2 items to FINANCEIRO group after 'Cheques':

   ```typescript
   { to: '/reconciliation', icon: FileSearch, label: 'Conciliacao' },
   { to: '/cashflow', icon: TrendingUp, label: 'Fluxo de Caixa' },
   ```

   Import `FileSearch` and `TrendingUp` from lucide-react. Place after the `{ to: '/checks', ... }` entry. The FINANCEIRO group will now have 9 items total: Dashboard, Contas bancarias, A Pagar, A Receber, Transferencias, Cartoes, Cheques, Conciliacao, Fluxo de Caixa.

3. **Backend dashboard endpoint** — Add a standalone negative balance alert endpoint:
   - In `financial-dashboard.service.ts`: add function `getNegativeBalanceAlertForDashboard(ctx, farmId?)` that imports and calls `getNegativeBalanceAlert` from `../cashflow/cashflow.service.ts`. Return `{negativeBalanceDate, negativeBalanceAmount}` or null.
   - In `financial-dashboard.routes.ts`: add `GET /negative-balance-alert` endpoint (BEFORE any `/:id` routes). Requires `financial:read`. Calls `getNegativeBalanceAlertForDashboard` and returns the result.

4. **Frontend dashboard alert card** — In `FinancialDashboardPage.tsx`:
   - Import `useNegativeBalanceAlert` from `useCashflow.ts` hook (or add fetch to `useFinancialDashboard` if simpler).
   - Add alert card in the alerts section (if one exists) or create a new alerts row after KPIs:
     - Conditional render: only when alert is not null
     - Card background: `--color-error-100` (#FFEBEE)
     - AlertCircle icon 20px in `--color-error-500` (#C62828)
     - Heading: "Saldo negativo previsto"
     - Body: "em {date} — R$ {amount}" (amount in JetBrains Mono)
     - Link: `<a href="/cashflow">Ver projecao</a>` (or React Router Link)
   - Add CSS class `.fin-dashboard__alert-card--negative` in FinancialDashboardPage.css with error-100 bg, error-500 text, padding 16px, border-radius 8px.

5. **Verify integration:**
   - Run `pnpm --filter @protos-farm/frontend exec tsc --noEmit` to verify no type errors
   - Run `pnpm --filter @protos-farm/backend test -- --testPathPattern="financial-dashboard"` to verify existing tests still pass
     </action>
     <verify>
     <automated>cd /Users/sergiosoares/Documents/it_projects/protos-farm-small && pnpm --filter @protos-farm/frontend exec tsc --noEmit --pretty 2>&1 | tail -10 && pnpm --filter @protos-farm/backend test -- --testPathPattern="financial-dashboard" 2>&1 | tail -10</automated>
     </verify>
     <acceptance_criteria>

- grep "ReconciliationPage" apps/frontend/src/App.tsx returns match (lazy import + route)
- grep "CashflowPage" apps/frontend/src/App.tsx returns match (lazy import + route)
- grep "/reconciliation" apps/frontend/src/App.tsx returns match (route path)
- grep "/cashflow" apps/frontend/src/App.tsx returns match (route path)
- grep "reconciliation\|Concilia" apps/frontend/src/components/layout/Sidebar.tsx returns match
- grep "cashflow\|Fluxo de Caixa" apps/frontend/src/components/layout/Sidebar.tsx returns match
- grep "FileSearch" apps/frontend/src/components/layout/Sidebar.tsx returns match (icon import)
- grep "TrendingUp" apps/frontend/src/components/layout/Sidebar.tsx returns match (icon import)
- grep "Saldo negativo previsto\|negativeBalance" apps/frontend/src/pages/FinancialDashboardPage.tsx returns match
- grep "/cashflow" apps/frontend/src/pages/FinancialDashboardPage.tsx returns match (link to cashflow)
- grep "GET.\*negative-balance-alert" apps/backend/src/modules/financial-dashboard/financial-dashboard.routes.ts returns match (standalone endpoint)
- tsc --noEmit succeeds for frontend
- financial-dashboard tests still pass
  </acceptance_criteria>
  <done>Sidebar has Conciliacao and Fluxo de Caixa entries, lazy routes registered, standalone GET /negative-balance-alert endpoint added to financial-dashboard routes, dashboard shows negative balance alert card linking to /cashflow, all existing tests pass</done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 5 flow</name>
  <what-built>
    Complete Conciliacao Bancaria and Fluxo de Caixa features:
    1. Bank statement import (OFX/CSV) with preview and duplicate detection
    2. Reconciliation matching with confidence scores (EXATO/PROVAVEL/SEM_MATCH)
    3. Accept/Reject/Manual-link/Ignore actions
    4. 12-month cashflow projection with 3 scenarios (Recharts chart)
    5. DFC classification table (Operacional/Investimento/Financiamento)
    6. Negative balance alert on dashboard and cashflow page
    7. Sidebar integration with both new pages
  </what-built>
  <how-to-verify>
    1. Start backend: `pnpm --filter @protos-farm/backend dev`
    2. Start frontend: `pnpm --filter @protos-farm/frontend dev`
    3. Navigate to sidebar FINANCEIRO group — verify "Conciliacao" and "Fluxo de Caixa" entries appear after "Cheques"
    4. Click "Conciliacao" — verify empty state with "Nenhum extrato importado ainda" message and "Nova Importacao" CTA
    5. Click "Nova Importacao" — upload a sample OFX or CSV file. Verify preview modal shows parsed lines with checkboxes.
    6. Confirm import — verify history table shows import record with line counts.
    7. Click import row — verify reconciliation session opens with lines grouped by confidence (EXATO/PROVAVEL/SEM_MATCH badges)
    8. Test Aceitar/Recusar buttons — verify status updates and toast messages
    9. Test "Vincular" button — verify ManualLinkModal opens with search and N:N sum validation
    10. Click "Fluxo de Caixa" — verify chart renders with 3 scenario lines (Realista area, Otimista dashed, Pessimista dashed)
    11. Verify ReferenceLine at y=0 in red
    12. Hover chart — verify tooltip shows saldo, entradas, saidas, cheques pendentes
    13. Verify DFC table below chart with Operacional/Investimento/Financiamento expandable sections
    14. Test farm filter dropdown — verify data reloads
    15. Test "Exportar PDF" and "Exportar Excel" buttons — verify file download
    16. If negative balance projected: verify alert banner on cashflow page AND alert card on Financial Dashboard
    17. Click dashboard alert card "Ver projecao" link — verify navigation to /cashflow
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Sidebar FINANCEIRO group has 9 items including Conciliacao and Fluxo de Caixa
- /reconciliation and /cashflow routes load correctly
- Dashboard shows negative balance alert when applicable
- Full end-to-end flow verified by human
</verification>

<success_criteria>

- Both new pages accessible from sidebar
- Import + reconciliation flow works end-to-end
- Cashflow projection chart renders correctly with 3 scenarios
- Dashboard alert card appears for negative balance projection
- Export PDF/Excel downloads work
- Human approves complete flow
  </success_criteria>

<output>
After completion, create `.planning/phases/05-concilia-o-e-fluxo-de-caixa/05-06-SUMMARY.md`
</output>
