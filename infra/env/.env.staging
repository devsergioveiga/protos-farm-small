# ===========================================
# Protos Farm — Staging Environment
# ===========================================
# Secrets reais ficam no GitHub Environments.
# Este arquivo contém apenas placeholders e valores não-sensíveis.

NODE_ENV=staging

# Backend
PORT=3000

# PostgreSQL
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=protos_staging
POSTGRES_PASSWORD=CHANGE_ME_IN_GITHUB_SECRETS
POSTGRES_DB=protos_farm_staging

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
